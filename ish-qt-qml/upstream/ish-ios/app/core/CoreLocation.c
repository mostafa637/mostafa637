/*
 * Qt port compatibility unit.
 *
 * Location services belonged to the original iSH iOS Objective-C layer and
 * are not part of the terminal core. The Qt application intentionally omits
 * that platform service; this source file preserves the upstream core layout
 * without introducing UIKit or JNI dependencies.
 */
