# Vendored from https://github.com/cberner/redb, tag v3.1.2.
# See UPSTREAM.md for the precise source revision and the small pure-Rust patch.
[package]
name = "redb"
description = "Pure-Rust embedded transactional key-value store"
homepage = "https://www.redb.org"
repository = "https://github.com/cberner/redb"
readme = "README.md"
license = "MIT OR Apache-2.0"
version = "3.1.2"
edition = "2024"
rust-version = "1.89"
build = "build.rs"

[lib]
crate-type = ["rlib"]

[features]
default = []
cache_metrics = []
