diff --git a/ish-rs/src/ipc.rs b/ish-rs/src/ipc.rs
index d3644c6..cdd7a18 100644
--- a/ish-rs/src/ipc.rs
+++ b/ish-rs/src/ipc.rs
@@ -32,13 +32,27 @@ pub struct IpcPerm {
 }
 
 impl IpcPerm {
-    pub fn new(key: u32, mode: u32) -> Self { Self { key, mode, ..Default::default() } }
-    pub fn is_private(&self) -> bool { self.key == IPC_PRIVATE }
+    pub fn new(key: u32, mode: u32) -> Self {
+        Self {
+            key,
+            mode,
+            ..Default::default()
+        }
+    }
+    pub fn is_private(&self) -> bool {
+        self.key == IPC_PRIVATE
+    }
     pub fn check_perm(&self, uid: u32, requested: u32) -> bool {
-        if uid == 0 { return true; }
-        if self.uid == uid { (self.mode & 0o700 & (requested << 6)) != 0 }
-        else if self.gid == uid { (self.mode & 0o070 & (requested << 3)) != 0 }
-        else { (self.mode & 0o007 & requested) != 0 }
+        if uid == 0 {
+            return true;
+        }
+        if self.uid == uid {
+            (self.mode & 0o700 & (requested << 6)) != 0
+        } else if self.gid == uid {
+            (self.mode & 0o070 & (requested << 3)) != 0
+        } else {
+            (self.mode & 0o007 & requested) != 0
+        }
     }
 }
 
@@ -51,10 +65,25 @@ pub struct ShmSegment {
 }
 
 impl ShmSegment {
-    pub fn new(perm: IpcPerm, size: usize) -> Self { Self { perm, size, ..Default::default() } }
-    pub fn attach(&mut self) -> u32 { self.attached += 1; self.shmaddr }
-    pub fn detach(&mut self) { if self.attached > 0 { self.attached -= 1; } }
-    pub fn is_attached(&self) -> bool { self.attached > 0 }
+    pub fn new(perm: IpcPerm, size: usize) -> Self {
+        Self {
+            perm,
+            size,
+            ..Default::default()
+        }
+    }
+    pub fn attach(&mut self) -> u32 {
+        self.attached += 1;
+        self.shmaddr
+    }
+    pub fn detach(&mut self) {
+        if self.attached > 0 {
+            self.attached -= 1;
+        }
+    }
+    pub fn is_attached(&self) -> bool {
+        self.attached > 0
+    }
 }
 
 #[derive(Debug, Default)]
@@ -64,10 +93,17 @@ pub struct IpcTable {
 }
 
 impl IpcTable {
-    pub fn new() -> Self { Self::default() }
+    pub fn new() -> Self {
+        Self::default()
+    }
     pub fn create_shm(&mut self, key: u32, size: usize, mode: u32) -> Result<u32, i32> {
         if key != IPC_PRIVATE {
-            if let Some((id, _)) = self.shm_segments.iter().enumerate().find(|(_, s)| s.perm.key == key) {
+            if let Some((id, _)) = self
+                .shm_segments
+                .iter()
+                .enumerate()
+                .find(|(_, s)| s.perm.key == key)
+            {
                 return Ok(id as u32);
             }
         }
@@ -78,12 +114,16 @@ impl IpcTable {
         self.next_id += 1;
         Ok(id)
     }
-    pub fn get_shm(&self, id: u32) -> Option<&ShmSegment> { self.shm_segments.get(id as usize) }
+    pub fn get_shm(&self, id: u32) -> Option<&ShmSegment> {
+        self.shm_segments.get(id as usize)
+    }
     pub fn remove_shm(&mut self, id: u32) -> Result<(), i32> {
         if (id as usize) < self.shm_segments.len() {
             self.shm_segments.remove(id as usize);
             Ok(())
-        } else { Err(-2) }
+        } else {
+            Err(-2)
+        }
     }
 }
 
@@ -100,7 +140,13 @@ mod tests {
 
     #[test]
     fn ipc_perm_check() {
-        let perm = IpcPerm { key: 123, uid: 1000, gid: 1000, mode: 0o644, ..Default::default() };
+        let perm = IpcPerm {
+            key: 123,
+            uid: 1000,
+            gid: 1000,
+            mode: 0o644,
+            ..Default::default()
+        };
         assert!(!perm.is_private());
         assert!(perm.check_perm(0, 4));
         assert!(perm.check_perm(1000, 4));
