import os

typst_dir = "/home/user/mostafa637/xv6-riscv-book-ar/typst"

# 1. acks_ar.typ
acks_typ = """= المقدمة والشكر والتقدير

هذا النص هو مسودة معدّة لمقرر دراسي في أنظمة التشغيل. يوضح المفاهيم الأساسية لأنظمة التشغيل من خلال دراسة نواة نموذجية تُدعى xv6. تقتدي xv6 بنظام Unix الإصدار السادس (v6) الذي طوره دينيس ريتشي (Dennis Ritchie) وكين ثومبسون (Ken Thompson). تتبع xv6 البنية والأسلوب العام لـ v6، ولكن تم تنفيذها بلغة ANSI C لمعمارية RISC-V متعددة الأنوية.

يجب قراءة هذا النص بالتزامن مع الشفرة المصدرية لـ xv6، وهو نهج مستوحى من كتاب جون ليونز "تعليق على نظام UNIX الإصدار السادس" (John Lions' Commentary on UNIX 6th Edition)؛ يحتوي النص على وصلات شعبية للشفرة المصدرية على #link("https://github.com/mit-pdos/xv6-riscv")[https://github.com/mit-pdos/xv6-riscv]. انظر #link("https://pdos.csail.mit.edu/6.1810")[https://pdos.csail.mit.edu/6.1810] للحصول على مؤشرات إضافية للموارد المتاحة على الإنترنت الخاصة بـ v6 و xv6، بما في ذلك العديد من الواجبات المعملية (Labs) التي تستخدم xv6.

لقد استخدمنا هذا النص في المقررين 6.828 و 6.1810، وهما مقررا أنظمة التشغيل في معهد ماساتشوستس للتكنولوجيا (MIT). نتوجه بالشكر لأعضاء هيئة التدريس، والمساعدين المدرسين، والطلاب في تلك المقررات الذين ساهموا جميعًا بشكل مباشر أو غير مباشر في xv6. ونخص بالذكر آدم بيلاي (Adam Belay)، وأوستن كليمنتس (Austin Clements)، ونيكولاي زيلدوفيتش (Nickolai Zeldovich). وأخيرًا، نود أن نشكر كل من أرسل لنا عبر البريد الإلكتروني أخطاءً في النص أو اقتراحات للتحسين.

إذا اكتشفت أخطاءً أو كانت لديك اقتراحات للتحسين، يُرجى إرسال بريد إلكتروني إلى فرانس كاشوك (Frans Kaashoek) وروبيرت موريس (Robert Morris) على: (kaashoek,rtm@csail.mit.edu).
"""

with open(os.path.join(typst_dir, "acks_ar.typ"), "w", encoding="utf-8") as f:
    f.write(acks_typ)

# 2. unix_ar.typ
unix_typ = """#import "listings.typ": *

= واجهات نظام التشغيل < CH:UNIX >

تتمثل المهمة الأساسية لنظام التشغيل في تنظيم مشاركة أجهزة الحاسوب بين برامج متعددة بأسلوب آمن ومنظم، وتقديم مجموعة من الخدمات المتقدمة التي يتجاوز نطاقها ما يقدمه العتاد بمفرده. يقوم نظام التشغيل بـ إدارة العتاد منخفض المستوى وتجريده؛ حتى لا يضطر برنامج معالجة النصوص — على سبيل المثال — للتعامل المباشر مع التفاصيل العتادية المعقدة لأقراص التخزين. كما يوزع نظام التشغيل موارد العتاد بين البرامج المختلفة لتستطيع التنفيذ في وقت واحد (أو تعطي الانطباع بالتنفيذ التزامني). وأخيرًا، يقدم نظام التشغيل وسائل آمنة ومحكومة تتيح للبرامج التفاعل وتبادل البيانات والعمل معًا بسلاسة.

تتواصل برامج المستخدم مع نظام التشغيل للحصول على خدماته من خلال واجهة محددة تُعرف بـ *واجهة استدعاءات النظام*.

يُعد تصميم واجهة برمجة متقنة ومحكمة تحديًا هندسيًا بالغ الدقة. فمن ناحية، نفضل أن تكون الواجهة بسيطة وضيقة؛ لأن البساطة تسهل كتابة الكود البرمجي للنظام والتحقق من صحته وخلوه من الثغرات. ومن ناحية أخرى، تشتد الحاجة لتقديم ميزات متقدمة ومتنوعة للتطبيقات. وتكمن المهارة في حل هذا التناقض في تصميم واجهات تعتمد على آليات جوهرية قليلة ولكنها مرنة للغاية بحيث يمكن دمجها لتوفير حلول شاملة وأسلوب عمل عميم.

يستخدم هذا الكتاب نظام تشغيل تعليمي عملي يُدعى *xv6* لتوضيح مفاهيم أنظمة التشغيل وشرحها خطوة بخطوة. يوفر نظام xv6 الواجهات الأساسية المبتكرة في نظام Unix الشهير الذي طوره كين ثومبسون ودينيس ريتشي، كما يحاكي الهيكلية الداخلية لنواة Unix. يُمثل Unix نموذجًا فريدًا للواجهات البسيطة ذات الآليات المرنة عالية العمومية؛ وقد حقق هذا التصميم نجاحًا باهرًا جعل معظم أنظمة التشغيل الحديثة اليوم — مثل Linux و macOS و BSD و Solaris وحتى Microsoft Windows بدرجات مختلفة — تعتمد واجهات شبيهة بـ Unix. لذا، يُعد فهم نظام xv6 خطوة تأسيسية ممتازة لاستيعاب أي من هذه الأنظمة.

كما يُظهر الشكل الآتي، يعتمد xv6 البنية التقليدية لـ *النواة* (kernel)، وهي برنامج محوري بامتيازات عتادية خاصة يتولى تقديم الخدمات للبرامج قيد التشغيل.
تُسمى كل مهمة أو برنامج قيد التنفيذ بـ *عملية* (process).
تتمتع كل عملية بفضاء ذاكرة خاص بها يحتوي على التعليمات والبيانات والمكدس (stack):
تنفذ التعليمات الحسابات الخاصة بالبرنامج، وتُمثل البيانات المتغيرات التي تعتمد عليها الحسابات، بينما ينظم المكدس استدعاءات الدوال والأنشطة الفرعية. يحتوي الحاسوب عادة على العديد من العمليات التي تنفذ بالتوازي، لكنه يضم نواة واحدة مركزية تديرها جميعًا.

عندما تحتاج عملية ما إلى خدمة من النواة، فإنها تنفذ *استدعاء نظام* (system call).
يدخل استدعاء النظام إلى النواة، فتفحص النواة الطلب وتنفذ الخدمة المطلوبة، ثم تعود بالنص إلى العملية.
وبذلك تتناوب العملية في تنفيذه بين *وضع المستخدم* (user mode) و *وضع النواة* (kernel mode).

تستغل النواة آليات الحماية العتادية التي توفرها وحدة المعالجة المركزية (CPU) لضمان أن كل عملية تنفذ في فضاء المستخدم تتمتع بعزل كامل ولا يمكنها القراءة أو الكتابة خارج ذاكرتها الافتراضية المخصصة. تنفذ النواة بامتيازات عتادية كاملة تطبق هذه الحماية، بينما تنفذ برامج المستخدم بدون هذه الامتيازات. وعندما يستدعي برنامج مستخدم استدعاء نظام، يرفع العتاد مستوى الامتياز تلقائيًا وينتقل لتنفيذ دالة نواتية محددة مسبقًا.

== العمليات والذاكرة

العملية في xv6 تتكون من فضاء ذاكرة (أو فضاء عناوين افتراضي) وسلسلة من خيوط التنفيذ المدارة بواسطة النواة.
يوفر xv6 زمن تنفيذي متضمنًا العزل بين العمليات: تمنع النواة أي عملية من الوصول إلى الذاكرة الخاصة بعملية أخرى.

تستخدم العملية استدعاء النظام #lstinline("fork") لإنشاء عملية جديدة.
يعطي #lstinline("fork") العملية الجديدة نسخة مطابقة تمامًا لذاكرة العملية المستدعية: ينخ #lstinline("fork") التعليمات، والبيانات، والمكدس الخاص بالعملية المستدعية إلى ذاكرة العملية الجديدة.
يعود الاستدعاء #lstinline("fork") في كل من العملية الأصلية والعملية الجديدة.
في العملية الأصلية (الأب)، يعيد #lstinline("fork") معرّف العملية الابن (PID).
أما في العملية الجديدة (الابن)، فيعيد #lstinline("fork") الصفر.

يوضح المقطع البرمجي الآتي الاستخدام المباشر لـ #lstinline("fork") و #lstinline("wait"):

#lstlisting[
int pid = fork();
if(pid > 0){
  printf("parent: child=%d\\n", pid);
  pid = wait((int *) 0);
  printf("child %d is done\\n", pid);
} else if(pid == 0){
  printf("child: exiting\\n");
  exit(0);
} else {
  printf("fork error\\n");
}
]

قد تكون مخرجات هذا الكود بالشكل الآتي:
#lstlisting[
parent: child=1234
child: exiting
]

تليها السطر التالي بعد انتهاء الابن:
#lstlisting[
parent: child 1234 is done
]

يقوم استدعاء النظام #lstinline("exec") بتعويض فضاء الذاكرة الخاص بالعملية المستدعية بصورة جديدة مأخوذة من ملف تنفيذي مُخزن في نظام الملفات:

#lstlisting[
char *argv[3];

argv[0] = "echo";
argv[1] = "hello";
argv[2] = 0;
exec("/bin/echo", argv);
printf("exec error\\n");
]

== الإدخال/الخرج وواصفات الملفات

*واصف الملف* (file descriptor) هو رقم صحيح صغير يعبر عن كائن مفتوح بواسطة العملية يمكن القراءة منه أو الكتابة فيه.
اصطلاحًا، تقرأ العملية من واصف الملف 0 (الإدخال القياسي)، وتكتب المخرجات إلى واصف الملف 1 (الإخراج القياسي)، وتكتب الأخطاء إلى واصف الملف 2 (الخطأ القياسي).

يوضح المثال التكرار الأساسي للقرأة والكتابة:
#lstlisting[
char buf[512];
int n;

for(;;){
  n = read(0, buf, sizeof buf);
  if(n == 0)
    break;
  if(n < 0){
    fprintf(2, "read error\\n");
    exit(1);
  }
  if(write(1, buf, n) != n){
    fprintf(2, "write error\\n");
    exit(1);
  }
}
]

تتيح آلية إعادة توجيه الإخراج والإدخال للشِل الاستفادة من واصفات الملفات المغلقة والمفتوحة حديثًا:
#lstlisting[
char *argv[2];

argv[0] = "cat";
argv[1] = 0;
if(fork() == 0) {
  close(0);
  open("input.txt", O_RDONLY);
  exec("cat", argv);
}
]

وتوضح الميكانيكية التالية استخدام النسخ عبر #lstinline("dup"):
#lstlisting[
fd = dup(1);
write(1, "hello ", 6);
write(fd, "world\\n", 6);
]

== الأنابيب

*الأنبوب* (pipe) هو قناة تدفق بيانات صغيرة تُعرض للعمليات في صورة زوج من واصفات الملفات: واحد للقراءة والآخر للكتابة.
الكتابة في أحد طرفي الأنبوب تجعل البيانات متاحة للقراءة من الطرف الآخر.

#lstlisting[
int p[2];
char *argv[2];

argv[0] = "wc";
argv[1] = 0;

pipe(p);
if(fork() == 0) {
  close(0);
  dup(p[0]);
  close(p[0]);
  close(p[1]);
  exec("/bin/wc", argv);
} else {
  close(p[0]);
  write(p[1], "hello world\\n", 12);
  close(p[1]);
}
]

تتيح الشِلات توصيل الأوامر مثل:
#lstlisting[
echo hello world | wc
]

أو إعادة توجيه الملفات:
#lstlisting[
echo hello world >/tmp/xyz; wc </tmp/xyz
]

== نظام الملفات

يوفر نظام الملفات في xv6 ملفات بيانات معنونة بأسماء ومُنظمة داخل شجرة من الدلائل (directories).

#lstlisting[
mkdir("/dir");
fd = open("/dir/file", O_CREATE|O_WRONLY);
close(fd);
mknod("/console", 1, 1);
]

تُعرف العقدة الميتاداتا لملف في نظام الملفات عبر الهيكل #lstinline("struct stat"):
#lstlisting[
#define T_DIR     1   // دليل
#define T_FILE    2   // ملف
#define T_DEVICE  3   // جهاز

struct stat {
  int dev;     // جهاز القرص
  uint ino;    // رقم Inode
  short type;  // نوع الملف
  short nlink; // عدد الوصلات الصلبة
  uint64 size; // حجم الملف بالبايتات
};
]

تتيح الوصلات الصلبة تسمية ملف بأسماء متعددة:
#lstlisting[
open("a", O_CREATE|O_WRONLY);
link("a", "b");
]

وعند استدعاء #lstinline("unlink(\"a\")") يتم حسم الاسم الأول مع الإبقاء على الملف المتاح باسم #lstinline("b").

== العالم الحقيقي

تستعرض الواجهات المعروضة في xv6 الأساس المحوري لأنظمة التشغيل الحديثة المعتمدة على معايير POSIX (مثل Linux و macOS و FreeBSD).

== تمارين

1. اكتب برنامجًا يستخدم استدعاءات النظام في xv6 لإرسال مصفوفة أرقام عبر أنبوب بين عمليتين وتجميعها.
2. قم بتعديل الشِل لدعم إعادة التوجيه لمدخلات ومخرجات الأوامر باستخدام #lstinline("<") و #lstinline(">").
"""

with open(os.path.join(typst_dir, "unix_ar.typ"), "w", encoding="utf-8") as f:
    f.write(unix_typ)

print("Manual conversion of acks_ar and unix_ar complete.")
