import os

typst_dir = "/home/user/mostafa637/xv6-riscv-book-ar/typst"

# 4. mem_ar.typ
mem_typ = """#import "listings.typ": *

= جداول الصفحات < CH:MEM >

جداول الصفحات هي الميكانيكية الأساسية التي يقدمها العتاد وتعتمد عليها النواة لتوفير الذاكرة الافتراضية وتحقيق العزل بين العمليات.
تسمح جداول الصفحات لنظام التشغيل برسم خريطة العناوين الافتراضية المشتغلة عليها البرامج وتحويلها إلى عناوين فيزيائية فعلية في ذاكرة الحاسوب (RAM).

يقدم هذا الفصل كيفية تنظيم جداول الصفحات في معمارية RISC-V SV39، وتخطيط الذاكرة لكل من النواة والعمليات في xv6، وكيفية تخصيص وتحرير الذاكرة الفيزيائية.

== عتاد التصفح

تستخدم معمارية RISC-V 64-bit نمط تصفح يُدعى *Sv39*، حيث تُستخدم 39 بتًا فقط من أصل 64 بت في العنوان الافتراضي.
يتم تقسيم الذاكرة إلى كتل ثابتة تُسمى *الصفحات* (pages)، ويبلغ حجم الصفحة الواحدة 4096 بايت (4 كيلوبايت).

يتكون العنوان الافتراضي ذو الـ 39 بت في Sv39 من:
- 27 بت تعبر عن *رقم الصفحة الافتراضية* (Virtual Page Number - VPN)، وتُقسم إلى ثلاثة مستويات بعرض 9 بتات لكل مستوى (VPN[2]، VPN[1]، VPN[0]).
- 12 بت تعبر عن *الإزاحة* (Offset) داخل الصفحة ($2^{12} = 4096$ بايت).

يتكون جدول الصفحات في Sv39 من هيكل شجري ثلاثي المستويات:
1. يحتفظ السجل العتادي #lstinline("satp") (Supervisor Address Translation and Paging) بالعنوان الفيزيائي لجدول الصفحات من المستوى الأعلى (Level 2).
2. تستخدم وحدة إدارة الذاكرة (MMU) الـ 9 بتات الأهم (VPN[2]) لفهرسة الجدول الأول للحصول على عنوان جدول المستوى التالي.
3. تُستخدم الـ 9 بتات الوسطى (VPN[1]) لفهرسة جدول المستوى الثاني.
4. تُستخدم الـ 9 بتات الأدنى (VPN[0]) لفهرسة جدول المستوى الثالث للحصول على *مدخل جدول الصفحات* (Page Table Entry - PTE).

يحتوي مدخل جدول الصفحات (PTE) ذو الـ 64 بت على رقم الصفحة الفيزيائية (Physical Page Number - PPN) ورايات التحكم والتحقق (Flags):
- #lstinline("PTE_V") (Valid): يحدد ما إذا كان المدخل صالحًا وموجودًا.
- #lstinline("PTE_R") (Readable): يسمح بقراءة الصفحة.
- #lstinline("PTE_W") (Writable): يسمح بالكتابة في الصفحة.
- #lstinline("PTE_X") (Executable): يسمح بتنفيذ التعليمات من الصفحة.
- #lstinline("PTE_U") (User): يحدد إمكانية الوصول للصفحة من وضع المستخدم (U-mode).

لتسريع ترجمة العناوين وتجنب الوصول المكرر للذاكرة، يحتوي المعالج على «مخزن الترجمة المؤقت (Translation Lookaside Buffer، TLB)». وعند تعديل جدول الصفحات، يجب على النواة فرز ومسح مخزن الترجمة المؤقت باستخدام تعليمة #lstinline("sfence.vma") .

== فضاء عناوين النواة

يقوم xv6 بإعداد *فضاء عناوين النواة* (kernel address space) لتمكين النواة من الوصول إلى العتاد والأجهزة الملحقة والذاكرة المادية.
يستخدم xv6 التعيين المباشر (Direct Mapping) لمعظم الذاكرة الفيزيائية داخل النواة:
- العناوين من #lstinline("0x00000000") إلى #lstinline("0x80000000") تُخصص لمتحكمات الأجهزة والملحقات العتادية (مثل UART، PLIC، ومتحكمات virtio).
- العنوان #lstinline("0x80000000") هو بداية الذاكرة الفيزيائية الرئيسية (RAM). تُعين النواة كودها وبياناتها بدءًا من هذا العنوان.
- يتواجد أقصى عنوان في الذاكرة المادية عند #lstinline("PHYSTOP") ( #lstinline("0x88000000") ، بحد أقصى 128 ميجابايت في xv6).

هناك استثناءان مهمان للتعيين المباشر في فضاء عناوين النواة:
1. *صفحة المنصة القافزة (Trampoline Page)*: تُعين عند أعلى عنوان افتراضي ممكن ( #lstinline("MAXVA - PGSIZE") ) لتمكين التحويل بين فضاء المستخدم وفضاء النواة.
2. *مكدسات النواة (Kernel Stacks)*: تُخصص لكل عملية مكدس نواتي خاص مع وضع *صفحة حارس* (Guard Page) غير صالحة تحته مباشرةً لاكتشاف طفح المكدس (Stack Overflow) والتسبب في خطأ حماية فورية.

== الكود البرمجي: إنشاء فضاء العناوين

تُدار جداول الصفحات في xv6 عبر البرمجية #lstinline("kernel/vm.c") .
الدالة الأساسية للبحث والتنقل في شجرة جداول الصفحات هي #lstinline("walk()") :
#lstlisting[
pte_t *
walk(pagetable_t pagetable, uint64 va, int alloc)
]
تقوم #lstinline("walk()") بتطبيق عملية الترجمة ثلاثية المستويات عتاديًا وبرمجيًا. إذا كان خيار #lstinline("alloc") مفعلًا ولم يكن جدول الصفحات الفراعي موجودًا، تقوم #lstinline("walk()") بتخصيص صفحة جديدة باستخدام #lstinline("kalloc()") .

وتوفر الدالة #lstinline("mappages()") آلية ربط نطاق من العناوين الافتراضية بالعناوين الفيزيائية عن طريق إضافة المداخل المناسبة عبر #lstinline("walk()") .

== تخصيص الذاكرة الفيزيائية

تحتاج النواة إلى تخصيص وتحرير صفحات الذاكرة المادية بكتل حجمها 4096 بايت أثناء التشغيل لخدمة جداول الصفحات ومكدسات العمليات والذاكرات المؤقتة.

تُدار الذاكرة الفيزيائية الحرة بين نهاية كود النواة ( #lstinline("end") ) وأقصى الذاكرة ( #lstinline("PHYSTOP") ) بواسطة *مخصص الذاكرة الفيزيائية* (Physical Memory Allocator).

== الكود البرمجي: مخصص الذاكرة الفيزيائية

يتواجد مخصص الذاكرة في #lstinline("kernel/kalloc.c") .
يحتفظ المخصص بقائمة متصلة أحادية من الصفحات الحرة ( #lstinline("struct run") ):
#lstlisting[
struct run {
  struct run *next;
};

struct {
  struct spinlock lock;
  struct run *freelist;
} kmem;
]
تضمن #lstinline("spinlock") التزامن ومنع حالة التسابق (Race Condition) عند تخصيص أو تحرير الصفحات بين أنوية المعالج المختلفة:
- #lstinline("kalloc()") : تسحب صفحة حرّة من رأس القائمة #lstinline("freelist") وتعيد عنوانها الفيزيائي بعد تعبئته بقيمة مخفية خردة ( #lstinline("1") ) لاكتشاف الأخطاء.
- #lstinline("kfree(pa)") : تتأكد من صحة العنوان الفيزيائي، وتكتب فيه بيانات خردة ( #lstinline("1") ) لمنع استخدام الذاكرة المحررة، ثم تعيد الصفحات إلى أصل القائمة #lstinline("freelist") .

== فضاء عناوين العملية

تملك كل عملية في xv6 جدول صفحات مستقل يمثل فضاء عناوينها الافتراضي.
يبدأ فضاء عناوين العملية من العنوان الافتراضي #lstinline("0x0") :
- التعليمات والبيانات والمكدس الابتدائي تبدأ من العنوان #lstinline("0x0") .
- ينظر مكدس المستخدم إلى الأسفل ويتم توسيعه تلقائيًا.
- تعلو القمة المتحركة للذاكرة الديناميكية للعملية ( #lstinline("heap") ) وتُوسع بواسطة استدعاء النظام #lstinline("sbrk()") المعتمد على الدالة #lstinline("growproc()") .
- في أعلى فضاء العناوين الممكن ( #lstinline("MAXVA") )، تقع صفحة المنصة القافزة ( #lstinline("TRAMPOLINE") ) وتحتها مباشرة صفحة إطار المصيدة ( #lstinline("TRAPFRAME") ).

== الكود البرمجي: exec

يقوم استدعاء النظام #lstinline("exec()") بالخطوات التالية لتشغيل برنامج جديد:
1. يفتح الملف التنفيذي المنسق بصيغة ELF باستخدام #lstinline("namei()") .
2. يتفحص رأس ELF للتأكد من الماجيك هيدر الصحيح.
3. ينشئ جدول صفحات جديد فارغ للعملية باستخدام #lstinline("proc_pagetable()") .
4. يحمل أجزاء البرنامج (Program Segments) إلى الصفحات الفيزيائية المخصصة حديثًا عبر #lstinline("loadseg()") .
5. يخصص صفحة مكدس المستخدم ويدفع فيها الوسائط ( #lstinline("argv") ).
6. يحرر جدول الصفحات القديم والذاكرة القديمة عبر #lstinline("proc_freepagetable()") .
7. يربط جدول الصفحات الجديد بالعملية ويثبت نقطة الدخول #lstinline("epc") .

== العالم الحقيقي

تستخدم أنظمة التشغيل الحديثة آليات ذاكرة افتراضية متطورة للغاية متل:
- *النسخ عند الكتابة (Copy-on-Write - COW)*: مشاركة الصفحات الفيزيائية بين الأب والابن عند #lstinline("fork()") ونساخها فقط عند محاولة التعديل.
- *الصفحات الضخمة (Superpages)*: استخدام صفحات بحجم 2 ميجابايت أو 1 جيجابايت لتقليل الضغط على مخزن الترجمة المؤقت.
- *التصفح بالمطالبة (Demand Paging) والتبديل (Swapping)*: نقل الصفحات إلى القرص الصلب عند امتلاء الذاكرة الفيزيائية.

== تمارين

1. اشرح لماذا تحتاج النواة إلى صفحة المنصة القافزة #lstinline("TRAMPOLINE") في نفس العنوان الافتراضي لدى جميع العمليات والنواة.
2. قم بتنفيذ آليات تخصيص الصفحات الكبيرة لتقليل عدد مستويات جداول الصفحات في xv6.
"""

with open(os.path.join(typst_dir, "mem_ar.typ"), "w", encoding="utf-8") as f:
    f.write(mem_typ)

# 5. trap_ar.typ
trap_typ = """#import "listings.typ": *

= المصايد واستدعاءات النظام < CH:TRAP >

تحدث *المصيدة* (trap) عندما يحتاج المعالج إلى تحويل السيطرة والتنفيذ بصورة قسرية من البرمجية الحالية إلى النواة.
وتحدث المصايد في الحالات التالية:
1. استدعاء نظام (System Call): عندما تنفذ عملية مستخدم تعليمة #lstinline("ecall") .
2. استثناء (Exception): عندما تنفذ تعليمة غير صالحة أو يحدث خطأ في الذاكرة (مثل القسمة على صفر أو خطأ الصفحة).
3. مقاطعة عتادية (Interrupt): عندما يرسل جهاز عتادي (مثل مؤقت الساعة أو القرص الصلب) إشارة طلب خدمة.

يقدم هذا الفصل الآليات العتادية والبرمجية لمعالجة المصايد في RISC-V، وكيفية التعامل مع المصايد القادمة من فضاء المستخدم وفضاء النواة.

== عتاد المصايد في RISC-V

يحتوي كل معالج في RISC-V على مجموعة من السجلات العتادية الخاصة بوضع المشرف (Supervisor CSRs) للتحكم في المصايد:
- #lstinline("stvec") (Supervisor Trap Vector Base Address Register): يحتوي على عنوان دالة معالجة المصايد في النواة.
- #lstinline("sepc") (Supervisor Exception Program Counter): يحفظ فيه العتاد عنوان التعليمة التي حدثت عندها المصيدة.
- #lstinline("scause") (Supervisor Cause Register): يحفظ فيه العتاد سبب وقوع المصيدة (استدعاء نظام، مقاطعة، أو استثناء معين).
- #lstinline("sstatus") (Supervisor Status Register): يحتوي على رايات الحالة مثل #lstinline("SIE") (تفعيل المقاطعات) و #lstinline("SPP") (الوضع السابق قبل المصيدة).
- #lstinline("stval") (Supervisor Trap Value Register): يحفظ القيمة المصاحبة للخطأ (مثل العنوان غير الصالح عند خطأ الصفحة).

عند حدوث مصيدة، يقوم عتاد RISC-V تلقائيًا بالخطوات المباشرة التالية:
1. إذا كانت المصيدة مقاطعة عتادية وراية #lstinline("SIE") معطلة، يتم تجاهل المقاطعة.
2. يحفظ المعالج قيمة عداد البرنامج الحالي #lstinline("pc") في #lstinline("sepc") .
3. يحفظ المعالج الوضع الحالي (U-mode أو S-mode) في راية #lstinline("SPP") داخل #lstinline("sstatus") .
4. يكتب المعالج كود السبب في #lstinline("scause") .
5. يعطل المقاطعات بمسح الراية #lstinline("SIE") .
6. ينسخ وضع المشرف (S-mode) ليكون الوضع الحالي.
7. يحمل قيمة #lstinline("stvec") إلى عداد البرنامج #lstinline("pc") للقفز فورًا إلى معالج النواة.

== المصايد القادمة من فضاء المستخدم

عند حدوث مصيدة أثناء تنفيذ برنامج مستخدم، يتطلب الأمر تبديل جدول الصفحات وسجلات المعالج والمكدس بأمان تام دون تمكين المستخدم من التلاعب بالحالات النواتية.

تُدار العملية في xv6 بواسطة صفحة المنصة القافزة #lstinline("TRAMPOLINE") المتواجدة في نفس العنوان الافتراضي في كلي جدولَي الصفحات:
1. يضمن العتاد التحويل إلى #lstinline("stvec") المحتوي على عنوان #lstinline("uservec") داخل كود التجميع في #lstinline("kernel/trampoline.S") .
2. تقوم #lstinline("uservec") بحفظ كافة سجلات المعالج الـ 31 الخاصة بالمستخدم داخل هيكل #lstinline("trapframe") الخاص بالعملية.
3. تحمل #lstinline("uservec") عنوان مكدس النواة للعملية، وعنوان جدول صفحات النواة إلى #lstinline("satp") .
4. تقفز #lstinline("uservec") إلى الدالة #lstinline("usertrap()") المكتوبة بلغة C في #lstinline("kernel/trap.c") .

تتفحص #lstinline("usertrap()") السجل #lstinline("scause") :
- إذا كان السبب استدعاء نظام ( #lstinline("scause == 8") )، تزيد #lstinline("epc") بمقدار 4 لتخطي تعليمة #lstinline("ecall") وتستدعي #lstinline("syscall()") .
- إذا كانت مقاطعة عتادية، تستدعي #lstinline("devintr()") .
- خلاف ذلك، يكون استثناءً غير متوقع، فتنهي النواة العملية فورًا وتطبع رسالة خطأ.

بعد انتهاء المعالجة، تعود #lstinline("usertrap()") عبر #lstinline("usertrapret()") التي تستدعي #lstinline("userret") لإعادة استرجاع سجلات المستخدم وتحديث #lstinline("satp") بالعودة إلى فضاء المستخدم وتنفيذ #lstinline("sret") .

== الكود البرمجي: تنفيذ استدعاءات النظام

تقوم الدالة #lstinline("syscall()") المحددة في #lstinline("kernel/syscall.c") بقراءة رقم استدعاء النظام المخزن في سجل المستخدم #lstinline("a7") ( #lstinline("p->trapframe->a7") ).

تتحقق #lstinline("syscall()") من صحة الرقم وفهرسته داخل جدول الدوال #lstinline("syscalls[]") :
#lstlisting[
void
syscall(void)
{
  int num;
  struct proc *p = myproc();

  num = p->trapframe->a7;
  if(num > 0 && num < NELEM(syscalls) && syscalls[num]) {
    p->trapframe->a0 = syscalls[num]();
  } else {
    p->trapframe->a0 = -1;
  }
}
]
يُخزن المخرج الناتج عن دالة استدعاء النظام داخل سجل المستخدم #lstinline("a0") ليقرأه البرنامج.

== الكود البرمجي: وسائط استدعاءات النظام

تمرر برامج المستخدم وسائط استدعاءات النظام عبر سجلات المعالج القياسية ( #lstinline("a0") إلى #lstinline("a5") ).
توفر النواة دوال مساعدة لاستخراج هذه الوسائط بأمان في #lstinline("kernel/syscall.c") :
- #lstinline("argint(n, &ip)") : جلب الوسيط رقم $n$ كعدد صحيح (32-bit integer).
- #lstinline("argaddr(n, &ip)") : جلب الوسيط رقم $n$ كعنوان ذاكرة (64-bit address).
- #lstinline("argstr(n, buf, max)") : جلب سلسلة نصية من فضاء ذاكرة المستخدم ونسخها بأمان في ذاكرة النواة باستخدام #lstinline("fetchstr()") .

ولأن فضاء ذاكرة المستخدم منفصل عن فضاء النواة، توفر النواة الدوال #lstinline("copyin()") و #lstinline("copyout()") لنقل البيانات بين فضاء العناوين الافتراضي للعملية وفضاء النواة مع التحقق من صحة المداخل.

== المصايد القادمة من فضاء النواة

عندما تنفذ النواة كودًا في وضع المشرف (S-mode) وتحدث مقاطعة أو استثناء، يختلف التعامل قليلاً:
1. جدول الصفحات هو بالفعل جدول صفحات النواة، ومكدس النواة مثبت بالفعل.
2. يتم تحويل #lstinline("stvec") ليرير إلى الدالة #lstinline("kernelvec") في #lstinline("kernel/kernelvec.S") .
3. تحفظ #lstinline("kernelvec") سجلات المعالج على مكدس النواة الحالي وتستدعي #lstinline("kerneltrap()") المحددة في #lstinline("kernel/trap.c") .
4. بعد معالجة المقاطعة أو الاستثناء، تسترجع #lstinline("kernelvec") السجلات من المكدس وتنفذ #lstinline("sret") .

== العالم الحقيقي

تستخدم المعالجات الحديثة آليات مصايد معقدة تدعم المتجهات المباشرة (Vectored Interrupts) بحيث ترتبط كل مقاطعة بعنصر محدد في جدول المتجهات لتسريع الاستجابة. كما تتطلب آليات الأمن المتطورة مثل Meltdown و Spectre معالجة دقيقة للغاية لصفحات القفز وعزل جداول الصفحات لتجنب تسريب السجلات المخبئية.

== تمارين

1. تتبع خطوات نقل القيمة من سجل المستخدم #lstinline("a0") إلى متغير النواة في استدعاء النظام #lstinline("sys_write") .
2. قم بتعديل xv6 لدعم معالجة خطأ الصفحة لتقديم صفحة صفرية عند الحاجة (Null Page Guard).
"""

with open(os.path.join(typst_dir, "trap_ar.typ"), "w", encoding="utf-8") as f:
    f.write(trap_typ)

print("mem_ar.typ and trap_ar.typ converted.")
