#import "../template/listings.typ": *

= تسجيل المعاملات
<CH:LOG>

واحدة من أكثر المشاكل إثارة بداخل تصميم نظام الملفات هي التعافي من الانهيارات المفاجئة.
تنشأ المشكلة لأن العديد من عمليات نظام الملفات تتطلب كتابات متعددة للقرص،
والانهيار بعد طقم فرعي من الكتابات قد يترك نظام الملفات على القرص بداخل حالة غير متسقة.
على سبيل المثال،
افترض وقوع انهيار أثناء قطع ملف.
يتضمن القطع كتابة (على الأقل) كتلتي قرص،
واحدة تحوي العقدة الفهرسية والأخرى تحوي جزءاً من خريطة البتات الحرة:
يجب ضبط طول العقدة الفهرسية إلى الصفر وتصفير أرقام الكتل بداخل العقدة الفهرسية،
ويجب ضبط بتات الخريطة البتية لكتل الملف للصفر (معلمة إياها كحرة).
الانهيار بعد واحدة من الكتابات،
وقبل أن تحوز النواة فرصة لتنفيذ الكتابة الثانية،
سيترك نظام الملفات على القرص بداخل حالة غير صحيحة.
إذا وقعت كتابة الخريطة البتية فقط،
فإن النتيجة بعد الانهيار وإعادة الإقلاع ستكون عقدة فهرسية بطول غير صفري تشير لكتل معلمة كحرة؛
وإذا وقعت كتابة العقدة الفهرسية فقط،
فإن النتيجة ستكون كتلاً ليست مستخدمة بواسطة أي ملف ولكنها أيضاً ليست معلمة كحرة.

الأخيرة حميدة نسبياً،
لكن العقدة الفهرسية التي تشير لكتلة محررة يُرجح أن تتسبب بداخل مشاكل خطيرة بعد إعادة الإقلاع.
بعد إعادة الإقلاع قد تخصص النواة تلك الكتلة لملف آخر،
والآن نحوز ملفين مختلفين يشيران غير قصد لنفس الكتلة.
لو كانت `xv6` تدعم مستخدمين متعددين لكانت هذه الحالة مشكلة أمنية،
نظراً لأن مالك الملف القديم سيكون قادراً على قراءة وكتابة كتل بداخل الملف الجديد المملوك لمستخدم مختلف.

تحل `xv6` مشكلة الانهيارات أثناء عمليات نظام الملفات بصيغة بسيطة من التسجيل.
استدعاء نظام `xv6` لا يكتب مباشرة هياكل بيانات نظام الملفات على القرص.
بدلاً من ذلك،
يضع وصفاً لكافة كتابات القرص التي يرغب بإجرائها بداخل **سجل (Log)** على القرص.
بمجرد أن يسجل استدعاء النظام كافة كتاباته،
يكتب سجل **تثبيت (Commit)** خاص للقرص يشير إلى أن السجل يحوي عملية كاملة.
عند تلك النقطة ينسخ استدعاء النظام الكتابات لهياكل بيانات نظام الملفات على القرص.
بعد اكتمال تلك الكتابات،
يمسح استدعاء النظام السجل على القرص.

إذا انهار النظام وأعاد الإقلاع،
فإن شفرة نظام الملفات تتعافى من الانهيار كما يلي،
قبل تشغيل أية عمليات.
إذا كان السجل معلماً بكونه يحوي عملية كاملة،
فإن شفرة التعافي تنسخ الكتابات إلى حيث تنتمي بداخل نظام الملفات على القرص.
إذا لم يكن السجل معلماً بكونه يحوي عملية كاملة،
تتجاهل شفرة التعافي السجل.
تنهي شفرة التعافي عملها بمسح السجل.

لماذا يحل سجل `xv6` مشكلة الانهيارات أثناء عمليات نظام الملفات؟
إذا وقع الانهيار قبل تثبيت العملية،
فلن يعلم السجل على القرص كـ مكتمل،
وستتجاهله شفرة التعافي،
وستكون حالة القرص كما لو أن العملية لم تبدأ مطلقاً.
إذا وقع الانهيار بعد تثبيت العملية،
فإن التعافي سيعيد تشغيل كافة كتابات العملية،
ربما مكرراً إياها إذا كانت العملية قد بدأت كتابتها لهيكل البيانات على القرص.
بأية حال يجعل السجل العمليات ذرية فيما يتعلق بالانهيارات:
بعد التعافي إما تشير كافة كتابات العملية على القرص،
أو لا يظهر أي منها.

== تصميم السجل
<sec:log_design>

يستقر السجل بداخل موقع ثابت معروف (انظر الشكل @fig:fslayout)،
محدد بداخل الكتلة الفائقة.
يتكون من كتلة رأس متبوعة بمتتالية من نسخ الكتل المحدثة ("الكتل المسجلة").
تحتوي كتلة الرأس على مصفوفة من أرقام الكتل،
واحدة لكل من الكتل المسجلة،
وعداد كتل السجل.
العداد بداخل كتلة الرأس على القرص إما صفر،
مما يشير إلى عدم وجود معاملة بداخل السجل،
أو غير صفري،
مما يشير إلى أن السجل يحوي معاملة مثبتة كاملة بعدد الكتل المسجلة المحدد.
تكتب `xv6` كتلة الرأس عندما تثبت معاملة ما،
وليس قبل ذلك،
وتضبط العداد للصفر بعد نسخ الكتل المسجلة لنظام الملفات.
وهكذا فإن انهياراً بداخل منتصف معاملة سينتج عنه عداد صفري بداخل كتلة رأس السجل؛
والانهيار بعد التثبيت سينتج عنه عداد غير صفري.

تشير شفرة كل استدعاء نظام لبداية ونهاية متتالية الكتابات التي يجب أن تكون ذرية فيما يتعلق بالانهيارات.
للسماح بالتنفيذ المتزامن لعمليات نظام الملفات بواسطة عمليات مختلفة،
يمكن لنظام التسجيل تجميع كتابات استدعاءات نظام متعددة بداخل معاملة واحدة.
وهكذا فإن تثبيتاً واحداً قد يتضمن كتابات استدعاءات نظام مكتملة متعددة.
لتجنب تقسيم استدعاء نظام عبر المعاملات،
فإن نظام التسجيل يثبت فقط عندما لا تكون هناك استدعاءات نظام لنظام الملفات جارية.

فكرة تثبيت عدة معاملات معاً تُعرف بـ **التثبيت الجماعي (Group commit)**.
يقلل التثبيت الجماعي عدد عمليات القرص لأنه يوزع الكلفة الثابتة للتثبيت عبر عمليات متعددة.
كما يسلم التثبيت الجماعي لنظام القرص كتابات متزامنة أكثر بداخل نفس الوقت،
ربما متيحاً للقرص كتابتها جميعاً أثناء دوران قرصي واحد.
برنامج تشغيل virtio بـ `xv6` لا يدعم هذا النوع من التجميع الدفعي،
لكن تصميم نظام ملفات `xv6` يتيح ذلك.

أثر جانبي دقيق للتثبيت الجماعي هو أنه يجعل آثار استدعاء نظام الكتابة غير متزامنة:
أي أنه حتى بعد رجوع استدعاء نظام الكتابة للتطبيق،
فليس مضموناً أن الكتل التي عدلها أصبحت على القرص.
على سبيل المثال عند انتهاء معاملة كتابة،
قد تكون معاملة أخرى قد بدأت وستثبت معاملة الكتابة مع المعاملة الثانية كمجموعة واحدة،
مؤجلة آثار الكتابة على القرص حتى تثبت المعاملة الثانية.
إذا أراد تطبيق الانتظار حتى تصبح تعديلاته دائمة (أي على القرص)،
يمكنه استدعاء استدعاء النظام `sync` بعد استدعاء نظام الكتابة؛
تنتظر `sync` حتى يكتمل تثبيت جماعي قيد التنفيذ.

تخصص `xv6` مقداراً ثابتاً من المساحة على القرص لاحتجاز السجل.
العدد الإجمالي للكتل المكتوبة بواسطة استدعاءات النظام بداخل معاملة يجب أن يتسع بداخل تلك المساحة.
هذا يحوز أثرين.
لا يُسمح لأي استدعاء نظام بمفرده كتابة كتل متميزة أكثر مما توجد مساحة بداخل السجل.
هذه ليست مشكلة لمعظم استدعاءات النظام،
لكن اثنين منها يمكنهما كتابة كتل كثيرة:
`write` و `unlink`.
كتابة ملف ضخم قد تكتب كتل بيانات كثيرة وكتل خريطة بتية كثيرة بالإضافة لعقدة فهرسية؛
وإلغاء ربط ملف ضخم قد يكتب كتل خريطة بتية كثيرة وعقدة فهرسية.
يقسم استدعاء نظام الكتابة بـ `xv6` الكتابات الضخمة لكتابات أصغر متعددة تتسع بداخل السجل،
و `unlink` لا تتسبب بداخل مشاكل لأن نظام ملفات `xv6` بداخل الممارسة يستخدم كتلة خريطة بتية واحدة فقط.
الأثر الآخر لمساحة السجل المحدودة هو أن نظام التسجيل لا يمكنه السماح لاستدعاء نظام بالبدء ما لم يكن متأكداً من أن كتابات استدعاء النظام ستتسع بداخل المساحة المتبقية بداخل السجل.

== الكود البرمجي: التسجيل
<sec:code_logging>

يبدو الاستخدام المعهود للسجل بداخل استدعاء النظام كالتالي:

```c
  begin_op();
  ...
  bp = bread(...);
  bp->data[...] = ...;
  log_write(bp);
  ...
  end_op();
```

تنتظر `begin_op` حتى يكون نظام التسجيل غير مثبت حالياً،
وحتى توجد مساحة سجل غير محجوزة كافية لاحتجاز الكتابات من هذا الاستدعاء.
تحسب `log.outstanding` عدد استدعاءات النظام التي حجزت مساحة سجل؛
المساحة المحجوزة الإجمالية هي `log.outstanding` مضروبة بداخل `MAXOPBLOCKS`.
زيادة `log.outstanding` تحجز المساحة وتمنع وقوع تثبيت أثناء استدعاء النظام هذا.
تفترض الشفرة بأسلوب محافظ أن كل استدعاء نظام قد يكتب ما يصل إلى `MAXOPBLOCKS` كتلة متميزة.

تعمل `log_write` كوكيل لـ `bwrite`.
تسجل رقم الكتلة للكتلة بداخل الذاكرة،
حاجزة لها خانة بداخل السجل على القرص،
وتثبت الحاجز بداخل المخزن المؤقت للكتل لمنع المخزن المؤقت من إخلائه.
يجب أن تظل الكتلة بداخل المخزن المؤقت حتى تثبت:
حتى ذلك الحين فإن النسخة المخزنة مؤقتاً هي المسجل الوحيد للتعديل؛
لا يمكن كتابتها لمكانها على القرص إلا بعد التثبيت؛
والقراءات الأخرى بداخل نفس المعاملة يجب أن تشاهد التعديلات.
تلاحظ `log_write` عندما تُكتب كتلة ما مرات متعددة أثناء معاملة واحدة،
وتخصص لتلك الكتلة نفس الخانة بداخل السجل.
هذا التحسين يُسمى غالباً **الامتصاص (Absorption)**.
من الشائع،
على سبيل المثال،
أن كتلة القرص الحاوية لعقد فهرسية لعدة ملفات تُكتب مرات متعددة بداخل معاملة.
عبر امتصاص كتابات قرص متعددة بداخل واحدة،
يمكن لنظام الملفات توفير مساحة السجل وتحقيق أداء أفضل لأن نسخة واحدة فقط من كتلة القرص يجب أن تُكتب للقرص.

تخفض `end_op` أولاً عداد استدعاءات النظام الجارية.
إذا كان العداد صفراً الآن،
تثبت المعاملة الحالية عبر دعوة `commit()`.
توجد أربع مراحل بداخل هذه العملية.
تنسخ `write_log()` كل كتلة معدلة بداخل المعاملة من المخزن المؤقت للكتل لخانته بداخل السجل على القرص.
تكتب `write_head()` كتلة الرأس للقرص:
هذه هي نقطة التثبيت،
والانهيار بعد الكتابة سينتج عنه تعاف يعيد تشغيل كتابات المعاملة من السجل.
تقرأ `install_trans` كل كتلة من السجل وتكتبها للمكان المناسب بداخل نظام الملفات.
أخيراً تكتب `end_op` رأس السجل مع عداد صفر؛
هذا يجب أن يحدث قبل أن تبدأ المعاملة التالية كتابة الكتل المسجلة،
لكي لا ينتج عن انهيار استخدام التعافي لرأس معاملة مع الكتل المسجلة للمعاملة التالية.

فرضية رئيسية بداخل تنفيذ `end_op` هي أن `write_head` تحدّث كتلة الرأس ذرياً:
بالكامل أو لا شيء على الإطلاق.
تذكر من الفصل @CH:FS أن الكتلة بـ `xv6` هي قطاعا قرص،
لكن رأس السجل يتسع بداخل القطاع الأول،
وبذلك لا توجد مشكلة حتى وإن كان القرص يضمن فقط أن التحديث لقطاع واحد ذري.

تُدعى `recover_from_log` من `initlog` والتي تُدعى من `fsinit` أثناء الإقلاع قبل تنفيذ أول عملية مستخدم.
تقرأ رأس السجل،
وتحاكي أفعال `end_op` إذا كان الرأس يشير إلى أن السجل يحوي معاملة مثبتة.

مثال على استخدام السجل يقع بداخل `filewrite`.
تبدو المعاملة كالتالي:

```c
      begin_op();
      ilock(f->ip);
      r = writei(f->ip, ...);
      iunlock(f->ip);
      end_op();
```

هذه الشفرة محاطة بداخل حلقة تكرارية تقسم الكتابات الضخمة لمعاملات فردية من بضع كتل فقط بداخل الوقت الواحد،
لتجنب إفاضة السجل.
استدعاء `writei` يكتب كتل كثيرة كجزء من هذه المعاملة:
العقدة الفهرسية للملف،
كتلة أو أكثر من كتل الخريطة البتية،
وبعض كتل البيانات.

== العالم الحقيقي
<sec:real_world_logging>

نظام التسجيل بـ `xv6` غير كفء.
لا يمكن لوقوع تثبيت أن يتزامن مع استدعاءات نظام لنظام الملفات.
يسجل النظام كتل كاملة،
حتى وإن جرى تغيير بضع بايتات فقط بداخل كتلة.
ينفذ كتابات سجل متزامنة،
كتلة بداخل الوقت الواحد،
كل منها يرجح أن تتطلب كامل زمن دوران القرص.
نظم التسجيل الحقيقية تعالج كافة هذه المشاكل.

التسجيل ليس الطريقة الوحيدة لتوفير التعافي من الانهيارات.
استخدمت نظم الملفات المبكرة منظفاً أثناء إعادة الإقلاع (على سبيل المثال،
برنامج `fsck` بـ UNIX) لفحص كل ملف ومجلد وقوائم الكتل والعقد الفهرسية الحرة،
بحثاً عن ودعماً لحل عدم الاتساق.
المنظف قد يستغرق ساعات لنظم الملفات الضخمة،
وتوجد حالات لا يكون من الممكن فيها حل عدم الاتساق بطريقة تجعل استدعاءات النظام الأصلية ذرية.
التعافي من سجل أسرع بكثير ويتسبب بداخل جعل استدعاءات النظام ذرية بداخل مواجهة الانهيارات.

== تمارين
<sec:log_exercises>

1.
لفهم كيفية استخدام السجل،
أدخل السطر البرمجي التالي قبل عبارة `log_write` بداخل الدالة `writei` بداخل `kernel/fs.c`:
`printf("log write %d:
%d %d %d\n", addr, bp->blockno, off, n);`
نفذ make clean ثم make qemu،
وبداخل حث موجه أوامر `xv6` نفذ الأمر التالي:
`$ echo hi > f`
ينبغي لك مشاهدة مخرجات شبيهة بهذا:
`log write 47:
47 368 16`
`log write 936:
936 0 2`
`log write 936:
936 2 1`
تشير المخرجات إلى أن `log_write` دُعيت ثلاث مرات لـ أمر موجه الأوامر هذا.
لماذا دُعيت `log_write` 3 مرات؟
ماذا تكتب كل `log_write`؟
هل الكتابة الأولى أعلاه تناظر تحديث المجلد الرئيسي والكتابتان الأخريان تناظران كتابة الملف f؟
قد يكون الشكل @fig:fslayout نافعاً ولا تتردد بداخل إضافة عبارات `printf` أكثر لمساعدتك بداخل الإجابة عن السؤال.
