#import "../template/listings.typ": *

= طبقة سجل العَمَلِيَّات (Logging Layer)
<CH:LOG>

واحدة من أكبر المشاكل في تصميم أنظمة الملفات هي كيفية التعامل مع الانقطاع المفاجئ للطاقة أو انهيار النظام أثناء كتابة البيانات على القرص الصلب.
لأن العديد من عَمَلِيَّاتٌ نظام الملفات تتطلب تعديل عدة كتل مختلفة على القرص (مثل تخصيص كتلة جديدة، وتعديل خريطة البتات، وتحديث العُقْدَةُ الفَهْرَسِيَّةُ، وإضافة مدخل في الدليل).

إذا انقطعت الكهرباء في منتصف الطريق، أصبحت حالة القرص الصلب إما متهالكة أو مفقودة التناسق (Inconsistent State).

يحل `xv6` هذه المشكلة عبر تقديم *طبقة سجل العَمَلِيَّات* (Logging Layer / Journaling).

يقدم هذا الفصل ميكانيكية عمل سجل العَمَلِيَّات، والمعاملات الذرية (Atomic Transactions)، وكيفية التعافي عند تشغيل النظام.

== غاية السجل وتصميمه
<sec:log_design>

يقتضي مفهوم سجل العَمَلِيَّات عدم كتابة التعديلات مباشرةً على كتل القرص الأصلية.
بدلاً من ذلك، تتبع النَّوَاة الخطوات التالية:

  + تُسجل كافة الكتل المعدلة في منطقة معزولة خاصة على القرص تُدعى *منطقة السجل* (Log Area).
  + عند اكتمال جمع جميع كتل المعاملة، تكتب النَّوَاة كتلة رأس السجل (Header Block) على القرص. تُعرف هذه الخطوة بـ *الالتزام* (Commit).
  + بعد تأكيد الالتزام، تقوم النَّوَاة بنسخ التعديلات من منطقة السجل إلى مواقعها الفيزيائية الأصلية على القرص. تُسمى هذه الخطوة بـ *التفريغ* (Install).
  + بعد تفريغ كل الكتل، تصفح النَّوَاة السجل لتفريغه للعمليات الجديدة.
\end{enumerate">

إذا انهارت النَّوَاة أو انقطعت الكهرباء قبل الالتزام (قبل كتابة كتلة الرأس)، يتم تجاهل السجل بالكامل عند الإقلاع وتظل البيانات القديمة كما هي.
وإذا انهارت النَّوَاة بعد الالتزام وقبل إكتمال التفريغ، يقرأ النظام عند الإقلاع كتلة الرأس ويتولى إعادة تفريغ الكتل من السجل إلى مواقعها الأصلية بنجاح، مما يضمن الذرية التامة للمعاملات.

== الكود البرمجي: تنفيذ السجل
<sec:code_logging>

تُدار عَمَلِيَّاتٌ السجل في `kernel/log.c`.
تغلف النَّوَاة أية عَمَلِيَّةٌ تعديل على نظام الملفات بين الاستدعائين `begin\_op()` و `end\_op()`:
#lstlisting[

begin_op();
// تعديل العقد الفهرسية والكتل المخصصة عبر log_write()
ilock(ip);
ip->size = new_size;
iupdate(ip);
iunlock(ip);
end_op();
]

تبدأ الدالة `begin\_op()` بالانتظار حتى لا يكون السجل خاضعًا للتثبيت أو ممتلئًا بمعاملات جارية:

  + تزيد عداد المعاملات المشتركة الجارية `log.outstanding`.
  + عندما تدعو العَمَلِيَّات `log\_write(b)`، لا تُكتب الكتلة على القرص فورًا؛ بل تُضاف إلى مصفوفة السجل في الذاكرة وتسجل في الهيكل.


عند دعوة `end\_op()`:

  + ينقص عداد المعاملات الجارية.
  + إذا وصل العداد إلى الصفر وكانت هناك كتل معلقة، تدعو النَّوَاة الدالة `commit()`.
  + تقوم `commit()` بدعوة `write\_log()` لكتابة الكتل المعدلة في منطقة السجل على القرص.
  + تدعو `write\_head()` لكتابة كتلة رأس السجل على القرص (نقطة الذرية والالتزام).
  + تدعو `install\_trans()` بنسخ الكتل من منطقة السجل إلى أماكنها المخصصة في القرص.
  + تصفر كتلة الرأس وتدعو `write\_head()` لتفرغ السجل المعاملاتي.


== العالم الحقيقي
<sec:real_world_log>

تعتمد أنظمة الملفات الإنتاجية مثل ext4 و NTFS و XFS تقنيات سجل الميتاداتا (Metadata-only Logging) حيث تُسجل البيانات الوصفية فقط دون كتل البيانات العادية لتوفير الأداء والسرعة.

== تمارين
<sec:exercises_log>


  + اشرح ماذا يحدث لو انقطعت الكهرباء أثناء تنفيذ الدالة `install\_trans()` وكيف يتم التعافي عند الإقلاع القادم.
  + قم بزيادة حجم السجل واختبر تأثيره على سرعة المعاملات المتوازية في `xv6`.

