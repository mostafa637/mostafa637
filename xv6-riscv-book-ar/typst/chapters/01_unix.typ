#import "../template/listings.typ": *

= واجهات نظام التشغيل
<CH:UNIX>

تتمثل مهمة نظام التشغيل في مشاركة جهاز الحاسوب بين عدة برامج مختلفة،
وتقديم مجموعة من الخدمات الأهم والأكثر فائدة من تلك التي يوفرها العتاد الصلب بمفرده.
يدير نظام التشغيل العتاد المادي منخفض المستوى ويجرده؛
حتى لا يحتاج برنامج معالج النصوص---على سبيل المثال---إلى الاهتمام بنوع أو تفاصيل
قرص التخزين الصلب المستخدم في الحاسوب.
كما يقوم نظام التشغيل بتقاسم العتاد المادي بين برامج متعددة
بحيث تعمل (أو تبدو للمستخدم وكأنها تعمل) في الوقت نفسه بالتوازي.
وأخيرًا، يوفر نظام التشغيل وسائل محكومة ومنظمة لتفاعل البرامج فيما بينها،
بحيث يمكنها مشاركة البيانات أو العمل معًا بشكل تعاوني.

يقدم نظام التشغيل خدماته لبرامج التطبيقات عبر واجهة برمجة (Interface).
\index{interface design}
وتجدر الإشارة إلى أن تصميم واجهة نظام تشغيل جيدة ومتوازنة يُعد أمرًا صعبًا وتحديًا هندسيًا معقدًا.
فمن ناحية، نفضل أن تكون الواجهة بسيطة ومحدودة النطاق
لأن ذلك يسهل من مهمة التحقق من صحة التنفيذ البرمجي داخل النَّوَاةِ.
ومن ناحية أخرى، قد نغرى بتقديم العديد من الميزات المعقدة والمتقدمة للتطبيقات.
وتكمن المهارة الحقيقية في حل هذا التنازع في تصميم واجهات تعتمد على آليات قليلة وبسيطة
ولكن يمكن دمجها وتجميعها بمرونة لتقديم قدر كبير من العمومية والشمول.

يستخدم هذا الكتاب نظام تشغيل واحدًا ومحددًا كمثال عملي وملموس لتوضيح مفاهيم أنظمة التشغيل.
يقدم هذا النظام، وهو `xv6`، الواجهات والخدمات الأساسية التي قدمها
نظام التشغيل التاريخي Unix الذي صممه كين طومسون ودنيس ريتشي~\cite{unix}،
كما يحاكي التصميم الداخلي لنظام Unix.
يوفر Unix واجهة محدودة وبسيطة تتميز آلياتها بالتركيب والتكامل الجيد،
مما يقدم درجة مدهشة من العمومية.
وقد حققت هذه الواجهة نجاحًا باهرًا لدرجة أن أنظمة التشغيل الحديثة---مثل
BSD و Linux و macOS و Solaris وحتى Microsoft Windows إلى حد ما---تمتلك واجهات شبيهة بـ Unix.
وبناءً على ذلك، فإن فهم `xv6` يُعد بداية ممتازة ونقطة انطلاق قوية
نحو فهم أي من هذه الأنظمة والعديد من الأنظمة الأخرى.

كما يوضح الشكل~@fig:os، يأخذ `xv6` الشكل التقليدي المتمثل في وجود
_النواة_ (Kernel)،
وهي برنامج ممتاز وخاص يشتغل بصلاحيات عالية ويقدم الخدمات للبرامج الجارية الأخرى.
يُطلق على كل برنامج جاري ينفذ التعليمات اسم
_العملية_ (Process)،
وتملك كل عَمَلِيَّةٌ ذاكرة تحتوي على التعليمات، والبيانات، ومكدس الاستدعاءات (Stack).
تنفذ التعليمات الحسابات الخاصة بالبرنامج.
والبيانات هي المتغيرات التي تجري عليها هذه الحسابات.
بينما ينظم المكدس استدعاءات الدوال والإجراءات داخل البرنامج.
عادة ما يحتوي الحاسوب الواحد على العديد من العَمَلِيَّاتِ ولكن مع وجود نَوَاةٌ واحدة فقط.

عندما تحتاج العَمَلِيَّة إلى استدعاء خدمة من خدمات النَّوَاة، فإنها تستدعي
_استدعاء نظام_ (System Call)،
وهو أحد الاستدعاءات المعرفة في واجهة نظام التشغيل.
يدخل اِسْتِدْعَاءُ النِّظَامِ إلى النَّوَاةِ؛ فتُنَفِّذُ النَّوَاةُ الخدمة المطلوبة ثم تعود.
وبذلك تتناوب العَمَلِيَّة بين التنفيذ في
_فضاء المستخدم_ (User Space)
و
_فضاء النواة_ (Kernel Space).

وكما سنشرح بالتفصيل في الفصول القادمة، تستخدم النَّوَاة آليات الحماية العتادية التي يوفرها
المعالج#footnote[
يشير هذا النص عمومًا إلى العنصر العتادي الذي ينفذ الحسابات بـ CPU، وهو اختصار لوحدة المعالجة المركزية.
كما تستخدم وثائق أخرى (مثل مواصفات RISC-V) كلمات المعالج أو النَّوَاة أو الهارت بدلاً من CPU.
]
لضمان عدم تمكن كل عَمَلِيَّةٌ تنفذ في فضاء المستخدم إلا من الوصول إلى الذاكرة الخاصة بها فقط.
تُنَفِّذُ النَّوَاةُ امتيازات العتاد المطلوبة لتطبيق آليات الحماية هذه؛
بينما تنفذ برامج المستخدم بدون تلك الامتيازات.
عندما يستدعي برنامج مستخدم استدعاء نظام، يرفع العتاد مستوى الصلاحية ويبدأ في تنفيذ دالة مرتبة مسبقًا داخل النَّوَاةِ.

\center
#figure(image("fig/os.pdf"))
// النَّوَاة وعمليتان في فضاء المستخدم.
<fig:os>


تُمثل مجموعة اِسْتِدْعَاءَاتُ النِّظَامِ التي توفرها النَّوَاة الواجهة التي تراها برامج المستخدم.
يوفر جدار نَوَاةٌ `xv6` مجموعة فرعية من الخدمات واِسْتِدْعَاءَاتُ النِّظَامِ التي تقدمها نوى Unix تقليديًا.
يسرد الشكل~@fig:api جميع اِسْتِدْعَاءَاتُ النِّظَامِ الخاصة بـ `xv6`.

يستعرض بقية هذا الفصل خدمات `xv6`---العَمَلِيَّات، الذاكرة، وَاصِفَاتُ المَلَفَّاتِ، الأَنَابِيبُ، ونظام الملفات---ويوضحها بمقاطع من الكود ومناقشات حول كيفية استخدام
_مغلّف الأوامر_ (Shell)،
وهو واجهة المستخدم النصية لسطر الأوامر في Unix.
يوضح استخدام مغلّف الأوامر لاِسْتِدْعَاءَاتُ النِّظَامِ مدى الدقة التي تم بها تصميم هذه الاستدعاءات.

مغلّف الأوامر هو برنامج عادي يقرأ الأوامر من المستخدم وينفذها.
وحقيقة أن مغلّف الأوامر هو برنامج بلغة المستخدم وليس جزءًا من النَّوَاةِ يوضح قوة واجهة اِسْتِدْعَاءَاتُ النِّظَامِ:
فلا يوجد شيء استثنائي أو ممتاز في مغلّف الأوامر.
وهذا يعني أيضًا أنه من السهل استبدال مغلّف الأوامر؛ ونتيجة لذلك،
تمتلك أنظمة Unix الحديثة مجموعة متنوعة من مغلّفات الأوامر للاختيار من بينها،
ولكل منها واجهة مستخدم وميزات نصية خاصة بها.
مغلّف أوامر `xv6` هو تنفيذ بسيط لجوهر مغلّف أوامر Bourne الخاص بـ Unix.

يمكن العثور على تنفيذ مغلّف أوامر `xv6` في
`user/sh.c`.
الرابط هو وصلة تشعبية لكود مصدر `xv6` ذي الصلة في
#link("https://github.com/mit-pdos/xv6-riscv/")
ويشير الرقم المحدد إلى رقم الصفحة والسطر في
#link("xv6-src-booklet.pdf")[xv6-src-booklet.pdf]،
كما في تعليقات لايونز على الإصدار السادس من UNIX~\cite{lions}.
من الممارسات الجيدة محاولة قراءة الكود المصدر أولاً بنفسك
(في بيئة التطوير المفضلة لديك، أو على github، أو في قارئ PDF) ثم العودة إلى هذا الكتاب.
بحلول نهاية هذا الكتاب، يجب أن تكون قادرًا على فهم كل سطر من كود مصدر `xv6` دون الحاجة إلى الاستعانة بهذا الكتاب.

== العَمَلِيَّات والذاكرة
<sec:process_memory>

تتكون عَمَلِيَّةٌ `xv6` من ذاكرة فضاء المستخدم (التعليمات، والبيانات، والمكدس)
وحالة خاصة بالعَمَلِيَّة خافية ومحفوظة داخل النَّوَاةِ.
يقوم `xv6` بـ
_مشاركة الوقت_ (Time-share)
بين العَمَلِيَّاتِ: حيث يغير توزيع معالجات CPU المتاحة بشفافية بين مجموعة العَمَلِيَّات المنتظرة للتنفيذ.
عندما لا تكون العَمَلِيَّة قيد التنفيذ، يحفظ `xv6` سجلات CPU الخاصة بالعَمَلِيَّة،
ويسترجعها عندما ينفذ العَمَلِيَّة في المرة القادمة.
تربط النَّوَاة معرف العَمَلِيَّة، أو
`PID`،
بكل عَمَلِيَّةٌ.

\center

*اِسْتِدْعَاءُ النِّظَامِ* & *الوصف* \\

int fork() & إنشاء عَمَلِيَّةٌ جديدة، وإعادة معرف PID للابن. \\
int exit(int status) & إنهاء العَمَلِيَّة الحالية؛ إبلاغ wait() بالحالة. لا يعود. \\
int wait(int *status) & الانتظار حتى ينتهي الابن؛ تخزين حالة الخروج في *status؛ إعادة PID للابن. \\
int kill(int pid) & إنهاء العَمَلِيَّة ذات المعرف PID. يرجع 0، أو -1 في حالة الخطأ. \\
int getpid() & إعادة معرف PID للعملية الحالية. \\
int pause(int n) & التوقف المؤقت لمدة n من تكات الساعة. \\
int exec(char *file, char *argv[]) & تحميل ملف وتنفيذه مع وسائط؛ لا يعود إلا عند حدوث خطأ. \\
char *sbrk(int n) & زيادة ذاكرة العَمَلِيَّة بمقدار n بايت صفرية. إعادة بداية الذاكرة الجديدة. \\
int open(char *file, int flags) & فتح ملف؛ تحدد الرايات القراءة/الكتابة؛ إعادة واصف ملف (fd). \\
int write(int fd, char *buf, int n) & كتابة n بايت من buf إلى وَاصِفِ المَلَفِّ fd؛ إعادة n. \\
int read(int fd, char *buf, int n) & قراءة n بايت في buf؛ إعادة عدد البايتات المأخوذة؛ أو 0 عند نهاية الملف. \\
int close(int fd) & تحرير وَاصِفُ المَلَفِّ المفتوح fd. \\
int dup(int fd) & إعادة واصف ملف جديد يشير إلى نفس الملف كـ fd. \\
int pipe(int p[]) & إنشاء أنبوب، وتخزين واصفات قراءة/كتابة الملفات في p[0] و p[1]. \\
int chdir(char *dir) & تغيير الدليل الحالي. \\
int mkdir(char *dir) & إنشاء دليل جديد. \\
int mknod(char *file, int, int) & إنشاء ملف جهاز عتادي. \\
int fstat(int fd, struct stat *st) & وضع معلومات حول ملف مفتوح في *st. \\
int link(char *file1, char *file2) & إنشاء اسم آخر (file2) للملف file1. \\
int unlink(char *file) & إزالة ملف. \\

// استدعاءات نظام Xv6. ما لم يذكر خلاف ذلك، تعيد هذه الاستدعاءات 0 في حالة عدم وجود خطأ، و-1 في حالة حدوث خطأ.
<fig:api>


يمكن للعملية إنشاء عَمَلِيَّةٌ جديدة باستخدام اِسْتِدْعَاءُ النِّظَامِ
`fork`.
يعطي `fork` العَمَلِيَّة الجديدة نسخة مطابقة تمامًا لذاكرة العَمَلِيَّة المداعية:
ينسخ `fork` التعليمات والبيانات ومكدس العَمَلِيَّة المداعية في ذاكرة العَمَلِيَّة الجديدة.
يرجع `fork` في كل من العمليتين الأصلية والجديدة.
في العَمَلِيَّةِ الأصلية، يرجع `fork` معرّف PID للعملية الجديدة.
في العَمَلِيَّةِ الجديدة، يرجع `fork` الصفر.
تُسمى العمليتان الأصلية والجديدة غالبًا بـ
_الأب_
و
_الابن_.

على سبيل المثال، ألقِ النظر في مقطع البرنامج التالي المكتوب بلغة البرمجة C~\cite{kernighan}:
#lstlisting[
[]
int pid = fork();
if(pid > 0){
  printf("parent: child=%d\n", pid);
  pid = wait((int *) 0);
  printf("child %d is done\n", pid);
} else if(pid == 0){
  printf("child: exiting\n");
  exit(0);
} else {
  printf("fork error\n");
}
]

يتسبب اِسْتِدْعَاءُ النِّظَامِ
`exit`
في توقف العَمَلِيَّة المداعية عن التنفيذ وتحرير الموارد مثل الذاكرة والملفات المفتوحة.
يأخذ exit وسيط حالة عدد صحيح، جرت العادة أن يكون 0 للإشارة إلى النجاح و1 للإشارة إلى الفشل.
يعيد اِسْتِدْعَاءُ النِّظَامِ
`wait`
معرف PID لعملية ابن منتهية (أو مقتولة) للعملية الحالية وينسخ حالة خروج الابن إلى العنوان الممرر إلى wait؛
وإذا لم تكن إحدى العَمَلِيَّات الابن للمداعي قد انتهت بعد، فإن
`wait`
ينتظر حتى تنهي إحداها.
إذا لم يكن للمداعي أي أبناء، يرجع `wait` فورًا القيمة -1.
إذا كان الأب لا يهتم بحالة خروج الابن، فيمكنه تمرير العنوان 0 إلى `wait`.

في المثال، أسطر المخرجات
#lstlisting[
[]
parent: child=1234
child: exiting
]
قد تخرج بأي من الترتيبين (أو حتى مختلطة)، اعتمادًا على ما إذا كان الأب أو الابن يصل إلى استدعاء
`printf`
أولاً.
بعد خروج الابن، يعود استدعاء
`wait`
في الأب، مما يتسبب في طباعة الأب لـ
#lstlisting[
[]
parent: child 1234 is done
]
على الرغم من أن الابن يبدأ بنسخة من ذاكرة الأب، إلا أن الأب والابن ينفذان بذاكرة منفصلة وسجلات منفصلة:
فإن تغيير متغير في أحدهما لا يؤثر على الآخر. على سبيل المثال، عندما يتم تخزين القيمة المرجعة من
`wait`
في `pid` في عَمَلِيَّةٌ الأب، فإن ذلك لا يغير المتغير `pid` في الابن.
ستظل قيمة `pid` في الابن صفرًا.

يستبدل اِسْتِدْعَاءُ النِّظَامِ
`exec`
ذاكرة العَمَلِيَّة المداعية بصلب ذاكرة جديد محمل من ملف مخزن في نظام الملفات.
يجب أن يكون للملف تنسيق محدد، يحدد أي جزء من الملف يحتوي على التعليمات، وأي جزء هو البيانات، وعند أي تعليمة يبدأ التنفيذ، إلخ.
يستخدم Xv6 تنسيق ELF، والذي يناقشه الفصل~@CH:MEM بمزيد من التفصيل.
عادة ما يكون الملف نتيجة لتجميع كود مصدر برنامج.
عندما ينجح `exec`، فإنه لا يعود إلى البرنامج المداعي؛
بدلاً من ذلك، تبدأ التعليمات المحملة من الملف في التنفيذ عند نقطة الدخول المعلنة في رأس ELF.
يأخذ `exec` وسيطين: اسم الملف الذي يحتوي على الملف التنفيذي ومصفوفة من الوسائط النصية.
على سبيل المثال:
#lstlisting[
[]
char *argv[3];

argv[0] = "echo";
argv[1] = "hello";
argv[2] = 0;
exec("/bin/echo", argv);
printf("exec error\n");
]
يستبدل هذا المقطع البرنامج المداعي بنسخة من البرنامج
`/bin/echo`
الذي يعمل مع قائمة الوسائط `echo` `hello`.
تتجاهل معظم البرامج العنصر الأول من مصفوفة الوسائط، وهو تقليديًا اسم البرنامج.

يستخدم مغلّف أوامر xv6 الاستدعاءات المذكورة أعلاه لتشغيل البرامج نيابة عن المستخدمين.
الهيكل الرئيسي لمغلّف الأوامر بسيط؛ انظر `main`.
تقرأ الحلقة الرئيسية سطر إدخال من المستخدم باستخدام `getcmd`.
ثم تستدعي `fork`، والذي ينشئ نسخة من عَمَلِيَّةٌ مغلّف الأوامر.
يستدعي الأب `wait`، بينما ينفذ الابن الأمر.
على سبيل المثال، إذا كان المستخدم قد كتب " `echo hello` " لمغلّف الأوامر،
فسيتم استدعاء `runcmd` مع " `echo hello` " كوسيط.
ينفذ `runcmd` الأمر الفعلي.
بالنسبة لـ " `echo hello` "، فإنه يستدعي `exec`.
إذا نجح `exec`، فسينفذ الابن التعليمات من `echo` بدلاً من `runcmd`.
في مرحلة ما، يستدعي `echo` استدعاء `exit`،
مما يتسبب في عودة الأب من `wait` في `main`.

قد تتساءل لماذا لا يتم دمج `fork` و `exec` في استدعاء واحد؛
سنرى لاحقًا أن مغلّف الأوامر يستغل هذا الفصل في تنفيذه لإعادة توجيه الإدخال/الإخراج.
لتجنب الهدر الناتج عن إنشاء عَمَلِيَّةٌ مكررة ثم استبدالها فورًا (بـ `exec`)،
تحسن نوى أنظمة التشغيل تنفيذ `fork` لهذه الحالة باستخدام تقنيات الذاكرة الافتراضية مثل النسخ عند الكتابة (انظر الفصل~@CH:PGFAULT).

يخصص Xv6 معظم ذاكرة فضاء المستخدم ضمنيًا:
يخصص `fork` الذاكرة المطلوبة لنسخة الابن من ذاكرة الأب،
ويخصص `exec` ذاكرة كافية لاستيعاب الملف التنفيذي.
العَمَلِيَّة التي تحتاج إلى مزيد من الذاكرة في وقت التشغيل (ربما لـ `malloc`)
يمكنها استدعاء `sbrk(n)` لتنمية ذاكرة بياناتها بمقدار `n` بايت صفرية؛
يعيد `sbrk` موقع الذاكرة الجديدة.

== الإدخال والإخراج ووَاصِفَاتُ المَلَفَّاتِ
<sec:io_fds>

_واصف الملف_
هو عدد صحيح صغير يمثل كائنًا تُديره النَّوَاة ويمكن للعملية القراءة منه أو الكتابة فيه.
قد تحصل العَمَلِيَّة على واصف ملف عن طريق فتح ملف، أو دليل، أو جهاز، أو عن طريق إنشاء أنبوب، أو عن طريق نسخ واصف حالي.
للتبسيط، سنشير غالبًا إلى الكائن الذي يشير إليه وَاصِفُ المَلَفِّ بـ "ملف"؛
تجرد واجهة وَاصِفُ المَلَفِّ الاختلافات بين الملفات والأَنَابِيبُ والأجهزة، مما يجعلها جميعًا تبدو كتدفقات من البايتات.
سنشير إلى الإدخال والإخراج بـ _I/O_.

داخليًا، تستخدم نَوَاةٌ xv6 وَاصِفُ المَلَفِّ كفهرس في جدول خاص بكل عَمَلِيَّةٌ،
بحيث تملك كل عَمَلِيَّةٌ فضاءً خاصًا من وَاصِفَاتُ المَلَفَّاتِ يبدأ من الصفر.
حسب العرف، تقرأ العَمَلِيَّة من وَاصِفِ المَلَفِّ 0 (الإدخال القياسي)،
وتكتب المخرجات إلى وَاصِفِ المَلَفِّ 1 (الخرج القياسي)،
وتكتب رسائل الخطأ إلى وَاصِفِ المَلَفِّ 2 (الخطأ القياسي).
وكما سنرى، يستغل مغلّف الأوامر هذا العرف لتنفيذ إعادة توجيه الإدخال/الإخراج والسلاسل المترابطة.
يضمن مغلّف الأوامر دائمًا أن لديه ثلاثة واصفات ملفات مفتوحة،
والتي تكون افتراضيًا واصفات ملفات لشاشة المنصة.

تقرأ اِسْتِدْعَاءَاتُ النِّظَامِ `read` و `write` البايتات وتكتبها من وإلى الملفات المفتوحة المحددة بواسطة وَاصِفَاتُ المَلَفَّاتِ.
يقوم الاستدعاء `read(fd, buf, n)` بقراءة `n` بايت كحد أقصى من وَاصِفِ المَلَفِّ `fd`،
وينسخها في `buf`، ويعيد عدد البايتات المقروءة.
كل واصف ملف يشير إلى ملف يرتبط بإزاحة خاصة به.
يقرأ `read` البيانات من إزاحة الملف الحالية ثم يقدم تلك الإزاحة بمقدار عدد البايتات المقروءة:
سيسترجع الاستدعاء `read` التالي البايتات التالية لتلك التي أعادها الاستدعاء الأول.
عندما لا تكون هناك بايتات أخرى للقراءة، يعيد `read` الصفر للإشارة إلى نهاية الملف.

يقوم الاستدعاء `write(fd, buf, n)` بكتابة `n` بايت من `buf` إلى وَاصِفِ المَلَفِّ `fd` ويعيد عدد البايتات المكتوبة.
لا يتم كتابة أقل من `n` بايت إلا عند حدوث خطأ.
مثل `read`، يكتب `write` البيانات عند إزاحة الملف الحالية ثم يقدم تلك الإزاحة بمقدار عدد البايتات المكتوبة:
يبدأ كل `write` من حيث انتهى الاستدعاء السابق.

ينسخ مقطع البرنامج التالي (والذي يشكل جوهر برنامج `cat`) البيانات من إدخاله القياسي إلى خرجه القياسي.
إذا حدث خطأ، فإنه يكتب رسالة إلى الخطأ القياسي.
#lstlisting[
[]
char buf[512];
int n;

for(;;){
  n = read(0, buf, sizeof buf);
  if(n == 0)
    break;
  if(n < 0){
    fprintf(2, "read error\n");
    exit(1);
  }
  if(write(1, buf, n) != n){
    fprintf(2, "write error\n");
    exit(1);
  }
}
]
الشيء المهم الذي يجب ملاحظته في مقطع الكود هو أن `cat` لا يعرف ما إذا كان يقرأ من ملف أو منصة أو أنبوب.
وبالمثل، لا يعرف `cat` ما إذا كان يطبع إلى منصة أو ملف أو أي شيء آخر.
إن استخدام وَاصِفَاتُ المَلَفَّاتِ والعرف المتمثل في أن وَاصِفُ المَلَفِّ 0 هو الإدخال ووَاصِفُ المَلَفِّ 1 هو الخرج يسمح بتنفيذ بسيط لـ `cat`.

يحرر اِسْتِدْعَاءُ النِّظَامِ `close` وَاصِفُ المَلَفِّ، مما يجعله فارغًا لإعادة الاستخدام بواسطة استدعاء نظام مستقبلًا مثل `open` أو `pipe` أو `dup`.
يكون وَاصِفُ المَلَفِّ المخصص حديثًا دائمًا هو أصغر واصف غير مستخدم في العَمَلِيَّةِ الحالية.

تتفاعل وَاصِفَاتُ المَلَفَّاتِ و `fork` لجعل تنفيذ إعادة توجيه الإدخال/الإخراج سهلاً.
ينسخ `fork` جدول وَاصِفَاتُ المَلَفَّاتِ الخاص بالأب جنباً إلى جنب مع ذاكرته،
بحيث يبدأ الابن بنفس الملفات المفتوحة تماماً مثل الأب.
يستبدل اِسْتِدْعَاءُ النِّظَامِ `exec` ذاكرة العَمَلِيَّة المداعية ولكنه يحافظ على جدول ملفاتها.
يسمح هذا السلوك لمغلّف الأوامر بتنفيذ
_إعادة توجيه الإدخال/الإخراج_
عن طريق التفريع، وإغلاق وإعادة فتح واصفات ملفات محددة في الابن، ثم استدعاء `exec` لتشغيل البرنامج الجديد.
فيما يلي نسخة مبسطة من الكود الذي ينفذه مغلّف الأوامر للأمر `cat < input.txt`:
#lstlisting[
[]
char *argv[2];

argv[0] = "cat";
argv[1] = 0;
if(fork() == 0) {
  close(0);
  open("input.txt", O_RDONLY);
  exec("cat", argv);
}
]

بعد أن يغلق الابن وَاصِفُ المَلَفِّ 0، يضمن استدعاء `open` استخدام وَاصِفُ المَلَفِّ هذا للملف المفتوح حديثًا `input.txt`:
سيكون 0 هو أصغر واصف ملف متاح.
ثم ينفذ `cat` مع وَاصِفِ المَلَفِّ 0 (الإدخال القياسي) مشيرًا إلى `input.txt`.
لا تتغير واصفات ملفات العَمَلِيَّة الأب بواسطة تسلسل الأوامر هذا، لأنه يعدل واصفات الابن فقط.

يعمل الكود الخاص بإعادة توجيه الإدخال/الإخراج في مغلّف أوامر xv6 بهذه الطريقة تمامًا.
تذكر أنه عند هذه النقطة في الكود، يكون مغلّف الأوامر قد فرع مغلّف الأوامر الابن بالفعل وأن `runcmd` سيستدعي `exec` لتحميل البرنامج الجديد.

يتكون الوسيط الثاني لـ `open` من مجموعة من الرايات، المتبورة كبتات، والتي تتحكم فيما يفعله `open`.
القيم الممكنة محددة في رأس التحكم بالملفات (fcntl):
`O_RDONLY`، `O_WRONLY`، `O_RDWR`، `O_CREATE`، و `O_TRUNC`،
والتي توجه `open` لفتح الملف للقراءة، أو للكتابة، أو لكليهما، ولإنشاء الملف إذا لم يكن موجودًا، ولقص الملف إلى طول صفر.

الآن يجب أن يكون واضحًا سبب كون فصل `fork` و `exec` أمرًا مفيدًا:
بين الاستدعائين، تتاح لمغلّف الأوامر فرصة لـ إعادة توجيه الإدخال/الإخراج للابن دون إزعاج إعدادات الإدخال/الإخراج لمغلّف الأوامر الرئيسي.
يمكن للمرء بدلاً من ذلك تخيل استدعاء نظام افتراضي مدمج باسم `forkexec`،
ولكن خيارات إجراء إعادة توجيه الإدخال/الإخراج مع مثل هذا الاستدعاء تبدو غير مريحة.
إما أن يعدل مغلّف الأوامر إعدادات الإدخال/الإخراج الخاصة به قبل استدعاء `forkexec` (ثم يلغي تلك التعديلات)؛
أو أن يأخذ `forkexec` تعليمات إعادة توجيه الإدخال/الإخراج كـ وسائط؛
أو (وهو الأقل جاذبية) يتم تعليم كل برنامج مثل `cat` كيفية إجراء إعادة توجيه الإدخال/الإخراج الخاص به.

على الرغم من أن `fork` ينسخ جدول وَاصِفَاتُ المَلَفَّاتِ، إلا أن كل إزاحة ملف أساسية يتم مشاركتها بين الأب والابن.
ألقِ النظر في هذا المثال:
#lstlisting[
[]
if(fork() == 0) {
  write(1, "hello ", 6);
  exit(0);
} else {
  wait(0);
  write(1, "world\n", 6);
}
]
في نهاية هذا المقطع، سيحتوي الملف المرفق بوَاصِفُ المَلَفِّ 1 على البيانات `hello world`.
يبدأ الاستدعاء `write` في الأب (والذي يعمل فقط بعد انتهاء الابن بفضل `wait`) من حيث انتهى الاستدعاء `write` الخاص بالابن.
يساعد هذا السلوك في إنتاج مخرجات متسلسلة من تسلسلات أوامر مغلّف الأوامر، مثل `(echo hello; echo world) > output.txt`.

ينسخ اِسْتِدْعَاءُ النِّظَامِ `dup` واصف ملف حالي، ويعيد واصفًا جديدًا يشير إلى نفس كائن الإدخال/الإخراج الأساسي.
يتشارك كلا واصفي الملفات الإزاحة، تمامًا كما تفعل وَاصِفَاتُ المَلَفَّاتِ المنسوخة بواسطة `fork`.
هذه طريقة أخرى لكتابة `hello world` في ملف:
#lstlisting[
[]
fd = dup(1);
write(1, "hello ", 6);
write(fd, "world\n", 6);
]

يتشارك واصفا ملفات الإزاحة إذا تم اشتقاقهما من نفس وَاصِفُ المَلَفِّ الأصلي بواسطة تسلسل من استدعاءات `fork` و `dup`.
بخلاف ذلك، لا تتشارك وَاصِفَاتُ المَلَفَّاتِ الإزاحات، حتى لو نتجت عن استدعاءات `open` لنفس الملف.
يسمح `dup` لمغلّفات الأوامر بتنفيذ أوامر مثل هذا:
`ls existing-file non-existing-file > tmp1 2>&1`.
يخبر `2>&1` مغلّف الأوامر بإعطاء الأمر واصف ملف 2 يكون نسخة مكررة من الواصف 1.
سيظهر كل من اسم الملف الموجود ورسالة الخطأ للملف غير الموجود في الملف `tmp1`.
لا يدعم مغلّف أوامر xv6 إعادة توجيه الإدخال/الإخراج لواصف ملف الخطأ، ولكنك الآن تعرف كيفية تنفيذه.

وَاصِفَاتُ المَلَفَّاتِ هي تجريد قوي، لأنها تخفي تفاصيل ما هي متصلة به:
العَمَلِيَّة التي تكتب إلى وَاصِفِ المَلَفِّ 1 قد تكون تكتب إلى ملف، أو إلى جهاز مثل المنصة، أو إلى أنبوب.

== الأَنَابِيبُ
<sec:pipes>

_الأنبوب_
هو مخزن مؤقت صغير في النَّوَاةِ يُكشف للعمليات كـ زوج من وَاصِفَاتُ المَلَفَّاتِ، أحدهما للقراءة والآخر للكتابة.
كتابة البيانات إلى أحد طرفي الأُنْبُوبِ تجعل تلك البيانات متاحة للقراءة من الطرف الآخر للأنبوب.
توفر الأَنَابِيبُ طريقة للعمليات للتواصل فيما بينها.

يشغل كود المثال التالي البرنامج `wc` مع ربط الإدخال القياسي بطرف القراءة للأنبوب.
#lstlisting[
[]
int p[2];
char *argv[2];

argv[0] = "wc";
argv[1] = 0;

pipe(p);
if(fork() == 0) {
  close(0);
  dup(p[0]);
  close(p[0]);
  close(p[1]);
  exec("/bin/wc", argv);
} else {
  close(p[0]);
  write(p[1], "hello world\n", 12);
  close(p[1]);
}
]

يستدعي البرنامج `pipe`، والذي ينشئ أنبوبًا جديدًا ويسجل واصفات ملفات القراءة والكتابة في المصفوفة `p`.
بعد `fork`، يملك كل من الأب والابن واصفات ملفات تشير إلى الأُنْبُوبِ.
يستدعي الابن `close` و `dup` ليرسل وَاصِفُ المَلَفِّ صفر إلى طرف القراءة للأنبوب،
ويغلق وَاصِفَاتُ المَلَفَّاتِ في `p`، ويستدعي `exec` لتشغيل `wc`.
عندما يقرأ `wc` من إدخاله القياسي، فإنه يقرأ من الأُنْبُوبِ.
يغلق الأب جانب القراءة للأنبوب، ويكتب في الأُنْبُوبِ، ثم يغلق جانب الكتابة.

إذا لم تكن هناك بيانات متاحة، ينتظر الاستدعاء `read` على الأُنْبُوبِ إما كتابة بيانات أو إغلاق جميع وَاصِفَاتُ المَلَفَّاتِ التي تشير إلى طرف الكتابة؛
في الحالة الأخيرة، سيسترجع `read` الصفر، تمامًا كما لو تم الوصول إلى نهاية ملف بيانات.
إن حقيقة أن `read` يحجب (*blocking*) حتى يصبح من المستحيل وصول بيانات جديدة هي أحد أسباب أهمية إغلاق الابن لطرف الكتابة في الأُنْبُوبِ قبل تنفيذ `wc` أعلاه:
لو أشار أحد واصفات ملفات `wc` إلى طرف الكتابة للأنبوب، فلن يرى `wc` نهاية الملف أبداً.

ينفذ مغلّف أوامر xv6 السلاسل المترابطة مثل `grep fork sh.c | wc -l` بطريقة مشابهة للكود أعلاه.
تنقذ العَمَلِيَّة الابن أنبوبًا لربط الطرف الأيسر من السلسلة المترابطة بالطرف الأيمن.
ثم تستدعي `fork` و `runcmd` للطرف الأيسر من السلسلة المترابطة
و `fork` و `runcmd` للطرف الأيمن، وتنتظر حتى ينتهي كلاهما.
قد يكون الطرف الأيمن للسلسلة المترابطة أمرًا يتضمن في حد ذاته أنبوبًا (على سبيل المثال، `a | b | c`)،
والذي بدوره يتفرع إلى عمليتين ابن جديدتين (واحدة لـ `b` وواحدة لـ `c`).
وبذلك، قد ينشئ مغلّف الأوامر شجرة من العَمَلِيَّاتِ. أوراق هذه الشجرة هي الأوامر والعقد الداخلية هي عَمَلِيَّاتٌ تنتظر حتى ينتهي الأبناء الأيسر والأيمن.

قد لا تبدو الأَنَابِيبُ أكثر قوة من الملفات المؤقتة:
السلسلة المترابطة
#lstlisting[
[]
echo hello world | wc
]
يمكن تنفيذها بدون أنابيب كـ
#lstlisting[
[]
echo hello world >/tmp/xyz; wc </tmp/xyz
]
تمتلك الأَنَابِيبُ ثلاث ميزات على الأقل مقارنة بالملفات المؤقتة في هذا الموقف.
أولاً، تنظف الأَنَابِيبُ نفسها تلقائيًا؛
مع إعادة توجيه الملفات، سيتعين على مغلّف الأوامر الحرص على إزالة `/tmp/xyz` عند الانتهاء.
ثانيًا، يمكن للأنابيب تمرير تدفقات طويلة من البيانات بشكل تعسفي، بينما يتطلب إعادة توجيه الملفات مساحة خالية كافية على القرص لتخزين جميع البيانات.
ثالثًا، تسمح الأَنَابِيبُ بالتنفيذ المتوازي لمراحل السلسلة المترابطة، بينما يتطلب نهج الملفات انقضاء البرنامج الأول قبل أن يبدأ الثاني.

== نظام الملفات
<sec:filesystem>

يوفر نظام ملفات xv6 ملفات بيانات، والتي تحتوي على مصفوفات بايتات غير مفسرة،
ودلائل، والتي تحتوي على مراجع معنونة لملفات البيانات والدلائل الأخرى.
تشكل الدلائل شجرة، تبدأ عند دليل خاص يُسمى
_الجذر_ (Root).
مسار مثل `/a/b/c` يشير إلى الملف أو الدليل المسمى `c` داخل الدليل المسمى `b` داخل الدليل المسمى `a` في الدليل الجذر `/`.
المسارات التي لا تبدأ بـ `/` تُقيم بالنسبة إلى
_الدليل الحالي_
للعملية المداعية، والذي يمكن تغييره باستخدام اِسْتِدْعَاءُ النِّظَامِ `chdir`.
يفتح كلا مقطعي الكود هذين نفس الملف (بافتراض وجود جميع الدلائل المعنية):
#lstlisting[
[]
chdir("/a");
chdir("b");
open("c", O_RDONLY);

open("/a/b/c", O_RDONLY);
]
يغير المقطع الأول الدليل الحالي للعملية إلى `/a/b`؛
بينما لا يشير المقطع الثاني ولا يغير الدليل الحالي للعملية.

توجد استدعاءات نظام لإنشاء ملفات ودلائل جديدة:
ينشئ `mkdir` دليلًا جديدًا،
وينشئ `open` مع راية `O_CREATE` ملف بيانات جديدًا،
وينشئ `mknod` ملف جهاز عتادي جديدًا.
يوضح هذا المثال جميع الاستدعاءات الثلاثة:
#lstlisting[
[]
mkdir("/dir");
fd = open("/dir/file", O_CREATE|O_WRONLY);
close(fd);
mknod("/console", 1, 1);
]
ينشئ `mknod` ملفًا خاصًا يشير إلى جهاز.
ترتبط بملف الجهاز أرقام الأجهزة الرئيسية والفرعية (الوسيطان لـ `mknod`)،
والتي تحدد جهاز النَّوَاة بشكل فريد.
عندما تفتح عَمَلِيَّةٌ ملف جهاز لاحقًا، تحول النَّوَاة اِسْتِدْعَاءَاتُ النِّظَامِ `read` و `write` إلى تنفيذ جهاز النَّوَاة بدلاً من تمريرها إلى نظام الملفات.

اسم الملف متميز عن الملف نفسه؛
نفس الملف الأساسي، المسمى
_عقدة فهرسية_ (Inode)،
يمكن أن يكون له أسماء متعددة، تُسمى
_وصلات_ (Links).
تتكون كل وصلة من مدخل في دليل؛ يحتوي المدخل على اسم ملف ومرجع إلى عقدة فهرسية.
تحتفظ العُقْدَةُ الفَهْرَسِيَّةُ بـ
_البيانات الوصفية_ (Metadata)
عن الملف، بما في ذلك نوعه (ملف أو دليل أو جهاز)، وطوله، وموقع محتوى الملف على القرص، وعدد الوصلات للملف.

يجلب اِسْتِدْعَاءُ النِّظَامِ `fstat` المعلومات من العُقْدَةِ الفَهْرَسِيَّةِ التي يشير إليها وَاصِفُ المَلَفِّ.
ويملأ هيكل `struct stat` المكون في `stat.h` كـ:
#lstlisting[
[]
#define T_DIR     1   // Directory
#define T_FILE    2   // File
#define T_DEVICE  3   // Device

struct stat {
  int dev;     // File system's disk device
  uint ino;    // Inode number
  short type;  // Type of file
  short nlink; // Number of links to file
  uint64 size; // Size of file in bytes
};
]

ينشئ اِسْتِدْعَاءُ النِّظَامِ `link` اسمًا آخر في نظام الملفات يشير إلى نفس العُقْدَةُ الفَهْرَسِيَّةُ كملف موجود.
ينشئ هذا المقطع ملفًا جديدًا مسمى بـ `a` و `b`.
#lstlisting[
[]
open("a", O_CREATE|O_WRONLY);
link("a", "b");
]
القراءة من أو الكتابة إلى `a` هي نفسها القراءة من أو الكتابة إلى `b`.
تحدد كل عقدة فهرسية بواسطة _رقم_ _عقدة_ _فهرسية_ فريد.
بعد تسلسل الكود أعلاه، من الممكن تحديد أن `a` و `b` يشيران إلى نفس المحتويات الأساسية بفحص نتيجة `fstat`:
سيعيد كلاهما نفس رقم العُقْدَةُ الفَهْرَسِيَّةُ (`ino`)، وسيكون عدد `nlink` مضبوطًا على 2.

يزيل اِسْتِدْعَاءُ النِّظَامِ `unlink` اسمًا من نظام الملفات.
تُحرر العُقْدَةُ الفَهْرَسِيَّةُ للملف والمساحة على القرص التي تحتفظ بالمحتوى فقط عندما يكون عدد وصلات الملف صفرًا ولا تشير أي واصفات ملفات إليه.
وبذلك فإن إضافة
#lstlisting[
[]
unlink("a");
]
إلى تسلسل الكود الأخير يترك العُقْدَةُ الفَهْرَسِيَّةُ ومحتوى الملف متاحين كـ `b`.
علاوة على ذلك، فإن
#lstlisting[
[]
fd = open("/tmp/xyz", O_CREATE|O_RDWR);
unlink("/tmp/xyz");
]
هي طريقة نمطية لإنشاء عقدة فهرسية مؤقتة بدون اسم سيتم تنظيفها عندما تغلق العَمَلِيَّة `fd` أو تخرج.

يوفر Unix أدوات ملفات يمكن استدعاؤها من مغلّف الأوامر كبرامج على مستوى المستخدم، على سبيل المثال `mkdir`، و `ln`، و `rm`.
يسمح هذا التصميم لأي شخص بتوسيع واجهة سطر الأوامر عن طريق إضافة برامج جديدة على مستوى المستخدم.
في وقت لاحق يبدو هذا المخطط واضحًا، ولكن الأنظمة الأخرى التي تم تصميمها في وقت Unix غالبًا ما بَنَت مثل هذه الأوامر داخل مغلّف الأوامر (وبنت مغلّف الأوامر داخل النَّوَاةِ).

أحد الاستثناءات هو `cd`، المدمج في مغلّف الأوامر.
يجب أن يغير `cd` دليل العمل الحالي لمغلّف الأوامر نفسه.
لو تم تشغيل `cd` كأمر عادي، لفرع مغلّف الأوامر عَمَلِيَّةٌ ابن، ولشغل الابن `cd`، ولقام `cd` بتغيير دليل عمل *الابن*.
ولن يتغير دليل عمل الأب (أي مغلّف الأوامر).

== العالم الحقيقي
<sec:real_world>

كان الجمع بين وَاصِفَاتُ المَلَفَّاتِ "القياسية" والأَنَابِيبُ وبناء جملة مغلّف الأوامر المريح للعمليات عليها تقدمًا رئيسيًا في كتابة برامج عامة الغرض قابلة لإعادة الاستخدام.
أشعلت الفكرة ثقافة "أدوات البرمجيات" التي كانت مسؤولة عن جزء كبير من قوة Unix وشعبيته، وكان مغلّف الأوامر هو أول ما يسمى بـ "لغة النص البرمجي" (Scripting Language).
تستمر واجهة استدعاءات نظام Unix حتى اليوم في أنظمة مثل BSD و Linux و macOS.

تم توحيد واجهة استدعاءات نظام Unix من خلال معيار واجهة نظام التشغيل المحمول (POSIX).
Xv6 *ليس* متوافقًا مع POSIX: فهو يفتقر إلى العديد من اِسْتِدْعَاءَاتُ النِّظَامِ (بما في ذلك الاستدعاءات الأساسية مثل `lseek`)، وتختلف العديد من اِسْتِدْعَاءَاتُ النِّظَامِ التي يوفرها عن المعيار.
أهدافنا الرئيسية لـ xv6 هي البساطة ووضوح الكود مع تقديم واجهة استدعاءات نظام شبيهة بـ UNIX.
قام العديد من الأشخاص بتوسيع xv6 ببضعة استدعاءات نظام إضافية ومكتبة C بسيطة لتشغيل برامج Unix الأساسية.
ومع ذلك، توفر النوى الحديثة استدعاءات نظام أكثر بكثير، وأنواعًا أكثر بكثير من خدمات النَّوَاة، مقارنة بـ xv6.
على سبيل المثال، فهي تدعم الشبكات، وأنظمة النوافذ، وخيوط المستخدم، وبرامج تشغيل العديد من الأجهزة، وما إلى ذلك.
تتطور النوى الحديثة باستمرار وبسرعة، وتقدم العديد من الميزات التي تتجاوز POSIX.

وحد Unix الوصول إلى أنواع متعددة من الموارد (الملفات، والدلائل، والأجهزة) باستخدام مجموعة واحدة من واجهات أسماء الملفات ووَاصِفَاتُ المَلَفَّاتِ.
يمكن تعميم هذه الفكرة على المزيد من أنواع الموارد؛
ومثال جيد على ذلك نظام Plan 9~\cite{Presotto91plan9}، الذي طبق مفهوم "الموارد هي ملفات" على الشبكات والرومات والرسوميات والمزيد.
ومع ذلك، فإن معظم أنظمة التشغيل المشتقة من Unix لم تتبع هذا المسار.

كان نظام الملفات ووَاصِفَاتُ المَلَفَّاتِ تجريدات قوية.
ومع ذلك، هناك نماذج أخرى لواجهات أنظمة التشغيل.
قام Multics، وهو نظام سلف لـ Unix، بتجريد تخزين الملفات بطريقة جعلته يبدو كالذاكرة، مما أنتج طابعًا مختلفًا تمامًا للواجهة.
كان لتعقيد تصميم Multics تأثير مباشر على مصممي Unix، الذين هدفوا إلى بناء شيء أسرع وأبسط.

لا يوفر Xv6 مفهومًا للمستخدمين أو حماية مستخدم من آخر؛ بمصطلحات Unix، تعمل جميع عَمَلِيَّاتٌ xv6 كجذر (Root).

يتفحص هذا الكتاب كيف ينفذ xv6 واجهته الشبيهة بـ Unix، ولكن الأفكار والمفاهيم تنطبق على ما هو أكثر من مجرد Unix.
يجب على أي نظام تشغيل أن يحقق تعددية المشاركة للعمليات على العتاد المادي الأساسي، ويعزل العَمَلِيَّات عن بعضها البعض، ويوفر آليات للاتصال المحكوم بين العَمَلِيَّاتِ.
بعد دراسة xv6، يجب أن تكون قادرًا على النظر إلى أنظمة تشغيل أخرى أكثر تعقيدًا ورؤية المفاهيم الكامنة وراء xv6 في تلك الأنظمة أيضًا.

== تمارين
<sec:exercises>


+ اكتب برنامجًا يستخدم استدعاءات نظام UNIX لتبادل بايت بين عمليتين ("ping-pong") عبر زوج من الأَنَابِيبِ، واحد لكل اتجاه. قس أداء البرنامج بالتبادلات في الثانية.

