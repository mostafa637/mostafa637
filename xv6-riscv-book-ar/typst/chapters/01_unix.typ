#import "../template/listings.typ": *

= واجهات نظام التشغيل
<CH:UNIX>

وظيفة نظام التشغيل هي مشاركة حاسوب بين برامج متعددة وتوفير طقم نافع من الخدمات يفوق ما يدعمه العتاد بمفرده.
يدير نظام التشغيل العتاد منخفض المستوى ويجرده،
لكي لا يحتاج،
على سبيل المثال،
معالج نصوص للانشغال بأي نوع من عتاد القرص المعتمد.
يشارك نظام التشغيل العتاد بين برامج متعددة لكي تنفذ (أو تبدو أنها تنفذ) بداخل نفس الوقت.
أخيراً،
توفر نظم التشغيل طرقاً خاضعة للتحكم للبرامج للتفاعل،
لكي تتمكن من مشاركة البيانات أو العمل معاً.

يوفر نظام التشغيل الخدمات لبرامج المستخدم عبر واجهة.
تصميم واجهة جيدة يتبين كونه أمراً صعباً.
من ناحية،
نود أن تكون الواجهة ضيقة وبسيطة لأن ذلك يجعل من الأسهل جعل التنفيذ صحيحاً.
من ناحية أخرى،
قد نكون مغرين بتقديم العديد من الميزات المعقدة للتطبيقات.
الحيلة بداخل حل هذا التوتر هي تصميم واجهات تعتمد على بضع آليات يمكن دمجها لتوفير عمومية كبيرة.

يستخدم هذا الكتاب نظام تشغيل واحداً كمثال ملموس لتوضيح مفاهيم نظام التشغيل.
ذلك النظام،
`xv6`،
يوفر الواجهات الأساسية المقدمة بواسطة نظام تشغيل Unix الكائن لـ Ken Thompson و Dennis Ritchie،
فضلاً عن محاكاة التصميم الداخلي لـ Unix.
يوفر Unix واجهة ضيقة تندمج آلياتها جيداً،
مقدمة درجة مذهلة من العمومية.
كانت هذه الواجهة ناجحة جداً لدرجة أن نظم التشغيل الحديثة—BSD،
Linux،
macOS،
Solaris،
وحتى بدرجة أقل،
Microsoft Windows—تحوز واجهات شبيهة بـ Unix.
فهم `xv6` بداية جيدة نحو فهم أي من هذه النظم والنظم الأخرى الكثيرة.

كما يوضح الشكل @fig:os،
يأخذ `xv6` الشكل التقليدي لـ **النواة (Kernel)**،
وهو برنامج خاص يوفر الخدمات للبرامج المنفذة.
كل برنامج منفذ،
يُسمى **عملية (Process)**،
يحوز ذاكرة تحوي التعليمات،
البيانات،
والمكدس.
تنفذ التعليمات حسابات البرنامج.
البيانات هي المتغيرات التي تعمل عليها الحسابات.
ينظم المكدس استدعاءات إجراأت البرنامج.
حاسوب محدد تحوز عادة عمليات كثيرة ولكن نواة واحدة فقط.

عندما تحتاج عملية لاستدعاء خدمة من النواة،
تدعو **استدعاء نظام (System call)**،
واحداً من الاستدعاءات بداخل واجهة نظام التشغيل.
يدخل استدعاء النظام النواة؛
تنفذ النواة الخدمة وترجع.
وهكذا تتناوب العملية بين التنفيذ بداخل **مساحة المستخدم (User space)** و **مساحة النواة (Kernel space)**.

كما هو موصف بالتفصيل بداخل الفصول التالية،
تستخدم النواة آليات الحماية العتادية الموفرة بواسطة المعالج لضمان أن كل عملية تنفذ بداخل مساحة المستخدم يمكنها الوصول فقط لذاكرتها الخاصة.
تنفذ النواة بالامتيازات العتادية المطلوبة لتنفيذ هذه الحمايات؛
تنفذ برامج المستخدم بدون تلك الامتيازات.
عندما يدعو برنامج مستخدم استدعاء نظام،
يرفع العتاد مستوى الامتياز ويبدأ تنفيذ دالة مرتبة مسبقاً بداخل النواة.

#figure(image("fig/os.svg"), caption: [نواة وعمليتا مستخدم.]) <fig:os>

مجموعة استدعاءات النظام التي توفرها النواة هي الواجهة التي تشاهدها برامج المستخدم.
توفر نواة `xv6` طقماً فرعياً من الخدمات واستدعاءات النظام التي تقدمها نوى Unix تقليدياً.
يعرض الجدول @fig:api كافة استدعاءات النظام لـ `xv6`.

يرسم بقية هذا الفصل خدمات `xv6`---العمليات، الذاكرة، واصفات الملفات، الأنابيب، ونظام الملفات---ويوضحها بمقاطع شفرة ومناقشات لكيفية استخدام **موجه الأوامر (Shell)**، واجهة مستخدم سطر الأوامر بـ Unix، لها.
استخدام موجه الأوامر لاستدعاءات النظام يوضح مدى الحذر الذي صُممت به.

موجه الأوامر برنامج عادي يقرأ الأوامر من المستخدم وينفذها.
حقيقة كون موجه الأوامر برنامج مستخدم، وليس جزءاً من النواة، توضح قوة واجهة استدعاءات النظام: لا يوجد شيء خاص بخصوص موجه الأوامر.
كما يعني هذا أن موجه الأوامر سهل الاستبدال؛ ونتيجة لذلك فإن نظم Unix الحديثة تحوز تنوعاً من موجاهات الأوامر للاختيار من بينها، كل منها بواجهة مستخدمها وميزات برمجتها النصية.
موجه أوامر `xv6` تنفيذ بسيط لجوهر موجه أوامر Bourne بـ Unix.

يمكن العثور على تنفيذ موجه أوامر `xv6` بداخل `user/sh.c`.
ممارسة جيدة هي محاولة قراءة الشفرة المصدرية أولاً بمفردك ثم العودة لهذا الكتاب.
بحلول نهاية هذا الكتاب ينبغي أن تكون قادراً على فهم كل سطر من الشفرة المصدرية لـ `xv6` دون الحاجة لاستشارة هذا الكتاب.

== العمليات والذاكرة
<sec:processes_and_memory>

تتكون عملية `xv6` من ذاكرة مساحة مستخدم (تعليمات، بيانات، ومكدس) وحالة خاصة بكل عملية حصرية بالنواة.
تتقاسم `xv6` العمليات بالمشاركات الزمنية: تبدل شفافياً المعالجات المتاحة بين طقم العمليات المنتظرة للتنفيذ.
عندما لا تكون العملية تنفذ، تحفظ `xv6` سجلات المعالج للعملية، مستعادة إياها عندما تنفذ العملية تالياً.
تربط النواة معرف عملية، أو **PID**، مع كل عملية.

#figure(
  table(
    columns: (1fr, 2fr),
    align: (col, row) => if col == 0 { left } else { right },
    [*استدعاء النظام*], [*الوصف*],
    [`int fork()`], [إنشاء عملية، إرجاع PID الابن.],
    [`int exit(int status)`], [إنهاء العملية الحالية؛ إبلاغ الحالات لـ wait(). لا رجوع.],
    [`int wait(int *status)`], [الانتظار لخروج ابن؛ حالة الخروج بـ `*status`؛ إرجاع PID الابن.],
    [`int kill(int pid)`], [إنهاء العملية PID. إرجاع 0، أو -1 للخطأ.],
    [`int getpid()`], [إرجاع PID العملية الحالية.],
    [`int pause(int n)`], [إيقاف مؤقت لـ n دقة ساعة.],
    [`int exec(char *file, char *argv[])`], [تحميل ملف وتنفيده بمعاملات؛ يرجع فقط عند الخطأ.],
    [`char *sbrk(int n)`], [تنمية ذاكرة العملية بـ n بايت صفري. إرجاع بداية الذاكرة الجديدة.],
    [`int open(char *file, int flags)`], [فتح ملف؛ العلامات تشير للقراءة/الكتابة؛ إرجاع واصف ملف (fd).],
    [`int write(int fd, char *buf, int n)`], [كتابة n بايت من buf لواصف الملف fd؛ إرجاع n.],
    [`int read(int fd, char *buf, int n)`], [قراءة n بايت بداخل buf؛ إرجاع عدد المقروء؛ أو 0 لنهاية الملف.],
    [`int close(int fd)`], [تحرير الملف المفتوح fd.],
    [`int dup(int fd)`], [إرجاع واصف ملف جديد يشير لنفس الملف كـ fd.],
    [`int pipe(int p[])`], [إنشاء أنبوب، وضع واصفات ملفات القراءة/الكتابة بـ p[0] و p[1].],
    [`int chdir(char *dir)`], [تغيير المجلد الحالي.],
    [`int mkdir(char *dir)`], [إنشاء مجلد جديد.],
    [`int mknod(char *file, int, int)`], [إنشاء ملف جهاز.],
    [`int fstat(int fd, struct stat *st)`], [وضع معلومات عن ملف مفتوح بداخل `*st`.],
    [`int link(char *file1, char *file2)`], [إنشاء اسم آخر (file2) للملف file1.],
    [`int unlink(char *file)`], [إزالة ملف.]
  ),
  caption:
[استدعاءات نظام xv6.
ما لم يُذكر غير ذلك،
ترجع 0 لعدم وجود خطأ،
و -1 بداخل حالة الخطأ.]
) <fig:api>

قد تنشئ عملية ما عملية جديدة باستخدام استدعاء النظام `fork`.
تعطي `fork` العملية الجديدة نسخة متطابقة من ذاكرة العملية المستدعية:
تنسخ `fork` التعليمات،
البيانات،
والمكدس للعملية المستدعية بداخل ذاكرة العملية الجديدة.
ترجع `fork` بداخل كل من العمليتين الأصلية والجديدة.
بداخل العملية الأصلية ترجع `fork` PID العملية الجديدة.
بداخل العملية الجديدة ترجع `fork` صفراً.
تُسمى العمليتان الأصلية والجديدة غالباً بـ **الأب** و **الابن**.

على سبيل المثال،
راجع المقطع البرمجي التالي المكتوب بلغة البرمجة C:

```c
int pid = fork();
if(pid > 0){
  printf("parent: child=%d\n", pid);
  pid = wait((int *) 0);
  printf("child %d is done\n", pid);
} else if(pid == 0){
  printf("child: exiting\n");
  exit(0);
} else {
  printf("fork error\n");
}
```

يتسبب استدعاء النظام `exit` بداخل إيقاف العملية المستدعية عن التنفيذ وتحرير الموارد مثل الذاكرة والملفات المفتوحة.
يأخذ Exit معامل حالة كعدد صحيح،
تقليدياً 0 للإشارة للنجاح و 1 للإشارة للفشل.
يرجع استدعاء النظام `wait` PID ابن خارج (أو مقتول) للعملية الحالية وينسخ حالة خروج الابن للعنوان الممرر لـ wait؛
إذا لم يكن أي من أبناء المستدعي قد خرج،
تنتظر `wait` واحداً ليفعل ذلك.
إذا لم يحز المستدعي أبناء،
ترجع `wait` فورياً -1.
إذا كان الأب لا يهتم بحالة خروج ابن،
يمكنه تمرير عنوان 0 لـ `wait`.

بداخل المثال،
أسطر المخرجات:

```text
parent: child=1234
child: exiting
```

قد تخرج بداخل أي من الترتيبين (أو حتى مختلطة)،
اعتماداً على ما إذا كان الأب أو الابن يصل لاستدعائه لـ `printf` أولاً.
بعد خروج الابن،
يرجع `wait` الأب،
متسبباً بداخل جعل الأب يطبع:

```text
parent: child 1234 is done
```

على الرغم من أن الابن يبدأ بنسخة من ذاكرة الأب،
إلا أن الأب والابن ينفذان بذاكرة منفصلة وسجلات منفصلة:
تغيير متغير بداخل أحدهما لا يؤثر على الآخر.
على سبيل المثال،
عندما تُخزن القيمة المرجوعة لـ `wait` بداخل `pid` بداخل عملية الأب،
فإن ذلك لا يغير المتغير `pid` بداخل الابن.
قيمة `pid` بداخل الابن ستظل صفراً.

استدعاء النظام `exec` يستبدل ذاكرة العملية المستدعية بصورة ذاكرة جديدة ممررة من ملف مخزن بداخل نظام الملفات.
يجب أن يحوز الملف طرازاً محدداً،
يحدد أي جزء من الملف يحوي التعليمات،
أي جزء هو البيانات،
عند أية تعليمة يبدأ،
إلخ.
تستخدم `xv6` طراز ELF،
والذي يناقشه الفصل @CH:MEM بتفصيل أكثر.
عادة ما يكون الملف نتيجة ترجمة شفرة مصدرية لبرنامج.
عندما تنجح `exec` فإنها لا ترجع للبرنامج المستدعي؛
بدلاً من ذلك تبدأ التعليمات المحملة من الملف التنفيذ عند نقطة الدخول المعلنة بداخل رأس ELF.
تأخذ `exec` معاملين:
اسم الملف الحاوي للملف التنفيذي ومصفوفة من المعاملات النصية.
على سبيل المثال:

```c
char *argv[3];

argv[0] = "echo";
argv[1] = "hello";
argv[2] = 0;
exec("/bin/echo", argv);
printf("exec error\n");
```

هذا المقطع يستبدل البرنامج المستدعي بحالة من البرنامج `/bin/echo` ينفذ مع قائمة المعاملات `echo` `hello`.
معظم البرامج تتجاهل العنصر الأول من مصفوفة المعاملات،
والذي يكون تقليدياً اسم البرنامج.

يستخدم موجه أوامر `xv6` الاستدعاءات أعلاه لتشغيل البرامج نيابة عن المستخدمين.
التركيب الرئيسي لموجه الأوامر بسيط؛
انظر `main` بداخل `user/sh.c`.
تقرأ الحلقة التكرارية الرئيسية سطراً من المدخلات من المستخدم بـ `getcmd`.
ثم تدعو `fork` التي تنشئ نسخة من عملية موجه الأوامر.
يدعو الأب `wait` بينما ينفذ الابن الأمر.
على سبيل المثال لو كتب المستخدم `echo hello` لموجه الأوامر،
لكانت `runcmd` قد دُعيت مع `echo hello` كـ معامل.
تنفذ `runcmd` الأمر الحقيقي.
لـ `echo hello` لكانت قد دعت `exec`.
إذا نجحت `exec` فإن الابن سينفذ التعليمات من `echo` بدلاً من `runcmd`.
بداخل نقطة ما ستدعو `echo` الدالة `exit` والتي ستتسبب بداخل جعل الأب يرجع من `wait` بداخل `main`.

قد تتساءل لماذا لا يندمج `fork` و `exec` بداخل استدعاء واحد؛
سنرى لاحقاً أن موجه الأوامر يستغل الفصل بداخل تنفيذه لإعادة توجيه الإدخال/الإخراج.
لتجنب هدر إنشاء عملية مكررة ثم استبدالها فورياً (بـ `exec`)،
تحسن نوى التشغيل تنفيذ `fork` لهذه حالة الاستخدام باستخدام تقنيات الذاكرة الافتراضية مثل النسخ عند الكتابة (انظر الفصل @CH:PGFAULTS).

تخصص `xv6` معظم ذاكرة مساحة المستخدم ضمناً:
تخصص `fork` الذاكرة المطلوبة لنسخة الابن من ذاكرة الأب،
وتخصص `exec` ذاكرة كافية لاحتجاز الملف التنفيذي.
العملية التي تحتاج ذاكرة أكثر بداخل وقت التنفيذ (ربما لـ `malloc`) يمكنها دعوة `sbrk(n)` لتنمية ذاكرة بياناتها بمقدار `n` بايت صفري؛
ترجع `sbrk` موقع الذاكرة الجديدة.

== الإدخال/الإخراج وواصفات الملفات
<sec:io_and_file_descriptors>

**واصف الملف (File descriptor)** هو عدد صحيح صغير يمثل كائناً تدار بواسطة النواة يمكن للعملية القراءة منه أو الكتابة إليه.
قد تحصل العملية على واصف ملف عبر فتح ملف،
مجلد،
أو جهاز،
أو عبر إنشاء أنبوب،
أو عبر مضاعفة واصف قائم.
للتبسيط سنشير غالباً للكائن الذي يشير إليه واصف الملف كـ "ملف"؛
واجهة واصف الملف تجرد الفروق بين الملفات،
الأنابيب،
والأجهزة،
جاعلة إياها جميعاً تبدو كتدفقات من البايتات.
سنشير للإدخال والمخرجات كـ **I/O**.

داخلياً تستخدم نواة `xv6` واصف الملف كـ فهرس بداخل جدول خاص بكل عملية،
لكي تحوز كل عملية مساحة خاصة من واصفات الملفات تبدأ عند الصفر.
تقليدياً تقرأ العملية من واصف الملف 0 (المدخل المعياري)،
وتكتب المخرجات لواصف الملف 1 (المخرج المعياري)،
وتكتب رسائل الأخطاء لواصف الملف 2 (الخطأ المعياري).
كما سنرى،
يستغل موجه الأوامر هذا العرف لتنفيذ إعادة توجيه الإدخال/الإخراج والأنابيب.
يضمن موجه الأوامر حوزته دائماً لثلاثة واصفات ملفات مفتوحة،
والتي تكون افتراضياً واصفات ملفات للوحة التحكم.

يقرأ استدعاءا النظام `read` و `write` بايتات من ويكتبان بايتات إلى ملفات مفتوحة مسماة بواسطة واصفات الملفات.
الاستدعاء `read(fd, buf, n)` يقرأ بحد أقصى `n` بايت من واصف الملف `fd` وينسخها بداخل `buf` ويرجع عدد البايتات المقروءة.
كل واصف ملف يشير لملف يحوز إزاحة مرتبطة به.
تقرأ `read` البيانات من إزاحة الملف الحالية ثم تقدم تلك الإزاحة بعدد البايتات المقروءة:
قراءة لاحقة ترجع البايتات التالية لتلك المرجوعة بواسطة القراءة الأولى.
عندما لا تتوفر بايتات أخرى للقراءة،
ترجع `read` صفراً للإشارة لنهاية الملف.

الاستدعاء `write(fd, buf, n)` يكتب `n` بايت من `buf` لواصف الملف `fd` ويرجع عدد البايتات المكتوبة.
يُكتب أقل من `n` بايت فقط عند وقوع خطأ.
مثل `read` تكتب `write` البيانات عند إزاحة الملف الحالية ثم تقدم تلك الإزاحة بعدد البايتات المكتوبة:
كل `write` تبدأ من حيث انتهت السابقة.

المقطع البرمجي التالي (والذي يشكل جوهر البرنامج `cat`) ينسخ البيانات من مدخله المعياري لمخرجه المعياري.
إذا وقع خطأ يكتب رسالة للخطأ المعياري.

```c
char buf[512];
int n;

for(;;){
  n = read(0, buf, sizeof buf);
  if(n == 0)
    break;
  if(n < 0){
    fprintf(2, "read error\n");
    exit(1);
  }
  if(write(1, buf, n) != n){
    fprintf(2, "write error\n");
    exit(1);
  }
}
```

الشيء الهام الملاحظة بداخل المقطع البرمجي هو أن `cat` لا تعلم ما إذا كانت تقرأ من ملف،
لوحة تحكم،
أو أنبوب.
بالمثل لا تعلم `cat` ما إذا كانت تطبع للوحة تحكم،
ملف،
أو أي شيء.
استخدام واصفات الملفات والعرف بأن واصف الملف 0 هو المدخل وواصف الملف 1 هو المخرج يتيح تنفيذاً بسيطاً لـ `cat`.

استدعاء النظام `close` يحرر واصف ملف،
جاعلاً إياه حراً لإعادة الاستخدام بواسطة استدعاء نظام `open` أو `pipe` أو `dup` مستقبلي.
واصف الملف المخصص حديثاً يكون دائماً هو الواصف غير المستخدم الأقل رقماً للعملية الحالية.

واصفات الملفات و `fork` يتفاعلان لجعل إعادة توجيه الإدخال/الإخراج سهلة التنفيذ.
تنسخ `fork` جدول واصفات ملفات الأب جنباً إلى جنب مع ذاكرته،
لكي يبدأ الابن بنفس الملفات المفتوحة بالضبط كالأب.
استدعاء النظام `exec` يستبدل ذاكرة العملية المستدعية لكنه يحتفظ بجدول ملفاتها.
هذا السلوك يتيح لموجه الأوامر تنفيذ **إعادة توجيه الإدخال/الإخراج (I/O redirection)** عبر الاستدعاء لـ fork،
إغلاق وإعادة فتح واصفات ملفات مختارة بداخل الابن،
ثم دعوة `exec` لتشغيل البرنامج الجديد.
فيما يلي نسخة مبسطة من الشفرة التي ينفذها موجه الأوامر للأمر `cat < input.txt`:

```c
char *argv[2];

argv[0] = "cat";
argv[1] = 0;
if(fork() == 0) {
  close(0);
  open("input.txt", O_RDONLY);
  exec("cat", argv);
}
```

بعد أن يغلق الابن واصف الملف 0،
تضمن `open` استخدام ذلك الواصف للملف المفتوح حديثاً `input.txt`:
0 سيكون الواصف المتاح الأصغر.
تنفذ `cat` عندئذ مع واصف الملف 0 (المدخل المعياري) مشيراً لـ `input.txt`.
واصفات ملفات عملية الأب لا تتغير بهذا التسلسل،
نظراً لأنه يعدل فقط واصفات الابن.

شفرة إعادة توجيه الإدخال/الإخراج بداخل موجه أوامر `xv6` تعمل بنفس هذه الطريقة بالضبط.
تذكر أنه بداخل هذه النقطة بداخل الشفرة يكون موجه الأوامر قد استدعى بالفعل `fork` لالابن وأن `runcmd` ستدعو `exec` لتحميل البرنامج الجديد.

المعامل الثاني لـ `open` يتكون من طقم من العلامات،
معبر عنها كـ بتات،
للتحكم بداخل ما تفعله `open`.
القيم الممكنة معرفة بداخل رأس fcntl:
`O_RDONLY`, `O_WRONLY`, `O_RDWR`, `O_CREATE`, `O_TRUNC` والتي توجه `open` لفتح الملف للقراءة،
أو الكتابة،
أو للقراءة والكتابة،
إنشاء الملف إذا لم يكن موجوداً،
وقطع الملف لـ صفر طول.

الآن ينبغي أن يكون واضحاً لماذا يُعد نافعاً أن تكون `fork` و `exec` استدعاءين منفصلين:
بينهما يحوز موجه الأوامر فرصة لإعادة توجيه إدخال/إخاج الابن دون اضطراب إعداد إدخال/إخراج موجه الأوامر الرئيسي.
يمكن للمرء بدلاً من ذلك تخيل استدعاء نظام افتراضي مدمج `forkexec` لكن خيارات القيام بإعادة توجيه الإدخال/الإخراج مع مثل هذا الاستدعاء تبدو غير ملائمة.

على الرغم من أن `fork` تنسخ جدول واصفات الملفات،
فإن كل إزاحة ملف تحتية تكون مشتركة بين الأب والابن.
راجع هذا المثال:

```c
if(fork() == 0) {
  write(1, "hello ", 6);
  exit(0);
} else {
  wait(0);
  write(1, "world\n", 6);
}
```

بداخل نهاية هذا المقطع،
فإن الملف المربوط بواصف الملف 1 سيحتوي البيانات `hello world`.
الـ `write` بداخل الأب (والتي،
بفضل `wait` تنفذ فقط بعد انتهائه) تبدأ من حيث انتهت `write` الابن.
هذا السلوك يساعد بداخل إنتاج مخرجات متتالية من تسلسلات أوامر موجه الأوامر،
مثل `(echo hello; echo world) > output.txt`.

استدعاء النظام `dup` يضاعف واصف ملف قائم،
مرجعاً واصفاً جديداً يشير لنفس كائن الإدخال/الإخراج التحي.
كلا واصفي الملفات يتشاركان الإزاحة،
تماماً كما تفعل واصفات الملفات المضاعفة بواسطة `fork`.
هذه طريقة أخرى لكتابة `hello world` بداخل ملف:

```c
fd = dup(1);
write(1, "hello ", 6);
write(fd, "world\n", 6);
```

واصفا ملفات يتشاركان الإزاحة إذا كانا مشتقين من نفس واصف الملف الأصلي بتسلسل من استدعاءات `fork` و `dup`.
وإلا فإن واصفات الملفات لا تتشارك الإزاحات،
حتى وإن نتجت عن استدعاءات `open` لنفس الملف.
تتيح `dup` لموجهات الأوامر تنفيذ أوامر مثل هذا:
`ls existing-file non-existing-file > tmp1 2>&1`.
الـ `2>&1` تخبر موجه الأوامر بإعطاء الأمر واصف ملف 2 يكون مضاعفاً للواصف 1.
كلا اسم الملف القائم ورسالة الخطأ للملف غير القائم سيظهران بداخل الملف `tmp1`.
موجه أوامر `xv6` لا يدعم إعادة توجيه الإدخال/الإخراج لواصف ملف الخطأ،
لكنك تعلم الآن كيفية تنفيذه.

واصفات الملفات تجريد قوي،
لأنها تخفي تفاصيل ما هي متصلة به:
عملية تكتب لواصف الملف 1 قد تكون تكتب لملف،
لجهاز مثل لوحة التحكم،
أو لأنبوب.

== الأنابيب
<sec:pipes>

**الأنبوب (Pipe)** هو حاوي نواة صغير مخرج للعمليات كـ زوج من واصفات الملفات،
واحد للقراءة وواحد للكتابة.
كتابة البيانات لأحد طرفي الأنبوب تجعل تلك البيانات متاحة للقراءة من الطرف الآخر للأنبوب.
توفر الأنابيب طريقة للعمليات للتواصل.

مثال الشفرة التالي ينفذ البرنامج `wc` مع إدخال معياري متصل بطرف القراءة لأنبوب.

```c
int p[2];
char *argv[2];

argv[0] = "wc";
argv[1] = 0;

pipe(p);
if(fork() == 0) {
  close(0);
  dup(p[0]);
  close(p[0]);
  close(p[1]);
  exec("/bin/wc", argv);
} else {
  close(p[0]);
  write(p[1], "hello world\n", 12);
  close(p[1]);
}
```

يدعو البرنامج `pipe` التي تنشئ أنبوباً جديداً وتسجل واصفي ملفات القراءة والكتابة بداخل المصفوفة `p`.
بعد `fork` يحوز كل من الأب والابن واصفات ملفات تشير للأنبوب.
يدعو الابن `close` و `dup` لجعل واصف الملف صفر يشير لطرف القراءة للأنبوب،
ويغلق واصفات الملفات بـ `p` ويدعو `exec` لتشغيل `wc`.
عندما تقرأ `wc` من مدخلها المعياري،
فإنها تقرأ من الأنبوب.
يغلق الأب جانب القراءة للأنبوب،
ويكتب للأنبوب،
ثم يغلق جانب الكتابة.

إذا لم تتوفر بيانات،
فإن `read` على أنبوب تنتظر إما كتابة بيانات أو إغلاق كافة واصفات الملفات المشارية لطرف الكتابة؛
بداخل الحالة الأخيرة ترجع `read` صفراً،
تماماً كما لو أن نهاية ملف بيانات قد وصلت.
حقيقة أن `read` تحظر حتى يكون من المستحيل وصول بيانات جديدة هي أحد الأسباب بداخل كون الهام للابن إغلاق طرف الكتابة للأنبوب قبل تنفيذ `wc` أعلاه:
لو كان أحد واصفات ملفات `wc` يشير لطرف الكتابة للأنبوب فلن تشاهد `wc` نهاية الملف مطلقاً.

ينفذ موجه أوامر `xv6` الأنابيب مثل `grep fork sh.c | wc -l` بطريقة شبيهة بالشفرة أعلاه.
تنشئ عملية الابن أنبوباً لربط الطرف الأيسر من الأنبوب بالطرف الأيمن.
ثم تدعو `fork` و `runcmd` للطرف الأيسر و `fork` و `runcmd` للطرف الأيمن،
وتنتظر كلاهما لينهيا.
قد يكون الطرف الأيمن أمراً يتضمن هو نفسه أنبوباً (مثلاً `a | b | c`)،
والذي يحوز بدوره استدعائي fork لعمليتي ابن جديدتين (واحدة لـ b وواحدة لـ c).
وهكذا قد ينشئ موجه الأوامر شجرة من العمليات.
أوراق هذه الشجرة هي أوامر والعقد الداخلية هي عمليات تنتظر حتى يكتمل الأبناء الأيسر والأيمن.

قد تبدو الأنابيب ليست أكثر قوة من الملفات المؤقتة:
فالأنبوب `echo hello world | wc` يمكن تنفيذه بدون أنابيب كـ `echo hello world >/tmp/xyz; wc </tmp/xyz`.
تحوز الأنابيب ثلاث ميزات على الأقل مقارنة بالملفات المؤقتة بداخل هذه الحالة.
أولاً تنظف الأنابيب نفسها تلقائياً؛
مع إعادة توجيه الملف يتعين على موجه الأوامر الحذر لإزالة `/tmp/xyz` عند الانتهاء.
ثانياً يمكن للأنابيب تمرير تدفقات بيانات طويلة عشوائياً،
بينما تتطلب إعادة توجيه الملف مساحة حرة كافية على القرص لتخزين كافة البيانات.
ثالثاً تتيح الأنابيب التنفيذ المتوازي لمراحل الأنبوب،
بينما يتطلب نهج الملف أن ينهي البرنامج الأول قبل بدء الثاني.

== نظام الملفات
<sec:file_system>

يوفر نظام ملفات `xv6` ملفات البيانات،
التي تحوي مصفوفات بايتات غير مفسرة،
والدلائل،
التي تحوي مراجع مسماة لملفات بيانات ودلائل أخرى.
تشكل الدلائل شجرة تبدأ عند مجلد خاص يُسمى **الجذر (Root)**.
مسار مثل `/a/b/c` يشير للملف أو المجلد المسمى `c` بداخل المجلد المسمى `b` بداخل المجلد المسمى `a` بداخل المجلد الرئيسي `/`.
المسارات التي لا تبدأ بـ `/` تُقيم بالنسبة لـ **المجلد الحالي** للعملية المستدعية،
والذي يمكن تغييره باستدعاء النظام `chdir`.
كلا مقطعي الشفرة هذين يفتحان نفس الملف (بافتراض أن كافة الدلائل المتضمنة موجودة):

```c
chdir("/a");
chdir("b");
open("c", O_RDONLY);

open("/a/b/c", O_RDONLY);
```

المقطع الأول يغير المجلد الحالي للعملية لـ `/a/b`؛
الثاني لا يشير ولا يغير المجلد الحالي للعملية.

توجد استدعاءات نظام لإنشاء ملفات ودلائل جديدة:
`mkdir` تنشئ مجلداً جديداً،
`open` مع العلامة `O_CREATE` تنشئ ملف بيانات جديداً،
و `mknod` تنشئ ملف جهاز جديداً.
هذا المثال يوضح الثلاثة جميعاً:

```c
mkdir("/dir");
fd = open("/dir/file", O_CREATE|O_WRONLY);
close(fd);
mknod("/console", 1, 1);
```

تنشئ `mknod` ملفاً خاصاً يشير لجهاز.
المرتبط بملف الجهاز هما رقما الجهاز الرئيسي والفرعي (المعاملان لـ `mknod`)،
اللذان يحددان بفرادة جهاز النواة.
عندما تفتح عملية لاحقاً ملف جهاز،
تحول النواة استدعاءات النظام `read` و `write` لتنفيذ جهاز النواة بدلاً من تمريرها لنظام الملفات.

اسم الملف متميز عن الملف نفسه؛
نفس الملف التحتي،
المسمى **العقدة الفهرسية (Inode)**،
يمكن أن يحوز أسماء متعددة،
تُسمى **الروابط (Links)**.
كل رابط يتكون من مدخل بداخل مجلد؛
يحوي المدخل اسم ملف ومرجعاً لعقدة فهرسية.
تحتفظ العقدة الفهرسية بـ **بيانات وصفية (Metadata)** عن ملف،
يتضمن نوعه (ملف أو مجلد أو جهاز)،
طوله،
موقع محتوى الملف على القرص،
وعدد الروابط للملف.

استدعاء النظام `fstat` يستعيد معلومات من العقدة الفهرسية التي يشير إليها واصف ملف.
يملا هيكل `struct stat` المعرف بـ `stat.h` كـ:

```c
#define T_DIR     1   // Directory
#define T_FILE    2   // File
#define T_DEVICE  3   // Device

struct stat {
  int dev;     // File system's disk device
  uint ino;    // Inode number
  short type;  // Type of file
  short nlink; // Number of links to file
  uint64 size; // Size of file in bytes
};
```

استدعاء النظام `link` ينشئ اسم نظام ملفات آخر يشير لنفس العقدة الفهرسية لملف قائم.
هذا المقطع ينشئ ملفاً جديداً مسماً كلاً من `a` و `b`.

```c
open("a", O_CREATE|O_WRONLY);
link("a", "b");
```

القراءة من أو الكتابة لـ `a` هي نفسها القراءة من أو الكتابة لـ `b`.
كل عقدة فهرسية تحدد بـ **رقم عقدة فهرسية** فريد.
بعد تسلسل الشفرة أعلاه،
يكون من الممكن القرار بكون `a` و `b` يشيران لنفس المحتويات التحتية عبر فحص نتيجة `fstat`:
كلاهما سيرجعان نفس رقم العقدة الفهرسية (`ino`) وعداد `nlink` مضبوطاً لـ 2.

استدعاء النظام `unlink` يزيل اسماً من نظام الملفات.
العقدة الفهرسية للملف والمساحة القرصية الحائزة لمحتواه تُحرران فقط عندما يكون عداد روابط الملف صفراً ولا توجد واصفات ملفات تشير إليه.
وهكذا فإن إضافة `unlink("a");` لتسلسل الشفرة الأخير تترك العقدة الفهرسية ومحتوى الملف قابلي للوصول كـ `b`.
علاوة على ذلك:

```c
fd = open("/tmp/xyz", O_CREATE|O_RDWR);
unlink("/tmp/xyz");
```

طريقة اصطلاحية لإنشاء عقدة فهرسية مؤقتة دون اسم ستُنظف عندما تغلق العملية `fd` أو تخرج.

توفر Unix أدوات ملفات قابلة للدعوة من موجه الأوامر كبرامج مستوى مستخدم،
على سبيل المثال `mkdir` و `ln` و `rm`.
يتيح هذا التصميم لأي شخص توسيع واجهة سطر الأوامر عبر إضافة برامج مستوى مستخدم جديدة.
بإعادة النظر يبدو هذا المخطط واضحاً،
لكن النظم الأخرى المصممة بداخل وقت Unix كانت تبني غالباً مثل هذه الأوامر بداخل موجه الأوامر (وتبني موجه الأوامر بداخل النواة).

استثناء واحد هو `cd` المدمجة بداخل موجه الأوامر.
يجب على `cd` تغيير المجلد الحالي لموجه الأوامر نفسه.
لو نُفذت `cd` كأمر عادي،
لكان موجه الأوامر قد استدعى fork لعملية ابن،
ولنفذت عملية الابن `cd` ولغيرت `cd` المجلد الحالي لـ **الابن**.
المجلد الحالي للأب (أي موجه الأوامر) لن يتغير.

== العالم الحقيقي
<sec:real_world_unix>

دمج Unix لواصفات الملفات القياسية،
الأنابيب،
وبنية موجه الأوامر المريحة للعمليات عليها كان تقدماً رئيسياً بداخل كتابة برامج عامة الغرض قابلة لإعادة الاستخدام.
أشعلت الفكرة ثقافة "أدوات البرمجيات" التي كانت مسؤولة عن معظم قوة وشعبية Unix،
وكان موجه الأوامر أول ما يُسمى بـ "لغات البرمجة النصية".
واجهة استدعاءات نظام Unix تستمر اليوم بداخل نظم مثل BSD و Linux و macOS.

جرت معايرة واجهة استدعاءات نظام Unix عبر معيار POSIX.
`xv6` ليست متوافقة مع POSIX:
تفتقد استدعاءات نظام كثيرة (تتضمن الأساسية مثل `lseek`)،
والعديد من استدعاءات النظام التي توفرها تختلف عن المعيار.
أهدافنا الرئيسية لـ `xv6` هي البساطة والوضوح مع توفير واجهة استدعاءات نظام بسيطة شبيهة بـ UNIX.
قام العديد من الأشخاص بتوسيع `xv6` ببضعة استدعاءات نظام إضافية ومكتبة C بسيطة لتشغيل برامج Unix الأساسية.
النوى الحديثة توفر استدعاءات نظام أكثر بكثير،
وأنواعاً أكثر من خدمات النواة مقارنة بـ `xv6`.
على سبيل المثال تدعم الشبكات،
نظم النوافذ،
خيوط مستوى المستخدم،
برامج تشغيل لأجهزة كثيرة،
وما إلى ذلك.
تتطور النوى الحديثة باستمرار وسرعة،
وتوفر ميزات كثيرة يتجاوز POSIX.

وحدت Unix الوصول لأنواع متعددة من الموارد (الملفات،
الدلائل،
والأجهزة) بطقم واحد من واجهات أسماء الملفات وواصفات الملفات.
يمكن توسيع هذه الفكرة لأنواع أكثر من الموارد؛
مثال جيد هو Plan 9 الذي طبق مفهوم "الموارد ملفات" على الشبكات،
الرسوميات،
وأكثر.
ومع ذلك فإن معظم نظم التشغيل المشتقة من Unix لم تتبع هذا المسار.

كان نظام الملفات وواصفات الملفات تجريدات قوية.
ورغم ذلك توجد نماذج أخرى لواجهات نظم التشغيل.
Multics،
أسلاف Unix،
جردت تخزين الملفات بطريقة جعلته يبدو كالذاكرة،
منتجة طابعاً مختلفاً جداً للواجهة.
تعقيد تصميم Multics كان له أثر مباشر على مصممي Unix الذين هدفت بناؤهم لشيء أبسط.

لا توفر `xv6` مفهوماً للمستخدمين أو حماية مستخدم من آخر؛
بالترتيب لـ Unix تنفذ كافة عمليات `xv6` كـ root.

يفحص هذا الكتاب كيف تنفذ `xv6` واجهتها الشبيهة بـ Unix،
لكن الأفكار والمفاهيم تنطبق على ما هو أكثر من مجرد Unix.
أي نظام تشغيل يجب عليه تقاسم العمليات بالمشاركات الزمنية على العتاد التحتي،
عزل العمليات عن بعضها البعض،
وتوفير آليات للتواصل الخاضع للتحكم بين العمليات.
بعد دراسة `xv6` ينبغي لك القدرة على النظر بداخل نظم تشغيل أخرى أكثر تعقيداً ومشاهدة المفاهيم التحتية لـ `xv6` بداخل تلك النظم أيضاً.

== تمارين
<sec:unix_exercises>

1.
اكتب برنامجاً يستخدم استدعاءات نظام UNIX لتبادل بايت بين عمليتين عبر زوج من الأنابيب (ping-pong)،
واحد لكل اتجاه.
قس أداء البرنامج بداخل التبادلات بداخل الثانية.
