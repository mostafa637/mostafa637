#import "../template/listings.typ": *

= تنظيم نظام التشغيل
<CH:FIRST>

المطلب الرئيسي لنظام التشغيل هو دعم عدة أنشطة في الوقت الواحد.
على سبيل المثال،
يمكن استخدام استدعاءات النظام `fork` و `exec` من الفصل @CH:UNIX لبدء مترجم ومحرر نصوص كعمليات.
يجب على نظام التشغيل تقاسم الموارد **بالمشاركات الزمنية (Time-share)** مثل المعالجات والذاكرة بين هذه العمليات.
يجب على نظام التشغيل أيضاً الترتيب لـ **العزل (Isolation)** بين العمليات.
إذا احتوت إحدى العمليات على خطأ برمجي وتعطلت،
فلا ينبغي أن تؤثر على العمليات غير المرتبطة بها.
ومع ذلك،
فإن العزل الكامل قوي جداً،
نظراً لأنه ينبغي أن يكون بمقدور العمليات التفاعل قصداً؛
والأنابيب مثال على ذلك.
وهكذا يجب على نظام التشغيل تلبية ثلاثة متطلبات:
المضاعفة،
العزل،
والتفاعل.

يقدم هذا الفصل نظرة عامة عن كيفية تنظيم نظم التشغيل لتحقيق هذه المتطلبات الثلاثة.
يتبين وجود طرق عديدة القيام بذلك،
لكن هذا النص يركز على التصاميم السائدة التي تتمركز حول **النواة أحادية القالب (Monolithic Kernel)**،
والمستخدمة بواسطة العديد من نظم تشغيل Unix.
يقدم هذا الفصل أيضاً نظرة عامة عن عملية `xv6`،
وهي وحدة العزل بـ `xv6`.

تنفذ `xv6` على معالج RISC-V دقيق متعدد النوى (تُعنى بـ "متعدد النوى" في هذا النص المعالجات الفيزيائية المتعددة التي تتشارك الذاكرة ولكن تنفذ بالتوازي،
لكل منها طقم سجلاته الخاص.
يستخدم هذا النص أحياناً مصطلح **متعدد المعالجات** كمرادف لمتعدد النوى،
على الرغم من أن متعدد المعالجات قد يشير تحديداً لحاسوب بشرائح معالجات متعددة متميزة).
كثير من وظائفها منخفضة المستوى (على سبيل المثال،
تنفيذ العملية) محدد بـ RISC-V.
RISC-V هو معالج 64-بت،
و `xv6` مكتوبة بلغة C ذات طراز "LP64"،
مما يعني أن `long` و المؤشرات (`P`) في لغة البرمجة C تكون 64-بت،
ولكن `int` يكون 32-بت.
يفترض هذا الكتاب أن القارئ قام ببعض البرمجة على مستوى الآلة في بنية ما،
وسيقدم الأفكار المحددة بـ RISC-V عند ظهورها.
الوثائق الخاصة بـ ISA لمستوى المستخدم والبنية المتميزة هي الوثائق الكاملة.
يمكنك أيضاً الرجوع لكتاب "The RISC-V Reader:
An Open Architecture Atlas".

المعالج في الحاسوب الكامل محاط بعتاد مساند،
معظمه في شكل واجهات إدخال/إخراج.
`xv6` مكتوبة للعتاد المساند المحاكى بواسطة خيار qemu المسمى "-machine virt".
يتضمن هذا ذاكرة الوصول العشوائي RAM،
وذاكرة ROM حواية شفرة الإقلاع،
واتصال تسلسلي للوحة مفاتيح/شاشة المستخدم،
وقرصاً للتخزين.

== تجريد الموارد الفيزيائية
<sec:abstracting_resources>

السؤال الأول الذي قد يطرحه المرء عند مصادفة نظام تشغيل هو لماذا نحوزه أساساً؟
أي أنه يمكن تنفيذ استدعاءات النظام في الشكل القياسي كمكتبة،
ترتبط بها التطبيقات.
في هذه الخطة،
يمكن لكل تطبيق حوز مكتبته المخصصة لاحتياجاته.
يمكن للتطبيقات التفاعل مباشرة مع الموارد العتادية واستخدام تلك الموارد بأفضل طريقة للتطبيق (مثلاً،
لتحقيق أداء عالٍ أو متوقع).
بعض نظم التشغيل للأجهزة المدمجة أو النظم لحظية الاستجابة تُنظم بهذه الطريقة.

الجانب السلبي لنهج المكتبة هذا هو أنه إذا كان هناك أكثر من تطبيق ينفذ،
فيجب أن تكون التطبيقات حسنة السلوك.
على سبيل المثال،
يجب على كل تطبيق التخلي دورياً عن المعالج لكي تتيح للتطبيقات الأخرى التنفيذ.
مخطط المشاركة الزمنية التعاوني هذا قد يكون مقبولاً إذا كانت كافة التطبيقات تثق ببعضها البعض ولا تحوي أخطاء برمجية.
المعهود أكثر هو ألا تثق التطبيقات ببعضها البعض،
وتحوي أخطاء برمجية،
لذا يحتاج المرء لعزل أقوى مما يوفره المخطط التعاوني.

لتحقيق عزل قوي يُعد من النافع منع التطبيقات من الوصول المباشر للموارد العتادية الحساسة،
وبدلاً من ذلك تجريد الموارد في خدمات.
على سبيل المثال،
تتفاعل تطبيقات Unix مع التخزين فقط عبر استدعاءات نظام الملفات `open` و `read` و `write` و `close` بدلاً من قراءة وكتابة القرص مباشرة.
يوفر هذا للتطبيق راحة أسماء المسارات،
ويسمح لنظام التشغيل (الذي يوفر الواجهة) بإدارة القرص.
حتى إن لم يكن العزل هماً،
فإن البرامج التي تتفاعل قصداً (أو ترغب مجرداً في الابتعاد عن طريق بعضها البعض) يُرجح أن تجد نظام الملفات تجريداً أكثر راحة من الاستخدام المباشر للقرص.

بالمثل،
تبدل Unix شفافياً المعالجات العتادية بين العمليات،
حافزة ومستعادة حالة السجلات حسب الضرورة،
لكي لا تحتاج التطبيقات لتكون مدركة للمشاركة الزمنية.
تتيح هذه الشفافية لنظام التشغيل مشاركة المعالجات حتى وإن كانت بعض التطبيقات في حلقات تكرارية نهائية.

كمثال آخر،
تستخدم عمليات Unix الاستدعاء `exec` لبناء صورة ذاكرتها،
بدلاً من التفاعل المباشر مع الذاكرة الفيزيائية.
يتيح هذا لنظام التشغيل القرار بخصوص مكان وضع العملية في الذاكرة؛
وإذا كانت الذاكرة ضيقة،
فقد يخزن نظام التشغيل بعض بيانات العملية على القرص.
يوفر `exec` أيضاً للمستخدمين راحة نظام الملفات لتخزين صور البرامج التنفيذية.

عديد أشكال التفاعل بين عمليات Unix تقع عبر واصفات الملفات.
ليس فقط تجرد واصفات الملفات العديد من التفاصيل (مثلاً،
أين تُخزن البيانات في الأنبوب أو الملف)،
بل إنها معرفة بطريقة تبسط التفاعل.
على سبيل المثال،
إذا خرج تطبيق في أنبوب أو فشل،
فإن النواة تنشئ تلقائياً إشارة نهاية-الملف للعملية التالية في الأنبوب.

واجهة استدعاء النظام مصممة بحذر لتوفير راحة المبرمج واحتمالية العزل القوي في الوقت نفسه.
واجهة Unix ليست الطريقة الوحيدة لتجريد الموارد،
لكنها أثبتت كونها طريقة جيدة.

== نمط المستخدم، نمط المشرف، واستدعاءات النظام
<sec:user_supervisor_modes>

يتطلب العزل القوي حداً صلباً بين التطبيقات ونظام التشغيل.
لا ينبغي السماح للتطبيقات باضطراب تشغيل نظام التشغيل أو البرامج الأخرى،
حتى وإن احتوى التطبيق على خطأ برمجي أو كان خبيثاً.
لتحقيق عزل قوي،
يجب على نظام التشغيل الترتيب بحيث لا تتمكن التطبيقات من تعديل (أو حتى قراءة) هياكل بيانات نظام التشغيل وتعليماته وألا تتمكن التطبيقات من الوصول لذاكرة العمليات الأخرى.

توفر المعالجات دعماً عتادياً للعزل القوي.
على سبيل المثال،
يمتلك RISC-V ثلاثة مستويات امتياز تقيد ما يمكن للشفرة فعله:
**نمط الآلة (Machine mode)**،
**نمط المشرف (Supervisor mode)**،
و **نمط المستخدم (User mode)**.
التعليمات المنفذة في نمط الآلة تحوز الامتياز الكامل؛
يبدأ المعالج في نمط الآلة.
نمط الآلة مخصص أساساً لإعداد الحاسوب أثناء الإقلاع.
تنفذ `xv6` موجزاً في نمط الآلة ثم تتغير إلى نمط المشرف.

في نمط المشرف يُسمح للمعالج بتنفيذ **التعليمات المتميزة (Privileged instructions)**:
على سبيل المثال،
تفعيل وتعطيل المقاطعات،
قراءة وكتابة المسجل الحائز عنوان جدول الصفحات،
إلخ.
إذا حاول تطبيق في نمط المستخدم تنفيذ تعليمة متميزة،
فإن المعالج لا ينفذ التعليمة،
بل يقع في مصيدة للشفرة الخاصة في نمط المشرف التي يمكنها إنهاء التطبيق.
التطبيق يمكنه تنفيذ تعليمات نمط المستخدم فقط (مثلاً،
جمع الأرقام،
إلخ) ويُقال إنه ينفذ في **مساحة المستخدم (User space)**،
بينما البرمجيات في نمط المشرف يمكنها أيضاً تنفيذ التعليمات المتميزة ويُقال إنها تنفذ في **مساحة النواة (Kernel space)**.
البرمجيات التي تنفذ في مساحة النواة (أو في نمط المشرف) تُسمى **النواة (Kernel)**.

تتفاعل التطبيقات مع النواة عبر استدعاءات النظام مثل `read`.
لا يُسمح للتطبيقات بالاستدعاء المباشر لدوال النواة أو الوصول لذاكرة النواة.
يوفر RISC-V تعليمة `ecall` لاستدعاءات النظام؛
تبدل المعالج من نمط المستخدم إلى المشرف وتقفز لنقطة دخول محدودة بالنواة.
بمجرد تبديل المعالج لنمط المشرف،
يمكن للنواة التحقق من معاملات استدعاء النظام (مثلاً،
فحص ما إذا كان العنوان الممرر لاستدعاء النظام جزءاً من ذاكرة التطبيق)،
والقرار بخصوص ما إذا كان التطبيق مسموحاً له بالقيام بالعملية المطلوبة،
ثم رفضها أو تنفيذها.
من المهم أن تتحكم النواة بنقطة الدخول للانتقالات لنمط المشرف؛
فلو استطاع التطبيق القرار بنقطة دخول النواة،
لتمكن تطبيق خبيث،
على سبيل المثال،
من دخول النواة عند نقطة حيث يُتجاوز الفحص للمعاملات.

== تنظيم النواة
<sec:kernel_organization>

السؤال التصميمي الرئيسي هو ما هي الأجزاء من نظام التشغيل التي ينبغي أن تنفذ في نمط المشرف.
أحد الاحتمالات هو أن يستقر كامل نظام التشغيل في النواة،
لكي تنفذ كافة تنفيذات استدعاءات النظام في نمط المشرف.
هذا التنظيم يُسمى **النواة أحادية القالب (Monolithic kernel)**.

في التنظيم أحادي القالب،
يتكون كامل نظام التشغيل من برنامج واحد ينفذ في نمط المشرف.
أحد الأسباب لجعل هذا التنظيم مريحاً هو أن مصمم نظام التشغيل لا يتعين عليه تقسيم الشفرة إلى أجزاء تتطلب وأجزاء لا تتطلب امتيازات المشرف.
علاوة على ذلك،
يسهل للأجزاء المختلفة من نظام التشغيل التعاون،
نظراً لأنها أجزاء من برنامج واحد.
على سبيل المثال،
النواة أحادية القالب قد تتشارك مخزن كتل القرص المؤقت مع نظام الملفات ونظام الذاكرة الافتراضية.

الجانب السلبي هو أن النوى أحادية القالب تميل لأن تنمو ضخمة ومعقدة،
لكي لا يفهم أي مطور بمفرده كافة التفاعلات بين الأجزاء المختلفة من الشفرة؛
هذه وصفة للأخطاء البرمجية.
الخطأ البرمجي في النواة مزعج بصفة خاصة لأنه قد يتسبب في انهيار كامل الحاسوب،
أو يتسبب في تعطل العديد من التطبيقات،
أو يجعل كامل الحاسوب عرضة للهجمات الأمنية.

تهدف **النواة الدقيقة (Microkernel)** إلى تقليل وقوع الأخطاء البرمجية في النواة.
الفكرة هي وضع حد أدنى مطلق من الوظائف في النواة نفسها،
لكي تنفذ شفرة قليلة في نمط المشرف،
ولكي تكون النواة سهلة الفهم والتحليل للصحة.
معظم نظام التشغيل ينفذ كخوادم عمليات في مستوى المستخدم.
على سبيل المثال،
شفرة نظام الملفات ستنفذ كخادم عملية،
في نمط المستخدم بدلاً من نمط المشرف.

#figure(image("fig/mkernel.svg"), caption: [نواة دقيقة مع خادم نظام ملفات.]) <fig:mkernel>

يوضح الشكل @fig:mkernel تصميم النواة الدقيقة هذا. في الشكل ينفذ نظام الملفات كخادم عملية في مستوى المستخدم.
للسماح للتطبيقات بالتفاعل مع خادم الملفات، توفر النواة آلية تواصل بين العمليات لإرسال الرسائل من عملية في نمط المستخدم لأخرى.
على سبيل المثال، إذا أراد تطبيق مثل موجه الأوامر قراءة أو كتابة ملف، فإنه يرسل رسالة لخادم الملفات وينتظر الاستجابة.

في النواة الدقيقة، تتكون واجهة النواة من بضع دوال منخفضة المستوى لبدء التطبيقات، إرسال الرسائل، الوصول لعتاد الأجهزة، إلخ.
يتيح هذا التنظيم للنواة أن تكون بسيطة نسبياً، نظراً لأن معظم نظام التشغيل يستقر في خوادم مستوى المستخدم.

في العالم الحقيقي، النوى أحادية القالب والنوى الدقيقة كلاهما شائع.
العديد من نوى Unix أحادية القالب. على سبيل المثال يحوز Linux نواة أحادية القالب، على الرغم من أن بعض وظائف نظام التشغيل تنفذ كخوادم في مستوى المستخدم (مثل نظام النوافذ).
يوفر Linux أداءً عالياً للتطبيقات المكثفة لنظام التشغيل، جزئياً لأن النظم الفرعية للنواة يمكن ربطها بكثافة.

نظم التشغيل مثل Minix و L4 و QNX تُنظم كـ نواة دقيقة مع خوادم، وشهدت انتشاراً واسعاً في الإعدادات المدمجة.
متغير من L4، المسمى seL4، صغير بما يكفي لدرجة أنه جرى التحقق منه لأمان الذاكرة والخصائص الأمنية الأخرى.

هناك نقاش كبير بين مطوري نظم التشغيل بخصوص أيهما أفضل، لكن لا يوجد دليل قاطع في أحد الاتجاهين.
علاوة على ذلك، فإنه يعتمد كثيراً على ما يعنيه "أفضل": أداء أسرع، حجم شفرة أصغر، موثوقية النواة، موثوقية كامل نظام التشغيل (بما في ذلك خدمات مستوى المستخدم)، إلخ.

توجد أيضاً اعتبارات عملية قد تكون أكثر أهمية من سؤال أيهما أنسب.
بعض نظم التشغيل تحوز نواة دقيقة لكنها تنفذ بعض خدمات مستوى المستخدم في مساحة النواة لأسباب تتعلق بالأداء.
بعض نظم التشغيل تحوز نوى أحادية القالب لأن تلك هي الطريقة التي بدأت بها ويوجد دافع قليل للانتقال لتنظيم نواة دقيقة خالص، لأن الميزات الجديدة قد تكون أكثر أهمية من إعادة كتابة نظام التشغيل القائم ليلائم تصميم النواة الدقيقة.

من منظور هذا الكتاب، تتشارك النوى الدقيقة والنوى أحادية القالب العديد من الأفكار الرئيسية.
تنفذ استدعاءات النظام، وتستخدم جداول الصفحات، وتتعامل مع المقاطعات، وتدعم العمليات، وتستخدم الأقفال للتحكم في التزامن، وتنفذ نظام الملفات، إلخ.
يركز هذا الكتاب على هذه الأفكار الجوهرية.

تُنقذ `xv6` كـ نواة أحادية القالب، مثل معظم نظم تشغيل Unix.
وهكذا فإن واجهة نواة `xv6` تناظر واجهة نظام التشغيل، وتنفذ النواة كامل نظام التشغيل.
نظراً لأن `xv6` لا توفر خدمات كثيرة، فإن نواتها أصغر من بعض النوى الدقيقة، ولكن من الناحية المفهومية يُعد `xv6` أحادي القالب.

== الكود البرمجي: تنظيم xv6
<sec:code_xv6_organization>

تستقر شفرة المصدر لنواة `xv6` في المجلد الفرعي `kernel/`.
توزع الملفات مقسمة في المجالات الرئيسية لمسؤولية النواة: بدء النظام (الإقلاع)، إنشاء والتحكم في العمليات، معالجة المصايد (المقاطعات واستدعاءات النظام)، تخصيص الذاكرة وتكوين العناوين الافتراضية، التحكم في الأجهزة، وإدارة نظام الملفات.

== نظرة عامة على العملية
<sec:process_overview>

وحدة العزل بـ `xv6` (كما في نظم تشغيل Unix الأخرى) هي **العملية (Process)**.
تمنع مجرد العملية عملية ما من تخريب أو التجسس على ذاكرة، معالج، واصفات ملفات، إلخ، لعملية أخرى.
كما تمنع العملية من تخريب النواة نفسها، لكي لا تتمكن عملية من تقويض آليات العزل للنواة.
يجب على النواة تنفيذ مجرد العملية بحذر لأن تطبيقاً يحوي خطأ برمجي أو خبيثاً قد يخدع النواة أو العتاد للقيام بشيء سيئ (مثل الالتفاف على العزل).
الآليات المستخدمة بواسطة النواة لتنفيذ العمليات تتضمن علامة نمط المستخدم/المشرف، مساحات العناوين، والتجزئة الزمنية للخيوط.

للمساعدة في فرض العزل، يوفر مجرد العملية الوهم للبرنامج بأن له آليته الخاصة.
توفر العملية للبرنامج ما يبدو أنه نظام ذاكرة خاص، أو **مساحة عناوين (Address space)**، لا يمكن للعمليات الأخرى قراءته أو كتابته.
كما توفر العملية للبرنامج ما يبدو أنه معالجه الخاص لتنفيذ تعليمات البرنامج.

تستخدم `xv6` جداول الصفحات (المنفذة بواسطة العتاد) لإعطاء كل عملية مساحة عناوينها الخاصة.
يترجم جدول صفحات RISC-V (أو "يرسم") **العنوان الافتراضي (Virtual address)** (العنوان الذي تتعامل معه تعليمة RISC-V) إلى **عنوان فيزيائي (Physical address)** (عنوان يرسله المعالج للذاكرة الرئيسية).

#figure(image("fig/as.svg"), caption: [تخطيط الذاكرة الافتراضية للعملية.]) <fig:as>

تحتفظ `xv6` بجدول صفحات مستقل لكل عملية يحدد مساحة عناوين تلك العملية.
كما يوضح الشكل @fig:as، تتضمن مساحة العناوين ذاكرة المستخدم للعملية بدءاً من العنوان الافتراضي صفر.
تأتي التعليمات أولاً، تليها المتغيرات العامة، ثم المكدس، وأخيراً منطقة "الهيب" (لـ malloc) التي يمكن للعملية توسيعها حسب الحاجة.
توجد عوامل تحد من الحد الأقصى لحجم مساحة عناوين العملية: المؤشرات بـ RISC-V بعرض 64 بت؛ العتاد يستخدم فقط الـ 39 بت الأدنى عند البحث في العناوين الافتراضية في جداول الصفحات؛ وتستخدم `xv6` فقط 38 من تلك الـ 39 بت.
وهكذا فإن العنوان الأقصى هو $2^{38}-1 =$ "0x3fffffffff" وهو الثابت `MAXVA`.
في أعلى مساحة العناوين تضع `xv6` صفحة المنطاد `trampoline` (4096 بايت) وصفحة `trapframe`.
تستخدم `xv6` هاتين صفحتي للانتقال في النواة والعودة؛ تحوي صفحة المنطاد الشفرة للانتقال من وإلى النواة، وتكون صفحة `trapframe` حيث تحفظ النواة سجلات المستخدم للعملية، كما يشرح الفصل @CH:TRAP.

تحتفظ نواة `xv6` بأجزاء حالة عديدة لكل عملية، والتي تجمعها في `struct proc`.
أهم أجزاء حالة النواة للعملية هي جدول صفحاتها، مكدس نواتها، وحالة تشغيلها.
سنستخدم الترميز `p->xxx` للإشارة لعناصر هيكل `proc`؛ على سبيل المثال `p->pagetable` مؤشر لجدول صفحات العملية.

عند هذه النقطة يُرجى قراءة `kernel/proc.h` والذي يحدد `struct proc`.
شفرة `xv6` أكثر أهمية لفهمك من هذا الكتاب؛ ينبغي لك إعطاء الأولوية للشفرة واستشارة هذا الكتاب حسب الحاجة لتوضيح الشفرة.
غرض بعض الشفرة قد لا يكون ظاهراً في البداية، لكن القراءة والاستكشاف في الشفرة سيساعدان. لا تتردد في استكشاف وتعديل الشفرة.

لكل عملية خيط تنفيذ (أو **خيط Thread** اختصاراً) يحتفظ بالحالة المطلوبة لتنفيذ العملية.
في أي وقت محدد، قد يكون الخيط ينفذ على معالج، أو معلقاً (لا ينفذ، ولكن قادر على استئناف التنفيذ في المستقبل).
لتبديل معالج بين العمليات، تعطل النواة الخيط المنفذ حالياً على ذلك المعالج وتحفظ حالته، وتستعيد حالة خيط عملية أخرى معلق سابقاً.
كثير من حالة الخيط (المتغيرات المحلية، عناوين العودة لدوال الاستدعاء) تُخزن على مكدسات الخيط.
لكل عملية مكدسان: مكدس مستخدم ومكدس نواة (`p->kstack`).
عندما تنفذ العملية تعليمات مستخدم، يكون مكدس مستخدمها فقط قيد الاستخدام، ومكدس نواتها فارغاً.
عندما تدخل العملية النواة (لاستدعاء نظام أو مقاطعة)، تنفذ شفرة النواة على مكدس نواة العملية؛ بينما تكون العملية في النواة فإن مكدس مستخدمها يظل يحوي بيانات محفوظة، لكنه ليس قيد الاستخدام الفعال.
يتناوب خيط العملية بين الاستخدام الفعال لمكدس مستخدمه ومكدس نواته.
مكدس النواة منفصل (ومحمي من شفرة المستخدم) لكي تتمكن النواة من التنفيذ حتى وإن دمرت العملية مكدس مستخدمها.

يمكن لشفرة المستخدم للعملية تنفيذ استدعاء نظام عبر تنفيذ تعليمة `ecall` لـ RISC-V.
تبدل هذه التعليمة إلى نمط المشرف وتغير مؤشر البرنامج لنقطة دخول محدودة بالنواة.
تبدل الشفرة عند نقطة الدخول لمكدس نواة العملية وتنفذ تعليمات النواة التي تنفذ استدعاء النظام.
عندما يكتمل استدعاء النظام، ترجع النواة لمساحة المستخدم عبر تنفيذ تعليمة `sret` التي تبدل لنمط المستخدم وتستأنف تنفيذ تعليمات المستخدم بعد تعليمة استدعاء النظام مباشرة.
يمكن لخيط العملية أن يحظر في النواة للانتظار في الإدخال/الإخراج، ويستأنف من حيث توقف عندما يكتمل الإدخال/الإخراج.

يشير `p->state` إلى ما إذا كانت العملية مخصصة، جاهزة للتشغيل، تنفذ حالياً على معالج، تنتظر الإدخال/الإخراج، أو تخرج.

يحوي `p->pagetable` جدول صفحات العملية، في الشكل الذي يتوقعه عتاد RISC-V.
تتسبب `xv6` في جعل عتاد الصفحات يستخدم `p->pagetable` للعملية عند تنفيذ تلك العملية في مساحة المستخدم.
يعمل جدول صفحات العملية أيضاً كمسجل لعناوين الصفحات الفيزيائية المخصصة لتخزين ذاكرة العملية.

باختصار، تجمع العملية فكرتي تصميم: مساحة عناوين لإعطاء العملية الوهم بذاكرتها الخاصة، وخيطاً لإعطاء العملية الوهم بمعالجها الخاص.
بـ `xv6` تتكون العملية من مساحة عناوين واحدة وخيط واحد. في نظم التشغيل الحقيقية قد تحوز العملية أكثر من خيط للاستفادة من المعالجات المتعددة.

== الكود البرمجي: بدء تشغيل xv6 والعملية الأولى
<sec:code_starting_xv6>

لجعل `xv6` أكثر ملموسية، سنخطط كيف تبدأ النواة وتنفذ العملية الأولى.
ستصف الفصول التالية الآليات التي تظهر في هذه النظرة العامة بالتفصيل.
يُرجى قراءة `kernel/entry.S` و `kernel/start.c` و `kernel/main.c` و `user/init.c`.

عندما يعمل حاسوب RISC-V، يهيئ نفسه وينفذ برنامج الإقلاع المخزن في ذاكرة القراءة فقط ROM.
ينسخ برنامج الإقلاع نواة `xv6` في الذاكرة عند العنوان الفيزيائي `0x80000000`.
السبب في وضعه للنواة عند `0x80000000` بدلاً من `0x0` هو أن النطاق `0x0:0x80000000` يحوي أجهزة الإدخال/الإخراج.

ثم يقفز برنامج الإقلاع إلى `xv6` بدءاً من `_entry`.
يبدأ RISC-V مع تعطيل عتاد الصفحات: العناوين الافتراضية ترسم مباشرة لعناوين فيزيائية.
تعدل التعليمات بـ `_entry` مكدساً لكي تتمكن `xv6` من تنفيذ شفرة C.
تعلن `xv6` مساحة لهذا المكدس، `stack0` في الملف `start.c`.
تحمل الشفرة بـ `_entry` مسجل مؤشر المكدس `sp` بالعنوان `stack0+4096` وهو أعلى المكدس، لأن المكدس بـ RISC-V ينظر للأسفل.
الآن بعد أن حازت النواة مكدساً، تدعو `_entry` شفرة C بـ `start`.

تنفذ الدالة `start` بعض الإعدادات التي يتيحها المعالج فقط في نمط الآلة، وأهمها برمجة شريحة الساعة لتوليد مقاطعات الميقاتي.
ثم تستخدم `start` تعليمة `mret` لـ RISC-V للتبديل لنمط المشرف والقفز إلى `main`.
تتطلب `mret` بعض الإعداد: تضبط `start` النمط السابق للمشرف في المسجل `mstatus`، وتضبط عنوان الوجهة لـ `main` عبر كتابة عنوان `main` في المسجل `mepc`، وتعطل ترجمة العناوين الافتراضية في نمط المشرف عبر كتابة 0 في مسجل جدول الصفحات `satp`، وتفوض كافة المقاطعات والاستثناءات لنمط المشرف.

بعد أن تهيئ `main` عدة أجهزة ونظم فرعية، تنشئ العملية الأولى عبر دعوة `userinit`.
كافة العمليات المنشأة حديثاً تبدأ التنفيذ في النواة في `forkret`.
كحالة خاصة للعملية الأولى تدعو `forkret` الدالة `kexec` لتحميل برنامج المستخدم `/init`.

بعد دعوة `kexec` ترجع `forkret` لمساحة المستخدم في العملية `/init`.
تنشئ `init` ملف جهاز لوحة تحكم جديد إذا لزم الأمر ثم تفتحه كواصفات ملفات 0 و 1 و 2.
ثم تبدأ موجه الأوامر على لوحة التحكم. النظام يعمل الآن.

== نموذج الأمان
<sec:security_model>

قد تتساءل كيف يتعامل نظام التشغيل مع الشفرة التي تحوي أخطاء برمجية أو الخبيثة.
نظراً لأن مواجهة الخبث أصعب من التعامل مع الأخطاء غير المقصودة، فمن المنطقي التركيز أساساً على توفير الأمان ضد الخبث.
فيما يلي نظرة رفيعة المستوى للافتراضات والأهداف الأمنية المعهودة في تصميم نظام التشغيل.

يجب على نظام التشغيل افتراض أن شفرة مستوى المستخدم للعملية ستبذل قصارى جهدها لتخريب النواة أو العمليات الأخرى.
قد تحاول شفرة المستخدم فك إشارة مؤشرات خارج مساحة عناوينها المسموحة؛ وقد تحاول تنفيذ تعليمات ليست مخصصة لشفرة المستخدم؛ وقد تحاول قراءة وكتابة سجلات تحكم RISC-V؛ وقد تحاول الوصول لعتاد الأجهزة؛ وقد تمرر قيماً خبيثة لاستدعاءات النظام في محاولة لخداع النواة للانهيار أو القيام بشيء غبي.

هدف النواة هو تقييد كل عملية مستخدم لكي تتمكن فقط من الوصول لذاكرة مستخدمها الخاصة، استخدام الـ 32 مسجلاً عام الاستخدام بـ RISC-V، والتأثير على النواة والعمليات الأخرى بالطرق التي صُممت استدعاءات النظام للسماح بها.
يجب على النواة منع أية أفعال أخرى. هذه متطلبات مطلقة عادة في تصميم النواة.

التوقعات لشفرة النواة نفسها مختلفة. يُفترض أن شفرة النواة مكتوبة بواسطة مبرمجين حسني النية وحذرين، خالية من الأخطاء البرمجية، ولا تحوي أي شيء خبيث.
هذا الافتراض يؤثر على كيفية تحلينا لشفرة النواة.
على سبيل المثال، توجد دوال داخلية كثيرة في النواة (مثل أقفال الدوران) قد تتسبب في مشاكل خطيرة إذا استخدمتها شفرة النواة بأسلوب خاطئ.
نفترض، مع ذلك، أن النواة تستخدم دوالها الخاصة بأسلوب صحيح.
على مستوى العتاد يُفترض أن معالج RISC-V، RAM، القرص، إلخ يعملون كما مُعلن في الوثائق، بدون أخطاء عتادية.

الحياة الحقيقية ليست بسيطة. من الصعب منع برامج المستخدم المساوءة من دعوة استدعاءات النظام بأسلوب يجعل النظام غير قابل للاستخدام عبر استهلاك الموارد المحمية بالنواة: المساحة القرصية، وقت المعالج، خانات جدول العمليات، إلخ.
من المستحيل عادة كتابة شفرة نواة 100% خالية من الأخطاء أو تصميم عتاد خال من الأخطاء؛ وإذا كان كتاب الشفرات الخبيثة مدركين لأخطاء النواة أو العتاد، فسيستغلونها.
حتى في نوى ناضجة وشائعة الاستخدام مثل Linux يكتشف الناس غالباً ثغرات غير معروفة سابقاً.
أخيراً، التمييز بين شفرة المستخدم والنواة يكون غير واضح أحياناً: بعض عمليات مستوى المستخدم المتميزة قد توفر خدمات أساسية وتكون فعلياً جزءاً من نظام التشغيل، وفي بعض نظم التشغيل يمكن لشفرة المستخدم المتميزة إدخال شفرة جديدة في النواة (كما مع وحدات النواة القابلة للتحميل بـ Linux و eBPF).

كدفاع جزئي ضد أخطاء النواة البرمجية، تتضمن شفرة `xv6` فحوصات لعدم الاتساق والأخطاء غير القابلة للتعافي، وستنفذ الهلع "panic" استجابة لذلك، عبر دعوة `panic()`.
تطبع هذه الدالة رسالة خطأ وتوقف النظام. الهلع ليس مرغوباً، ولكنه أفضل من الاستمرار في التنفيذ.
ينتج الهلع عادة عن خطأ برمجي في النواة يتسبب في جعل بيانات النواة غير صحيحة أو يجعل النواة تنفذ فعلاً غير صالح مثل الإشارة لذاكرة غير موجودة؛ في مثل هذه الحالة يكون من الآمن إيقاف التنفيذ بـ `panic()` بدلاً من محاولة الاستمرار في حالة غير متسقة.
مطور النواة سيتفاعل مع الهلع عبر العمل للتعرف على الخطأ البرمجي التعتي وإصلاحه.

== العالم الحقيقي
<sec:real_world_first>

اعتمدت معظم نظم التشغيل مفهوم العملية، ومعظم العمليات تبدو شبيهة بـ `xv6`.
نظم التشغيل الحديثة تدعم خيوطاً متعددة في العملية الواحدة لتتيح لعملية واحدة الاستفادة من المعالجات المتعددة.
دعم خيوط متعددة في العملية يتضمن آليات كثيرة لا تحوزها `xv6` تتضمن غالباً تغييرات في الواجهة (مثل `clone` بـ Linux وهي متغير من `fork`) للتحكم في أية جوانب من العملية تتشاركها الخيوط.

== تمارين
<sec:first_exercises>

1. أضف استدعاء نظام لـ `xv6` يرجع مقدار الذاكرة الحرة المتاحة.
