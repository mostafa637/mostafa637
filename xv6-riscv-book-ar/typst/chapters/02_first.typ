#import "../template/listings.typ": *

= تنظيم نظام التشغيل
<CH:FIRST>

تتمثل إحدى المتطلبات الأساسية والتطبيقة في تصميم أنظمة التشغيل في تحقيق
\indextext{العزل القوي} (Strong Isolation).
يقتضي العزل منع العمليات من إتلاف أو التجسس أو التعديل على ذاكرة وحالات العمليات الأخرى،
وكذلك حماية نظام التشغيل نفسه من البرامج الخبيثة أو المعطوبة التي تعمل في فضاء المستخدم.
في هذا الفصل،
نبين كيف ينظم #lstinline("xv6") نظام التشغيل حول هذا الهدف،
والطريقة التي تعتمد بها النواة على العتاد المادي لتحقيق العزل،
وكيفية إقلاع النواة وتفريغ العملية الأولى.

== تجريد الموارد الفيزيائية
<sec:abstracting_resources>

منهجًا إبتدائيًا للتفاعل مع الموارد المادية هو السماح لبرامج التطبيقات بالوصول المباشر إلى العتاد الصلب.
على سبيل المثال،
يمكن لكل تطبيق استخدام تعليمات الذاكرة المباشرة أو الوصول لبايتات القرص الصلب.
ولكن هذا النهج المباشر يفشل في تقديم العزل؛
إذ يمكن لبرنامج معطوب أن يكتب فوق ذاكرة برنامج آخر أو يسيطر على القرص الصلب للوصول لمحتويات البرامج الأخرى.
وحتى بدون أخطاء،
فإن البرامج التي تتشارك العتاد المادي المباشر تجعل التطبيقات صعبة البناء للغاية.

لذلك،
يحل نظام التشغيل هذه المشاكل عن طريق حظر الوصول المباشر للعتاد،
وبدلاً من ذلك يقدم نظام التشغيل *تجريدات محكمة* (Abstractions) فوق العتاد المادي:
- بدلاً من السماح للبرامج بالوصول المباشر للذاكرة الفيزيائية، يوفر نظام التشغيل لكل عملية *فضاء العناوين الافتراضي* (Virtual Address Space) خاصًا بها.
- بدلاً من الوصول المباشر لقطاعات القرص الصلب، يقدم نظام التشغيل تجريد *نظام الملفات* (File System).
- بدلاً من التحكم المباشر بأجهزة الإدخال والإخراج، توفر النواة واجهة واصفات الملفات.
- بدلاً من التحكم المباشر بأنوية المعالج، يوفر نظام التشغيل تجريد *العملية* (Process) والجدولة الزمنية.

هذه التجريدات ليست فقط تجعل كتابة البرامج أسهل،
بل تضمن العزل المحكم؛
لأن البرامج تتفاعل فقط مع التجريدات النواتية وليس مع الموارد المادية المباشرة،
مما يتيح للنواة تفحص وتفتيش كل طلب ومنع أي تجاوز للحدود المسموح بها.

== وضع المستخدم، وضع المشرف، واستدعاءات النظام
<sec:user_supervisor_syscalls>

يتطلب تحقيق العزل القوي دعمًا صريحًا ومحددًا من عتاد وحدة المعالجة المركزية (CPU).
توفر معمارية \textsf{RISC-V} ثلاثة مستويات من الصلاحيات والأنماط التي ينفذ فيها المعالج التعليمات:
+ *وضع الآلة* (Machine Mode - M-mode): أعلى مستوى صلاحية، يمتلك وصولاً كاملاً وغير مشروط للعتاد المادي، ويُستخدم عادة لتهيئة العتاد عند الإقلاع الأول (مثل البرمجيات الثابتة Firmware / BIOS).
+ *وضع المشرف* (Supervisor Mode - S-mode): المستوى المخصص لنواة نظام التشغيل. يسمح بتنفيذ التعليمات الممتازة (Privileged Instructions) مثل تعديل سجلات إدارة الذاكرة وتفعيل المقاطعات وتحويل وضعيات المعالج.
+ *وضع المستخدم* (User Mode - U-mode): المستوى المخصص لبرامج المستخدم العادية. يُمنع المعالج في هذا الوضع من تنفيذ التعليمات الممتازة أو الوصول المباشر للسجلات العتادية الحساسة.

عندما تحاول عملية تعمل في وضع المستخدم تنفيذ تعليمة ممتازة (مثل تعديل سجل التحكم بالتصفح #lstinline("satp"))،
يرفض العتاد التعليمة ولا ينفذها ويولّد *استثناء* (Exception) يُحاط به فورًا وتنتقل السيطرة إلى معالج النواة.

للإنتقال المنظم من وضع المستخدم إلى وضع المشرف،
يوفر عتاد RISC-V تعليمة خاصة تُدعى #lstinline("ecall") (Environment Call).
عند تنفيذ #lstinline("ecall"):
+ يحفظ المعالج حالة البرنامج الحالية ويعيد رفع مستوى الصلاحية إلى وضع المشرف (S-mode).
+ يقفز المعالج إلى عنوان معالج المصايد (Trap Handler) المسجل مسبقًا في السجل الممتاز #lstinline("stvec").
+ تنفذ النواة الخدمة المطلوبة وتتحقق من صحة المدخلات.
+ تعود النواة إلى وضع المستخدم باستخدام تعليمة #lstinline("sret") (Supervisor Return).

== تنظيم النواة
<sec:kernel_org>

السؤال الهيكلي في تصميم نظام التشغيل هو:
ما الأجزاء التي ينبغي أن تعمل في وضع المشرف (S-mode) وتكون داخل النواة؟
تتوزع أنظمة التشغيل في هيكلة النواة إلى نمطين رئيسين:
- *النواة المتجانسة* (Monolithic Kernel): تنفذ جميع مكونات نظام التشغيل (إدارة الذاكرة، الجدولة، برامج تشغيل الأجهزة، نظام الملفات، الأنابيب، شبكة الاتصالات) داخل فضاء النواة وبصلاحيات كاملة (S-mode). يوفر هذا التصميم أداءً عاليًا وسرعة في استدعاءات الخدمات لأن جميع الخدمات تعمل في فضاء واحد ولا تتطلب تبديل السياق. يعتمد نظام #lstinline("xv6")، مثل Linux و FreeBSD، هيكلة النواة المتجانسة.
- *نواة مصغّرة* (Microkernel): تنفذ النواة الحد الأدنى من الخدمات (الجدولة، الاتصال بين العمليات IPC، إدارة الذاكرة المباشرة)، بينما تنفذ بقية الخدمات مثل نظام الملفات وبرامج تشغيل الأجهزة كعمليات عادية في فضاء المستخدم (U-mode). يزيد هذا التصميم من أمان النظام واستقراره لأن خلل برامج تشغيل الأجهزة لا ينهي النظام بأكمله، ولكنه يتطلب تكلفة إضافية في تبديل السياق والاتصالات بين العمليات.

== الكود البرمجي: هيكلة xv6
<sec:code_xv6_org>

تتواجد الشفرة المصدرية لنواة #lstinline("xv6") داخل الدليل #lstinline("kernel/").
وتُقسم إلى وحدات برمجية واضحة:
- #lstinline("kernel/main.c"): نقطة بداية تشغيل النواة عند الإقلاع وتوزيع المهام الأولية.
- #lstinline("kernel/proc.c"): إدارة العمليات، والجدولة، وإنشاء وتدمير العمليات، وتغيير السياق.
- #lstinline("kernel/vm.c"): إدارة الذاكرة الافتراضية وجداول الصفحات وتعيين العناوين.
- #lstinline("kernel/kalloc.c"): مخصص الذاكرة المادية والصفحات الحرة.
- #lstinline("kernel/trap.c"): معالجة المصايد، الاستثناءات، المقاطعات العتادية، واستدعاءات النظام.
- #lstinline("kernel/spinlock.c") و #lstinline("kernel/sleeplock.c"): أقفال التناوب وأقفال النوم للتزامن وحماية هياكل البيانات.
- #lstinline("kernel/bio.c") و #lstinline("kernel/fs.c") و #lstinline("kernel/log.c"): طبقات المخزن المؤقت ونظام الملفات وسجل المعاملات.
- #lstinline("kernel/file.c") و #lstinline("kernel/pipe.c") و #lstinline("kernel/sysfile.c"): إدارة واصفات الملفات والأنابيب واستدعاءات نظام الملفات.
- #lstinline("kernel/uart.c") و #lstinline("kernel/virtio\_disk.c"): برامج تشغيل الأجهزة للمنفس التسلسلي UART والقرص المادي.

== نظرة عامة على العمليات
<sec:process_overview>

تُمثَّل كل عملية في #lstinline("xv6") بواسطة هيكل بيانات C يُدعى #lstinline("struct proc") (المعرف في #lstinline("kernel/proc.h")).
يحتفظ هذا الهيكل بجميع معلومات حالة العملية:
- حالة العملية (#lstinline("enum procstate")): هل هي #lstinline("UNUSED")، #lstinline("EMBRYO")، #lstinline("SLEEPING")، #lstinline("RUNNABLE")، #lstinline("RUNNING")، أو #lstinline("ZOMBIE").
- معرّف العملية (#lstinline("pid")).
- جدول الصفحات الخاص بالعملية (#lstinline("pagetable\_t pagetable")).
- المكدس النواتي الخاص بالعملية (#lstinline("uint64 kstack")).
- إطار المصيدة (#lstinline("struct trapframe *trapframe")) للحفظ والاسترجاع لسجلات المستخدم.
- سياق العملية (#lstinline("struct context context")) لحفظ سجلات النواة أثناء تبديل المجدول.
- قائمة الملفات المفتوحة (#lstinline("struct file *ofile[NOFILE]")).
- الدليل الحالي للعملية (#lstinline("struct inode *cwd")).

لكل عملية مكدسان اثنان:
مكدس المستخدم (User Stack) ومكدس النواة (Kernel Stack).
عندما تنفذ العملية تعليمات في فضاء المستخدم،
يُستخدم مكدس المستخدم.
وعندما تتنقل العملية إلى النواة عبر مصيدة أو استدعاء نظام،
تحول النواة التنفيذ فورًا إلى مكدس النواة الخاص بهذه العملية والمخصص في ذاكرة المشرف.

== الكود البرمجي: إقلاع xv6، العملية الأولى واستدعاء النظام
<sec:code_boot_first_proc>

عند تشغيل الحاسوب (أو محاكي QEMU):
+ يبدأ المعالج في تنفيذ كود الـ ROM الإقلاعي في وضع الآلة (M-mode)، والذي يحمل النواة إلى الذاكرة عند العنوان الفيزيائي #lstinline("0x80000000").
+ ينفذ المعالج كود التجميع في #lstinline("kernel/entry.S")، لتهيئ مكدس النواة الابتدائي لكل نوات معالجة (Hart).
+ تنتقل السيطرة إلى الدالة #lstinline("main()") في #lstinline("kernel/main.c") في وضع المشرف (S-mode).
+ تقوم النواة بتهيئة الأقفال، ووحدة إدارة الذاكرة، ومجمع المقاطعات PLIC، والذاكرة المخبئية.
+ تدعو #lstinline("main()") الدالة #lstinline("userinit()") لإنشاء أول عملية مستخدم.
+ تنفذ العملية الأولى كوداً مجمعاً صغيراً بداخل #lstinline("initcode.S") يتضمن استدعاء النظام #lstinline("exec("/init")") ليعمل برنامج البدء الرسمي الشارح لمغلف الأوامر #lstinline("sh").

== نموذج الأمان
<sec:security_model>

يفترض نموذج الأمان في #lstinline("xv6") أن النواة موثوقة تمامًا ومحصنة ضد الثغرات البرمجية،
بينما برامج المستخدم غير موثوقة ومفصولة تمامًا.
تقوم النواة بالتحقق من جميع العناوين الممررة من برامج المستخدم للتأكد من وقوعها ضمن النطاق المسموح به للعملية قبل إجراء القراءة أو الكتابة.

== العالم الحقيقي
<sec:real_world_first>

تستخدم أنظمة التشغيل الإنتاجية مثل Linux آليات أمان متطورة مثل مساحات الأسماء (Namespaces) والحاويات (Containers) وASLR (عشوائية تخطيط فضاء العناوين) لتعزيز العزل.
ومع ذلك،
تشترك جميع هذه الأنظمة مع #lstinline("xv6") في الأعمدة الثنائية ذاتها:
العزل العتادي بين وضع المستخدم ووضع المشرف،
واستخدام استدعاءات النظام المحكومة عبر التعليمة #lstinline("ecall").

== تمارين
<sec:exercises_first>

+ تتبع خطوات تنفيذ تعليمة #lstinline("ecall") بدءًا من وضع المستخدم وصولاً إلى معالج #lstinline("syscall()") في #lstinline("kernel/syscall.c").
+ أضف استدعاء نظام جديد باسم #lstinline("trace(mask)") يقوم بطباعة أسماء استدعاءات النظام المنفذة لاحقاً.