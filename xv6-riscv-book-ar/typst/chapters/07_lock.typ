#import "../template/listings.typ": *

= الأقفال
<CH:LOCK>

معظم النوى،
بما فيها `xv6`،
تداخل تنفيذ الأنشطة المتعددة.
أحد مصادر التداخل هو عتاد متعدد المعالجات:
حواسيب بمعالجات متعددة تنفذ بأسلوب مستقل،
مثل معالج RISC-V الخاص بـ `xv6`.
تتشارك هذه المعالجات المتعددة الذاكرة العشوائية الفيزيائية RAM،
وتستغل `xv6` المشاركة للحفاظ على هياكل بيانات النواة التي تقرؤها وتكتبها كافة المعالجات.
هذه المشاركة تطرح احتمالية قراءة معالج ما لهيكل بيانات بينما معالج آخر في منتصف تحديثه،
أو حتى تحديث معالجات متعددة لنفس البيانات في الوقت نفسه؛
وبدون تصميم حذر يُرجح أن يتسبب هذا الوصول المتوازي في إعطاء نتائج غير صحيحة أو هيكل بيانات مدمر.
حتى على نظام أحادي المعالج،
قد تبدل النواة المعالج بين عدد من الخيوط،
متسببة في جعل تنفيذها متداخلاً.
أخيراً،
فإن معالج مقاطعة جهاز الذي يعدل نفس البيانات كشفرة قابلة للقطع قد يلف البيانات إذا وقعت المقاطعة في نفس اللحظة التي تعدل فيها الشفرة المنقطعة البيانات أيضاً.
تشر كلمة **التزامن (Concurrency)** للحالات التي تتداخل فيها تدفقات تعليمات متعددة،
بفضل التوازي متعدد المعالجات،
تبديل الخيوط،
أو المقاطعات.

النوى ملأى ببيانات قابلة للوصول المتزامن.
على سبيل المثال،
معالجان يمكنهما الاستدعاء المتزامن لـ `kalloc` في الوقت نفسه،
وبالتالي السحب المتزامن من رأس القائمة الحرة.
يحب مصممو النوى السماح بكثرة من التزامن،
نظراً لأنه يزيد الأداء والاستجابة.
ومع ذلك يتعين عليهم إقناع أنفسهم بالصحة على الرغم من التزامن.
الاستراتيجيات الهادفة للصحة تحت التزامن،
والمجردات الداعمة لها،
تُسمى تقنيات **التحكم في التزامن (Concurrency control)**.

يركز هذا الفصل على تقنية تحكم في التزامن واسعة الاستخدام:
**القفل (Lock)**.
عاين،
فيما يلي كيف يبدو استخدام القفل في نواة `xv6`:

```c
struct spinlock lk;
...;
acquire(&lk);
... قراءة أو كتابة بعض البيانات المشتركة ...
release(&lk);
```

يوفر القفل الاستبعاد المتبادل،
متأكداً من أن معالجاً واحداً فقط في الوقت الواحد يمكنه احتجاز القفل.
إذا ربط المبرمج قفلاً مع كل عنصر بيانات مشترك،
وكانت الشفرة تحتجز دائماً القفل المرتبط عند استخدام عنصر ما،
فإن العنصر سيُستخدم بواسطة معالج واحد فقط في الوقت الواحد.
في هذه الحالة،
نقول إن القفل يحمي عنصر البيانات.
على الرغم من أن القفل آلية تحكم في التزامن سهلة الفهم،
إلا أنه قد يحد من الأداء عبر تسلسل العمليات المتزامنة على البيانات التي يحميها.

يشرح بقية هذا الفصل لماذا تحتاج `xv6` الأقفال،
كيف تنفذها `xv6` وكيف تستخدمها.

== حالات التنافس
<sec:races>

#figure(image("fig/smp.svg"), caption: [بنية SMP مبسطة.]) <fig:smp>

كمثال على لماذا نحتاج الأقفال، انظر لعمليتين بحوزتهما أبناء خارجون تدعوان استدعاء النظام `wait` على معالجين مختلفين.
تفرغ `wait` ذاكرة الابن.
وهكذا على كل معالج، ستدعو النواة `kfree` لتحرير صفحات ذاكرة الأبناء.
يحتفظ مخصص النواة بقائمة ترابطية للصفحات الحرة: تسحب `kalloc()` صفحة ذاكرة من القائمة، وتدفع `kfree()` صفحة في القائمة.
لأداء أفضل، قد نأمل أن الاستدعاءات لـ `kfree` لعمليتي الأب تنفذان بالتوازي دون حاجة أي منهما للانتظار للأخرى، لكن هذا لن يكون صحيحاً بناءً على تنفيذ `kfree` بـ `xv6`.

يوضح الشكل @fig:smp الإعداد بتفصيل أكثر: القائمة الترابطية للصفحات الحرة تتواجد في الذاكرة المشتركة بواسطة المعالجين، واللذان يتعاملان مع القائمة باستخدام تعليمات التحميل والتخزين.
(في الواقع المعالجات تحوز مخازن مؤقتة، لكن منطقياً تتصرف النظم متعددة المعالجات كما لو كانت هناك ذاكرة واحدة مشتركة).
لو لم تكن هناك طلبات متزامنة، لكان بمقدورك تنفيذ عملية دفع لداخل قائمة كالتالي:

```c
struct element {
  int data;
  struct element *next;
};

struct element *list = 0;

void
push(int data)
{
  struct element *l;

  l = malloc(sizeof *l);
  l->data = data;
  l->next = list;
  list = l;
}
```

#figure(image("fig/race.svg"), caption: [مثال لـ حالة تنافس.]) <fig:race>

هذا التنفيذ صحيح لو نُفذ بمعزل.
ومع ذلك، الشفرة ليست صحيحة لو تنفذت أكثر من نسخة بالتزامن.
لو نفذ معالجان `push` في الوقت نفسه، قد ينفذ كلاهما السطر `l->next = list` كما يوضح الشكل @fig:smp، قبل أن ينفذ أي منهما السطر `list = l` والذي ينتج عنه نتيجة غير صحيحة كما يوضح الشكل @fig:race.
سيتواجد عندئذ عنصرا قائمة مع ضبط `next` في كلاهما لنفس القيمة السابقة لـ `list`.
عندما يقع التعيينان لـ `list` في السطر الأخير، فإن الثاني سيكتب فوق الأول؛ والعنصر المتضمن في التعيين الأول سيضيع.

التحديث المفقود بالسطر الأخير هو مثال على **حالة تنافس (Race)**.
حالة التنافس هي موقف يُوصل فيه لموقع ذاكرة بالتزامن، ويكون وصول واحد على الأقل هو كتابة.
غالباً ما تكون حالة التنافس علامة على خطأ برمجي، إما تحديث مفقود (إذا كانت الوصولات كتابات) أو قراءة لهيكل بيانات غير مكتمل التحديث.
نتيجة حالة التنافس تعتمد على شفرة الآلة المولدة بواسطة المترجم، التوقيت للمعالجين المتضمنين، وكيفية ترتيب عمليتهما للذاكرة بواسطة نظام الذاكرة، مما قد يجعل الأخطاء الناتجة عن التنافس صعبة التكرار والاستكشاف.
على سبيل المثال إضافة عبارات طباعة أثناء استكشاف أخطاء `push` قد يغير توقيت التنفيذ بدرجة كافية لجعل التنافس يختفي.

الطريقة المعهودة لتجنب التنافس هي استخدام قفل.
تضمن الأقفال **الاستبعاد المتبادل (Mutual exclusion)**، لكي يتمكن معالج واحد فقط في الوقت الواحد من تنفيذ الأسطر الحساسة من `push`؛ هذا يجعل السيناريو أعلاه مستحيلاً.
النسخة المقفولة بأسلوب صحيح من الشفرة أعلاه تضيف مجرد بضع أسطر:

```c
struct element *list = 0;
struct lock listlock;

void
push(int data)
{
  struct element *l;
  l = malloc(sizeof *l);
  l->data = data;

  acquire(&listlock);
  l->next = list;
  list = l;
  release(&listlock);
}
```

متتالية التعليمات بين `acquire` و `release` تُسمى غالباً بـ **المقطع الحرج (Critical section)**.
يُقال إن القفل يحمي `list`.

عندما نقول إن قفلاً يحمي بيانات، فنحن نعني بفعالية أن القفل يحمي بعض طقم الثوابت الملازمة التي تنطبق على البيانات.
الثوابت الملازمة هي خصائص لهياكل البيانات المحافظ عليها عبر العمليات.
عادةً ما يعتمد السلوك الصحيح لعملية ما على كون الثوابت الملازمة صحيحة عندما تبدأ العملية.
قد تخرق العملية الثوابت الملازمة مؤقتاً ولكن يجب عليها إعادة إرثائها قبل الانتهاء.
على سبيل المثال، في حالة القائمة الترابطية فإن الثابت الملازم هو أن `list` تشير للعنصر الأول في القائمة وأن الحقل `next` لكل عنصر يشير للعنصر التالي.
تنفيذ `push` يخرق هذا الثابت الملازم مؤقتاً: في السطر `l->next = list` يشير `l` لعنصر القائمة التالي لكن `list` لا تشير لـ `l` بعد.
التنافس الذي فحصناه أعلاه وقع لأن معالجاً ثانياً نفذ شفرة اعتمدت على ثوابت القائمة الملازمة بينما كانت (مؤقتاً) مخروقة.
الاستخدام السليم للقفل يضمن أن معالجاً واحداً فقط في الوقت الواحد يمكنه العمل على هيكل البيانات في المقطع الحرج، لكي لا ينفذ أي معالج عملية لـ هيكل البيانات عندما تكون ثوابته الملازمة غير صالحة.

يمكنك التفكير في القفل كـ متسلسل للمقاطع الحرجة المتزامنة لكي تنفذ واحدة في الوقت الواحد، وبذلك تحافظ على الثوابت الملازمة (بافتراض كون المقاطع الحرجة صحيحة بمعزل).
يمكنك أيضاً التفكير في المقاطع الحرجة المحروسة بنفس القفل بكونها ذرية فيما يتعلق ببعضها البعض، لكي تشاهد كل منها فقط الطقم الكامل للتغيرات من المقاطع الحرجة الأبكر، ولا تشاهد مطلقاً تحديثات مكتملة جزئياً.

على الرغم من نفعها للصحة، فإن الأقفال تقيد بطبيعتها الأداء.
على سبيل المثال لو دعت عمليتان `kfree` بالتزامن، فإن الأقفال ستسلسل المقطعين الحرجين، لكي لا توجد فائدة من تشغيلهما على معالجات مختلفة.
نقول إن عمليات متعددة **تتضارب** إذا أرادت نفس القفل في الوقت نفسه، أو إن القفل يتعرض لـ **تنافس (Contention)**.
التحدي الرئيسي في تصميم النواة هو تجنب التنافس على الأقفال سعياً للتوازي.
تفعل `xv6` القليل من ذلك، لكن النوى المعقدة تنظم هياكل البيانات والخوارزميات تحديداً لتجنب التنافس على الأقفال.
في مثال القائمة، قد تحتفظ النواة بـ قائمة حرة منفصلة لكل معالج وتلمس فقط القائمة الحرة لمعالج آخر إذا كانت قائمة المعالج الحالي فارغة ويجب سرقة ذاكرة من معالج آخر.
حالات الاستخدام الأخرى قد تتطلب تصاميم أكثر تعقيداً.

موقع الأقفال هام أيضاً للأداء.
على سبيل المثال سيكون من الصحيح نقل `acquire` لأبكر في `push` قبل الاستدعاء لـ `malloc`.
لكن هذا سيقلل يرجح الأداء لأن الاستدعاءات لـ `malloc` ستصبح مسلسلة عندئذ.
يوفر القيد "استخدام الأقفال" أدناه بعض الإرشادات لإدراج استدعاءات `acquire` و `release`.

== الكود البرمجي: الأقفال
<sec:code_locks>

يُرجى قراءة `kernel/spinlock.h` و `kernel/spinlock.c`.

تحوز `xv6` نوعين من الأقفال: أقفال الدوران (Spinlocks) وأقفال النوم (Sleep-locks).
سنبدأ بأقفال الدوران.
تمثل `xv6` قفل الدوران كـ `struct spinlock`.
الحقل الهام في الهيكل هو `locked` وهو كلمة تكون صفراً عندما يكون القفل متاحاً وغير صفري عندما يكون محتجزاً.
لو لم تكن هناك تزامنات لتمكنت `xv6` من الاستحواذ على قفل بشفرة كـ:

```c
void
acquire(struct spinlock *lk) // لا تعمل!
{
  for(;;) {
    if(lk->locked == 0) {
      lk->locked = 1;
      break;
    }
  }
}
```

لسوء الحظ هذا التنفيذ لا يضمن الاستبعاد المتبادل في مواجهة التزامن.
معالجان يمكنهما في الوقت نفسه الوصول للسطر `if(lk->locked == 0)` ومشاهدة أن `lk->locked` صفر ثم يمسك كلاهما القفل بتنفيذ `lk->locked = 1`.
في هذه النقطة محتجز معالجان مختلفان القفل، مما يخرق خاصية الاستبعاد المتبادل.
ما نحتاجه هو طريقة لجعل السطرين ينفذان كـ خطوة **ذرية (Atomic)** (غير قابلة للتقسيم).

لأن الأقفال واسعة الاستخدام، فإن المعالجات متعددة النوى توفر عادة تعليمة يمكن استخدامها لجعل السطرين ذريين.
بـ RISC-V هذه التعليمة هي `amoswap r, r, a`.
تقرأ التعليمة `amoswap` القيمة عند العنوان $a$ في الذاكرة، وتكتب محتويات المسجل $r$ لذلك العنوان، وتضع القيمة التي قرأتها في $r$.
أي أنها تبدل محتويات المسجل والموقع في الذاكرة.
تنفذ هذا التسلسل ذرياً باستخدام عتاد خاص لمنع أي معالج آخر من استخدام عنوان الذاكرة بين القراءة والكتابة.

يوفر مترجم C عائلة من الدوال المدمجة "الذرية" التي تولد تعليمات ذرية.
تستخدم `acquire()` بـ `xv6` الدالة `__atomic_exchange_n(addr, value, ...)` والتي تتلخص في التعليمة `amoswap`؛ ترجع الدالة المحتويات القديمة (المبدلة) في `*addr`.
فيما يلي طريقة جيدة لكتابة الحلقة التكرارية بـ `acquire`:

```c
while (__atomic_exchange_n(&lk->locked, 1, __ATOMIC_ACQUIRE) != 0)
  ;
```

تستخدم `acquire` بـ `xv6` الحلقة التكرارية أعلاه.
كل تكرار يبدل واحداً في `lk->locked` ويفحص القيمة السابقة؛ إذا كانت القيمة السابقة صفراً فإننا نكون قد استحوذنا على القفل، والمبادلة ستكون قد ضبطت `lk->locked` لـ واحد.
إذا كانت القيمة السابقة واحداً فإن معالجاً آخر يحتجز القفل، وحقيقة أننا بادلنا ذرياً واحداً في `lk->locked` لم تغير قيمته.

بمجرد الاستحواذ على القفل تسجل `acquire` المعالج الذي استحوذ على القفل لاستكشاف الأخطاء.
الحقل `lk->cpu` محمي بالقفل ويجب ألا يتغير إلا أثناء احتجاز القفل.

الدالة `release` هي العكس لـ `acquire`: تصفر الحقل `lk->cpu` ثم تحرر القفل.
منطقياً، التحرير يكتفي بتعيين صفر لـ `lk->locked`.
يتيح معيار C للمترجمات تنفيذ التعيين بتعليمات تخزين متعددة، لذا التعيين بـ C قد يكون غير ذري فيما يتعلق بالشفرة المتزامنة.
بدلاً من ذلك تستخدم `release` الدالة الذرية لـ C `__atomic_store_n(addr, 0, ...)` والتي تخزن الصفر للعنوان في عملية واحدة غير قابلة للتقسيم.

== الكود البرمجي: استخدام الأقفال
<sec:code_using_locks>

تستخدم `xv6` الأقفال في أماكن كثيرة لتجنب التنافسات.
كما وُصف أعلاه تشكل `kalloc` و `kfree` مثالاً جيداً.
جرب التمرينين 1 و 2 لمشاهدة ما يحدث لو أغفلت تلك الدوال الأقفال.
ستجد يرجح أنه من الصعب إطلاق سلوك غير صحيح، مما يوحي بكونه من الصعب الاختبار الموثوق لكون الشفرة خالية من أخطاء الأقفال والتنافسات.
قد تحوز `xv6` تنافسات لم تُكتشف بعد.

جزء صلب بخصوص استخدام الأقفال هو القرار بمقدار الأقفال للبرمجة وأي البيانات والثوابت الملازمة ينبغي لكل قفل حمايتها.
توجد بضع مبادئ أساسية.
أولاً في أية شفرة قد تتسبب في كتابة موقع ذاكرة بواسطة معالج واحد في الوقت نفسه الذي يقرؤه أو يكتبه فيه معالج آخر، يجب استخدام قفل لمنع التداخل بين العمليتين.
ثانياً تذكر أن الأقفال تحمي الثوابت الملازمة: لو يتضمن ثابت ملازم مواقع ذاكرة متعددة فإن جميعها يرجح احتياجها للحماية بقفل واحد لضمان الحفاظ على الثابت الملازم.

القواعد أعلاه تخبر متى تكون الأقفال ضرورية لكنها لا تقول شيئاً عن متى تكون غير ضرورية، ومن الهام للكفاءة عدم القفل بكثرة، لأن الأقفال تقلل التوازي.
لو لم يكن التوازي هاماً لتمكن المرء من الترتيب لحوز خيط واحد فقط وعدم القلق بخصوص الأقفال.
نواة بسيطة يمكنها فعل هذا على متعدد معالجات عبر حوز قفل واحد؛ تستحوذ النواة على القفل في كل مرة تُدخل فيها النواة من مساحة المستخدم لاستدعاء نظام أو مقاطعة؛ وتحرر النواة القفل عندما ترجع لمساحة المستخدم.
عديد نظم التشغيل أحادية المعالج تحولت للتنفيذ على متعددة المعالجات باستخدام هذا النهج، المسمى أحياناً بـ **قفل النواة الضخم (Big kernel lock)**، لكن النهج يضحي بالتوازي: معالج واحد فقط يمكنه التنفيذ في النواة في الوقت الواحد.
لو كانت النواة تستهلك وقتاً كبيراً من المعالج، يمكن الحصول على توازٍ أكثر عبر حماية كائنات أو وحدات مختلفة بأقفال مختلفة، لكي تتمكن معالجات مختلفة من التنفيذ في أجزاء مختلفة من النواة في الوقت نفسه.

كمثال على القفل غليظ الدرجة، يحوز مخصص `kalloc.c` بـ `xv6` قائمة حرة واحدة محمية بقفل واحد.
لو حاولت عمليات متعددة على معالجات مختلفة تخصيص صفحات في الوقت نفسه، فإن كل منها سيتعين عليها الانتظار لدورها عبر الدوران في `acquire`.
الدوران يضيع وقت المعالج نظراً لأنه ليس عملاً نافعاً.
لو أضاع التنافس على القفل جزءاً كبيراً من وقت المعالج فربما أمكن تحسين الأداء عبر تغيير المخصص ليحوز قائمة حرة منفصلة لكل معالج، كل بقفلها الخاص، للسماح بتخصيص متواز حقاً.

كمثال على القفل دقيق الدرجة، تحوز `xv6` قفلاً منفصلاً لكل ملف، لكي تتمكن العمليات التي تتعامل مع ملفات مختلفة غالباً من المتابعة دون انتظار أقفال بعضها البعض.
مخطط قفل الملفات يمكن جعله حتى أكثر دقة الدرجة لو أراد المرء السماح للعمليات بالكتابة المتزامنة لمناطق مختلفة من نفس الملف.
في النهاية قرارات درجة الأقفال يجب أن تقاد بمتطلبات الأداء بالإضافة لاعتبارات التعقيد.

كما تشرح الفصول التالية كل جزء من `xv6` فإنها ستذكر أمثلة لاستخدام `xv6` للأقفال للتعامل مع التزامن.
كمعاينة، يعرض الجدول @fig:locktable كافة الأقفال بـ `xv6`.

#figure(
  table(
    columns: (1fr, 2fr),
    align: (col, row) => if col == 0 { left } else { right },
    [*القفل*], [*الوصف*],
    [`bcache.lock`], [حماية تخصيص مدخلات المخزن المؤقت لكتل القرص.],
    [`cons.lock`], [تسلسل معالجة قراءة مدخلات لوحة التحكم.],
    [`tx_lock`], [تسلسل الوصول لعتاد مخرجات لوحة التحكم (uart).],
    [`ftable.lock`], [تسلسل تخصيص هيكل file في جدول الملفات.],
    [`itable.lock`], [حماية تخصيص مدخلات العقد الفهرسية في الذاكرة.],
    [`vdisk_lock`], [تسلسل الوصول لعتاد القرص وطابور واصفات DMA.],
    [`kmem.lock`], [تسلسل تخصيص الذاكرة.],
    [`log.lock`], [تسلسل العمليات على سجل المعاملات.],
    [`pipe's pi->lock`], [تسلسل العمليات على كل أنبوب.],
    [`pid_lock`], [تسلسل زيادات next_pid.],
    [`proc's p->lock`], [تسلسل التغيرات لحالة كل عملية.],
    [`wait_lock`], [مساعدة wait لتجنب الاستيقاظ المفقود.],
    [`tickslock`], [تسلسل العمليات على عداد الدقات ticks.],
    [`inode's ip->lock`], [تسلسل العمليات على كل عقدة فهرسية ومحتواها.],
    [`buf's b->lock`], [تسلسل العمليات على كل مخزن مؤقت للكتل.]
  ),
  caption:
[الأقفال بـ xv6.]
) <fig:locktable>

== الانسداد المتبادل وترتيب الأقفال
<sec:deadlock_and_lock_ordering>

افترض أن الدوال المنفذة على المعالجين C1 و C2 كلاهما تحوز نقطة يحتاج عندها كل منهما احتجاز كل من القفل A والقفل B،
واستحواذا عليهما في ترتيبين مختلفين:

```text
المعالج C1:          المعالج C2:
acquire(A);          acquire(B);
acquire(B);          acquire(A);
```

مع القليل من سوء الحظ قد ينفذ C1 و C2 أول استدعاء acquire لهما في نفس اللحظة بالضبط؛
كلاهما يمكنه النجاح نظراً لأنهما يطلبان أقفالاً مختلفة.
ولكن عندئذ سيتعين على كل من C1 و C2 الانتظار في استدعاءاتهما الثانية لـ `acquire()` نظراً لأن كلا القفلين محتجز بالفعل بواسطة المعالج الآخر.
لأن كلا المعالجين ينتظران بعضهما البعض فلن يحرر أي منهما قفلاً أبدًا وكلاهما سينتظر للأبد.
هذه الحالة تُسمى **الانسداد المتبادل (Deadlock)**.

المشكلة الرئيسية في مثال C1/C2 هي أن المعالجين استحوذا على الأقفال في ترتيبين مختلفين.
لو أنهما حاولا الاستحواذ على A أولاً لكان أحدهما قد استحوذ على A ثم B ثم حررهما كلاهما،
وكان المعالج الآخر قد تمكن عندئذ من المتابعة.
على نحو أعم فإن شفرة الأقفال يجب أن تتبع هذه القاعدة لتجنب الانسداد المتبادل:
كافة مسارات الشفرة التي تحتجز أقفالاً متعددة يجب أن تستحوذ على الأقفال في نفس الترتيب.
الحاجة لترتيب الاستحواذ العام هذا تعني أن الأقفال هي بفعالية جزء من مواصفة كل دالة:
المستدعون يجب أن يدعوا الدوال بطريقة تتسبب في جعل الأقفال تُستحوذ في الترتيب المتفق عليه.

تحوز `xv6` سلاسل ترتيب أقفال كثيرة بطول اثنين تتضمن الأقفال الخاصة بكل عملية (القفل في كل `struct proc`) بفضل الطريقة التي تعمل بها آلية `sleep`/`wakeup` (انظر الفصل @CH:SLEEP).
على سبيل المثال `consoleintr` هي روتين المقاطعة الذي يتعامل مع الحروف المكتوبة.
عندما يصل سطر جديد فإن أية عملية تنتظر مدخلات لوحة التحكم ينبغي إيقاظها.
لقيام بذلك تحتجز `consoleintr` القفل `cons.lock` أثناء دعوة `wakeup` والتي تستحوذ على قفل العملية المنتظرة من أجل إيقاظها.
ونتيجة لذلك فإن ترتيب الأقفال العام المتجنب للانسداد المتبادل يتضمن القاعدة بكون `cons.lock` يجب الاستحواذ عليه قبل أي قفل عملية.
شفرة نظام الملفات تحوي أطول سلاسل أقفال بـ `xv6`.
على سبيل المثال إنشاء ملف يتطلب في الوقت نفسه احتجاز قفل على المجلد،
قفل على العقدة الفهرسية للملف الجديد،
قفل على حاجز كتلة القرص،
القفل `vdisk_lock` لبرنامج تشغيل القرص،
والقفل `p->lock` للعملية المستدعية.
لتجنب الانسداد المتبادل فإن شفرة نظام الملفات تستحوذ دائماً على الأقفال في الترتيب المذكور في الجملة السابقة.

احترام ترتيب عام متجنب للانسداد المتبادل قد يكون صلب بشكل مفاجئ.
أحياناً يتضارب ترتيب الأقفال مع البنية المنطقية للبرنامج،
مثلاً ربما وحدة الشفرة M1 تدعو الدالة M2 لكن ترتيب الأقفال يتطلب أن قفلاً في M2 يُستحوذ عليه قبل قفل في M1.
أحياناً لا تكون هويات الأقفال معروفة مقدماً،
ربما لأن قفلاً واحداً يجب احتجازه من أجل اكتشاف هوية القفل المراد الاستحواذ عليه تالياً.
هذا النوع من الحالات ينشأ في نظام الملفات بينما يبحث عن المكونات المتتالية في اسم المسار،
وفي الشفرة المخصصة لاستدعاءي النظام `wait` و `exit` بينما تبحثان في جدول العمليات عن العمليات الابن.
أخيراً فإن خطر الانسداد المتبادل يكون غالباً قيداً على مدى دقة الدرجة التي يمكن بها جعل مخطط الأقفال،
نظراً لأن الأقفال الأكثر تعني فرصاً أكثر للانسداد المتبادل.
الحاجة لتجنب الانسداد المتبادل تكون غالباً عاملاً رئيسياً في تنفيذ النواة.

سؤال ينشأ أحياناً هو ماذا ينبغي أن يحدث لو حاول معالج الاستحواذ على قفل يحتجزه ذلك المعالج بالفعل.
خط من التفكير يقول إن هذا ينبغي السماح به:
لا يمكن لمعالج آخر احتجاز القفل،
لذا لا حاجة للقلق بخصوص تعارض المعالجات مع استخدام بعضها البعض للبيانات المحمية.
نظم الأقفال التي تتيح للمعالج إعادة الاستحواذ على قفل يحتجزه بالفعل تُسمى **معادة الدخول (Re-entrant)** أو **تكرارية (Recursive)**.
من ناحية أخرى لو كان القفل محتجزاً بالفعل،
حتى على نفس المعالج،
فهذا يعني أن عملية ما ربما خرقَت مؤقتاً ولم تستعد بعد بعض الثوابت الملازمة؛
السماح لعملية جديدة بالبدء بينما الثوابت الملازمة غير قائمة يبدو كـ دعوة للأخطاء البرمجية.
تأخذ `xv6` هذا الرأي الأخير،
وتحظر على معالج يحتجز حالياً قفلاً إعادة الاستحواذ عليه.
اكتشاف هذه الحالة هو الغرض من الاستدعاء لـ `holding` في `acquire`.

== الأقفال والمقاطعات
<sec:locks_and_interrupts>

بعض أقفال الدوران بـ `xv6` تحمي بيانات تُستخدم بواسطة كل من الخيوط ومعالجات المقاطعة.
على سبيل المثال معالج مقاطعة الميقاتي `clockintr` قد يزيد `ticks` في الوقت نفسه تقريباً الذي يقرأ فيه خيط نواة `ticks` في `sys_pause`.
القفل `tickslock` يسلسل الوصولين.

تفاعل أقفال الدوران والمقاطعات يطرح خطراً محتملاً.
افترض أن `sys_pause` تحتجز `tickslock` و قُطع معالجها بمقاطعة ميقاتي.
ستحاول `clockintr` الاستحواذ على `tickslock` وتكتشف كونه محتجزاً وتنتظر تحريره.
في هذه الحالة لن يُحرر `tickslock` مطلقاً:
فقط `sys_pause` يمكنها تحريره،
لكن `sys_pause` لن تستمر في التشغيل حتى ترجع `clockintr`.
لذا سيتعرض المعالج لانسداد متبادل،
وأية شفرة تحتاج أياً من القفلين ستتجمد أيضاً.

لتجنب هذه الحالة،
لو كان قفل دوران يُستخدم بواسطة معالج مقاطعة فلا يجوز لمعالج احتجاز ذلك القفل مطلقاً مع تفعيل المقاطعات.
`xv6` أكثر تحافظاً:
عندما يستحوذ معالج على أي قفل فإن `xv6` تعطل دائماً المقاطعات على ذلك المعالج.
المقاطعات قد تقع لا تزال على معالجات أخرى،
لذا فإن `acquire` مقاطعة يمكنها الانتظار لخيط ليحرر قفل دوران؛
مجرد ليس على نفس المعالج.

تعيد `xv6` تفعيل المقاطعات عندما لا يحتجز معالج أية أقفال دوران؛
يجب عليها القيام بقليل من دفتر التتبع للتعامل مع المقاطع الحرجة المتداخلة.
تدعو `acquire` الدالة `push_off` وتدعو `release` الدالة `pop_off` لتتبع مستوى التداخل للأقفال على المعالج الحالي.
عندما يصل ذلك العداد للصفر تستعيد `pop_off` حالة تفعيل المقاطعات التي تواجدت في بداية المقطع الحرج الخارجي.
تخبر `push_off` و `pop_off` المعالج بتفعيل وتعطيل المقاطعات عبر تغيير البت `SIE` في المسجل `sstatus` باستخدام `intr_on` و `rc_sstatus(SSTATUS_SIE)`؛
والأخيرة تصفر البت `SIE`.

من الهام أن تدعو `acquire` الدالة `push_off` صرامة قبل ضبط `lk->locked`.
لو عُكس الاثنان لكانت هناك نافذة صغيرة عندما يكون القفل محتجزاً مع تفعيل المقاطعات،
ومقاطعة بتوقيت سئ كانت ستتسبب في انسداد متبادل للنظام.
بالمثل من الهام أن تدعو `release` الدالة `pop_off` فقط بعد تحرير القفل.

== ترتيب التعليمات والذاكرة
<sec:instruction_and_memory_ordering>

من الطبيعي التفكير في البرامج بكونها تنفذ في الترتيب الذي تظهر به عبارات الشفرة المصدرية.
هذا نموذج عقلي معقول للشفرة أحادية الخيط،
ولكنه ليس ما يحدث في الواقع.

أحد الأسباب هو أن المترجمات تصدر تعليمات تحميل وتخزين في ترتيبات مختلفة عن تلك الضمنية في الشفرة المصدرية،
وقد تحذفها تماماً (على سبيل المثال عبر التخزين المؤقت للبيانات في السجلات).
سبب آخر هو أن المعالج قد ينفذ التعليمات خارج الترتيب لزيادة الأداء.
على سبيل المثال قد يلاحظ معالج أنه في متتالية تسلسلية من التعليمات فإن A و B ليستا معتمدتين على بعضهما البعض.
قد يبدأ المعالج التعليمة B أولاً،
إما لأن مدخلاتها متجهزة قبل مدخلات A،
أو من أجل تداخل تنفيذ A و B.

إعادة ترتيب الوصول للذاكرة بواسطة المعالج والمترجم قد تتسبب في مشاكل عندما تتفاعل خيوط متعددة عبر ذاكرة مشتركة.
كمثال على ما قد يمر بأسلوب خاطئ،
في هذه الشفرة لـ `push` سيكون كارثة لو نقل المترجم أو المعالج التخزين المناظر لـ `l->next = list` لنقطة بعد `release`:

```c
l = malloc(sizeof *l);
l->data = data;
acquire(&listlock);
l->next = list;
list = l;
release(&listlock);
```

لو وقعت مثل هذه إعادة الترتيب لكانت هناك نافذة يمكن خلالها لمعالج آخر الاستحواذ على القفل وملاحظة `list` المحدثة،
لكن مع مشاهدة `list->next` غير مهيأ.

الخبر الجيد هو أن المترجمات والمعالجات توفر طرقاً للمبرمجين لمنع إعادة الترتيب.
تظهر هذه بـ `xv6` كـ معاملات `__ATOMIC_ACQUIRE` و `__ATOMIC_RELEASE` للدوال الذرية لـ C المستخدمة بـ `acquire` و `release`.
لكل منها أثران.
أولاً يخبر كل منهما المترجم بعدم نقل مراجع الذاكرة بعد الاستدعاء الذري.
ثانياً يخبر كل منهما المترجم بإنشاء تعليمات تخبر المعالج بعدم نقل مراجع ذاكرة وقت التنفيذ بعد التعليمات المولدة للاستدعاء الذري.
مثل هذه النقطة "لا تنقل بعد" تُسمى **حاجز ذاكرة (Memory barrier)** أو **سياجاً (Fence)**.

الحواجز في دوال أقفال الدوران بـ `xv6` تحوز أثرين هامين على الشفرة التي تستخدم الأقفال.
أولاً كافة مراجع الذاكرة (القراءات والكتابات) التي تقع في شفرة C قبل `release()` يُضمن لها أن تأخذ أثرها على نظام الذاكرة قبل أن يكتمل `release()`.
يتضمن هذا كلاً من مراجع الذاكرة في المقطع الحرج ومراجع الذاكرة قبل المقطع الحرج.
ثانياً كافة مراجع الذاكرة في شفرة C بعد `acquire` يُضمن لها عدم أخذ أثر قبل الإتمام الناجح لـ `amoswap` في `acquire()`.
مجدداً يتضمن هذا كلاً من المراجع في المقطع الحرج والمراجع بعد `release`.

تنتج هذه الحواجز أثراً متعدياً هاماً.
على سبيل المثال لو كتب معالج لكائن بيانات مخصص حديثاً (دون احتجاز أقفال) ثم،
في مقطع حرج،
ربط ذلك الكائن في قائمة،
فإن أي معالج يستحوذ لاحقاً على قفل ويحصل على مؤشر للكائن من القائمة يُضمن له مشاهدة كتابات المعالج الأول.

الحواجز بـ `acquire` و `release` الخاصة بـ `xv6` تجبر الترتيب في كافة الحالات تقريباً التي يهم فيها ذلك،
نظراً لأن `xv6` تستخدم الأقفال حول الوصولات للبيانات المشتركة.
يناقش الفصل @CH:LOCK2 بضع استثناءات.

== البرمجة بالعمليات الذرية
<sec:programming_with_atomics>

على الرغم من أن `xv6` لا تفعل هذا،
إلا أنه من الممكن أحياناً تحديث البيانات المشتركة مع تجنب التنافسات عبر الاستخدام المباشر للتعليمات الذرية،
دون استخدام الأقفال.
معظم المعالجات توفر بمرونة عشرات التعليمات الذرية،
عادة بالنمط الذي تقرأ فيه موقع ذاكرة،
تعدل القيمة،
وتكتب القيمة الجديدة لنفس موقع الذاكرة،
ذرياً.
التعليمة `amoadd` بـ RISC-V مثال على ذلك:
تضيف ذرياً محتويات مسجل لموقع في الذاكرة.
يوفر مترجم C أظرفة "ذرية" لهذه التعليمات.
وهكذا يمكن للشفرة زيادة عداد مشترك دون أقفال باستخدام:

```c
__atomic_fetch_add(&counter, 1, __ATOMIC_SEQ_CST);
```

قد يكون نافعاً تجنب الأقفال لأنها تستهلك وقتاً من المعالج،
أو لأن سبباً هيكلياً مثل تجنب الانسداد المتبادل يجعل استخدام الأقفال غير ملائم.

== أقفال النوم
<sec:sleep_locks>

أحياناً تحتاج `xv6` لاحتجاز قفل لوقت طويل.
على سبيل المثال يحتفظ نظام الملفات بملف مقفول بينما يقرأ ويكتب محتواه على القرص،
وهذه العمليات القرصية قد تستغرق عشرات الملي ثانية.
احتجاز قفل دوران لذلك الوقت الطويل سيقود لهدر لو أرادت عملية أخرى الاستحواذ عليه،
نظراً لأن العملية المستحوذة ستضيع الوقت في الدوران لوقت طويل.
عيب آخر لأقفال الدوران هو أن العملية لا يمكنها التنازل عن المعالج مع الاحتفاظ بقفل دوران؛
نود فعل هذا لكي تتمكن عمليات أخرى من استخدام المعالج بينما تنتظر العملية الحائزة للقفل القرص.
التنازل أثناء احتجاز قفل دوران غير قانوني لأنه قد يقود لانسداد متبادل لو حاول خيط ثان عندئذ الاستحواذ على قفل الدوران؛
نظراً لأن `acquire` لا تتنازل عن المعالج فإن دوران الخيط الثاني قد يمنع الخيط الأول من التشغيل وتحرير القفل.
التنازل أثناء احتجاز قفل يخرق أيضاً المتطلب بكون المقاطعات يجب أن تكون معطلة أثناء احتجاز قفل دوران.
وهكذا نود نوعاً من الأقفال يتنازل عن المعالج أثناء انتظار الاستحواذ،
ويسمح بالتنازلات (والمقاطعات) أثناء احتجاز القفل.

توفر `xv6` مثل هذه الأقفال في شكل **أقفال النوم (Sleep-locks)**.
تتنازل `acquiresleep` عن المعالج أثناء الانتظار،
باستخدام تقنيات ستُشرح في الفصل @CH:SLEEP.
على مستوى عال يحوز قفل النوم حقل `locked` محمي بقفل دوران،
ودعوة `acquiresleep` لـ `sleep` تتنازل ذرياً عن المعالج وتحرر قفل الدوران.
النتيجة هي أن خيوطاً أخرى يمكنها التنفيذ بينما تنتظر `acquiresleep`.

لأن أقفال النوم تترك المقاطعات مفعلة فلا يمكن استخدامها في معالجات المقاطعة.
ولأن `acquiresleep` قد تتنازل عن المعالج فلا يمكن استخدام أقفال النوم في المقاطع الحرجة لأقفال الدوران (على الرغم من إمكانية استخدام أقفال الدوران في المقاطع الحرجة لأقفال النوم).

أقفال الدوران تكون أنسب للمقاطع الحرجة القصيرة،
نظراً لأن انتظارها يضيع وقت المعالج؛
وأقفال النوم تعمل جيداً للعمليات المطولة.

== العالم الحقيقي
<sec:real_world_lock>

البرمجة بالأقفال تظل مليئة بالتحدي على الرغم من سنين البحث في مجردات التزامن والتوازي.
من الأفضل غالباً إخفاء الأقفال في تركيبات أعلى مستوى كـ الطوابير المتزامنة،
على الرغم من أن `xv6` لا تفعل هذا.
لو برمجت بأقفال فمن الحكمة استخدام أداة تحاول التعرف على التنافسات،
لأنه من السهل إغفال ثابت ملازم يتطلب قفلاً.

معظم نظم التشغيل تدعم خيوط POSIX (المسماة Pthreads)،
والتي تتيح لعملية مستخدم حوز عدة خيوط تنفذ بالتوازي على معالجات مختلفة.
تحوز Pthreads دعماً لأقفال مستوى المستخدم،
الحواجز،
إلخ.
تتيح Pthreads أيضاً للمبرمج اختيارياً التحديد بكون القفل ينبغي أن يكون معاد الدخول.

دعم Pthreads في مستوى المستخدم يتطلب دعماً من نظام التشغيل.
على سبيل المثال ينبغي أن يكون الحال أنه لو حظر p-thread عند استدعاء نظام فإن p-thread آخر لنفس العملية ينبغي أن يكون قادراً على التشغيل على ذلك المعالج.
كمثال آخر لو غير p-thread مساحة عناوين عمليته (مثلاً،
رسم أو ألغى رسم ذاكرة) فيجب على النواة الترتيب بحيث المعالجات الأخرى التي تشغل خيوطاً لنفس العملية تحدّث جداول صفحتها العتادية لتعكس التغير في مساحة العناوين.

من الممكن تنفيذ الأقفال دون تعليمات ذرية،
ولكنه مكلف،
لذا معظم نظم التشغيل تستخدم تعليمات ذرية.

الأقفال قد تكون مكلفة لو حاولت معالجات كثيرة الاستحواذ على نفس القفل في الوقت نفسه.
لو حاز معالج قفلاً مخزناً مؤقتاً في مخزنه المحلي وحاول معالج آخر الاستحواذ على القفل،
فإن التعليمة الذرية لتحديث خط المخزن المؤقت الحائز للقفل يجب أن تنقل الخط من مخزن المعالج الأول لمخزن المعالج الآخر،
وربما إلغاء صلاحية أية نسخ أخرى لخط المخزن المؤقت.
جلب خط مخزن مؤقت من مخزن معالج آخر قد يكون أضعافاً أكثر كلفة من جلب خط من مخزن محلي.

لتجنب النفقات المرتبطة بالأقفال،
تستخدم العديد من نظم التشغيل هياكل بيانات وخوارزميات خالية من الأقفال (Lock-free).
على سبيل المثال من الممكن تنفيذ قائمة ترابطية كـ تلك في بداية الفصل لا تتطلب أقفالاً أثناء أبحاث القائمة،
وتعليمة ذرية واحدة لإدراج عنصر في قائمة.
البرمجة الخالية من الأقفال،
مع ذلك،
أكثر تعقيداً من البرمجة بأقفال؛
على سبيل المثال يتعين على المرء القلق بخصوص إعادة ترتيب التعليمات والذاكرة.
البرمجة بأقفال هي بالفعل صلبة،
لذا تتجنب `xv6` التعقيد الإضافي للبرمجة الخالية من الأقفال.

== تمارين
<sec:lock_exercises>

1.
علّق الاستدعاءات لـ `acquire` و `release` في `kalloc`.
يبدو أن هذا يتسبب في مشاكل لشفرة النواة التي تدعو `kalloc`؛
ما الأعراض التي تتوقع مشاهدتها؟
عندما تشغل `xv6` هل تشاهد هذه الأعراض؟
ماذا عن تشغيل `usertests`؟
لو لم تشاهد مشكلة فلماذا؟
انظر ما إذا كان بمقدورك إثارة مشكلة عبر إدراج حلقات تكرارية وهمية في المقطع الحرج لـ `kalloc`.
2.
افترض أنك بدلاً من ذلك علقت القفل في `kfree` (بعد استعادة القفل في `kalloc`).
ماذا قد يمر بأسلوب خاطئ الآن؟
هل غياب الأقفال في `kfree` أقل ضرراً منه في `kalloc`؟
3.
لو دعت عمليتان `kalloc` في الوقت نفسه فستعين على إحداهما الانتظار للأخرى وهو ما يُعد سيئاً للأداء.
عدل `kalloc.c` لتحوز توازياً أكثر لكي تتمن الاستدعاءات المتزامنة لـ `kalloc` من معالجات مختلفة من المتابعة دون انتظار بعضها البعض.
4.
اكتب برنامجاً متوازياً باستخدام خيوط POSIX،
المدعومة على معظم نظم التشغيل.
على سبيل المثال نفذ جدول تجزئة متوازياً وقس ما إذا كان عدد عمليات الوضع/الجلب يتوسع مع زيادة عدد المعالجات.
5.
نفذ طقماً فرعياً من Pthreads بـ `xv6`.
أي نفذ مكتبة خيوط بمستوى المستخدم لكي تتمكن عملية مستخدم من حوز أكثر من خيط واحد والترتيب بحيث يمكن لهذه الخيوط التنفيذ بالتوازي على معالجات مختلفة.
اخرج بتصميم يتعامل بأسلوب صحيح مع خيط ينفذ استدعاء نظام حاجب ويغير مساحة عناوينه المشتركة.
