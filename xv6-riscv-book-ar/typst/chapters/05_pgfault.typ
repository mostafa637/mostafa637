#import "../template/listings.typ": *

= استثناءات أخطاء الصفحات
<chap:pgfault>

يقوم المعالج بتوليد _استثناء خطأ الصفحة (Page Fault Exception)_ عندما تحاول إحدى التعليمات الوصول إلى عنوان افتراضي لا تقابله مدخلة صالحة في جدول الصفحات،
أو عندما تنتهك العملية بتات الصلاحيات والحماية بالصفحة.

يحفظ المعالج العنوان الافتراضي الذي تسبب في الاستثناء بداخل السجل الخاص `stval`،
ويحفظ سبب الاستثناء بداخل السجل `scause`.

تستغل أنظمة التشغيل الحديثة ونواة `xv6` استثناءات أخطاء الصفحات لتقديم تقنيات متقدمة لإدارة الذاكرة بكفاءة:

+ *التخصيص الكسول للذاكرة (Lazy Allocation)*: عدم تخصيص صفحات فيزيائية فور استدعاء `sbrk`، بل الانتظار حتى تحاول العملية الوصول الفعلي للعنوان، فيحدث خطأ صفحة وتخصص النواة الصفحة حينها.
+ *النسخ عند الكتابة (Copy-on-Write - COW)*: مشاركة الصفحات الفيزيائية بين الأب والابن بقراءة فقط عند `fork`، وعندما تحاول إحدى العمليات الكتابة، يتولد خطأ صفحة فتنسخ النواة تلك الصفحة المحددة فقط.
+ *الذاكرة المضمومة بملف (Memory Mapped Files - mmap)*: ربط كتل الملف بالعناوين الافتراضية للعملية وتحميل الصفحات من القرص تدريجياً عند وقوع خطأ الصفحة.

