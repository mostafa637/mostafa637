#import "../template/listings.typ": *

= استثناءات أخطاء الصفحات
<CH:PGFAULT>

*خطأ الصفحة* (Page Fault) هو استثناء يُولّده العتاد عندما لا تستطيع وحدة إدارة الذاكرة (MMU) ترجمة عنوان افتراضي إلى عنوان فيزيائي.
ويحدث ذلك عندما يكون مدخل جَدْوَلُ الصَّفَحَاتِ (PTE) المقابل للعنوان إما غير صالح (`PTE\_V == 0`)، أو غير يمتلك الصلاحيات المطلوبة (مثل كتابة في صفحة للقراءة فقط `PTE\_W == 0`).

بينما تعتبر العديد من الأنظمة أخطاء الصفحات مؤشرًا لخلل في البرنامج، تستغل أنظمة التشغيل الحديثة أخطاء الصفحات لتطبيق تقنيات ذاكرة افتراضية متطورة للغاية تزود الأداء والكفاءة.

يقدم هذا الفصل كيفية استغلال أخطاء الصفحات لتنفيذ التخصيص التكاسلي للذاكرة (Lazy Allocation)، والنسخ عند الكتابة (Copy-On-Write)، والتصفح بالمطالبة (Demand Paging)، والملفات المربوطة بالذاكرة.

== التخصيص التكاسلي
<sec:lazy_allocation>

عندما يطلب برنامج زيادة فضاء ذاكرته عن طريق اِسْتِدْعَاءُ النِّظَامِ `sbrk(n)`، ينطوي التصميم التقليدي على تخصيص كافة الصفحات الفيزيائية المطلوبة فورًا ورسم خريطتها في جَدْوَلِ الصَّفَحَاتِ.
لكن البرامج غالبًا ما تطلب ذاكرة كبيرة ولا تستخدم سوى جزء صغير منها لاحقًا.

تتيح تقنية *التخصيص التكاسلي* (Lazy Allocation) زيادة حجم ذاكرة العَمَلِيَّة الافتراضي (`p->sz`) دون تخصيص صفحات فيزيائية أو إضافة مداخل في جَدْوَلِ الصَّفَحَاتِ في لحظة الاستدعاء.
عندما يحاول البرنامج القراءة أو الكتابة في العنوان الجديد، يُولّد العتاد خطأ صفحة.
تستجيب النَّوَاة للخطأ بتخصيص صفحة فيزيائية واحدة وتعيينها فورًا ثم إعادة استئناف التعليمة، مما يجعل العَمَلِيَّة شفافة تمامًا بالنسبة لبرنامج المستخدم.

== الكود البرمجي
<sec:code_pgfault>

يتطلب إضافة التخصيص التكاسلي إلى `xv6` تعديل الاستدعاء `sys\_sbrk()` ليقوم فقط بزيادة `p->sz` دون تخصيص الذاكرة:
#lstlisting[

uint64
sys_sbrk(void)
{
  int n;
  struct proc *p = myproc();

  if(argint(0, &n) < 0)
    return -1;
  uint64 addr = p->sz;
  p->sz += n;
  return addr;
}
]

ثم نعدل معالج المَصَايِدُ `usertrap()` في `kernel/trap.c` للتعامل مع كود سبب خطأ الصفحة في RISC-V (`r\_scause() == 13` لخطأ قراءة/تنفيذ، أو `15` لخطأ كتابة):

  + تقرأ النَّوَاة العنوان الافتراضي المسبب للخطأ من السجل `r\_stval()`.
  + تتأكد النَّوَاة من أن العنوان يقع في النطاق الصالح للعملية (`va < p->sz` وأعلى من المكدس).
  + تخصص صفحة فيزيائية جديدة باستخدام `kalloc()`.
  + ترسم خريطة الصفحة في جَدْوَلِ الصَّفَحَاتِ بالصلاحيات `PTE\_U | PTE\_R | PTE\_W | PTE\_V`.
  + تعيد النَّوَاة التنفيذ لتبدأ التعليمة المسببة للخطأ عملها بنجاح.


== العالم الحقيقي: النسخ عند الكتابة (COW fork)
<sec:cow_fork>

عندما تُنشئ النَّوَاة عَمَلِيَّةٌ ابن باستخدام `fork()`، يتطلب التصميم الساذج نسخ جميع الصفحات الفيزيائية للأب.
ولأن العَمَلِيَّة الابن غالبًا ما تستدعي `exec()` فورًا بعد `fork()`، تنطوي عَمَلِيَّةٌ النسخ هذه على هدر كبير لموارد الذاكرة والوقت.

تعتمد تقنية *النسخ عند الكتابة* (Copy-On-Write - COW) على الخطوات التالية:

  + ينشئ `fork()` جدول صفحات للابن يشارك نفس الصفحات الفيزيائية مع الأب.
  + تُزيل النَّوَاة راية الكتابة `PTE\_W` من مدخلات كلا الجدولين وتفعل راية COW مخصصة.
  + عند محاولة أي من العمليتين الكتابة في الصفحة، يطلق العتاد خطأ صفحة كتابة.
  + تلتقط النَّوَاة الخطأ، وتتحقق من راية COW، ثم تخصص صفحة فيزيائية جديدة وتنسخ البيانات وتفعل `PTE\_W` للعملية المكاتبة فقط.


== العالم الحقيقي: التصفح بالمطالبة
<sec:demand_paging>

تسمح تقنية *التصفح بالمطالبة* (Demand Paging) بتحميل أجزاء البرامج التنفيذية من القرص الصلب إلى الذاكرة فقط عند محاولة الوصول إليها.
عند قراءة كود البرنامج عبر `exec()`، لا تُحمل جميع الأقسام إلى الذاكرة؛ بل تُترك المداخل غير صالحة.
عند تشغيل الكود، يطلق العتاد خطأ صفحة فتجلب النَّوَاة الكتلة المطلوبة من القرص ورسمها في الذاكرة.

== العالم الحقيقي: الملفات المربوطة بالذاكرة
<sec:mmap_files>

تتيح تقنية *الملفات المربوطة بالذاكرة* (Memory-Mapped Files - `mmap`) ربط محتويات ملف بقطاع من فضاء العناوين الافتراضي للعملية.
تتم القراءة والكتابة من وإلى الملف مباشرة عبر تعليمات الذاكرة، ويتم تحديث القرص بواسطة خطأ الصفحة عند كتابة الصفحات المتسخة (Dirty Pages).

== تمارين
<sec:exercises_pgfault>


  + نفّذ ميزاني `lazy allocation` و `COW fork` واختبر أداء التشغيل في `xv6`.
  + ما السلوك المتوقع إذا حاول برنامج المستخدم الوصول لعنوان فوق `p->sz`؟

