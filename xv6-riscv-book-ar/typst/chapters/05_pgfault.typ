#import "../template/listings.typ": *

= أخطاء الصفحات
<CH:PGFAULTS>

يولد معالج RISC-V استثناء **خطأ الصفحة (Page fault)** عندما يُستخدم عنوان افتراضي لا يحوز رسماً بداخل جدول الصفحات، أو يحوز رسماً علامته `PTE_V` صفرية، أو رسماً حظرت بتات أذوناته (`PTE_R`, `PTE_W`, `PTE_X`, `PTE_U`) العملية المجهزة.
يميز RISC-V ثلاثة أنواع من أخطاء الصفحات: أخطاء صفحات التحميل (الناتجة عن تعليمات التحميل)، أخطاء صفحات التخزين (الناتجة عن تعليمات التخزين)، وأخطاء صفحات التعليمات (الناتجة عن استدعاءات التعليمات المراد تنفيذها).
يشير المسجل `scause` لنوع خطأ الصفحة، ويحتوي المسجل `stval` على العنوان الذي لم يمكن ترجمته.

المزج بين جداول الصفحات وأخطاء الصفحات أداة قوية.
تمنح جداول الصفحات النواة مستوى من عدم المباشرة بين العناوين الافتراضية والفيزيائية، لكي تتمكن النواة من التحكم بتركيب ومحتوى مساحات العناوين.
تتيح أخطاء الصفحات للنواة الاعتراض للتحميل والتخزين، وعبر تعديل جدول الصفحات، التحديد بداخل الهواء لما تشير إليه تلك المراجع من بيانات.
يمكن للنواة استخدام هذه الإمكانيات لزيادة الكفاءة: على سبيل المثال، `fork` بـ النسخ عند الكتابة تتيح للنواة مشاركة الذاكرة بأسلوب شفاف بين الأب والابن، متجنبة كلفة نسخ الصفحات التي لا يكتبها أي منهما.
مبرمجو التطبيقات يمكنهم الاستفادة أيضاً. أحد الاحتمالات هو الملفات المربوطة بالذاكرة، حيث تستخدم النواة تحويل الصفحات لتتسبب بداخل جعل محتوى ملف يظهر بداخل مساحة عناوين تطبيق ما، قراءة كتل الملف بشفافية استجابة لأخطاء الصفحات.
احتمال آخر هو التخصيص الكسول للذاكرة، والذي يتيح لبرنامج طلب مساحة عناوين افتراضية عملاقة، ولكن مع دفع كلفة تخصيص ذاكرة فيزيائية فقط للصفحات التي يقرؤها ويكتبها البرنامج بالفعل.
تستخدم `xv6` أخطاء الصفحات لغرض واحد فقط: التخصيص الكسول.

قبل المتابعة، يُرجى قراءة الدوال البرمجية `sys_sbrk()` بداخل `kernel/sysproc.c` و `vmfault` بداخل `kernel/vm.c`.
ابحث عن الاستدعاءات لـ `vmfault` بداخل `kernel/trap.c` و `kernel/vm.c`.

== التخصيص الكسول للذاكرة
<sec:lazy>

يحوز **التخصيص الكسول (Lazy allocation)** بـ `xv6` جزأين.
أولاً، عندما يطلب تطبيق ما ذاكرة عبر دعوة `sbrk` مع العلامة `SBRK_LAZY`، تسجل النواة الزيادة بداخل الحجم، لكنها لا تخصص ذاكرة فيزيائية ولا تنشئ PTEs للنطاق الجديد من العناوين الافتراضية.
ثانياً، عند وقوع خطأ صفحة على أحد تلك العناوين الجديدة، تخصص النواة صفحة ذاكرة فيزيائية وترسمها بداخل جدول الصفحات.
تنفذ النواة التخصيص الكسول بأسلوب شفاف للتطبيقات: لا حاجة لأية تعديلات على التطبيقات لكي تستفيد منه.

التخصيص الكسول مريح للتطبيقات لأنها لا يتعين عليها التنبؤ بدقة بمقدار الذاكرة التي ستحتاجها.
على سبيل المثال، تطبيق قد يعالج مدخلات، لكنه لا يعلم مسبقاً كم سيكون حجم المدخلات.
مع التخصيص الكسول، يمكن للتطبيق طلب ذاكرة للحالة الأسوأ، لكن دون دفع ثمن تلك الحالة الأسوأ: لا يتعين على النواة القيام بأي عمل للصفحات التي لا يستخدمها التطبيق مطلقاً، والتطبيقات الأخرى يمكنها تخصيص الصفحات غير المستخدمة.

علاوة على ذلك، إذا كان التطبيق يطلب تنمية مساحة العناوين بدرجة كبيرة، فإن `sbrk` بدون تخصيص كسول يكون مكلفاً: إذا طلب تطبيق غيغابايت من الذاكرة، يتعين على النواة تخصيص وتصفير 262,144 صفحة فيزيائية بحجم 4096 بايت.
يتيح التخصيص الكسول توزيع هذه الكلفة عبر الوقت.
من ناحية أخرى، يتسبب التخصيص الكسول بداخل نفقات إضافية لأخطاء الصفحات، والتي تتضمن انتقالاً بين المستخدم والنواة.
يمكن لنظم التشغيل تقليل هذه الكلفة عبر تخصيص دفعة من الصفحات المتتالية لكل خطأ صفحة بدلاً من صفحة واحدة وعبر تخصيص شفرة دخول/خروج النواة لمثل أخطاء الصفحات تلك (رغم أن `xv6` لا تفعل أي منهما).

من ناحية أخرى، عند التعامل مع خطأ صفحة لصفحة مخصصة كسولاً، قد تجد النواة أنه لا تتوفر ذاكرة حرة للتخصيص.
بداخل هذه الحالة، لا تجد النواة طريقة سهلة لإرجاع خطأ نفاذ-الذاكرة للتطبيق وتكتفي بقتل التطبيق بدلاً من ذلك.
بالنسبة للتطبيقات التي تفضل خطأ عند تخصيص فاشل، تتيح `xv6` للتطبيق تخصيص الذاكرة بشغف عبر دعوة `sbrk` مع العلامة `SBRK_EAGER`.

== الكود البرمجي: التخصيص الكسول
<sec:code_pgfault>

استدعاء النظام `sbrk(n)` ينمي (أو يقلص إذا كانت `n` سالبة) حجم ذاكرة العملية بمقدار `n` بايت، ويرجع بداية المنطقة المخصصة حديثاً (أي الحجم القديم).
تنفيذ النواة هو `sys_sbrk`.

إذا حدد التطبيق `SBRK_EAGER`، يُنفذ استدعاء النظام بواسطة الدالة `growproc`.
تدعو `growproc` الدالة `uvmalloc`.
تخصص `uvmalloc` ذاكرة فيزيائية بـ `kalloc`، وتصفر الذاكرة المخصصة، وتضيف PTEs لجدول صفحات المستخدم بـ `mappages`.

إذا خصص التطبيق الذاكرة كسولاً، تكتفي `sys_sbrk` بزيادة حجم العملية (`myproc()->sz`) بمقدار `n` وترجع الحجم القديم؛ لا تخصص ذاكرة فيزيائية ولا تضيف PTEs لجدول صفحات العملية.

عندما تحمل العملية أو تخزن بداخل عنوان افتراضي يفتقر لرسم صالح بجدول الصفحات، سيولد المعالج استثناء **خطأ الصفحة (Page-fault exception)**.
تفحص `usertrap` هذه الحالة وتدعو `vmfault` لمعالجة خطأ الصفحة.
تفحص `vmfault` بكون العنوان المسبب للخطأ بداخل المنطقة الممنوحة سابقاً بواسطة `sbrk`، وتخصص صفحة ذاكرة فيزيائية بـ `kalloc`، وتصفر الصفحة المخصصة، وتضيف PTE لجدول صفحات المستخدم بـ `mappages`.
تضبط `xv6` العلامات `PTE_W`, `PTE_R`, `PTE_U`, `PTE_V` بداخل PTE للصفحة الجديدة.
ثم تستأنف `usertrap` العملية عند التعليمة التي تسببت بداخل الخطأ.
نظراً لأن PTE أصبح صالحاً الآن، فإن تعليمة التحميل أو التخزين المعاد تنفيذها ستنفذ بدون خطأ.

إذا أفرغ تطبيق ما ذاكرة باستخدام `sbrk`، تدعو `sys_sbrk` الدالة `shrinkproc` والتي تدعو `uvmdealloc`.
العمل الحقيقي ينفذ بواسطة `uvmunmap` التي تستخدم `walk` للعثور على PTEs.
نظراً لأن بعض الصفحات قد تكون لم تُستخدم مطلقاً بواسطة العملية وبالتالي لم تُخصص بـ `vmfault` مطلقاً، تتجاوز `uvmunmap` الـ PTEs ذات العلامة `PTE_V` الصفري.
إذا كان رسم PTE صالحاً، تدعو `uvmunmap` الدالة `kfree` لتحرير الذاكرة الفيزيائية التي تشير إليها.

لاحظ أن `xv6` تستخدم جدول صفحات العملية ليس فقط لإخبار العتاد بكيفية رسم العناوين الافتراضية للمستخدم، ولكن أيضاً كمسجل وحيد لصفحات الذاكرة الفيزيائية المخصصة لتلك العملية.
هذا هو السبب بداخل أن تحرير ذاكرة المستخدم (بداخل `uvmunmap`) يتطلب فحص جدول صفحات المستخدم.

== العالم الحقيقي: النسخ عند الكتابة (COW Fork)
<sec:real_world_cow>

تستخدم نظم كثيرة (رغم أن `xv6` ليست منها) أخطاء الصفحات لتنفيذ **النسخ عند الكتابة (Copy-On-Write / COW fork)**.
عد استدعاء النظام `fork` بكون الابن يشاهد ذاكرة محتواها الأولي متطابق مع ذاكرة الأب بداخل وقت الاستدعاء.
إحدى الطرق لتنفيذ هذا هي نسخ كامل ذاكرة الأب لذاكرة فيزيائية مخصصة حديثاً للابن؛ وهذا ما تفعله `xv6`.
النسخ قد يكون بطيئاً، ويكون أكثر كفاءة لو استطاع الابن مشاركة ذاكرة الأب الفيزيائية.
تنفيذ مباشر لهذا لن يعمل، نظراً لأنه يتسبب بداخل اضطراب تنفيذ الأب والابن لبعضهما البعض بكتاباتهما بداخل المكدس والهيب المشتركين.

النسخ عند الكتابة يتسبب بداخل جعل الأب والابن يتشاركان الذاكرة الفيزيائية بأمان عبر الاستخدام المناسب لأذونات جداول الصفحات وأخطاء الصفحات.
الخطة الأساسية هي أن يتشارك الأب والابن أولياً كافة الصفحات الفيزيائية، ولكن مع رسم كل منهما لها كـ للقراءة فقط (مع تصفير العلامة `PTE_W`).
يمكن للأب والابن عندئذ القراءة من الذاكرة الفيزيائية المشتركة.
إذا كتب أحدهما صفحة مشتركة، يولد معالج RISC-V استثناء خطأ صفحة.
النواة الداعمة لـ COW تستجيب عبر تخصيص صفحة جديدة من الذاكرة الفيزيائية ونسخ الصفحة المشتركة بداخل تلك الصفحة الجديدة.
ثم تغير النواة الـ PTE المناظر بداخل جدول صفحات العملية المسببة للخطأ للإشارة للنسخة والسماح بالكتابة والقراءة، ثم تستأنف العملية المسببة للخطأ عند التعليمة التي تسببت بداخل الخطأ.
نظراً لأن الـ PTE يتيح الكتابة الآن، فإن تعليمة التخزين المعاد تنفيذها ستنفذ بدون خطأ، وتعدل نسخة خاصة من الصفحة بدلاً من الصفحة المشتركة.

يتطلب النسخ عند الكتابة حوسبة ودفتر تتبع للمساعدة بداخل القرار بخصوص متى يمكن تحرير الصفحات الفيزيائية، نظراً لأن كل صفحة يمكن الإشارة إليها بواسطة عدد متغير من جداول الصفحات اعتماداً على تاريخ الاستدعاءات لـ fork وأخطاء الصفحات و exec و exit.
يتيح هذا الدفتر تحسيناً هاماً: إذا تعرضت عملية لخطأ صفحة تخزين وكانت الصفحة الفيزيائية مشاراً إليها فقط من جدول صفحات تلك العملية، فلا حاجة للنسخ.

يجعل النسخ عند الكتابة `fork` أسرع، نظراً لأن `fork` لا تحتاج لنسخ الذاكرة.
بعض الذاكرة سيتعين نسخها لاحقاً عند الكتابة، ولكن غالباً ما تكون معظم الذاكرة لا تحتاج للنسخ مطلقاً.
مثال شائع هو `fork` متبوعة بـ `exec`: بضع صفحات قد تُكتب بعد `fork` ولكن بعد ذلك يفرغ `exec` الابن معظم الذاكرة الموروثة من الأب.
النسخ عند الكتابة بـ `fork` يلغي الحاجة لنسخ هذه الذاكرة مطلقاً.
علاوة على ذلك فإن COW fork شفاف: لا حاجة لأية تعديلات على التطبيقات لكي تستفيد منه.

== العالم الحقيقي: استدعاء الصفحات عند الطلب (Demand Paging)
<sec:real_world_demand_paging>

ميزة أخرى واسعة الاستخدام تستغل أخطاء الصفحات هي **استدعاء الصفحات عند الطلب (Demand paging)**.
بداخل استدعاء النظام `exec` تحمل `xv6` كافة نصوص وبيانات التطبيق بداخل الذاكرة قبل بدء التطبيق.
نظراً لأن التطبيقات قد تكون ضخمة والقراءة من القرص تستغرق وقتاً، فإن كلفة البدء هذه قد تكون ملحوظة للمستخدمين.
لتقليل وقت البدء، لا تحمل النواة الحديثة الملف التنفيذي أولياً بداخل الذاكرة، وتكتفي بإنشاء جدول صفحات المستخدم مع تعليم كافة الـ PTEs كغير صالحة.
تبدأ النواة تشغيل البرنامج؛ وفي كل مرة يستخدم فيها البرنامج صفحة للمرة الأولى، يقع خطأ صفحة، واستجابة لذلك تقرأ النواة محتوى الصفحة من القرص وترسمه بداخل مساحة عناوين المستخدم.
مثل COW fork والتخصيص الكسول، يمكن للنواة تنفيذ هذه الميزة بأسلوب شفاف للتطبيقات.

البرامج المنفذة على حاسوب قد تحتاج ذاكرة أكثر مما يحوزه الحاسوب من RAM.
للتعامل مع ذلك بمرونة، قد تنفذ نظام التشغيل **تحويل الصفحات للقرص (Paging to disk)**.
الفكرة هي تخزين جزء فقط من صفحات المستخدم بداخل RAM، وتخزين الباقي على القرص بداخل **منطقة تحويل الصفحات (Paging area)**.
تعلم النواة الـ PTEs المناظرة للذاكرة المخزنة بداخل منطقة التبديل (وبالتالي ليست بداخل RAM) كغير صالحة.
إذا حاول تطبيق استخدام إحدى الصفحات التي جرى **تحويلها للقرص (paged out)**، سيتعرض التطبيق لخطأ صفحة، وتجب **استعادة الصفحة (paged in)**: سيعالج معالج المصيدة بالنواة الأمر عبر تخصيص صفحة RAM فيزيائية، وقراءة الصفحة من القرص بداخل RAM، وتعديل الـ PTE المناظر للإشارة لـ RAM.

ماذا يحدث إذا احتاجت صفحة لاستعادتها للقرص لكن لا تتوفر RAM فيزيائية حرة؟
بداخل هذه الحالة يجب على النواة أولاً تحرير صفحة فيزيائية عبر تحويلها للقرص أو **إخلائها (evicting)** لمنطقة تحويل الصفحات على القرص، وتعليم الـ PTEs المشارية لتلك الصفحة الفيزيائية كغير صالحة.
الإخلاء مكلف، لذا فإن تحويل الصفحات يؤدي بأفضل شكل إذا كان غير متكرر: إذا كانت التطبيقات تستخدم فقط طقماً فرعياً من صفحات ذاكرتها واتحاد الأطقم الفرعية يتسع بداخل RAM.
هذه الخاصية يُشار إليها غالباً بـ حوز مكانية مرجعية جيدة.
كما هو الحال مع تقنيات الذاكرة الافتراضية، تنفذ النوى عادة تحويل الصفحات للقرص بأسلوب شفاف للتطبيقات.

تنفذ الحاسوبات غالباً مع ذاكرة فيزيائية حرة قليلة أو معدومة، بغض النظر عن مقدار RAM الذي يوفر العتاد.
على سبيل المثال، يوفر موفرو السحابة مضاعفة لعملاء متعددين على آلة واحدة لاستخدام عتادهم بكفاءة كلفة.
كمثال آخر، ينفذ المستخدمون تطبيقات كثيرة على الهواتف الذكية بداخل مقدار صغير من الذاكرة الفيزيائية.
بداخل مثل هذه الإعدادات فإن تخصيص صفحة قد يتطلب أولاً إخلاء صفحة قائمة.
وهكذا عندما تكون الذاكرة الفيزيائية الحرة شحيحة، فإن التخصيص يكون مكلفاً.

التخصيص الكسول واستدعاء الصفحات عند الطلب نافعان بصفة خاصة عندما تكون الذاكرة الحرة شحيحة وتستخدم البرامج بفعالية جزءاً فقط من ذاكرتها المخصصة.
يمكن لهذه التقنيات أيضاً تجنب العمل المهدور عندما تُخصص صفحة أو تُحمل ولكنها إما لا تُستخدم مطلقاً أو تُخلى قبل الاستخدام.

== العالم الحقيقي: الملفات المربوطة بالذاكرة (Memory-Mapped Files)
<sec:real_world_mmap>

الميزات الأخرى التي تجمع تحويل الصفحات واستثناءات أخطاء الصفحات تتضمن توسيع المكدسات تلقائياً و **الملفات المربوطة بالذاكرة (Memory-mapped files)**، وهي ملفات يرسمها البرنامج بداخل مساحة عناوينه باستخدام استدعاء النظام `mmap` لكي يتمكن البرنامج من قراءتها وكتابتها باستخدام تعليمات التحميل والتخزين.

== تمارين
<sec:pgfault_exercises>

1. اكتب برنامج مستخدم ينمي مساحة عناوينه ببايت واحد عبر دعوة `sbrk(1)`. نفذ البرنامج وافحص جدول الصفحات للبرنامج قبل الاستدعاء لـ `sbrk` وبعد الاستدعاء لـ `sbrk`. كم مساحة خصصت النواة؟ ماذا يحوي PTE للذاكرة الجديدة؟
2. نفذ النسخ عند الكتابة (COW fork).
3. نفذ `mmap`.
