<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 500 250" width="500" height="250" style="background:#fff; font-family: monospace; font-size: 13px;">
  <rect x="20" y="20" width="280" height="200" fill="#f8f9fa" stroke="#333" stroke-width="1" rx="5" />
  <text x="35" y="45">piperead() {</text>
  <text x="50" y="65">acquire(&amp;pipe->lock);</text>
  <text x="50" y="85">while(no data...) {</text>
  <text x="65" y="105">sleep(&amp;pipe, &amp;lock) {</text>
  <text x="80" y="125">acquire(&amp;p->lock)</text>
  <text x="80" y="145">release(&amp;pipe->lock)</text>
  <text x="80" y="165">p->state = SLEEPING</text>
  <text x="80" y="185">swtch() { ...</text>

  <rect x="320" y="40" width="160" height="40" fill="#e2e8f0" stroke="#333" stroke-width="1" rx="3" />
  <text x="400" y="65" text-anchor="middle" font-family="sans-serif" font-size="12">Holding pipe->lock</text>

  <rect x="320" y="130" width="160" height="40" fill="#e2e8f0" stroke="#333" stroke-width="1" rx="3" />
  <text x="400" y="155" text-anchor="middle" font-family="sans-serif" font-size="12">Holding p->lock</text>
</svg>