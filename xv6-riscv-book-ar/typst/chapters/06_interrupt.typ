#import "../template/listings.typ": *

= المقاطعات وبرامج التشغيل
<chap:interrupt>

يتولى _برنامج التشغيل (Driver)_ إدارة أحد ملحقات العتاد الفيزيائي بداخل النواة (مثل وحدة UART لتوصيل لوحة المفاتيح والأنبوب النصفي، أو محرك الأقراص).
ينظم برنامج التشغيل إرسال الأوامر إلى الجهاز واستقبال البيانات منه عبر المقاطعات وإشارات العتاد.

تصل _مقاطعات الأجهزة (Device Interrupts)_ بصورة غير متزامنة مع تنفيذ التعليمات؛
حيث يرسل موصل المقاطعات بـ RISC-V والمسمى _PLIC (Platform-Level Interrupt Controller)_ إشارة المقاطعة للمعالج عند وصول بايت جديد من اللوحة أو اكتمال القراءة من القرص.

تقوم النواة بمعالجة المقاطعة عبر الدالة `devintr()`، التي تتعرف على مصدر المقاطعة وتستدعي برنامج التشغيل المناسب (مثل `uartintr()` أو `virtio_disk_intr()`).
يقوم برنامج التشغيل بنقل البيانات من سجلات الجهاز المربوطة بالذاكرة (MMIO) إلى ذاكرة النواة، ثم يوقظ العمليات المنتظرة لتلك البيانات.
