#import "../template/listings.typ": *

= المجدول وتنقيل السياق
<chap:sched>

يتولى _المجدول (Scheduler)_ توزيع وقت معالجة المعالج بين عدة عمليات جارية بالتناوب،
مما يمنح المستخدم انطباعاً بتنفيذ كافة البرامج متزامنة في نفس الوقت.

== تنقيل السياق (Context Switch)

يُمثل _تنقيل السياق (Context Switch)_ العملية الأساسية لتبديل تنفيذ المعالج من عملية إلى عملية أخرى.
يتطلب تنقيل السياق حفظ السجلات التنفيذية للعملية الحالية بداخل هيكل البيانات المخصص `struct context`،
ثم تحميل السجلات التنفيذية للعملية الجديدة أو المجدول.

تنفذ النواة تنقيل السياق عبر الدالة المجمّعة `swtch(old_context, new_context)`.
تحفظ `swtch` سجلات المكلّف (`ra`, `sp`, `s0`-`s11`) بداخل بنية السياق القديم،
وتحمل السجلات المقابلة بداخل بنية السياق الجديد، ثم تعود عبر تعليمية `ret` ليبدأ التنفيذ في المسار الجديد.

تحتفظ كل نواة معالج بمسار تنفيذ خاص بالمجدول المسمى `scheduler()`.
تنفذ الدالة `scheduler()` حلقة تكرارية مستمرة تتصفح جدول العمليات بحثاً عن عملية بوضع الجاهزية `RUNNABLE`،
فتستدعي `swtch` لنقل التحكم إليها؛ وعندما تنتهي فترة العمل المخصصة للعملية أو تنتظر حَدَثاً، تستدعي العملية الدالة `yield()` أو `sleep()` لتعود بالتحكم مجدداً لمسار المجدول.
