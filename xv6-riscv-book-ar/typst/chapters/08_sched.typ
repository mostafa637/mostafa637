#import "../template/listings.typ": *

= المُجَدْوِلُ وتنقيل السياق
<CH:SCHED>

من المرجح أن يعمل أي نظام تشغيل بـ عدد عَمَلِيَّاتٌ أكبر مما يملكه الحاسوب من معالجات (CPUs)، لذلك هناك حاجة إلى خطة لـ مشاركة الوقت للمعالجات بين العَمَلِيَّاتِ.
من الناحية المثالية، ستكون المشاركة شفافة بالنسبة للعمليات المستخدمة.
النهج الشائع هو توفير وهم لكل عَمَلِيَّةٌ بأنها تملك معالجها الافتراضي الخاص بـ _تعدد المشاركة_ (Multiplexing) للعمليات على المعالجات العتادية.
يشرح هذا الفصل كيف يحقق `xv6` هذا التعدد في المشاركة.

قبل المتابعة في هذا الفصل، يرجى قراءة `kernel/proc.h` `kernel/proc.h`، و `kernel/swtch.S` `kernel/swtch.S`، و `yield()`، و `sched()`، و `scheduler()` في `kernel/proc.c` `kernel/proc.c`.

== تعدد المشاركة
<sec:multiplexing>

يحقق `xv6` تعدد المشاركة عن طريق تحويل كل معالج من عَمَلِيَّةٌ إلى أخرى في حالتين.
أولاً، يتحول `xv6` عندما يجري العَمَلِيَّة استدعاء نظام حاجبًا (يجب أن ينتظر)، على سبيل المثال `read` أو `wait`.
ثانيًا، يجبر `xv6` التحويل دوريًا للتعامل مع العَمَلِيَّاتِ التي تحسب لفترات طويلة دون حاجب.
تُسمى الأولى التحويلات الطوعية، والثانية غير الطوعية.

يطرح تنفيذ تعدد المشاركة بضعة تحديات.
أولاً، كيف يتم التحويل من عَمَلِيَّةٌ إلى أخرى؟
الفكرة الأساسية هي حفظ واسترجاع سجلات CPU، على الرغم من أن حقيقة أنه لا يمكن التعبير عن ذلك بلغة C تجعل الأمر معقدًا.
ثانيًا، كيف يتم إجبار التحويلات بطريقة شفافة لعمليات المستخدم؟
يستخدم `xv6` التقنية القياسية التي تقود فيها مقاطعات المؤقت العتادي تبديل السياق.
ثالثًا، تتحول جميع المعالجات بين نفس المجموعة من العَمَلِيَّاتِ، لذا يلزم خطة إغلاق لتجنب أخطاء مثل قرار معالجين تشغيل نفس العَمَلِيَّة في نفس الوقت.
رابعًا، يجب تحرير ذاكرة العَمَلِيَّة والموارد الأخرى عندما تخرج العَمَلِيَّة، ولكن لا يمكنها إنهاء كل هذا بنفسها.
خامسًا، يجب على كل معالج في جهاز متعدد الأنوية تذكر العَمَلِيَّة التي ينفذها بحيث تؤثر اِسْتِدْعَاءَاتُ النِّظَامِ على حالة النَّوَاة للعملية الصحيحة.

== نظرة عامة على تبديل السياق
<sec:context_switch_overview>

يشير مصطلح "تبديل السياق" إلى الخطوات المتضمنة في مغادرة المعالج لتنفيذ خيط نَوَاةٌ واحد (عادةً من أجل الاستئناف لاحقًا)، واستئناف تنفيذ خيط نَوَاةٌ مختلف؛ هذا التحويل هو جوهر تعدد المشاركة.
لا يقوم `xv6` بـ تبديل السياق مباشرة من خيط نَوَاةٌ عَمَلِيَّةٌ إلى خيط نَوَاةٌ عَمَلِيَّةٌ أخرى؛
بدلاً من ذلك، يتخلى خيط النَّوَاة عن المعالج عن طريق تبديل السياق إلى "خيط المُجَدْوِلُ" الخاص بذلك المعالج، ويختار خيط المُجَدْوِلُ خيط نَوَاةٌ لعملية مختلفة لتشغيلها، ويقوم بـ تبديل السياق إلى ذلك الخيط.

\center
#figure(image("fig/switch.pdf"))
// التحويل من عَمَلِيَّةٌ مستخدم إلى أخرى. في هذا المثال، يعمل xv6 بـ معالج واحد (وبالتالي خيط مجدول واحد).
<fig:switch>


على نطاق أوسع، يوضح الشكل~@fig:switch الخطوات المتضمنة في التحويل من عَمَلِيَّةٌ مستخدم إلى أخرى: مصيدة (استدعاء نظام أو مقاطعة) من فضاء مستخدم العَمَلِيَّة القديمة إلى خيط النَّوَاة الخاص بها، وتبديل سياق إلى خيط المُجَدْوِلُ للمعالج الحالي، وتبديل سياق إلى خيط النَّوَاة لعملية جديدة، وعودة مصيدة إلى عَمَلِيَّةٌ مستوى المستخدم.

== الكود البرمجي: تبديل السياق
<sec:code_context_switching>

حتوي الدالة `swtch()` في `kernel/swtch.S` على جوهر تبديل سياق الخيط: فهي تحفظ سجلات CPU للخيط المحول منه، وتسترجع السجلات المحفوظة سابقًا للخيط المحول إليه.
السبب الأساسي في أن هذا كافٍ هو أن حالة الخيط تتكون من بيانات في الذاكرة (مثل مكدسه) بالإضافة إلى سجلات CPU الخاصة به؛
لا يلزم حفظ واسترجاع ذاكرة الخيط لأن الخيوط المختلفة تحتفظ ببياناتها في مناطق مختلفة من RAM؛ ولكن المعالج يملك مجموعة واحدة فقط من السجلات لذا يجب تبديلها (حفظها واسترجاعها) بين الخيوط.

تحتوي `struct proc` لكل خيط على `struct context` يحتفظ بـ سجلات الخيط المحفوظة عندما لا يكون قيد التشغيل.
يقع `struct context` لخيط مجدول المعالج في `struct cpu` الخاص بذلك المعالج.
عندما يرغب الخيط X في التحويل إلى الخيط Y، يستدعي الخيط X الدالة `swtch(\&X's context, \&Y's context)`.
تحفظ `swtch()` سجلات المعالج الحالية في سياق X، ثم تحمل محتوى سياق Y في سجلات المعالج، ثم تعود.

إليك نسخة مختصرة من `swtch`:
\begin{verbatim}
swtch:
        sd ra, 0(a0)
        sd sp, 8(a0)
        sd s0, 16(a0)
        ...
        sd s11, 104(a0)

        ld ra, 0(a1)
        ld sp, 8(a1)
        ld s0, 16(a1)
        ...
        ld s11, 104(a1)
        
        ret
\end{verbatim}

يحتفظ `a0` بـ وسيط الدالة الأول، و `a1` بـ الثاني؛ في هذه الحالة، مؤشرا `struct context`.
يشير `16(a0)` إلى إزاحة 16 بايت في الذاكرة المومأ إليها بواسطة `a0`؛
بالرجوع إلى تعريف `struct context` في `kernel/proc.h` سطر `kernel/proc.h:/^struct.context/`، هذا هو حقل الهيكل المسمى `s0`.

إلى أين تعود تعليمة `ret` الخاصة بـ `swtch`؟ تعود إلى التعليمة التي يشير إليها سجل `ra`.
في المثال الذي يستدعي فيه الخيط X الدالة `swtch()` للتحويل إلى Y، عندما تنفذ `ret`، يكون قد تم تحميل `ra` للتو من `struct context` الخاص بـ Y.
و كان `ra` في `struct context` الخاص بـ Y قد حُفظ أصلًا بواسطة استدعاء Y لـ `swtch` عندما تخلى Y عن المعالج في الماضي.
لذا فإن `ret` تعود إلى التعليمة التالية للنقطة التي استدعى عندها Y الدالة `swtch()`؛ أي أن استدعاء X لـ `swtch()` يعود كما لو كان يعود من استدعاء Y الأصلي لـ `swtch()`.
وسيكون `sp` هو مؤشر مكدس Y، حيث حملت `swtch` الـ `sp` من `struct context` الخاص بـ Y؛ وبالتالي عند العودة، سينفذ Y على مكدسه الخاص.
لا تحتاج `swtch()` إلى حفظ أو استرجاع عداد البرنامج مباشرة؛ يكفي حفظ واسترجاع `ra`.

تحفظ `swtch` السجلات المحفوظة بواسطة المداعى عليه (`ra,sp,s0..s11`) ولكن ليس السجلات المحفوظة بواسطة المداعي.
تتطلب اتفاقية استدعاء RISC-V أنه إذا كان الكود بحاجة إلى الحفاظ على القيمة في سجل محفوظ بواسطة المداعي عبر استدعاء دالة، فيجب على المترجم توليد تعليمات تحفظ السجل في المكدس قبل استدعاء الدالة، وتسترجع من المكدس عندما تعود الدالة.
لذا يمكن لـ `swtch` الاعتماد على الدالة التي استدعتها في أنها قد حفظت بالفعل السجلات المحفوظة بواسطة المداعي (إما ذلك، أو أن الدالة المداعية لم تكن بحاجة إلى القيم في السجلات).

== الكود البرمجي: المُجَدْوِلُ
<sec:code_scheduling>

تفحص المقطع السابق الأحشاء الداخلية لـ `swtch`؛ الآن دعنا نأخذ `swtch` كـ أمر مسلم به ونتفحص التحويل من خيط نَوَاةٌ عَمَلِيَّةٌ عبر المُجَدْوِلُ إلى عَمَلِيَّةٌ أخرى.
يوجد المُجَدْوِلُ في شكل خيط خاص لكل معالج، يقود كل منهما دالة `scheduler`.
هذه الدالة مسؤولة عن اختيار العَمَلِيَّة التي سيتم تشغيلها لاحقًا.
يمتلك كل معالج خيط مجدول خاص به لأن أكثر من معالج قد يبحث عن شيء لتشغيله في أي وقت معين.
يمر تبديل العَمَلِيَّات دائمًا عبر خيط المُجَدْوِلُ، بدلاً من المباشرة من عَمَلِيَّةٌ إلى أخرى، لتجنب بعض الحالات التي لن يكون فيها مكدس يمكن تشغيل المُجَدْوِلُ عليه (على سبيل المثال إذا خرجت العَمَلِيَّة القديمة، أو لم تكن هناك عَمَلِيَّةٌ أخرى تريد التشغيل حاليًا).

يجب على العَمَلِيَّةِ التي تريد التخلي عن CPU حيازة قفل العَمَلِيَّة الخاص بها `p->lock`، وتحديد وتحرير أي أقفال أخرى تحتفظ بها، وتحديث حالتها الخاصة (`p->state`)، ثم استدعاء `sched`.
يمكنك رؤية هذا التسلسل في `yield` سطر `kernel/proc.c:/^yield/`، و `sleep`، و `kexit`.
تستدعي `sched` الدالة `swtch` لحفظ السياق الحالي في `p->context` والتحويل إلى سياق المُجَدْوِلُ في `cpu->context`.
تعود `swtch` على مكدس المُجَدْوِلُ كما لو كانت `swtch` الخاصة بـ `scheduler` قد عادت.

تشغل `scheduler` حلقة:
تجد عَمَلِيَّةٌ لتشغيلها، و `swtch()` إليها، وفي النهاية ستعود بـ `swtch()` إلى المُجَدْوِلِ، الذي يستمر في حلقته.
يدور المُجَدْوِلُ عبر جدول العَمَلِيَّات بحثًا عن عَمَلِيَّةٌ قابلة للتنفيذ، عَمَلِيَّةٌ تحتوي على `p->state` `==` `RUNNABLE`.
بمجرد أن يجد عَمَلِيَّةٌ، يعين متغير العَمَلِيَّة الحالية لكل معالج `c->proc`، ويعلم العَمَلِيَّة كـ `RUNNING`، ثم يستدعي `swtch` لبدء تشغيلها.
في مرحلة ما في الماضي، يجب أن تكون العَمَلِيَّة المستهدفة قد استدعت `swtch()`؛ ويعود استدعاء المُجَدْوِلُ لـ `swtch()` بفعالية من ذلك الاستدعاء المبكر.
يوضح الشكل~@fig:inout هذا النمط.

\input{fig/switch.tex}
// تكون `swtch()` دائمًا خيط المُجَدْوِلُ كـ مصدر أو وجهة، ويتم الاحتفاظ بـ `p->lock` ذات الصلة دائمًا.
<fig:inout>
\end{figure">

يحتفظ xv6 بـ `p->lock` عبر الاستدعاءات لـ `swtch`:
يحوز المداعي لـ `swtch` القُفْلُ، ولكنه يحرر في المستهدف بعد عودة `swtch`.
هذا الترتيب غير معتاد: فمن الشائع أن يقوم الخيط الذي يحوز القُفْلُ بتحريره أيضًا.
يكسر تبديل السياق في Xv6 هذا العرف لأن `p->state` و `p->context` يجب تحديثهما معًا ذريًا.
على سبيل المثال، لو تم تحرير `p->lock` قبل استدعاء `sched`، فقد يتخذ معالج مختلف $c$ قرارًا بـ تشغيل العَمَلِيَّة لأن حالتها `RUNNABLE`.
سيستدعي المعالج $c$ الدالة `swtch` والتي ستسترجع من `p->context` بينما لا يزال المعالج الأصلي يحفظ في `p->context`.
ستكون النتيجة هي استرجاع العَمَلِيَّة بسجلات محفوظة جزئيًا على المعالج $c$ وأن كلا المعالجين سيستخدمان نفس المكدس، مما يتسبب في فوضى.
بمجرد أن تبدأ `yield` في تعديل حالة عَمَلِيَّةٌ جارية لجعلها `RUNNABLE`، يجب أن يظل `p->lock` محوزًا حتى تحفظ العَمَلِيَّة جميع سجلاتها ويعمل المُجَدْوِلُ على مكدسه.
أبكر نقطة تحرير صحيحة هي بعد أن يمسح `scheduler` (الذي يعمل على مكدسه الخاص) `c->proc`.
وبالمثل، بمجرد أن تبدأ `scheduler` في تحويل عَمَلِيَّةٌ `RUNNABLE` إلى `RUNNING`، لا يمكن تحرير القُفْلُ حتى يعمل خيط نَوَاةٌ العَمَلِيَّة بالكامل (بعد `swtch`، على سبيل المثال في `yield`).

هناك حالة واحدة لا ينتهي فيها استدعاء المُجَدْوِلُ لـ `swtch` في `sched`.
تعين `allocproc` سجل السياق `ra` لعملية جديدة إلى `forkret` سطر `kernel/proc.c:/^forkret/`، بحيث يعود أول `swtch` لها إلى بداية تلك الدالة.
توجد `forkret` لتحرير `p->lock` وإعداد بعض سجلات التحكم وحقول إطار المِصْيَدَةُ المطلوبة من أجل العودة إلى فضاء المستخدم.
في النهاية، تحاكي `forkret` مسار العودة العادي من استدعاء نظام إلى فضاء المستخدم.

== الكود البرمجي: mycpu و myproc
<sec:code_mycpu_myproc>

غالباً ما يحتاج Xv6 إلى مؤشر لهيكل `proc` الخاص بالعَمَلِيَّة الحالية.
على المعالج الواحد، يمكن للمرء الحصول على متغير عالمي يشير إلى `proc` الحالي.
هذا لا يعمل على جهاز متعدد الأنوية، لأن كل معالج ينفذ عَمَلِيَّةٌ مختلفة.
طريقة حل هذه المشكلة هي استغلال حقيقة أن كل معالج يملك مجموعته الخاصة من السجلات.

بينما ينفذ معالج معين في النَّوَاةِ، يضمن xv6 أن سجل `tp` الخاص بالمعالج يحتفظ دائمًا بـ hartid الخاص بالمعالج.
يرقم RISC-V معالجاته، معطيًا كل منها _hartid_ فريدًا.
تستخدم `mycpu` سطر `kernel/proc.c:/^mycpu/` سجل `tp` لفهرسة مصفوفة من هياكل `cpu` وإعادة الهيكل الخاص بـ المعالج الحالي.
حتفظ `struct cpu` بـ مؤشر لهيكل `proc` للعملية الجارية حاليًا على ذلك المعالج (إن وجدت)،
والسجلات المحفوظة لخيط مجدول المعالج، وعدد أقفال التناوب المتداخلة المطلوبة لإدارة تعطيل المُقَاطَعَاتُ.

تأكيد أن `tp` الخاص بالمعالج يحتفظ بـ hartid الخاص بالمعالج أمر معقد قليلاً، لأن كود المستخدم حر في تعديل `tp`.
تعين `start` سجل `tp` مبكرًا في تسلسل إقلاع المعالج، بينما لا يزال في وضع الآلة.
أثناء التحضير للعودة إلى فضاء المستخدم، تحفظ `prepare_return` سجل `tp` في صفحة المنصة القافزة، تحسبًا لتعديل كود المستخدم له.
أخيرًا، تسترجع `uservec` ذلك `tp` المحفوظ عند دخول النَّوَاة من فضاء المستخدم.
يضمن المترجم عدم تعديل `tp` مطلقًا في كود النَّوَاة.
سيكون من الأنسب لو كان بإمكان xv6 سؤال عتاد RISC-V عن hartid الحالي كلما دعت الحاجة، ولكن RISC-V يسمح بذلك فقط في وضع الآلة، وليس في وضع المشرف.

القيم المرجعة لـ `cpuid` و `mycpu` هشّة: إذا قاطعت المُقَاطَعَةُ وقاطعت المؤقت وتسببت في تخلي الخيط واستئناف التنفيذ لاحقًا على معالج مختلف، فلن تكون القيمة المرجعة سابقًا صحيحة بعد الآن.
لتجنب هذه المشكلة، يتطلب xv6 من الكود تعطيل المُقَاطَعَاتُ قبل استدعاء `cpuid()` أو `mycpu()`، وتفعيل المُقَاطَعَاتُ فقط عند الانتهاء من استخدام القيمة المرجعة.

تعيد الدالة `myproc` سطر `kernel/proc.c:/^myproc/` مؤشر `struct proc` للعملية التي تعمل على المعالج الحالي.
تعطل `myproc` المُقَاطَعَاتُ، وتستدعي `mycpu`، وتجلب مؤشر العَمَلِيَّة الحالية (`c->proc`) من `struct cpu`، ثم تفعل المُقَاطَعَاتُ.
قيمة العودة لـ `myproc` آمنة للاستخدام حتى لو كانت المُقَاطَعَاتُ مفعلة:
إذا نقلت مقاطعة المؤقت الزمني العَمَلِيَّة المداعية إلى معالج مختلف، فسيظل مؤشر `struct proc` الخاص بها كما هو.

== العالم الحقيقي
<sec:real_world_sched>

ينفذ مجدول xv6 سياسة جدولة بسيطة تشغل كل عَمَلِيَّةٌ بدروها.
تُسمى هذه السياسة بـ _الدوران العام_ (Round Robin).
تنفذ أنظمة التشغيل الحقيقية سياسات أكثر تعقيدًا تسمح، على سبيل المثال، للعمليات بوجود أولويات.
الفكرة هي أن العَمَلِيَّة ذات الأولوية العالية القابلة للتنفيذ ستُفضل بواسطة المُجَدْوِلُ على عَمَلِيَّةٌ ذات أولوية منخفضة قابلة للتنفيذ.
يمكن أن تصبح هذه السياسات معقدة لأن هناك غالبًا أهدافًا متنافسة: على سبيل المثال، قد يرغب نظام التشغيل أيضًا في ضمان العدالة والإنتاجية العالية.

== تمارين
<sec:exercises_sched>


  + عدل xv6 لاستخدام تبديل سياق واحد فقط عند التحويل من خيط نَوَاةٌ عَمَلِيَّةٌ إلى أخرى، بدلاً من التحويل عبر خيط المُجَدْوِلُ. سيحتاج الخيط المتخلي إلى اختيار الخيط التالي بنفسه واستدعاء `swtch`. ستكون التحديات هي منع معالجات متعددة من تنفيذ نفس الخيط عن طريق الخطأ؛ والحصول على الإغلاق بشكل صحيح؛ وتجنب الجمود.

