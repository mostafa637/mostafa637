#import "../template/listings.typ": *

= نظام الملفات
<CH:FS>

الغرض من نظام الملفات هو تنظيم وتخزين البيانات.
تدعم أنظمة الملفات عادةً مشاركة البيانات بين المستخدمين والتطبيقات،
بالإضافة إلى \indextext{ثبات البيانات} (Persistence) حتى تظل البيانات متاحة بعد إعادة تشغيل النظام.

يوفر نظام ملفات #lstinline("xv6") ملفات ودلائل وأسماء مسارات شبيهة بـ Unix (انظر الفصل~@CH:UNIX)،
ويخزن بياناته على قرص صلب لضمان ثباتها.
يعالج نظام الملفات عدة تحديات رئيسية:
- يحتاج نظام الملفات إلى هياكل بيانات على القرص لتمثيل شجرة الدلائل المسمة والملفات، وتسجيل هويات الكتل التي تحتفظ بمحتوى كل ملف، وتسجيل المناطق الحرة على القرص.
- قد تعمل عمليات مختلفة على نظام الملفات في الوقت نفسه، لذا يجب أن ينسق كود نظام الملفات للمحافظة على الثوابت.
- الوصول إلى القرص الصلب أبطأ بـ درجات عديدة من الوصول إلى الذاكرة، لذا يجب أن يحتفظ نظام الملفات بـ ذاكرة مخبئية في الذاكرة للكتل الشائعة الاستخدام.
- يجب أن يدعم نظام الملفات \indextext{التعافي عند الانهيار} (Crash Recovery). أي إذا حدث انهيار (مثل انقطاع الكهرباء)، فيجب أن يظل نظام الملفات يعمل بشكل صحيح بعد إعادة التشغيل. الخطر هو أن الانهيار قد يقاطع تسلسلاً من التحديثات ويترك هياكل البيانات على القرص غير متسقة.

يشرح بقية هذا الفصل كيف يعالج #lstinline("xv6") التحديات الثلاثة الأولى،
بينما يركز الفصل~@CH:LOG على التعافي عند الانهيار.

== نظرة عامة
<sec:fs_overview>

تم تنظيم تنفيذ نظام ملفات #lstinline("xv6") في سبع طبقات،
موضحة في الشكل~@fig:fslayer.
تقرأ وتكتب طبقة القرص كتل القرص على قرص virtio صلب محاكى يوفر بواسطة QEMU.
تخزن طبقة المخزن المؤقت للكتل كتل القرص مؤقتًا وتنسق الوصول إليها،
محققة أن عملية نواة واحدة فقط في كل مرة يمكنها تعديل البيانات المخزنة في أي كتلة معينة.
تسمح طبقة سجل العمليات للطبقات الأعلى بـ تغليف التحديثات لعدة كتل في \indextext{معاملة} (Transaction)،
وتضمن تحديث الكتل ذريًا عند الانهيارات (أي إما تحديثها جميعًا أو لا شيء).
توفر طبقة العقد الفهرسية ملفات فردية،
تُمثل كل منها كـ \indextext{عقدة فهرسية} (Inode) برقم i فريد وبعض الكتل التي تحتفظ ببيانات الملف.
تنقذ طبقة الدلائل كل دليل كـ نوع خاص من العقد الفهرسية التي يكون محتواها تسلسلاً من مدخلات الدلائل،
يحتوي كل منها على اسم ملف ورقم i.
توفر طبقة أسماء المسارات أسماء مسارات هرمية مثل #lstinline("/usr/rtm/xv6/fs.c")،
وتحللها بـ بحث تكراري.
تُجرد طبقة واصفات الملفات العديد من موارد Unix (مثل الأنابيب والأجهزة والملفات) باستخدام واجهة نظام الملفات،
مما يسهل حياة المبرمجين.

\begin{figure}[t]
\center
\includegraphics[scale=0.5]{fig/fslayer.pdf}
\caption{طبقات نظام ملفات xv6.}
<fig:fslayer>
\end{figure}

يقدم عتاد القرص التقليدي البيانات على القرص كـ تسلسل مرقم من _القطاعات_ بحجم 512 بايت:
القطاع 0 هو أول 512 بايت، والقطاع 1 هو التالي، وهكذا.
يدعم عتاد القرص القراءات والكتابات فقط بـ مضاعفات كاملة من القطاعات.
وبالتالي،
إذا احتاج نظام التشغيل إلى تغيير بايت واحد على القرص،
فيجب عليه قراءة القطاع المحيط بأكمله في الذاكرة،
وتحديث البايت الواحد،
ثم كتابة القطاع بأكمله مرة أخرى إلى القرص.
لهذا السبب،
تخصص أنظمة الملفات مساحة القرص للملفات بـ وحدات من قطاع واحد أو أكثر.
يسمى حجم التخصيص الذي يستخدمه نظام الملفات بـ حجم الكتلة (Block).
تستخدم معظم أنظمة الملفات كتلًا متعددة القطاعات لكفاءة أفضل؛
يستخدم #lstinline("xv6") حجم كتلة من قطاعين (محدد بواسطة {\tt BSIZE} في {\tt fs.h}).

يحتفظ #lstinline("xv6") بـ نسخ من الكتل التي قرأها في الذاكرة في كائنات من نوع #lstinline("struct buf").
البيانات المخزنة في هذا الهيكل ليست في بعض الأحيان هي نفسها البيانات الموجودة على القرص:
ربما لم يتم قراءتها بعد من القرص،
أو ربما تم تحديثها بواسطة البرمجية ولكن لم يتم كتابتها بعد إلى القرص.

\begin{figure}[t]
\center
\includegraphics[scale=0.5]{fig/fslayout.pdf}
\caption{بنية تخطيط نظام ملفات xv6.}
<fig:fslayout>
\end{figure}

يجب أن يملك نظام الملفات خطة لمكان تخزين العقد الفهرسية وكتل المحتوى على القرص.
وللقيام بذلك،
يقسم #lstinline("xv6") القرص إلى عدة أجزاء،
كما يوضح الشكل~@fig:fslayout.
لا يستخدم نظام الملفات الكتلة 0 (غالبًا ما تحتفظ الكتلة 0 بـ كود الإقلاع).
تُسمى الكتلة 1 بـ \indextext{الكتلة الفائقة} (Superblock)؛
وتحتوي على البيانات الوصفية حول نظام الملفات (حجم نظام الملفات بالكتل،
وعدد كتل البيانات، وعدد العقد الفهرسية،
وعدد الكتل في السجل).
تحتفظ الكتل التي تبدأ عند 2 بـ السجل.
بعد السجل تأتي العقد الفهرسية،
مع وجود عقد فهرسية متعددة في كل كتلة.
بعد ذلك تأتي كتل خريطة البتات التي تتتبع كتل البيانات قيد الاستخدام.
الكتل المتبقية هي كتل البيانات؛
إما علم عليها كـ حرة في كتل خريطة البتات،
أو تحتفظ بـ محتوى لملف أو دليل.

يتم إنشاء نظام ملفات #lstinline("xv6") في البداية بواسطة برنامج خارج #lstinline("xv6") يُسمى \indexcode{mkfs}.
يكتب {\tt mkfs} الكتلة الفائقة،
ويهيئ عقدة فهرسية لـ الدليل الجذر (الفارغ)،
ويعلم جميع العقد الفهرسية وكتل البيانات كـ حرة.

== طبقة المخزن المؤقت للكتل
<s:bcache>

يملك المخزن المؤقت للكتل وظيفتين:
(1) مزامنة الوصول إلى كتل القرص للتأكد من وجود نسخة واحدة فقط من الكتلة في الذاكرة وأن خيط نواة واحد فقط في كل مرة يستخدم تلك النسخة؛
(2) تخزين الكتل الشائعة المخبئية بحيث لا يلزم إعادة قراءتها من القرص البطيء.
الكود موجود في #lstinline("bio.c").

تتكون الواجهة الرئيسية التي تصدرها طبقة المخزن المؤقت للكتل من \indexcode{bread} و \indexcode{bwrite}؛
تحصل الأولى على \indextext{buf} يحتوي على نسخة من الكتلة يمكن قراءتها أو تعديلها في الذاكرة،
وتكتب الثانية مخزنًا مؤقتًا معدلاً إلى الكتلة المناسبة على القرص.
يجب على خيط النواة تحرير المخزن المؤقت عن طريق استدعاء \indexcode{brelse} عند الانتهاء منه.
يستخدم المخزن المؤقت للكتل قفل نوم لكل مخزن مؤقت لضمان استخدام خيط واحد فقط لكل مخزن مؤقت (وبالتالي كل كتلة قرص) في كل مرة؛
تعيد #lstinline("bread") مخزنًا مؤقتًا مقفلاً،
وتحرر #lstinline("brelse") القفل.

يملك المخزن المؤقت للكتل عددًا ثابتًا من المخازن المؤقتة للاحتفاظ بـ كتل القرص،
مما يعني أنه إذا طلب نظام الملفات كتلة ليست موجودة بالفعل في الذاكرة المخبئية،
فيجب على المخزن المؤقت للكتل إعادة تدوير مخزن مؤقت يحتفظ حاليًا بـ كتلة أخرى.
يعيد المخزن المؤقت للكتل تدوير المخزن المؤقت الأقل استخدامًا مؤخرًا (LRU) للكتلة الجديدة.
الافتراض هو أن المخزن المؤقت الأقل استخدامًا مؤخرًا هو الأقل ترجيحًا لاستخدامه مرة أخرى قريباً.

== الكود البرمجي: المخزن المؤقت للكتل
<sec:code_buffer_cache>

المخزن المؤقت للكتل هو قائمة متصلة مزدوجًا من المخازن المؤقتة.
تهيئ الدالة \indexcode{binit}،
المستدعاة بواسطة \indexcode{main}،
القائمة بـ \indexcode{NBUF} من المخازن المؤقتة في المصفوفة الثابتة #lstinline("buf").
تشير جميع الوصولات الأخرى إلى القائمة المتصلة عبر \indexcode{bcache.head}.

يرتبط بـ المخزن المؤقت حقلان للحالة.
يشير الحقل \indexcode{valid} إلى أن المخزن المؤقت يحتوي على نسخة من الكتلة.
ويشير الحقل \indexcode{disk} إلى أن محتوى المخزن المؤقت قد أُعطي للقرص،
والذي قد يغير المخزن المؤقت.

تستدعي #lstinline("bread") الدالة \indexcode{bget} للحصول على مخزن مؤقت لـ الكتلة المحددة.
إذا كان المخزن المؤقت بحاجة إلى القراءة من القرص،
تستدعي #lstinline("bread") الدالة \indexcode{virtio_disk_rw} للقيام بذلك قبل إعادة المخزن المؤقت.

تمسح #lstinline("bget") قائمة المخازن المؤقتة بحثًا عن مخزن مؤقت بـ رقم الجهاز والكتلة المحددين.
إذا كان هناك مثل هذا المخزن المؤقت،
تحوز \indexcode{bget} قفل النوم لـ المخزن المؤقت.
ثم تعيد #lstinline("bget") المخزن المؤقت المقفل.

إذا لم يكن هناك مخزن مؤقت مخبئي لـ الكتلة المحددة،
فيجب على \indexcode{bget} إنشاء واحد،
مع إمكانية إعادة استخدام مخزن مؤقت احتفظ بـ كتلة مختلفة.
وتمسح قائمة المخازن المؤقتة مرة ثانية،
بحثًا عن مخزن مؤقت غير قيد الاستخدام (#lstinline("b->refcnt = 0"))؛
ويمكن استخدام أي مخزن مؤقت من هذا القبيل.
تعدل #lstinline("bget") البيانات الوصفية للمخزن المؤقت لتسجيل رقم الجهاز والكتلة الجديدين وتحوز قفل النوم الخاص به.
لاحظ أن التعيين #lstinline("b->valid = 0") يضمن أن #lstinline("bread") ستقرأ بيانات الكتلة من القرص بدلاً من استخدام المحتويات السابقة للمخزن المؤقت بشكل غير صحيح.

من المهم وجود مخزن مؤقت مخبئي واحد فقط كحد أقصى لكل كتلة قرص،
لضمان رؤية القراء لـ الكتابات،
ولأن نظام الملفات يستخدم الأقفال على المخازن المؤقتة لـ المزامنة.
تضمن #lstinline("bget") هذا الثابت عن طريق حيازة #lstinline("bcache.lock") باستمرار من فحص الحلقة الأولى لـ ما إذا كانت الكتلة مخبئية حتى إعلان الحلقة الثانية أن الكتلة أصبحت مخبئية الآن.

== الكود البرمجي: مخصص الكتل
<sec:code_block_allocator>

يتم تخزين محتوى الملفات والدلائل في كتل القرص،
والتي يجب تخصيصها من مجمع حر.
يحتفظ مخصص كتل #lstinline("xv6") بـ خريطة بتات حرة على القرص،
مع بت واحد لكل كتلة.
يشير البت الصفر إلى أن الكتلة المقابلة حرة؛
ويشير البت الواحد إلى أنها قيد الاستخدام.
عندما ينشئ نظام ملفات جديدًا،
يعين #lstinline("mkfs") البتات المطابقة لـ قطاع الإقلاع،
والكتلة الفائقة، وكتل السجل، وكتل العقد الفهرسية،
وكتل خريطة البتات.

يوفر مخصص الكتل دالتين:
\indexcode{balloc} لتخصيص كتلة قرص جديدة،
و \indexcode{bfree} لتحرير كتلة.
تراعي الحلقة في #lstinline("balloc") كل كتلة،
تبدأ عند الكتلة 0 حتى #lstinline("sb.size")،
عدد الكتل في نظام الملفات.
وتبحث عن كتلة بت خريطة البتات الخاص بها صفر،
مما يشير إلى أنها حرة.
إذا وجدت #lstinline("balloc") مثل هذه الكتلة،
فإنها تحدث خريطة البتات وتعيد الكتلة.

== طبقة العقد الفهرسية
<sec:inode_layer>

يمكن أن يكون لـ مصطلح \indextext{عقدة فهرسية} (Inode) أحد معنيين مرتبطين.
قد يشير إلى هيكل البيانات على القرص المحتوي على حجم الملف وقائمة أرقام كتل البيانات.
أو قد تشير "العقدة الفهرسية" إلى عقدة فهرسية في الذاكرة،
تحتوي على نسخة من العقدة الفهرسية على القرص بالإضافة إلى معلومات إضافية مطلوبة داخل النواة.

تُعبأ العقد الفهرسية على القرص في منطقة متصلة من القرص تُسمى كتل العقد الفهرسية.
كل عقدة فهرسية تملك نفس الحجم، لذا من السهل،
مع إعطاء رقم n،
العثور على العقدة الفهرسية الـ n على القرص.
في الواقع، هذا الرقم n،
المسمى رقم العقدة الفهرسية أو رقم i،
هو كيفية التعرف على العقد الفهرسية في التنفيذ.

تُحدد العقدة الفهرسية على القرص بواسطة \indexcode{struct dinode}.
يميز حقل #lstinline("type") بين الملفات والدلائل والملفات الخاصة (الأجهزة).
يشير النوع صفر إلى أن العقدة الفهرسية على القرص حرة.
يحسب حقل #lstinline("nlink") عدد مدخلات الدلائل التي تشير إلى هذه العقدة الفهرسية،
للتعرف على متى ينبغي تحرير العقدة الفهرسية على القرص وكتل بياناتها.
يسجل حقل #lstinline("size") عدد بايتات المحتوى في الملف.
تسجل مصفوفة #lstinline("addrs") أرقام الكتل لـ كتل القرص التي تحتفظ بـ محتوى الملف.

تحتفظ النواة بـ مجموعة العقد الفهرسية النشطة في الذاكرة في جدول يُسمى \indexcode{itable}؛
\indexcode{struct inode} هي النسخة في الذاكرة لـ #lstinline("struct dinode") على القرص.
تخزن النواة عقدة فهرسية في الذاكرة فقط إذا كانت هناك مؤشرات C تشير إلى تلك العقدة الفهرسية.
يحسب حقل #lstinline("ref") عدد مؤشرات C التي تشير إلى العقدة الفهرسية في الذاكرة،
وتتخلص النواة من العقدة الفهرسية من الذاكرة إذا انخفض عدد المراجع إلى الصفر.
تحوز وتفرغ الدالتان \indexcode{iget} و \indexcode{iput} المؤشرات إلى عقدة فهرسية،
معدلتين عدد المراجع.

توجد أربع أقفال أو آليات شبيهة بـ الأقفال في كود العقد الفهرسية في #lstinline("xv6").
يحمي #lstinline("itable.lock") الثابت القائل بأن العقدة الفهرسية موجودة في جدول العقد الفهرسية مرة واحدة كحد أقصى،
والثابت القائل بأن حقل #lstinline("ref") للعقدة الفهرسية في الذاكرة يحسب عدد المؤشرات في الذاكرة إلى العقدة الفهرسية.
تحتوي كل عقدة فهرسية في الذاكرة على حقل #lstinline("lock") يحتوي على قفل نوم،
يضمن الوصول الحصري إلى حقول العقدة الفهرسية (مثل طول الملف) وكذلك إلى كتل محتوى الملف أو الدليل الخاصة بـ العقدة الفهرسية.
يتسبب #lstinline("ref") للعقدة الفهرسية،
إذا كان أكبر من الصفر،
في حفظ النظام لـ العقدة الفهرسية في الجدول،
وعدم إعادة استخدام مدخل الجدول لـ عقدة فهرسية مختلفة.
أخيرًا،
تحتوي كل عقدة فهرسية على حقل #lstinline("nlink") (على القرص ومأخوذ في الذاكرة إذا كانت في الذاكرة) يحسب عدد مدخلات الدلائل التي تشير إلى ملف؛
ولن يحرر #lstinline("xv6") عقدة فهرسية إذا كان عدد وصلاتها أكبر من الصفر.

== الكود البرمجي: محتوى العقد الفهرسية
<sec:code_inode_content>

تحتوي مصفوفة عناوين الكتلة #lstinline("addrs[]") على 13 عنصرًا:
أول 12 عنصرًا عبارة عن عناوين مباشرة لـ 12 كتلة بيانات ($12 \times 1024 = 12$ كيلوبايت).
العنصر الـ 13 عبارة عن عنوان لـ *كتلة غير مباشرة أحادية* (Singly-indirect block) تحوي 256 عنوانًا لكتل بيانات إضافية ($256 \times 1024 = 256$ كيلوبايت).
وبذلك يكون الحد الأقصى لحجم الملف في #lstinline("xv6") هو $12 + 256 = 268$ كتلة (268 كيلوبايت).

تستخدم النواة الدالة #lstinline("bmap(ip, bn)") للترجمة بين رقم الكتلة داخل الملف والرقم المادي المربوط بها على القرص الصلب.

== الكود البرمجي: طبقة الدلائل
<sec:code_directory_layer>

الدليل في #lstinline("xv6") هو ملف عادي يحتوي على مصفوفة من الإدخالات (#lstinline("struct dirent")).
يحتوي كل إدخال على رقم العقدة الفهرسية (#lstinline("inum")) واسم الملف النصي (#lstinline("name")،
بحد أقصى 14 حرفًا في #lstinline("xv6")).

تبحث الدالة #lstinline("dirlookup()") داخل الدليل عن اسم معين لتسترجع رقم العقدة الفهرسية الخاصة به.
بينما تقوم الدالة #lstinline("dirlink()") بفرز اسم جديد وإضافته إلى الدليل.

== العالم الحقيقي
<sec:real_world_fs>

تستخدم أنظمة الملفات الحديثة مثل ext4 و ZFS و NTFS آليات متطورة للغاية مثل مجموعات الأشجار النقطية (Extents)،
وقوائم التحكم بالوصول (ACLs)،
والتخزين المؤقت الذكي مع دعم ملفات ممتدة بأحجام تيرابايت.

== تمارين
<sec:exercises_fs>

+ احسب الحد الأقصى لحجم الملف في #lstinline("xv6") إذا تمت إضافة كتلة غير مباشرة ثنائية (Doubly-indirect block).
+ قم بتوسيع نظام الملفات لدعم أسماء الملفات الطويلة حتى 255 حرفًا.