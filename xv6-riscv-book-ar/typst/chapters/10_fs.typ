#import "../template/listings.typ": *

= نظام الملفات
<CH:FS>

تتمثل الغاية الجوهرية لنظام الملفات في تنظيم البيانات وتخزينها.
تدعم نظم الملفات عادةً مشاركة البيانات بين المستخدمين والتطبيقات المختلفة،
فضلاً عن توفير خاصية **الاستمرارية (Persistence)** بحيث تظل البيانات متاحة ومحفوظة حتى بعد إعادة تشغيل الجهاز.

يوفر نظام الملفات بـ `xv6` ملفات ودلائل وأسماء مسارات شجرية شبيهة بنظام Unix (انظر الفصل @CH:UNIX)،
ويخزن بياناته على القرص الصلب لضمان الاستمرارية.
يتولى نظام الملفات معالجة عدة تحديات هندسية رئيسية:
- يحتاج نظام الملفات إلى هياكل بيانات على القرص لتمثيل شجرة المجلدات والملفات المسمات،
وتسجيل معرّفات الكتل التي تحمل محتوى كل ملف،
وتسجيل المناطق الحرة المتاحة على القرص.
- قد تعمل عمليات متعددة على نظام الملفات في الوقت نفسه،
لذا يجب أن تتنسق شفرة نظام الملفات للحفاظ على الثوابت المحفوظة.
- يكون الوصول إلى القرص أبطأ بدرجات متعددة مقارنة بالوصول إلى الذاكرة الرئيسية RAM،
لذا يجب أن يحتفظ نظام الملفات بمخزن مؤقت في الذاكرة للكتل الأكثر استخداماً.
- يجب أن يدعم نظام الملفات **التعافي من الانهيارات المفاجئة (Crash recovery)**؛
أي عند وقوع انهيار (مثل انقطاع التيار الكهربائي)،
يجب أن يعمل نظام الملفات بشكل صحيح بعد إعادة التشغيل.
وتكمن المخاطرة في أن الانهيار قد يقاطع تسلسلاً من التحديثات ويترك هياكل بيانات القرص في حالة غير متسقة (مثل كتلة قرصية تكون مستخدمة في ملف ومسجلة في الوقت نفسه ككتلة حرة).

يشرح بقية هذا الفصل كيف يعالج `xv6` التحديات الثلاثة الأولى،
بينما يركز الفصل @CH:LOG على التعافي من الانهيارات.

== نظرة عامة
<sec:fs_overview>

يُنظم تنفيذ نظام الملفات في `xv6` في سبع طبقات متكاملة،
كما يتضح في الشكل @fig:fslayer:
تُقرأ طبقة القرص وتكتب الكتل على محرك الأقراص الصلبة virtio؛
و virtio هو قرص محاكى يتوفره `qemu`.
تخزن طبقة المخزن المؤقت للكتل كتل القرص مؤقتاً وتنسق الوصول إليها،
متأكدة من أن عملية نواة واحدة فقط في الوقت الواحد يمكنها تعديل البيانات المخزنة في أية كتلة محددة.
تتيح طبقة السجل للطبقات الأعلى تغليف التحديثات لعدة كتل في معاملة ذرية (**Transaction**)،
وتضمن تحديث الكتل ذرياً في مواجهة الانهيارات (أي يتم تحديثها جميعاً أو لا شيء منها).
توفر طبقة العقد الفهرسية ملفات فردية يمثل كل منها بـ **عقدة فهرسية (Inode)** تحمل رقماً فريداً وكتلاً تحمل بيانات الملف.
تنفذ طبقة الدلائل كل مجلد كنوع خاص من العقد الفهرسية يحتوي محتواه على متتالية من مدخلات الدليل،
كل مدخل يحوي اسم الملف ورقم عقدته الفهرسية.
توفر طبقة أسماء المسارات أسماء مسارات شجرية مثل `/usr/rtm/xv6/fs.c` وتحلها عبر البحث التكراري.
تجرد طبقة واصفات الملفات العديد من موارد Unix (مثل الأنابيب،
الأجهزة،
الملفات،
إلخ) باستخدام واجهة نظام الملفات،
مما يسهل حياة مبرمجي التطبيقات.

#figure(image("fig/fslayer.svg"), caption: [طبقات نظام الملفات في xv6.]) <fig:fslayer>

يعرض عتاد القرص البيانات على القرص كمتتالية مرقمة من القطاعات (**Sectors**) بحجم 512 بايتاً لكل قطاع: القطاع 0 هو أول 512 بايتاً، والقطاع 1 هو التالي، وهكذا.
يدعم عتاد القرص عمليات القراءة والكتابة فقط بمضاعفات صحيحة من القطاعات.
وهكذا، على سبيل المثال، إذا احتاج نظام التشغيل تغيير بايت واحد في القرص، يجب عليه قراءة القطاع المحيط كاملاً في الذاكرة، ثم تعديل البايت الواحد، ثم إعادة كتابة القطاع كاملاً للقرص.
لهذا السبب، تخصص نظم الملفات المساحة القرصية بكتل تتكون من قطاع واحد أو أكثر. تُسمى درجة التخصيص التي يستخدمها نظام الملفات بحجم الكتلة (**Block size**).
تستخدم معظم نظم الملفات كتيراً متعددة القطاعات لتحقيق كفاءة أفضل؛ ويستخدم `xv6` حجم كتلة يتكون من قطاعين (يحدد بالثابت `BSIZE` في `fs.h`).

يحتفظ `xv6` بنسخ من الكتل المقروءة في الذاكرة في كائنات من النوع `struct buf`.
البيانات المخزنة في هذا الهيكل تكون أحياناً ليست متطابقة مع البيانات على القرص: قد لا تكون قُرئت بعد من القرص (القرص يعمل عليها ولكنه لم يُعد محتوى الكتلة بعد)، أو قد تكون عُدلت بواسطة البرمجيات ولم تُكتب بعد على القرص.

#figure(image("fig/fslayout.svg"), caption: [هيكلية تقسيم القرص في نظام الملفات في xv6.]) <fig:fslayout>

يجب أن يمتلك نظام الملفات خطة لمكان تخزينه للعقد الفهرسية وكتل المحتوى على القرص.
ولقيام بذلك، يقسم `xv6` القرص إلى عدة أقسام، كما يوضح الشكل @fig:fslayout.
لا يستخدم نظام الملفات الكتلة 0 (تحمل الكتلة 0 غالباً شفرة الإقلاع، وإن كانت بـ `xv6` غير مستخدمة مجرداً).
تُسمى الكتلة 1 بـ **الكتلة الفائقة (Superblock)**؛ وتحتوي بيانات وصفية عن نظام الملفات (حجم نظام الملفات بالكتل، وعدد كتل البيانات، وعدد العقد الفهرسية، وعدد الكتل في السجل).
تستقر الكتل ابتداءً من 2 في السجل. بعد السجل تأتي العقد الفهرسية، مع عقد فهرسية متعددة في الكتلة الواحدة. بعد ذلك تأتي كتل الخريطة البتية التي تتتبع أية كتل بيانات مستخدمة.
الكتل المتبقية هي كتل البيانات؛ كل واحدة منها إما معلمة كحرّة في كتلة الخريطة البتية، أو تحمل محتوى لملف أو مجلد.

يُنقذ نظام ملفات `xv6` أولياً بواسطة برنامج خارج `xv6` يُسمى `mkfs`.
يكتب `mkfs` الكتلة الفائقة، ويهيئ عقدة فهرسية للمجلد الرئيسي المفرغ (Root Directory)، ويحدد كافة العقد الفهرسية وكتل البيانات كحرة.

عند هذه النقطة، يُرجى قراءة الملفات: `kernel/buf.h` و `kernel/fs.h` و `kernel/fs.c` و `kernel/sysfile.c` و `kernel/file.c`.

== طبقة المخزن المؤقت للكتل
<s:bcache>

يقوم المخزن المؤقت للكتل (Buffer Cache) بمهمتين: (1) تزامن الوصول لكتل القرص لضمان وجود نسخة واحدة فقط من الكتلة في الذاكرة وأن خيط نواة واحداً فقط في الوقت الواحد يستخدم تلك النسخة؛ (2) تخزين الكتل الشائعة مؤقتاً لكي لا يحتاج لإعادة قراءتها من القرص البطيء. الشفرة في `bio.c`.

تتكون الواجهة الرئيسية المخرجة بواسطة المخزن المؤقت من `bread` و `bwrite`؛
تحصل الأولى على `buf` يحوي نسخة من الكتلة المقروءة والتي يمكن قراءتها أو تعديلها في الذاكرة، وتكتب الأخيرة الحاجز المعدل للكتلة المناسبة في القرص.
يجب على خيط النواة تحرير الحاجز باستدعاء `brelse` فور انتهائه منه.
يستخدم المخزن المؤقت قفل نوم لكل حاجز لضمان أن خيطاً واحداً فقط في الوقت الواحد يستخدم كل حاجز (وبذلك كل كتلة قرصية)؛
ترجع `bread` حواشي مقفلة، وترفع `brelse` القفل.

يحتوي المخزن المؤقت على عدد ثابت من الحواجز لنقل كتل القرص، مما يعني أنه إذا طلب نظام الملفات كتلة ليست موجودة في المخزن المؤقت بالفعل، يجب على المخزن المؤقت إعادة تدوير حاجز يحتفظ حالياً بكتلة أخرى.
يعيد المخزن المؤقت تدوير الحاجز الأقل استخداماً مؤخراً للكتلة الجديدة.
الافتراض هو أن الحاجز الأقل استخداماً مؤخراً هو الأكثر ترجيحاً لأن لا يُستخدم مجدداً في القريب العاجل.

هناك تفاعلان بين طبقة المخزن المؤقت وطبقة السجل: 1) لتنفيذ المعاملات الذرية، تحدّث الطبقات الأعلى من نظام الملفات كتلة القرص عبر استدعاء `log_write` والذي يعمل كوكيل لـ `bwrite` وطبقة السجل فقط هي التي تدعو `bwrite`؛ 2) لا يمكن للمخزن المؤقت إعادة تدوير حاجز مستخدم بواسطة معاملة قائمة حتى تثبت طبقة السجل المعاملة (انظر الفصل @CH:LOG).
لتجنب إعادة التدوير المبكر جداً، تستخدم طبقة السجل دالتي المخزن المؤقت `bpin` و `bunpin` لتثبيت وحل ثبات الحاجز في المخزن المؤقت.

عند هذه النقطة، يُرجى قراءة الملفات: `kernel/bio.c` و `kernel/log.c`.

== الكود البرمجي: المخزن المؤقت للكتل
<sec:code_bcache>

المخزن المؤقت للكتل هو قائمة ترابطية ثنائية الاتجاه من الحواجز.
تُهيئ الدالة `binit` التي تستدعيها `main()` القائمة بالحواجز المحددة بـ `NBUF` في المصفوفة الثابتة `buf`.
وتتم كافة عمليات الوصول الأخرى للمخزن المؤقت عبر القائمة الترابطية بواسطة `bcache.head` وليس مصفوفة `buf`.

يرتبط بالحاجز حقلان للحالة.
الحقل `valid` يشير إلى أن الحاجز يحتوي على نسخة من الكتلة.
والحقل `disk` يشير إلى أن محتوى الحاجز سلم للقرص، والذي قد يغير الحاجز (مثل كتابة البيانات من القرص في `data`).

تستدعي `bread` الدالة `bget` للحصول على حاجز للكتلة المحددة.
إذا احتاج الحاجز للقراءة من القرص، تستدعي `bread` الدالة `virtio_disk_rw` لفعل ذلك قبل إرجاع الحاجز.

تفحص `bget` قائمة الحواجز بحثاً عن حاجز بالجهاز ورقم الكتلة المحددين.
إذا وجد ذلك الحاجز، تحصل `bget` على قفل النوم المخصص للحاجز.
ثم ترجع `bget` الحاجز المقفول.

إذا لم يجد حافز مخزن للكتلة المحددة، يجب على `bget` إنشاء واحد، ربما عبر إعادة استخدام حاجز كان يحتفظ بكتلة أخرى.
تتفحص قائمة الحواجز للمرة الثانية، بحثاً عن حاجز ليس قيد الاستخدام (`b->refcnt == 0`)؛ أي حاجز كذلك يمكن استخدامه.
تعدل `bget` البيانات الوصفية للحاجز لتسجيل الجهاز ورقم الكتلة الجديد وتستحوذ على قفل النوم المخصص له.
لاحظ أن التعيين `b->valid = 0` يضمن أن `bread` ستقرأ بيانات الكتلة من القرص بدلاً من الاستخدام الخاطئ لمحتويات الحاجز السابقة.

من المهم وجود حاجز مخزن واحد بحد أقصى لكل كتلة قرصية، لضمان أن القراء يشاهدون الكتابات، ولأن نظام الملفات يستخدم الأقفال على الحواجز للمزامنة.
تضمن `bget` هذا الثابت الملازم عبر احتجاز `bcache.lock` باستمرار من فحص الحلقة الأولى لما إذا كانت الكتلة مخزنة مؤقتاً وحتى إعلان الحلقة الثانية أن الكتلة مخزنة مؤقتاً الآن (عبر ضبط `dev` و `blockno` و `refcnt`).
يتسبب هذا في جعل الفحص لوجود الكتلة و(إذا لم تكن موجودة) تحديد حاجز لنقل الكتلة أمراً ذرياً.

من الآمن لـ `bget` الاستحواذ على قفل النوم الخاص بالحاجز خارج المقطع الحرج لـ `bcache.lock`، نظراً لأن القيمة غير الصفرية لـ `b->refcnt` تمنع إعادة استخدام الحاجز لكتلة قرصية مختلفة.
يحمي قفل النوم القراءات والكتابات لمحتوى الكتلة في الذاكرة، بينما يحمي `bcache.lock` المعلومات المتعلقة بأية الكتل مخزنة مؤقتاً.

إذا كانت كافة الحواجز مشغولة، فإن عمليات كثيرة تنفذ استدعاءات نظام الملفات بالتوازي في الوقت نفسه؛ وتنفذ `bget` الهلع `panic`.
استجابة أكثر ملاءمة قد تكون النوم حتى يصبح حاجز حرّاً، رغم أنه سيكون هناك عندئذ احتمالية للانسداد المتبادل (Deadlock).

بمجرد أن تقرأ `bread` القرص (عند الحاجة) وترجع الحاجز لمستدعيه، يكون للمستدعي الاستخدام الحصري للحاجز ويمكنه قراءة أو كتابة بايتات البيانات.
إذا قام المستدعي بتعديل الحاجز، يجب عليه استدعاء `bwrite` لكتابة البيانات المعدلة للقرص قبل تحرير الحاجز.
تدعو `bwrite` الدالة `virtio_disk_rw` للتحدث مع عتاد القرص.

عندما ينتهي المستدعي من الحاجز، يجب عليه استدعاء `brelse` لتحريره.
(الاسم `brelse` واختصاره b-release غامض ولكن يستحق التعلم: نشأ في Unix ويُستخدم بـ BSD و Linux و Solaris أيضاً).
تحرر `brelse` قفل النوم وتنقل الحاجز لمقدمة القائمة الترابطية.
يتسبب نقل الحاجز في جعل القائمة مرتبة بمدى استخدام الحواجز مؤقتاً (بمعنى تحريرها): الحاجز الأول في القائمة هو الأكثر استخداماً مؤخراً، والأخير هو الأقل استخداماً مؤخراً.
تستفيد الحلقات في `bget` من هذا: المسح عن حاجز موجود يجب أن يعالج كامل القائمة في أسوأ الحالات، لكن فحص الحواجز الأكثر استخداماً مؤخراً أولاً (بدءاً من `bcache.head` ومتابعة مؤشرات `next`) سيقلل وقت المسح عندما توجد مكانية جيدة للمراجع.
المسح لاختيار حاجز لإعادة الاستخدام يختار الحاجز الأقل استخداماً مؤخراً عبر المسح للخلف (متابعة مؤشرات `prev`).

== الكود البرمجي: مخصص الكتل
<sec:code_block_allocator>

تُخزن محتويات الملفات والدلائل في كتل القرص، والتي يجب تخصيصها من مجمع حر.
يحتفظ مخصص كتل `xv6` بخريطة بتية حرة على القرص، مع بت واحد لكل كتلة.
يشير البت الصفري إلى أن الكتلة المناظرة حرة؛ ويشير البت الأحد إلى أنها قيد الاستخدام.
عند إنشائه لنظام ملفات جديد، يضبط `mkfs` البتات المناظرة لقطاع الإقلاع، والكتلة الفائقة، وكتل السجل، وكتل العقد الفهرسية، وكتل الخريطة البتية.

يوفر مخصص الكتل دالتين: `balloc` تخصص كتلة قرصية جديدة، و `bfree` تحرر كتلة.
تراعي الحلقة في `balloc` كل كتلة، بدءاً من الكتلة 0 وحتى `sb.size` (عدد الكتل في نظام الملفات).
تبحث عن كتلة بت خريطتها البتية صفراً، مما يشير لأنها حرة.
إذا وجدت `balloc` كتلة كذلك، تحدّث الخريطة البتية وترجع الكتلة.
من أجل الكفاءة، تُقسم الحلقة إلى جزأين.
تقرأ الحلقة الخارجية كل كتلة من بتات الخريطة البتية.
تفحص الحلقة الداخلية كافة بتات البتات-لكل-كتلة (`BPB`) في كتلة خريطة بتية واحدة.
حالة التنافس التي قد تقع إذا حاولت عمليتان تخصيص كتلة في الوقت نفسه تُمنع بواسطة حقيقة أن المخزن المؤقت للكتل يتيح لعملية واحدة فقط استخدام أية كتلة خريطة بتية في الوقت الواحد.

تجد `bfree` كتلة الخريطة البتية المناسبة وتصفر البت المناسب.
مجدداً الاستخدام الحصري الضمني بـ `bread` و `brelse` يتجنب الحاجة لقفل صريح.

كما هو الحال مع معظم الشفرة الموصوفة في بقية هذا الفصل، يجب استدعاء `balloc` و `bfree` في معاملة سجل.

== طبقة العقد الفهرسية
<sec:inode_layer>

قد يحمل المصطلح **عقدة فهرسية (Inode)** واحداً من معنيين مرتبطين.
قد يشير إلى هيكل البيانات على القرص الحاوي حجم الملف وقائمة أرقام كتل البيانات.
أو قد تشير "العقدة الفهرسية" لعقدة فهرسية في الذاكرة، والتي تحتوي نسخة من عقدة القرص بالإضافة لمعلومات إضافية مطلوبة في النواة.

تُجمع العقد الفهرسية على القرص في منطقة متصلة من القرص تُسمى كتل العقد الفهرسية.
كل عقدة فهرسية تحظى بنفس الحجم، لذا فمن السهل، بإعطاء رقم n، العثور على العقدة الفهرسية رقم n على القرص.
في الواقع، هذا الرقم n، المسمى رقم العقدة الفهرسية أو رقم-i (i-number)، هو كيفية التعرف على العقد الفهرسية في التنفيذ.

تحدد العقدة الفهرسية على القرص بواسطة `struct dinode`.
يميز الحقل `type` بين الملفات، والدلائل، والملفات الخاصة (الأجهزة).
يشير النوع الصفري إلى أن العقدة الفهرسية على القرص حرة.
يحسب الحقل `nlink` عدد مدخلات الدليل التي تشير لتلك العقدة الفهرسية، من أجل التعرف على الوقت الذي ينبغي فيه تحرير العقدة الفهرسية وكتل بياناتها على القرص.
يسجل الحقل `size` عدد بايتات المحتوى في الملف.
تسجل مصفوفة `addrs` أرقام كتل القرص المحتفظة بمحتوى الملف.

تحتفظ النواة بطقم العقد الفهرسية النشطة في الذاكرة في جدول يُسمى `itable`؛
و `struct inode` هو النسخة في الذاكرة لـ `struct dinode` على القرص.
تخزن النواة العقدة الفهرسية في الذاكرة فقط إذا كانت هناك مؤشرات C تشير لتلك العقدة الفهرسية.
يحسب الحقل `ref` عدد مؤشرات C المشارية للعقدة الفهرسية في الذاكرة، وتتخلص النواة من العقدة الفهرسية من الذاكرة إذا هبط عداد المراجع إلى الصفر.
تستحوذ الدالتان `iget` و `iput` وتفرغان المؤشرات للعقدة الفهرسية، معدلتين عداد المراجع.
يمكن للمؤشرات للعقدة الفهرسية أن تأتي من واصفات الملفات، المجلدات الحالية للعمل، وشفرات النواة العابرة مثل `kexec`.

توجد أربع أقفال أو آليات شبيهة بالأقفال في شفرة العقد الفهرسية لـ `xv6`:
تحمي `itable.lock` الثابت الملازم بكون العقدة الفهرسية موجودة في جدول العقد الفهرسية بحد أقصى مرة واحدة، والثابت الملازم بكون الحقل `ref` في الذاكرة يحسب عدد المؤشرات في الذاكرة للعقدة الفهرسية.
تحتوي كل عقدة فهرسية في الذاكرة على حقل `lock` يحوي قفل نوم، والذي يضمن الوصول الحصري لحقول العقدة الفهرسية (مثل طول الملف) وكذلك لكتل المحتوى في الملف أو المجلد.
يتسبب الحقل `ref` للعقدة الفهرسية، إذا كان أكبر من الصفر، في جعل النظام يحتفظ بالعقدة في الجدول، وعدم إعادة استخدام مدخل الجدول لعقدة فهرسية مختلفة.
أخيراً، تحتوي كل عقدة فهرسية على حقل `nlink` (على القرص وممنسوخ في الذاكرة إذا كانت في الذاكرة) يحسب عدد مدخلات الدليل التي تشير لملف؛ لن تحرر `xv6` عقدة فهرسية إذا كان عداد روابطها أكبر من الصفر.

مؤشر `struct inode` المرجوع بواسطة `iget()` يضمن كونه صالحاً حتى الاستدعاء المناظر لـ `iput()`؛
لن تُحذف العقدة الفهرسية، والذاكرة المشار إليها بواسطة المؤشر لن تُعاد استخدامها لعقدة فهرسية مختلفة.
توفر `iget()` وصولاً غير حصري لعقدة فهرسية، لكي يمكن وجود مؤشرات عديدة لنفس العقدة الفهرسية.
تعتمد أجزاء كثيرة من شفرة نظام الملفات على هذا السلوك لـ `iget()`، سواء للاحتفاظ بمراجع طويلة الأمد للعقد الفهرسية (كملفات مفتوحة ومجلدات حالية) ولمنع التنافسات مع تجنب الانسداد المتبادل في الشفرة التي تتعامل مع عقد فهرسية متعددة (مثل البحث في اسم المسار).

هيكل `struct inode` المرجوع بواسطة `iget` قد لا يحتوي على أي محتوى نافع.
من أجل ضمان احتوائه نسسخة من العقدة الفهرسية على القرص، يجب على الشفرة دعوة `ilock`.
هذا يقفل العقدة الفهرسية (لكي لا تتمكن أية عملية أخرى من تنفيذ `ilock` عليها) ويقرأ العقدة الفهرسية من القرص، إذا لم تكن قد قُرئت بالفعل.
تحرر `iunlock` القفل على العقدة الفهرسية.
فصل الاستحواذ على مؤشرات العقد الفهرسية عن القفل يساعد في تجنب الانسداد المتبادل في بعض الحالات، على سبيل المثال أثناء البحث في الدليل.
يمكن لعمليات متعددة احتجاز مؤشر C لعقدة فهرسية مرجوعة بواسطة `iget`، ولكن عملية واحدة فقط يمكنها قفل العقدة الفهرسية في الوقت الواحد.

يخزن جدول العقد الفهرسية العقد الفهرسية التي تحتفظ برمجيات النواة أو هياكل بياناتها بمؤشرات C إليها.
مهمتها الرئيسية هي تزامن الوصول بواسطة عمليات متعددة.
يصادف جدول العقد الفهرسية أيضاً تخزين العقد الفهرسية كثرة الاستخدام مؤقتاً، لكن التخزين المؤقت ثانوني؛ إذا كانت العقدة الفهرسية تُستخدم بكثرة، فمن المرجح أن المخزن المؤقت للكتل سيحتفظ بها في الذاكرة.
الشفرة التي تعدل عقدة فهرسية في الذاكرة تكتبها للقرص بـ `iupdate`.

== الكود البرمجي: العقد الفهرسية
<sec:code_inodes>

لتخصيص عقدة فهرسية جديدة (على سبيل المثال، عند إنشاء ملف)، تدعو `xv6` الدالة `ialloc`.
تُعد `ialloc` شبيهة بـ `balloc`: تدور في حلقة تكرارية على هياكل العقد الفهرسية على القرص، كتلة في الوقت الواحد، بحثاً عن واحدة معلمة كحرة.
عندما تعثر على واحدة، تطالب بها عبر كتابة النوع الجديد `type` للقرص ثم ترجع مدخلاً من جدول العقد الفهرسية مع الاستدعاء الأخير لـ `iget`.
العمل الصحيح لـ `ialloc` يعتمد على حقيقة أن عملية واحدة فقط في الوقت الواحد يمكنها احتجاز مرجع لـ `bp`: يمكن لـ `ialloc` التأكد من أن عملية أخرى لن تشاهد في الوقت نفسه أن العقدة الفهرسية متاحة وتحاول المطالبة بها.

تبحث `iget` عبر جدول العقد الفهرسية عن مدخل نشط (`ip->ref > 0`) بالجهاز المطلوب ورقم العقدة الفهرسية.
إذا وجدت واحداً، ترجع مرجعاً جديداً لتلك العقدة الفهرسية.
بينما تمسح `iget` تنفذ تسجيلاً لموقع أول خانة فارغة، والتي تستخدمها إذا احتاجت لتخصيص مدخل في الجدول.

يجب على الشفرة قفل العقدة الفهرسية باستخدام `ilock` قبل قراءة أو كتابة بياناتها الوصفية أو محتواها.
تستخدم `ilock` قفل نوم لهذا الغرض.
بمجرد أن تحصل `ilock` على الوصول الحصري للعقدة الفهرسية، تقرأ العقدة الفهرسية من القرص (الأرجح من المخزن المؤقت) إذا لزم الأمر.
تحرر الدالة `iunlock` قفل النوم، مما قد يتسبب في إيقاظ أية عمليات نائمة.

تحرر `iput` مؤشر C لعقدة فهرسية عبر تنقيص عداد المراجع.
إذا كان هذا هو المرجع الأخير، تصبح خانة العقدة الفهرسية في جدول العقد الفهرسية حرة الآن ويمكن إعادة استخدامها لعقدة فهرسية مختلفة.

إذا شاهدت `iput` أنه لا توجد مراجع مؤشرات C لعقدة فهرسية وأن العقدة الفهرسية لا توجد روابط تشير إليها (تقع في لا مجلد)، فعندئذ يجب تحرير العقدة الفهرسية وكتل بياناتها.
تدعو `iput` الدالة `itrunc` لقطع الملف إلى صفر بايت، محربة كتل البيانات؛ وتضبط نوع العقدة الفهرسية إلى 0 (غير مخصصة)؛ وتكتب العقدة الفهرسية للقرص.

بروتوكول القفل في `iput` في الحالة التي تحرر فيها العقدة الفهرسية يستحق نظرة أقرب.
أحد المخاطر هو أن خيطاً متزامناً قد يكون منتظراً في `ilock` لاستخدام هذه العقدة الفهرسية (مثلاً، لقراءة ملف أو إدراج مجلد)، ولن يكون متجهزاً لاكتشاف أن العقدة الفهرسية لم تعد مخصصة.
لا يمكن حدوث هذا لأنه لا توجد طريقة لاستدعاء نظام للحصول على مؤشر لعقدة فهرسية في الذاكرة إذا لم تكن لها روابط وكان `ip->ref` واحداً.
ذلك المرجع الواحد هو المرجع المملوك بواسطة الخيط الداعي لـ `iput`.
الخطر الرئيسي الآخر هو أن استدعاءً متزامناً لـ `ialloc` قد يختار نفس العقدة الفهرسية التي تقوم `iput` بتحريرها.
يمكن حدوث هذا فقط بعد أن يكتب `iupdate` للقرص لكي تصبح العقدة الفهرسية بنوع صفر.
هذا التنافس حميد؛ الخيط المخصص سينتظر بأدب للاستحواذ على قفل نوم العقدة الفهرسية قبل قراءة أو كتابة العقدة الفهرسية، وفي تلك النقطة تكون `iput` قد انتهت منها.

يمكن لـ `iput()` الكتابة للقرص. هذا يعني أن أي استدعاء نظام يستخدم نظام الملفات قد يكتب للقرص، لأن استدعاء النظام قد يكون الأخير الحائز مرجعاً للملف.
حتى استدعاءات مثل `read()` التي تبدو للقراءة فقط، قد تنتهي بدعوة `iput()`.
وهذا، بدوره، يعني أنه حتى استدعاءات النظام المخصصة للقراءة فقط يجب أن تحاط في معاملات سجل إذا كانت تستخدم نظام الملفات.

توجد تفاعلات مليئة بالتحدي بين `iput()` والانهيارات.
لا تقطع `iput()` الملف فورياً عندما يهبط عداد الروابط للملف للصفر، لأن عملية ما قد تظل محتجزة لمرجع للعقدة الفهرسية في الذاكرة: عملية قد تظل تقرأ وتكتب للملف، لأنها فتحته بنجاح.
ولكن، إذا وقع انهيار قبل أن تغلق العملية الأخيرة واصف الملف للملف، فعندئذ سيعلم الملف كمخصص على القرص ولكن لا يوجد مدخل دليل يشير إليه.

تتعامل نظم الملفات مع هذه الحالة بإحدى طريقتين. الحل البسيط هو أنه عند التعافي، بعد إعادة التشغيل، يمسح نظام الملفات كامل نظام الملفات عن ملفات معلمة كمخصصة ولكن لا يوجد مدخل دليل يشير إليها. إذا وجد أي ملف كذلك، فيمكنه تحرير تلك الملفات.

الحل الثاني لا يتطلب مسح نظام الملفات. في هذا الحل، يسجل نظام الملفات على القرص (مثلاً، في الكتلة الفائقة) رقم-i للعقدة الفهرسية لملف هبط عداد روابطه للصفر ولكن عداد مراجعة ليس صفراً.
إذا أزال نظام الملفات الملف عندما يصل عداد مراجعة لـ 0، فعندئذ يحدّث القائمة في القرص عبر إزالة تلك العقدة الفهرسية من القائمة. عند التعافي، يحرر نظام الملفات أي ملف في القائمة.

لا تنفذ `xv6` أياً من الحلين، مما يعني أن العقد الفهرسية قد تظل معلمة كمخصصة على القرص، حتى وإن لم تعد قيد الاستخدام. هذا يعني أنه مع الوقت تواجه `xv6` خطر نفاذ المساحة القرصية.

== الكود البرمجي: محتوى العقد الفهرسية
<sec:code_inode_content>

#figure(image("fig/inode.svg"), caption: [تمثيل الملف على القرص.]) <fig:inode>

هيكل العقدة الفهرسية على القرص، `struct dinode`، يحتوي حجماً ومصفوفة من أرقام الكتل (انظر الشكل @fig:inode).
توجد بيانات العقدة الفهرسية في الكتل المدرجة في مصفوفة `addrs` بـ `dinode`.
الكتل الـ `NDIRECT` الأولى من البيانات مدرجة في المدخلات الـ `NDIRECT` الأولى في المصفوفة؛ تُسمى هذه الكتل بـ **الكتل المباشرة (Direct blocks)**.
الكتل الـ `NINDIRECT` التالية من البيانات ليست مدرجة في العقدة الفهرسية بل في كتلة بيانات تُسمى **الكتلة غير المباشرة (Indirect block)**.
المدخل الأخير في مصفوفة `addrs` يعطي عنوان الكتلة غير المباشرة.
وهكذا فإن الـ 12 كيلو بايت الأولى (`NDIRECT x BSIZE`) بايت من الملف يمكن تحميلها من كتل مدرجة في العقدة الفهرسية، بينما الـ 256 كيلو بايت التالية (`NINDIRECT x BSIZE`) بايت يمكن تحميلها فقط بعد استشارة الكتلة غير المباشرة.
هذا تمثيل جيد على القرص ولكنه معقد للعملاء.
تتولى الدالة `bmap` إدارة التمثيل لكي لا تحاج الروتينات أعلى المستوى، مثل `readi` و `writei` لإدارة هذا التعقيد.
ترجع `bmap` رقم كتلة القرص للكتلة المخصصة رقم `bn` في العقدة الفهرسية `ip`.
إذا لم تكن `ip` تحوز تلك الكتلة بعد، تخصص `bmap` واحدة.

تبدأ الدالة `bmap` بالتقاط الحالة السهلة: الكتل الـ `NDIRECT` الأولى مدرجة في العقدة الفهرسية نفسها.
الكتل الـ `NINDIRECT` التالية مدرجة في الكتلة غير المباشرة عند `ip->addrs[NDIRECT]`.
تقرأ `bmap` الكتلة غير المباشرة ثم تقرأ رقم كتلة من الموقع الصحيح في الكتلة.
إذا تجاوز رقم الكتلة `NDIRECT+NINDIRECT`؛ تنفذ `bmap` الهلع `panic`؛ وتتضمن `writei` الفحص الذي يمنع وقوع هذا.

تخصص `bmap` الكتل حسب الحاجة.
المدخل الصفري بـ `ip->addrs[]` أو المدخل غير المباشر الصفري يشير إلى أنه لا توجد كتلة مخصصة.
عندما تواجه `bmap` أصفاراً، تستبدلها بأرقام كتل طازجة، مخصصة عند الطلب.

تكرر `itrunc` تحرير كتل الملف، مجددة ضبط حجم العقدة الفهرسية للصفر.
تبدأ `itrunc` بتحرير الكتل المباشرة، ثم تلك المدرجة في الكتلة غير المباشرة، وأخيراً الكتلة غير المباشرة نفسها.

تجعل `bmap` من السهل لـ `readi` و `writei` الوصول لبيانات العقدة الفهرسية.
تبدأ `readi` بالتأكد من أن الإزاحة والعداد ليسا يتجاوزان نهاية الملف.
القراءات التي تبدأ بعد نهاية الملف ترجع خطأ بينما القراءات التي تبدأ عند أو تعبر نهاية الملف ترجع بايتات أقل من المطلوبة.
تعالج الحلقة الرئيسية كل كتلة في الملف، ناسخة البيانات من المخزن المؤقت إلى `dst`.
تُعد `writei` مطابقة لـ `readi` مع ثلاثة استثناءات: الكتابات التي تبدأ عند أو تعبر نهاية الملف تنمي الملف، وصولاً للحد الأقصى لحجم الملف؛ تكرر الحلقة نسخ البيانات في الحواشي بدلاً من الخارج؛ وإذا كانت الكتابة قد وسعت الملف، يجب على `writei` تحديث حجمه.

تنسخ الدالة `stati` البيانات الوصفية للعقدة الفهرسية في هيكل `stat` المخرج لبرامج المستخدم عبر استدعاء النظام `stat`.

== الكود البرمجي: طبقة الدلائل
<sec:code_directory_layer>

يُنقذ المجلد في الشفرة بشكل شبيه بالملف.
العقدة الفهرسية الخاصة به تحوز النوع `T_DIR` وبياناته متتالية من مدخلات الدليل.
كل مدخل هو `struct dirent` حاوٍ اسماً ورقم عقدة فهرسية.
الاسم بحد أقصى `DIRSIZ` (14) حرفاً؛ وإذا كان أقصر ينهى ببايت صفري NULL.
مدخلات الدليل برقم عقدة فهرسية صفر تكون حرة.

السبب في احتواء مدخل الدليل على رقم العقدة الفهرسية للفيلم بدلاً من العقدة الفهرسية الكاملة للملف هو دعم "الروابط" المنشأة بواسطة استدعاء النظام `link()`.
يمكن الإشارة للعقدة الفهرسية في مجلدات متعددة، وبذلك تحت أسماء مسارات متعددة؛ كل اسم من أسماء العقدة الفهرسية (أي مدخل دليل) يُسمى رابطاً.
بسبب احتمالية تسمية العقدة الفهرسية في مجلدات متعددة، فمن غير المناسب تخزين العقدة الفهرسية في أي من تلك المجلدات.
احتمالية الروابط المتعددة هي أيضاً السبب وراء الحقل `nlink` في العقدة الفهرسية.
حقيقة تخزين العقد الفهرسية بصفة مستقلة عن الدلائل تتيح أيضاً المعالجة المنطقية لإلغاء ربط ملف (إزالته) بينما تحوز عملية ما واصف ملف يشير للملف: واصف الملف يشير للعقدة الفهرسية، وليس لأي مدخل دليل، لذا فإن واصف الملف سيظل يعمل على الرغم من إزالة الملف (في الواقع، مجرد اسمه).

تبحث الدالة `dirlookup` في مجلد عن مدخل بالاسم المحدد.
إذا وجدت واحداً، ترجع مؤشراً للعقدة الفهرسية المناظرة، غير مقفول، وتضبط `*poff` لإزاحة البايت في المدخل في المجلد، في حالة رغبة المستدعي بتعديلها.
إذا وجدت `dirlookup` مدخلاً بالاسم الصحيح، تحدّث `*poff` وترجع عقدة فهرسية غير مقفولة متحصل عليها عبر `iget`.
`dirlookup` هي السبب في إرجاع `iget` لعقد الفهرسية غير مقفولة.
المستدعي قفل `dp`، لذا لو كان البحث عن `.`، واسم مستعار للمجلد الحالي، فإن محاولة قفل العقدة الفهرسية قبل الإرجاع ستداول إعادة قفل `dp` والانسداد المتبادل.
(توجد سيناريوهات انسداد متبادل أكثر تعقيداً تتضمن عمليات متعددة و `..` واسم مستعار للمجلد الأب؛ `.` ليست المشكلة الوحيدة).
يمكن للمستدعي تحرير قفل `dp` ثم قفل `ip` متأكداً من حيازة قفل واحد في الوقت الواحد.

تكتب الدالة `dirlink` مدخل دليل جديد بالاسم ورقم العقدة الفهرسية المحدد في المجلد `dp`.
إذا كان الاسم موجوداً بالفعل، ترجع `dirlink` خطأ.
تقرأ الحلقة الرئيسية مدخلات الدليل بحثاً عن مدخل غير مخصص.
عندما تجد واحداً تتوقف الحلقة التكرارية مبكراً مع ضبط `off` لإزاحة المدخل المتاح.
وإلا تنتهي الحلقة مع ضبط `off` لـ `dp->size`.
بأية حال تنشئ `dirlink` مدخلاً جديداً في المجلد عبر الكتابة عند الإزاحة `off`.

== الكود البرمجي: أسماء المسارات
<sec:code_path_names>

يتطلب البحث في اسم المسار متتالية استدعاءات لـ `dirlookup` لكل مكون مسار.
تقيم `namei` المسار وترجع العقدة الفهرسية المناظرة.
الدالة `nameiparent` متغير: تتوقف قبل العنصر الأخير، مرجعة العقدة الفهرسية للمجلد الأب وناسوخة العنصر النهائي في `name`.
كلاهما يدعوان الدالة العامة `namex` لتنفيذ العمل الحقيقي.

تبدأ `namex` بالقرار من أين يبدأ تقييم المسار.
إذا بدأ المسار بمائل، يبدأ التقييم عند الجذر؛ وإلا المجلد الحالي.
ثم تستخدم `skipelem` لمراعاة كل عنصر في المسار بالدور.
كل تكرار في الحلقة التكرارية يجب أن يبحث عن `name` في العقدة الفهرسية الحالية `ip`.
يبدأ التكرار بقفل `ip` والفحص بكونه مجلداً.
إذا لم يكن، يفشل البحث.
(قفل `ip` ضروري ليس لأن `ip->type` قد يتغير تحت الأقدام—لا يمكنه—بل لأنه حتى تنفذ `ilock` فإن `ip->type` ليس مضمونا كونه قُرئ من القرص).
إذا كان الاستدعاء هو `nameiparent` وهذا هو عنصر المسار الأخير، تتوقف الحلقة مبكراً بصفة تعريف `nameiparent`؛ عنصر المسار النهائي قُرئ مسبقاً في `name` لذا تحتاج `namex` فقط لإرجاع `ip` غير المقفولة.
أخيراً تبحث الحلقة عن عنصر المسار باستخدام `dirlookup` وتتجهز للتكرار التالي عبر ضبط `ip = next`.
عندما تنفذ الحلقة كافة عناصر المسار ترجع `ip`.

الإجراء `namex` قد يستغرق وقتاً طويلاً للإتمام: قد يتضمن عمليات قرص متعددة لقراءة العقد الفهرسية وكتل المجلدات للمجلدات المقطوعة في اسم المسار (إذا لم تكن في المخزن المؤقت للكتل).
صُممت `xv6` بحذر بحيث إذا أعيق استدعاء لـ `namex` بواسطة خيط نواة على إدخال/إخراج القرص، يمكن لخيط نواة آخر يبحث في اسم مسار مختلف المتابعة بالتوازي.
تقفل `namex` كل مجلد في المسار بصفة مستقلة لكي تتمكن الأبحاث في مجلدات مختلفة من المتابعة بالتوازي.

هذا التوازي يقدم بعض التحديات. على سبيل المثال بينما يبحث خيط نواة في اسم مسار قد يغير خيط نواة آخر شجرة المجلدات عبر إلغاء ربط مجلد.
خطر محتمل هو أن البحث قد ينفذ في مجلد تم حذفه بواسطة خيط نواة آخر وأُعيد استخدام كتله لمجلد أو ملف آخر.

تتجنب `xv6` هذه التنافسات. على سبيل المثال عند تنفيذ `dirlookup` في `namex` يحتجز خيط البحث القفل على المجلد وترجع `dirlookup` عقدة فهرسية قُرئت بـ `iget`.
تزيد `iget` عداد المراجع للعقدة الفهرسية.
فقط بعد استقبال العقدة الفهرسية من `dirlookup` تحرر `namex` القفل على المجلد.
الآن قد يلغي خيط آخر ربط العقدة الفهرسية من المجلد لكن `xv6` لن تحذف العقدة الفهرسية بعد، لأن عداد المراجع للعقدة الفهرسية لا يزال أكبر من الصفر.

خطر آخر هو الانسداد المتبادل. على سبيل المثال `next` يشير لنفس العقدة الفهرسية كـ `ip` عند البحث عن ".".
قفل `next` قبل تحرير القفل على `ip` سينتج عنه انسداد متبادل.
لتجنب هذا الانسداد المتبادل تحرر `namex` قفل المجلد قبل الاستحواذ على القفل بـ `next`.
مجدداً نرى هنا لماذا يُعد الفصل بين `iget` و `ilock` أمراً هاماً.

== طبقة واصفات الملفات
<sec:file_descriptor_layer>

جانب رائع من واجهة Unix هو أن معظم الموارد في Unix تمثل كملفات، بما في ذلك الأجهزة مثل لوحة التحكم، الأنابيب، وبالطبع الملفات الحقيقية.
طبقة واصفات الملفات هي الطبقة التي تحقق هذا التوحيد.

تعطي `xv6` كل عملية جدول ملفاتها المفتوحة، أو واصفات الملفات.
كل ملف مفتوح يمثل بـ `struct file` والذي يعطي غلافاً حول العقدة الفهرسية أو الأنبوب، بالإضافة لإزاحة الإدخال/الإخراج.
كل استدعاء لـ `open` ينشئ ملفاً مفتوحاً جديداً (`struct file` جديد): إذا فتحت عمليات متعددة نفس الملف بصفة مستقلة، ستحظى الحالات المختلفة بإزاحات إدخال/إخراج مختلفة.
من ناحية أخرى فإن ملفاً مفتوحاً واحداً قد يظهر مرات متعددة في جدول ملفات عملية واحدة وأيضاً في جداول ملفات عمليات متعددة.
قد يحدث هذا إذا استخدمت عملية ما `open` لفتح الملف ثم أنشأت أسماء مستعارة باستخدام `dup` أو تشاركتها مع ابن باستخدام `fork`.
عداد مراجع يتتبع عدد المراجع لملف مفتوح محدد.
يمكن فتح الملف للقراءة أو الكتابة أو كلاهما.
الحقلان `readable` و `writable` يتتبعان هذا.

كافة الملفات المفتوحة في النظام تحتفظ في جدول ملفات عام `ftable`.
جدول الملفات تحوزه دوال لتخصيص ملف (`filealloc`)، إنشاء مرجع مضاعف (`filedup`)، تحرير مرجع (`fileclose`)، وقراءة وكتابة البيانات (`fileread` و `filewrite`).

الدوال الثلاث الأولى تتبع النمط المعهود.
تمسح `filealloc` جدول الملفات عن ملف غير مشار إليه (`f->ref == 0`) وترجع مرجعاً جديداً؛
تزيد `filedup` عداد المراجع؛ وتنقصه `fileclose`.
عندما يصل عداد المراجع لملف ما إلى الصفر تحرر `fileclose` الأنبوب أو العقدة الفهرسية التحتية بناءً على النوع.

تنفذ الدوال `filestat` و `fileread` و `filewrite` عمليات `fstat` و `read` و `write` على الملفات.
تُسمح `filestat` فقط على العقد الفهرسية وتدعو `stati`.
تفحص `fileread` و `filewrite` بكون العملية مسموحة بنمط الفتح ثم تمرر الاستدعاء لتنفيذ الأنبوب أو العقدة الفهرسية.
إذا كان الملف يمثل عقدة فهرسية، تستخدم `fileread` و `filewrite` إزاحة الإدخال/الإخراج كإزاحة للعملية ثم تقدمها.
لا تحوز الأنابيب مفهوم الإزاحة.
تذكر أن دوال العقد الفهرسية تتطلب من المستدعي التعامل مع القفل.
قفل العقدة الفهرسية يتسبب في أثر جانبي مريح بحفظ إزاحات القراءة والكتابة محدثة ذرياً، لكي لا تتمكن عمليات كتابة متعددة لنفس الملف في الوقت نفسه من الكتابة فوق بيانات بعضها البعض.

== الكود البرمجي: استدعاءات النظام
<sec:code_fs_system_calls>

مع الدوال التي توفرها الطبقات الأدنى، فإن تنفيذ معظم استدعاءات النظام بسيط.
توجد بضعة استدعاءات تستحق نظرة أقرب.

تعدل الدالتان `sys_link` و `sys_unlink` الدلائل، منشئتين أو مريجتين مراجع للعقد الفهرسية.
هما مثال آخر جيد على قوة استخدام المعاملات.
تبدأ `sys_link` بجل المعاملات، نصان `old` و `new`.
بافتراض أن `old` موجود وليس مجلداً، تزيد `sys_link` عداد `ip->nlink`.
ثم تدعو `sys_link` الدالة `nameiparent` لإيجاد المجلد الأب وعنصر المسار النهائي بـ `new` وتنشئ مدخل دليل جديد يشير لعقدة `old` الفهرسية.
المجلد الأب الجديد يجب أن يكون موجوداً وعلى نفس الجهاز كالعقدة الفهرسية القائمة: أرقام العقد الفهرسية لها معنى فريد على قرص واحد فقط.
إذا وقع خطأ مثل هذا، يجب على `sys_link` العودة وتنقيص `ip->nlink`.

تبسط المعاملات التنفيذ لأنها تتطلب تحديث كتل قرص متعددة، ولكن لا يتعين علينا القلق بشأن الترتيب الذي نفعله به. إما ستنجح جميعاً أو لا شيء.
على سبيل المثال بدون معاملات فإن تحديث `ip->nlink` قبل إنشاء رابط سيضع نظام الملفات في حالة غير آمنة المؤقتة والانهيار في المنتصف سيتسبب في فوضى.
مع المعاملات لا يتعين علينا القلق بشأن هذا.

تنشئ `sys_link` اسماً جديداً لعقدة فهرسية قائمة.
تنشئ الدالة `create` اسماً جديداً لعقدة فهرسية جديدة.
هي تعميم لاستدعاءات نظام إنشاء الملفات الثلاثة: `open` مع علامة `O_CREATE` تنشئ ملفاً العادي جديداً، `mkdir` تنشئ مجلداً جديداً، و `mkdev` تنشئ ملف جهاز جديد.
مثل `sys_link` تبدأ `create` بدعوة `nameiparent` للحصول على العقدة الفهرسية للمجلد الأب.
ثم تدعو `dirlookup` للفحص بكون الاسم موجوداً بالفعل.
إذا كان الاسم موجوداً بالفعل يتوقف سلوك `create` على أية استدعاء نظام تُستخدم لأجله: `open` تحوز دلالات مختلفة عن `mkdir` و `mkdev`.
إذا كانت `create` تُستخدم نيابة عن `open` (`type == T_FILE`) والاسم الموجود هو نفسه ملف عادي، فإن `open` تعامل ذلك كنجاح، لذا تفعل `create` ذلك أيضاً.
وإلا فإنه خطأ.
إذا لم يكن الاسم موجوداً بالفعل تفعل `create` الآن تخصيص عقدة فهرسية جديدة بـ `ialloc`.
إذا كانت العقدة الفهرسية الجديدة مجلداً، تهيئها `create` بمدخلي `.` و `..`.
أخيراً، الآن بعد تهيئة البيانات بأسلوب صحيح، يمكن لـ `create` ربطها في المجلد الأب.
تستحوذ `create` مثل `sys_link` على قفلي عقدتين فهرسيتين في الوقت نفسه: `ip` و `dp`.
لا توجد احتمالية للانسداد المتبادل لأن العقدة الفهرسية `ip` مخصصة طازجة: لا توجد عملية أخرى في النظام تستحوذ على قفل `ip` وتدعو القفل لـ `dp`.

باستخدام `create` يسهل تنفيذ `sys_open` و `sys_mkdir` و `sys_mknod`.
تُعد `sys_open` الأكثر تعقيداً، لأن إنشاء ملف جديد مجرد جزء صغير مما يمكنها فعله.
إذا أُمضيت علامة `O_CREATE` لـ `open` تدعو `create`. وإلا تدعو `namei`.
ترجع `create` عقدة فهرسية مقفولة ولكن `namei` لا تفعل ذلك، لذا يجب على `sys_open` قفل العقدة الفهرسية بنفسها.
يوفر هذا مكاناً مناسباً للفحص بكون المجلدات تُفتح فقط للقراءة وليس الكتابة.
بافتراض الحصول على العقدة الفهرسية بأحد الطريقين، تخصص `sys_open` ملفاً وواصف ملف ثم تملأ الملف.
لاحظ أن أية عملية أخرى لا يمكنها الوصول للملف المهيأ جزئياً نظراً لأنه يوجد فقط في جدول العملية الحالية.

فحص الفصل @CH:SLEEP تنفيذ الأنابيب قبل حوزتنا لنظام ملفات.
تصل الدالة `sys_pipe` ذلك التنفيذ بنظام الملفات عبر توفير طريقة لإنشاء زوج أنابيب.
معاملها مؤشر لمساحة لعددين صحيحين، حيث ستسجل واصفي الملفات الجديدين.
ثم تخصص الأنبوب وتثبت واصفي الملفات.

== العالم الحقيقي
<sec:real_world_fs>

المخزن المؤقت للكتل في نظام تشغيل في العالم الحقيقي أكثر تعقيداً بدرجة كبيرة عن `xv6` لكنه يخدم نفس الغرضين: التخزين المؤقت وتزامن الوصول للقرص.
يستخدم المخزن المؤقت لـ `xv6` سياسة إخلاء الأقل استخداماً مؤخراً (LRU)؛ توجد سياسات أكثر تعقيداً يمكن تنفيذها.
مخزن مؤقت لـ LRU أكثر كفاءة سيلغي القائمة الترابطية مستخدماً بدلاً منها جدول تجزئة للأبحاث وكومة لإخلاءات LRU.
المخازن المؤقتة الحديثة تُدمج عادة بنظام الذاكرة الافتراضية لدعم الملفات المربوطة بالذاكرة.

تستخدم `xv6` تخطيط قرص للعقد الفهرسية والدلائل شبيه بـ UNIX المبكر؛ هذا المخطط استمر بدرجة ملحوظة عبر السنين.
UFS/FFS بـ BSD و ext2/ext3 بـ Linux تستخدم جوهرياً نفس هياكل البيانات.
الجزء الأكثر عدم كفاءة في تخطيط نظام الملفات هو المجلد، والذي يتطلب مسحاً خطياً عبر كافة كتل القرص أثناء كل بحث.
هذا معقول عندما تكون المجلدات مجرد بضع كتل قرصية، ولكنه مكلف للمجلدات المحتفظة بملفات كثيرة.
NTFS بـ Microsoft Windows و HFS بـ macOS و ZFS بـ Solaris تنفذ المجلد كشجرة متوازنة من الكتل على القرص.
هذا معقد ولكن يضمن أبحاث مجلدات في وقت لوغاريتمي.

تتطلب `xv6` ملائمة نظام الملفات على جهاز قرص واحد وعدم تغير الحجم.
مع قيادة قواعد البيانات العملاقة وملفات الوسائط المتعددة لمتطلبات التخزين أعلى من أي وقت مضى، تطور نظم التشغيل طرقاً لإلغاء اختناق "قرص واحد لكل نظام ملفات".
النهج الأساسي هو دمج أقراص متعددة في قرص منطقي واحد.
الحلول العتادية مثل RAID لا تزال الأكثر شعبية، لكن الاتجاه الحالي يتحرك نحو تنفيذ أكبر قدر ممكن من هذا المنطق في البرمجيات.
تلك التنفيذات البرمجية تتيح وظيفية غنية مثل تنمية أو تقليص الجهاز المنطقي عبر إضافة أو إزالة أقراص في الهواء.
بالطبع طبقة تخزين يمكنها التنمية أو التقليص في الهواء تتطلب نظام ملفات يمكنه فعل نفس الشيء: مصفوفة كتل العقد الفهرسية ثابتة الحجم المستخدمة بـ `xv6` لن تعمل جيداً في مثل تلك البيئات.
فصل إدارة القرص عن نظام الملفات قد يكون التصميم الأنسب، لكن الواجهة المعقدة بينهما قادت بعض النظم مثل ZFS لدعم دمجها.

يفتقر نظام ملفات `xv6` للعديد من الميزات الأخرى في نظم الملفات الحديثة؛ على سبيل المثال يفتقر للدعم لـ snapshots والنسخ الاحتياطي التزايدي.

تسمح نظم Unix الحديثة بالوصول لأنواع عديدة من المورد باستدعاءات النظام نفسها كالتخزين على القرص: الأنابيب المسمات، اتصالات الشبكة، نظم الملفات الشبكية عن بعد، وواجهات المراقبة والتحكم مثل `/proc`.
بدلاً من عبارات `if` بـ `xv6` في `fileread` و `filewrite` تعطي هذه النظم كل ملف مفتوح جدولاً من مؤشرات الدوال وتدعو مؤشر الدالة لاستدعاء تنفيذ تلك العقدة الفهرسية للاستدعاء.

== تمارين
<sec:fs_exercises>

1. لماذا الهلع `panic` في `balloc`؟ هل يمكن لـ `xv6` التعافي؟
2. لماذا الهلع `panic` في `ialloc`؟ هل يمكن لـ `xv6` التعافي؟
3. لماذا لا تنفذ `filealloc` الهلع `panic` عندما تنفذ منها الملفات؟ لماذا هذا أكثر شيوعاً ويستحق المعالجة؟
4. افترض أن الملف المناظر لـ `ip` أُلغي ربطه بواسطة عملية أخرى بين استدعاءات `sys_link` لـ `iunlock(ip)` و `dirlink`. هل سينشأ الرابط بأسلوب صحيح؟ لماذا أو لماذا لا؟
5. تنفذ `create` أربعة استدعاءات دوال (واحد لـ `ialloc` وثلاثة لـ `dirlink`) تتطلب النجاح. إذا لم ينجح أي منها تدعو `create` الهلع `panic`. لماذا يُعد هذا مقبولاً؟ لماذا لا يمكن لأي من الاستدعاءات الأربعة الفشل؟
6. تدعو `sys_chdir` الدالة `iunlock(ip)` قبل `iput(cp->cwd)` والتي قد تحاول قفل `cp->cwd` ورغم ذلك فإن تأجيل `iunlock(ip)` حتى بعد `iput` لن يتسبب في انسداد متبادل. لماذا لا؟
7. نفذ استدعاء النظام `lseek`. دعم `lseek` سيتطلب أيضاً تعديل `filewrite` لملء الثقوب في الملف بأصفار إذا ضبطت `lseek` الإزاحة `off` بعد `f->ip->size`.
8. أضف `O_TRUNC` و `O_APPEND` لـ `open` لكي تعمل المعاملات `<` و `<<` في الشاشة.
9. عدل نظام الملفات لدعم الروابط الرمزية (Symbolic links).
10. عدل نظام الملفات لدعم الأنابيب المسمات (Named pipes).
11. عدل نظام الملفات ونظام الذاكرة الافتراضية لدعم الملفات المربوطة بالذاكرة (`mmap`).
