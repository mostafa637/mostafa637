// fix-bidi.typ — BiDi text direction handling for code listings and mixed RTL/LTR text.

#let bidi-direction(content) = {
  rtl
}

#let process-bidi(body) = {
  body
}

#let fix-bidi-content(body) = {
  body
}
