import os

typst_dir = "/home/user/mostafa637/xv6-riscv-book-ar/typst"

# 11. fs_ar.typ
fs_typ = """#import "listings.typ": *

= نظام الملفات < CH:FS >

يوفر *نظام الملفات* (file system) تجريدًا للبيانات المحفوظة على أقراص التخزين لتنظيمها على شكل ملفات ودلائل شجرية متناسقة.
يضمن نظام الملفات سهولة الوصول للبيانات، ومشاركتها بين البرامج، واستمرار بقائها حتى بعد إيقاف تشغيل الحاسوب.

يتكون نظام الملفات في xv6 من 7 طبقات متسلسلة الهيكلة.
يقدم هذا الفصل الطبقات الأساسية بدءًا من العتاد، وطبقة المخزن المؤقت للكتل (Buffer Cache)، وطبقة العقد الفهرسية (Inodes)، والملفات، والدلائل، واستدعاءات النظام المفتوحة.

== طبقات نظام الملفات

تتكون بنية نظام الملفات في xv6 من الطبقات السبع التالية:
1. *الكتل العتادية (Disk Blocks)*: قراءة وكتابة الكتل الفيزيائية على القرص الصلب.
2. *المخزن المؤقت للكتل (Buffer Cache)*: تخزين الكتل المترددة في الذاكرة لتسريع القراءة والتزامن.
3. *سجل العمليات (Logging)*: تسجيل التعديلات وتجميع المعاملات لضمان التعافي عند انهيار النظام.
4. *العقد الفهرسية (Inodes)*: تمثيل البيانات الوصفية للملفات ومواقع كتلها على القرص.
5. *الدلائل (Directories)*: ربط أسماء الملفات النصية بـ أرقام العقد الفهرسية (Inode numbers).
6. *واصفات الملفات (File Descriptors)*: تقديم تجريد واصف الملف و الموارد المفتوحة.
7. *استدعاءات النظام (System Calls)*: واجهات الاستخدام برمجياً مثل #lstinline("open") ، #lstinline("read") ، #lstinline("write") ، و #lstinline("close") .

== تخطيط القرص الصلب

يُقسم القرص الصلب في xv6 إلى كتل متساوية الحجم، حجم كل كتلة 1024 بايت ( #lstinline("BSIZE") ).
ويتكون تخطيط القرص من الكتل التالية بالترتيب:
- الكتلة 0: *كتلة الإقلاع* (Boot block) - تحتوي على كود إقلاع النظام.
- الكتلة 1: *الكتلة الفائقة* (Superblock) - تحتوي على البيانات الوصفية لتخطيط القرص (حجم القرص، عدد كتل البيانات، عدد العقد الفهرسية، وكتلة السجل).
- الكتل بدءًا من 2: *منطقة السجل* (Log Area) - تُستخدم لحفظ المعاملات قبل كتابتها نهائيًا.
- الكتل التالية: *منطقة العقد الفهرسية* (Inodes Area) - تحتوي على هياكل العقد الفهرسية المجهزة على القرص ( #lstinline("struct dinode") ).
- الكتل التالية: *خريطة البتات* (Bitmap Area) - تُستخدم لتعقب الكتل الفيزيائية الحرة والمستخدمة.
- باقي الكتل: *كتل البيانات* (Data Blocks) - تحتوي على محتويات الملفات والدلائل الفعلية.

== المخزن المؤقت للكتل (Buffer Cache)

يقوم المخزن المؤقت للكتل بتوفير وظيفتين أساسيتين:
1. *تخزين مخصص للكتل (Caching)*: الاحتفاظ بالكتل الأكثر استخدامًا في الذاكرة الفيزيائية لتجنب القراءة المكررة والبطيئة من القرص.
2. *التزامن والاستبعاد المتبادل*: حيازة قفل لكل كتلة مؤقتة لضمان تعديل الكتلة بواسطة عملية واحدة فقط في نفس اللحظة.

تُدار الكتل عبر الهيكل #lstinline("struct buf") المعرف في #lstinline("kernel/buf.h") .
تحتفظ النواة بـ مفرغ حلقي متصل مزدوجًا من الأجزاء ( #lstinline("bcache.head") ):
- #lstinline("bread(dev, blockno)") : تبحث عن الكتلة في الذاكرة. إذا لم تجدها، تجلبها من القرص وتُحوز قفل النوم الخاض بالحيز.
- #lstinline("bwrite(b)") : تكتب محتويات المخزن المؤقت المحدثة إلى القرص الصلب.
- #lstinline("brelse(b)") : تحرر قفل النوم المربوط بالكتلة وتقلل عداد الاستخدام، وتضع الكتلة عند رأس القائمة (LRU - Least Recently Used) لإعادة استخدامها إذا لزم الأمر.

== الكود البرمجي: العقد الفهرسية (Inodes)

*العقدة الفهرسية* (Inode) هي هيكل البيانات الأساسي المعبر عن الملف أو الدليل.
هناك شكلان للعقدة الفهرسية:
1. *على القرص ( #lstinline("struct dinode") )*: يحتوي على نوع الملف (ملف عادي، دليل، أو جهاز)، عدد الوصلات ( #lstinline("nlink") )، حجم الملف بالبايتات ( #lstinline("size") )، ومصفوفة العناوين القاطعة المباشرة وغير المباشرة ( #lstinline("addrs[]") ).
2. *في الذاكرة ( #lstinline("struct inode") )*: نسخة مخصصة تحوي نفس الحقول بالإضافة إلى عداد الاستخدام في الذاكرة ( #lstinline("ref") ) وقفل النوم الخاض بها.

تحتوي مصفوفة عناوين الكتلة #lstinline("addrs[]") على 13 عنصرًا:
- أول 12 عنصرًا عبارة عن عناوين مباشرة لـ 12 كتلة بيانات ($12 \times 1024 = 12$ كيلوبايت).
- العنصر الـ 13 عبارة عن عنوان لـ *كتلة غير مباشرة أحادية* (Singly-indirect block) تحوي 256 عنوانًا لكتل بيانات إضافية ($256 \times 1024 = 256$ كيلوبايت).
وبذلك يكون الحد الأقصى لحجم الملف في xv6 هو $12 + 256 = 268$ كتلة (268 كيلوبايت).

تستخدم النواة الدالة #lstinline("bmap(ip, bn)") للترجمة بين رقم الكتلة داخل الملف والرقم المادي المربوط بها على القرص الصلب.

== الكود البرمجي: الدلائل وواصفات الملفات

الدليل في xv6 هو ملف عادي يحتوي على مصفوفة من الإدخالات ( #lstinline("struct dirent") ).
يحتوي كل إدخال على:
- رقم العقدة الفهرسية ( #lstinline("inum") ).
- اسم الملف النصي ( #lstinline("name") ، بحد أقصى 14 حرفًا في xv6).

تبحث الدالة #lstinline("dirlookup()") داخل الدليل عن اسم معين لتسترجع رقم العقدة الفهرسية الخاصة به. بينما تقوم الدالة #lstinline("dirlink()") بفرز اسم جديد وإضافته إلى الدليل.

أما واصفات الملفات (File Descriptors) فتُدار عبر هيكل النواة العام #lstinline("struct file") وطبقة الجدول العالمي للملفات المفتوحة ( #lstinline("ftable") )، لتربط الواصف بأجهزة النظام أو العقد الفهرسية أو الأنابيب.

== العالم الحقيقي

تستخدم أنظمة الملفات الحديثة مثل ext4 و ZFS و NTFS آليات متطورة للغاية مثل:
- *مجموعات الأشجار النقطية (Extents)*: استبدال مصفوفة الكتل بنطاقات متصلة لزيادة السرعة وتقليل المساحة الوصفية.
- *قوائم التحكم بالوصول (ACLs)*: دعم أذونات تفصيلية للغاية للمستخدمين والمجموعات.
- *التخزين المؤقت الذكي والتجميع Dynamic Allocation*: دعم ملفات ممتدة بأحجام تيرابايت.

== تمارين

1. احسب الحد الأقصى لحجم الملف في xv6 إذا تمت إضافة كتلة غير مباشرة ثنائية (Doubly-indirect block).
2. قم بتوسيع نظام الملفات لدعم أسماء الملفات الطويلة حتى 255 حرفًا.
"""

with open(os.path.join(typst_dir, "fs_ar.typ"), "w", encoding="utf-8") as f:
    f.write(fs_typ)

# 12. log_ar.typ
log_typ = """#import "listings.typ": *

= طبقة سجل العمليات (Logging Layer) < CH:LOG >

واحدة من أكبر المشاكل في تصميم أنظمة الملفات هي كيفية التعامل مع الانقطاع المفاجئ للطاقة أو انهيار النظام أثناء كتابة البيانات على القرص الصلب.
لأن العديد من عمليات نظام الملفات تتطلب تعديل عدة كتل مختلفة على القرص (مثل تخصيص كتلة جديدة، وتعديل خريطة البتات، وتحديث العقدة الفهرسية، وإضافة مدخل في الدليل).

إذا انقطعت الكهرباء في منتصف الطريق، أصبحت حالة القرص الصلب إما متهالكة أو مفقودة التناسق (Inconsistent State).

يحل xv6 هذه المشكلة عبر تقديم *طبقة سجل العمليات* (Logging Layer / Journaling).

يقدم هذا الفصل ميكانيكية عمل سجل العمليات، والمعاملات الذرية (Atomic Transactions)، وكيفية التعافي عند تشغيل النظام.

== غاية السجل وتصميمه

يقتضي مفهوم سجل العمليات عدم كتابة التعديلات مباشرةً على كتل القرص الأصلية.
بدلاً من ذلك، تتبع النواة الخطوات التالية:
1. تُسجل كافة الكتل المعدلة في منطقة معزولة خاصة على القرص تُدعى *منطقة السجل* (Log Area).
2. عند اكتمال جمع جميع كتل المعاملة، تكتب النواة كتلة رأس السجل (Header Block) على القرص. تُعرف هذه الخطوة بـ *الالتزام* (Commit).
3. بعد تأكيد الالتزام، تقوم النواة بنسخ التعديلات من منطقة السجل إلى مواقعها الفيزيائية الأصلية على القرص. تُسمى هذه الخطوة بـ *التفريغ* (Install).
4. بعد تفريغ كل الكتل، تصفح النواة السجل لتفريغه للعمليات الجديدة.

إذا انهارت النواة أو انقطعت الكهرباء قبل الالتزام (قبل كتابة كتلة الرأس)، يتم تجاهل السجل بالكامل عند الإقلاع وتظل البيانات القديمة كما هي.
وإذا انهارت النواة بعد الالتزام وقبل إكتمال التفريغ، يقرأ النظام عند الإقلاع كتلة الرأس ويتولى إعادة تفريغ الكتل من السجل إلى مواقعها الأصلية بنجاح، مما يضمن الذرية التامة للمعاملات.

== الكود البرمجي: تنفيذ السجل

تُدار عمليات السجل في #lstinline("kernel/log.c") .
تغلف النواة أية عملية تعديل على نظام الملفات بين الاستدعائين #lstinline("begin_op()") و #lstinline("end_op()") :
#lstlisting[
begin_op();
// تعديل العقد الفهرسية والكتل المخصصة عبر log_write()
ilock(ip);
ip->size = new_size;
iupdate(ip);
iunlock(ip);
end_op();
]

تبدأ الدالة #lstinline("begin_op()") بالانتظار حتى لا يكون السجل خاضعًا للتثبيت أو ممتلئًا بمعاملات جارية:
- تزيد عداد المعاملات المشتركة الجارية #lstinline("log.outstanding") .
- عندما تدعو العمليات #lstinline("log_write(b)") ، لا تُكتب الكتلة على القرص فورًا؛ بل تُضاف إلى مصفوفة السجل في الذاكرة وتسجل في الهيكل.

عند دعوة #lstinline("end_op()") :
1. ينقص عداد المعاملات الجارية.
2. إذا وصل العداد إلى الصفر وكانت هناك كتل معلقة، تدعو النواة الدالة #lstinline("commit()") .
3. تقوم #lstinline("commit()") بدعوة #lstinline("write_log()") لكتابة الكتل المعدلة في منطقة السجل على القرص.
4. تدعو #lstinline("write_head()") لكتابة كتلة رأس السجل على القرص (نقطة الذرية والالتزام).
5. تدعو #lstinline("install_trans()") بنسخ الكتل من منطقة السجل إلى أماكنها المخصصة في القرص.
6. تصفر كتلة الرأس وتدعو #lstinline("write_head()") لتفرغ السجل المعاملاتي.

== التحديات والتداخلات

تفرض طبقة السجل تحديات هامة:
- *تجميع المعاملات (Group Commit)*: يتيح xv6 لعدة عمليات دمج تعديلاتها داخل معاملة واحدة إذا نُفذت في نفس الوقت لتقليل عمليات الوصول للقرص.
- *حجم السجل المحدود*: يجب ألا يتجاوز الحجم الإجمالي للكتل المعدلة داخل أية معاملة سعة منطقة السجل المخصصة على القرص ( #lstinline("LOGSIZE") ، وهي 30 كتلة في xv6). لذلك تقوم العمليات الكبيرة (مثل حذف ملف ضخم) بتمزيق العمليات إلى معاملات صغيرة متعددة.

== العالم الحقيقي

تعتمد أنظمة الملفات الإنتاجية مثل ext4 و NTFS و XFS تقنيات سجل الميتاداتا (Metadata-only Logging) حيث تُسجل البيانات الوصفية فقط دون كتل البيانات العادية لتوفير الأداء والسرعة، بينما توفر خيارات التثبيت الكامل (Full Data Journaling) أقصى درجات الأمان ضد فقدان المحتويات.

== تمارين

1. اشرح ماذا يحدث لو انقطعت الكهرباء أثناء تنفيذ الدالة #lstinline("install_trans()") وكيف يتم التعافي عند الإقلاع القادم.
2. قم بزيادة حجم السجل واختبر تأثيره على سرعة المعاملات المتوازية في xv6.
"""

with open(os.path.join(typst_dir, "log_ar.typ"), "w", encoding="utf-8") as f:
    f.write(log_typ)

# 13. lock2_ar.typ
lock2_typ = """#import "listings.typ": *

= إعادة فحص الأقفال والتزامن المتقدم < CH:LOCK2 >

بعد دراسة جميع مكونات النواة (الذاكرة، المصايد، الجدولة، والمقاطعات، ونظام الملفات)، يقدم هذا الفصل مراجعة شاملة وإعادة تفحص متعمقة لكيفية تفاعل الأقفال وميكانيكيات التزامن المتقدمة لحماية هياكل البيانات في xv6.

يقدم هذا الفصل تحليل تفصيلي لاستخدام الأقفال في المخزن المؤقت للكتل، والعقد الفهرسية، الأنابيب، والعمليات.

== الأقفال في المخزن المؤقت للكتل

يحتوي المخزن المؤقت للكتل (Buffer Cache) في #lstinline("kernel/bio.c") على مستويين من الأقفال:
1. *القفل العام ( #lstinline("bcache.lock") )*: يحمي القائمة المتصلة المزدوجة للمخزن المؤقت ( #lstinline("bcache.head") ). يُستخدم فقط عند البحث عن كتلة أو إعادة استخدام كتلة حرة (LRU).
2. *القفل الفردي للكتلة ( #lstinline("b->lock") )*: قفل نوم ( #lstinline("sleeplock") ) مخصص لكل كتلة مؤقتة حية. يحمي محتويات البيانات داخل الكتلة ( #lstinline("b->data") ) ويمنع العمليات الأخرى من تعديل نفس الكتلة أثناء تنفيذ عمليات الإدخال والإخراج المادية على القرص.

يسمح هذا التصميم للأنوية المختلفة بالقراءة والكتابة في كتل قرصية مختلفة بالتوازي دون حيازة القفل العام للمخزن المؤقت، مما يحسن الأداء بصورة ملحوظة.

== الأقفال في نظام الملفات

تتبع العقد الفهرسية (Inodes) نمطًا مشابهًا للمخزن المؤقت:
- *القفل العام جدول العقد ( #lstinline("icache.lock") )*: يحمي القائمة الموزعة للجدول وتخصيص المداخل في الذاكرة.
- *القفل الفردي للعقدة ( #lstinline("ip->lock") )*: قفل نوم يحمي حقول العقدة الفهرسية ومحتويات كتلها المربوطة.

تطبيق هذا النموذج يتيح للعملية حيازة قفل عقدة دليل معين، والبحث فيه، ثم حيازة قفل ملف داخل ذلك الدليل، وتحرير قفل الدليل.
يجب مراعاة ترتيب الأقفال لتجنب الجمود: حيازة قفل الدليل دائمًا *قبل* حيازة قفل الملف الفرعي.

== الأقفال في الأنابيب (Pipes)

يوفر الأنبوب (Pipe) وسيلة للاتصال أحادي الاتجاه بين عمليتين (عملية كاتب وعملية قارئ).
تُدار القناة في #lstinline("kernel/pipe.c") بوساطة هيكل #lstinline("struct pipe") المحتوي على مخزن حلقي بسعة 512 بايت وقفل تناوب مخصص ( #lstinline("p->lock") ).

يحمي القفل مؤشرات القراءة والكتابة ( #lstinline("nread") و #lstinline("nwrite") ):
- إذا حاول القارئ القراءة من أنبوب فارغ، يحرر القفل ويدخل في حالة النوم ( #lstinline("sleep(&p->nread, &p->lock)") ).
- عندما يكتب الكاتب بيانات في الأنبوب، يحوز القفل ويضيف البيانات ويستدعي #lstinline("wakeup(&p->nread)") لإيقاظ القارئ.

== الأقفال وحماية هياكل العمليات

تعتبر حماية مصفوفة العمليات ( #lstinline("proc") ) من أصعب وظائف التزامن داخل النواة لأن العملية يمكن أن تُعدل حالتها بواسطة عمليات أخرى (مثل #lstinline("wait()") ، #lstinline("kill()") ، و #lstinline("exit()") ).

تستخدم النواة القفل المخصص لكل عملية ( #lstinline("p->lock") ):
- يحمي حالة العملية ( #lstinline("p->state") )، ومستقبل القناة ( #lstinline("p->chan") )، وراية الموت ( #lstinline("p->killed") )، ومعرف العملية ( #lstinline("p->pid") ).
- يُحاز القفل عند القراءة أو التعديل على حالة العملية من قبل المجدول أو العمليات الأب.

== العالم الحقيقي

تستخدم المعالجات والأنظمة الحديثة الأقفال المقسمة بدقة عالية (Fine-grained locking)، والأقفال غير المحجوزة (Lock-free data structures)، والتزامن المعتمد على النسخ عند التعديل (RCU) للوصول إلى أقصى درجات الاستفادة من المعالجات التي تحتوي على مئات الأنوية المترابطة.

== تمارين

1. اشرح لماذا يُفضل استخدام أقفال النوم (Sleep-locks) لحماية كتل القرص بدلاً من أقفال التناوب (Spinlocks)؟
2. تتبع ترتيب الأقفال المحوزة أثناء تنفيذ استدعاء النظام #lstinline("sys_link()") لمنع حدوث الجمود.
"""

with open(os.path.join(typst_dir, "lock2_ar.typ"), "w", encoding="utf-8") as f:
    f.write(lock2_typ)

# 14. sum_ar.typ
sum_typ = """#import "listings.typ": *

= الخاتمة والتطورات < CH:SUM >

يقدم هذا الفصل ملخصًا شاملاً لما تم استعراضه في كتاب xv6 RISC-V، وكيف تتجمع المكونات المختلفة للنواة لتشكيل نظام تشغيل متكامل، بالإضافة إلى استعراض الاتجاهات الحديثة في أبحاث وتصميم أنظمة التشغيل.

== ملخص المكونات الأساسية

على مدار الفصول السابقة، رأينا كيف يقوم xv6 بتحقيق الأهداف الرئيسة لنظام التشغيل:
1. *العزل والأمان*: عبر استغلال النمطين العتاديين للمجال (U-mode و S-mode)، وجداول الصفحات المستقلة، وتحويل المصايد والمقاطعات بصفحة المنصة القافزة.
2. *إدارة الذاكرة*: عبر جداول الصفحات ثلاثية المستويات Sv39، ومخصص الصفحات المادية، والتخصيص التكاسلي، والنسخ عند الكتابة.
3. *التزامن والتعددية*: عبر المجدول بالدوران العام، وتبديل السياق بالدالة #lstinline("swtch") ، وأقفال التناوب وأقفال النوم، وميكانيكية النوم واليقظة.
4. *التخزين والملفات*: عبر طبقات نظام الملفات السبع، والمخزن المؤقت للكتل، والعقد الفهرسية، وسجل المعاملات الذرية.

== الاتجاهات الحديثة في أنظمة التشغيل

تشهد أبحاث وتطوير أنظمة التشغيل تطورات مستمرة لمواجهة التحديات الحديثة:
- *الأنظمة غير المتطابقة والأنوية المتعددة المفرطة*: التعامل مع معالجات تحتوي على آلاف الأنوية غير المتماثلة (مثل الأنوية السريعة والأنوية الموفرة للطاقة).
- *الذاكرة غير المتطايرة (NVM / PMEM)*: دمج تقنيات التخزين فائق السرعة التي تحتفظ بالبيانات بعد انقطاع الكهرباء، مما يغير تصميم طبقات سجل الملفات والمخازن المؤقتة.
- *التحقق البرمجي الرسمي (Formal Verification)*: استخدام أساليب الرياضية لإثبات خلو النواة تمامًا من الثغرات والأخطاء (مثل نظام seL4).
- *الأنوية المكتوبة بلغات آمنة الذاكرة*: تطوير أنظمة تشغيل حديثة بلغات مثل Rust لضمان أمان الذاكرة وتجنب أخطاء المؤشرات المفخخة وحالات التسابق في وقت الترجمة.

ختامًا، يظل xv6 نموذجًا تعليميًا فريدًا وشفافًا يجسد المفاهيم الجوهرية لأنظمة التشغيل بأسلوب أنيق ومبسط.
"""

with open(os.path.join(typst_dir, "sum_ar.typ"), "w", encoding="utf-8") as f:
    f.write(sum_typ)

print("fs_ar, log_ar, lock2_ar, and sum_ar converted.")
