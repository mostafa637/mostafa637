import os

typst_dir = "/home/user/mostafa637/xv6-riscv-book-ar/typst"

# 6. pgfault_ar.typ
pgfault_typ = """#import "listings.typ": *

= استثناءات أخطاء الصفحات < CH:PGFAULT >

*خطأ الصفحة* (page fault) هو استثناء يُولّده العتاد عندما لا تستطيع وحدة إدارة الذاكرة (MMU) ترجمة عنوان افتراضي إلى عنوان فيزيائي.
ويحدث ذلك عندما يكون مدخل جدول الصفحات (PTE) المقابل للعنوان إما غير صالح ( #lstinline("PTE_V == 0") )، أو غير يمتلك الصلاحيات المطلوبة (مثل كتابة في صفحة للقراءة فقط #lstinline("PTE_W == 0") ).

بينما تعتبر العديد من الأنظمة أخطاء الصفحات مؤشرًا لخلل في البرنامج، تستغل أنظمة التشغيل الحديثة أخطاء الصفحات لتطبيق تقنيات ذاكرة افتراضية متطورة للغاية تزود الأداء والكفاءة.

يقدم هذا الفصل كيفية استغلال أخطاء الصفحات لتنفيذ التخصيص التكاسلي للذاكرة (Lazy Allocation)، والنسخ عند الكتابة (Copy-On-Write)، والتصفح بالمطالبة (Demand Paging).

== التخصيص التكاسلي

عندما يطلب برنامج زيادة فضاء ذاكرته عن طريق استدعاء النظام #lstinline("sbrk(n)") ، ينطوي التصميم التقليدي على تخصيص كافة الصفحات الفيزيائية المطلوبة فورًا ورسم خريطتها في جدول الصفحات.
لكن البرامج غالبًا ما تطلب ذاكرة كبيرة ولا تستخدم سوى جزء صغير منها لاحقًا.

تتيح تقنية *التخصيص التكاسلي* (lazy allocation) زيادة حجم ذاكرة العملية الافتراضي ( #lstinline("p->sz") ) دون تخصيص صفحات فيزيائية أو إضافة مداخل في جدول الصفحات في لحظة الاستدعاء.
عندما يحاول البرنامج القراءة أو الكتابة في العنوان الجديد، يُولّد العتاد خطأ صفحة.
تستجيب النواة للخطأ بتخصيص صفحة فيزيائية واحدة وتعيينها فورًا ثم إعادة استئناف التعليمة، مما يجعل العملية شفافة تمامًا بالنسبة لبرنامج المستخدم.

== الكود البرمجي

يتطلب إضافة التخصيص التكاسلي إلى xv6 تعديل الاستدعاء #lstinline("sys_sbrk()") ليقوم فقط بزيادة #lstinline("p->sz") دون تخصيص الذاكرة:
#lstlisting[
uint64
sys_sbrk(void)
{
  int n;
  struct proc *p = myproc();

  if(argint(0, &n) < 0)
    return -1;
  uint64 addr = p->sz;
  p->sz += n;
  return addr;
}
]

ثم نعدل معالج المصايد #lstinline("usertrap()") في #lstinline("kernel/trap.c") للتعامل مع كود سبب خطأ الصفحة في RISC-V ( #lstinline("r_scause() == 13") لخطأ قراءة/تنفيذ، أو #lstinline("15") لخطأ كتابة):
1. تقرأ النواة العنوان الافتراضي المسبب للخطأ من السجل #lstinline("r_stval()") .
2. تتأكد النواة من أن العنوان يقع في النطاق الصالح للعملية ( #lstinline("va < p->sz") وأعلى من المكدس).
3. تخصص صفحة فيزيائية جديدة باستخدام #lstinline("kalloc()") .
4. ترسم خريطة الصفحة في جدول الصفحات بالصلاحيات #lstinline("PTE_U | PTE_R | PTE_W | PTE_V") .
5. تعيد النواة التنفيذ لتبدأ التعليمة المسببة للخطأ عملها بنجاح.

== العالم الحقيقي: النسخ عند الكتابة (COW fork)

عندما تُنشئ النواة عملية ابن باستخدام #lstinline("fork()") ، يتطلب التصميم الساذج نسخ جميع الصفحات الفيزيائية للأب.
ولأن العملية الابن غالبًا ما تستدعي #lstinline("exec()") فورًا بعد #lstinline("fork()") ، تنطوي عملية النسخ هذه على هدر كبير لموارد الذاكرة والوقت.

تعتمد تقنية *النسخ عند الكتابة* (Copy-On-Write - COW) على الخطوات التالية:
1. ينشئ #lstinline("fork()") جدول صفحات للابن يشارك نفس الصفحات الفيزيائية مع الأب.
2. تُزيل النواة راية الكتابة #lstinline("PTE_W") من مدخلات كلا الجدولين وتفعل راية COW مخصصة.
3. عند محاولة أي من العمليتين الكتابة في الصفحة، يطلق العتاد خطأ صفحة كتابة.
4. تلتقط النواة الخطأ، وتتحقق من راية COW، ثم تخصص صفحة فيزيائية جديدة وتنسخ البيانات وتفعل #lstinline("PTE_W") للعملية المكاتبة فقط.

== العالم الحقيقي: التصفح بالمطالبة

تسمح تقنية *التصفح بالمطالبة* (Demand Paging) بتحميل أجزاء البرامج التنفيذية من القرص الصلب إلى الذاكرة فقط عند محاولة الوصول إليها.
عند قراءة كود البرنامج عبر #lstinline("exec()") ، لا تُحمل جميع الأقسام إلى الذاكرة؛ بل تُترك المداخل غير صالحة.
عند تشغيل الكود، يطلق العتاد خطأ صفحة فتجلب النواة الكتلة المطلوبة من القرص ورسمها في الذاكرة.

== العالم الحقيقي: الملفات المربوطة بالذاكرة

تتيح تقنية *الملفات المربوطة بالذاكرة* (Memory-Mapped Files - #lstinline("mmap") ) ربط محتويات ملف بقطاع من فضاء العناوين الافتراضي للعملية.
تتم القراءة والكتابة من وإلى الملف مباشرة عبر تعليمات الذاكرة، ويتم تحديث القرص بواسطة خطأ الصفحة عند كتابة الصفحات المتسخة (Dirty Pages).

== تمارين

1. نفّذ ميزاني #lstinline("lazy allocation") و #lstinline("COW fork") واختبر أداء التشغيل في xv6.
2. ما السلوك المتوقع إذا حاول برنامج المستخدم الوصول لعنوان فوق #lstinline("p->sz") ؟
"""

with open(os.path.join(typst_dir, "pgfault_ar.typ"), "w", encoding="utf-8") as f:
    f.write(pgfault_typ)

# 7. interrupt_ar.typ
interrupt_typ = """#import "listings.typ": *

= المقاطعات وبرامج تشغيل الأجهزة < CH:INTERRUPT >

*برنامج تشغيل الجهاز* (device driver) هو قطعة البرمجية داخل النواة التي تدير وتدعم التفاعل مع جهاز عتادي معين (مثل القرص الصلب، بطاقة الشبكة، أو لوحة المفاتيح).
تتصل برامج تشغيل الأجهزة بالعتاد عبر السجلات العتادية المربوطة بالذاكرة (Memory-Mapped I/O) وتتلقى الإشعارات غير المتزامنة عبر *المقاطعات* (interrupts).

يقدم هذا الفصل طريقة عمل المقاطعات العتادية، وإدارة الإدخال والإخراج عبر شاشة المنصة UART، ومقاطعات المؤقت الزمني في xv6.

== الكود البرمجي: مدخلات المنصة (UART Input)

يتواصل xv6 مع المستخدم عبر جهاز منفذ تسلسلي يُدعى *UART 16550*.
عندما يكتب المستخدم حرفًا على لوحة المفاتيح:
1. يولد عتاد UART إشارة مقاطعة متصلة بمجمع المقاطعات PLIC (Platform-Level Interrupt Controller).
2. يرسل PLIC المقاطعة إلى إحدى أنوية المعالج.
3. تنقل النواة السيطرة إلى دالة المصايد #lstinline("devintr()") في #lstinline("kernel/trap.c") .
4. تتعرف #lstinline("devintr()") على رقم المقاطعة القادمة من PLIC وتستدعي #lstinline("uartintr()") المحددة في #lstinline("kernel/uart.c") .
5. تقرأ #lstinline("uartintr()") الأحرف المتاحة من سجل UART الخارجي وتمررها إلى #lstinline("consoleintr()") في #lstinline("kernel/console.c") .
6. تخزن #lstinline("consoleintr()") الأحرف في مفرغ حلقي (circular buffer) وتستدعي #lstinline("wakeup()") لإيقاظ العمليات المنتظرة لإدخال سطر جديد عبر #lstinline("read()") .

== الكود البرمجي: مخرجات المنصة (UART Output)

عندما تقوم عملية بنسخ نصوص للطباعة على الشاشة عبر الاستدعاء #lstinline("write()") :
1. تتلقى النواة البيانات وتدعو #lstinline("uartputc()") .
2. تُضاف البيانات إلى المخزن المؤقت للإخراج المكتبي في #lstinline("kernel/uart.c") .
3. إذا كان جهاز UART جاهزًا لإرسال البايتات، ينقل المحرك البيانات إلى سجل الإرسال #lstinline("THR") .
4. عند اكتمال إرسال كل بايت، يطلق UART مقاطعة إكتمال الإرسال، فتستجيب النواة بإرسال البايت التالي في المخزن المؤقت.

== التزامن في برامج تشغيل الأجهزة

تفرض برامج تشغيل الأجهزة التعامل مع مستويين من التزامن:
- *التزامن بين النواة والجهاز*: يعالج العتاد الطلبات بطريقة غير متزامنة، مما يتطلب استخدام الأقفال ( #lstinline("spinlock") ) لتعليق العمليات حتى يكتمل نقل البيانات عبر #lstinline("sleep()") و #lstinline("wakeup()") .
- *التزامن بين المقاطعات والأكواد النواتية*: لتجنب حدوث الجمود إذا وقُعت مقاطعة أثناء حيازة قفل معين، تقوم النواة بتعطيل المقاطعات على النواة الحالية فور حيازة قفل التناوب عبر #lstinline("push_off()") ، وتفعلها بعد تحرير القفل عبر #lstinline("pop_off()") .

== مقاطعات المؤقت الزمني

يحتاج نظام التشغيل إلى مقاطعة دورية من المؤقت العتادي للتحكم في الجدولة الزمنية وتطبيق التعددية المشاركة (Preemptive Multitasking).

في RISC-V:
1. تصدر مقاطعات المؤقت العتادية من مجمع المقاطعات المحلي *CLINT* وتُعالج أولاً في وضع الآلة (M-mode) داخل البرمجيات الثابتة.
2. يقوم كود وضع الآلة ( #lstinline("kernel/start.c") ) بإعداد المؤقت الدوري عبر سجل #lstinline("mtimecmp") .
3. تحول البرمجية المقاطعة إلى مقاطعة برمجية في وضع المشرف (Supervisor Software Interrupt).
4. تتلقى النواة المقاطعة في #lstinline("clockintr()") وتدعو #lstinline("yield()") لإجبار العملية الحالية على التخلي عن وحدة المعالجة والسماح لعملية أخرى بالتنفيذ.

== العالم الحقيقي

في الأنظمة التجارية الحديثة، تتم أداء الإدخال والإخراج المتقدم للملفات والشبكات عبر تقنية *الوصول المباشر للذاكرة* (Direct Memory Access - DMA). تسمح تقنية DMA للأجهزة بنقل كتل كاملة من البيانات مباشرة من وإلى الذاكرة الفيزيائية دون إشغال وحدة المعالجة المركزية، حيث يقتصر دور المقاطعة على إعلام النواة بإنهاء الكتلة فقط.

== تمارين

1. ما السبب وراء تحذير استدعاء الأقفال داخل معالجات المقاطعات دون تعطيل المقاطعات أولاً؟
2. أضف دعماً لجهاز إدخال لوحة مفاتيح حقيقي أو افتراضي إضافي في xv6.
"""

with open(os.path.join(typst_dir, "interrupt_ar.typ"), "w", encoding="utf-8") as f:
    f.write(interrupt_typ)

# 8. lock_ar.typ
lock_typ = """#import "listings.typ": *

= الأقفال والتزامن < CH:LOCK >

في الحاسوب متعدد الأنوية (Multi-core)، تنفذ الأنوية المختلفة أجزاءً من الكود بالتوازي.
عندما تحاول أكثر من نوات معالجة الوصول إلى هيكل بيانات مشتركة في الذاكرة وتعديلها في آنٍ واحد، تنشأ المشاكل المعقدة للتزامن وحالات التسابق.

توفر *الأقفال* (locks) آلية الاستبعاد المتبادل (Mutual Exclusion) لحماية الهياكل والموارد المشتركة، وتضمن تنفيذ الأقسام الحرجة بواسطة نوات معالجة واحدة فقط في أي لحظة زمنية.

يقدم هذا الفصل مفاهيم حالات التسابق، أقفال التناوب (Spinlocks)، ترتيب الأقفال وحالات الجمود (Deadlocks)، وأقفال النوم (Sleep-locks).

== حالات التسابق

*حالة التسابق* (race condition) هي خلل تزامن يتوقف فيه الناتج النهائي للبرنامج أو حالة الذاكرة على تسلسل وترتيب الأنوية في قراءة وتعديل البيانات المشتركة.

مثال: لنفترض وجود دالة تحرير الذاكرة #lstinline("kfree()") التي تضيف صفحة إلى قائمة الذاكرة الحرة #lstinline("freelist") :
#lstlisting[
struct run *r = (struct run*)pa;
r->next = kmem.freelist;
kmem.freelist = r;
]
إذا حاولت نواتان تنفيذ هذا الكود في آنٍ واحد بدون أقفال، فقد تقرأ كل منهما نفس القيمة لـ #lstinline("kmem.freelist") . ثم تقوم النواة الأولى بنسخ القيمة وتعديل الرأس، تليها النواة الثانية لتمحو تعديل النواة الأولى كليًا، مما يتسبب في تسريب الذاكرة (Memory Leak) وإفساد الهيكل.

تُسمى المقطع البرمجي المحتوي على قراءة وتعديل بيانات مشتركة بـ *القسم الحرج* (critical section).

== الكود البرمجي: الأقفال

يقدم xv6 نوعين من الأقفال: *قفل التناوب* (spinlock) و *قفل النوم* (sleeplock).

تُمثَّل أقفال التناوب في #lstinline("kernel/spinlock.h") :
#lstlisting[
struct spinlock {
  uint locked;       // 0 إذا كان القفل فارغًا، 1 إذا كان القفل محوزًا
  char *name;        // اسم القفل للتنقيح
  struct cpu *cpu;   // وحدة المعالجة التي تحوز القفل حاليًا
};
]

لضمان الذرية، تستخدم النواة تعليمة ذرية عتادية يقدمها معالج RISC-V تُدعى #lstinline("amoswap") (Atomic Memory Swap).
تنفذ الدالة #lstinline("acquire()") بالاستعانة بالدالة الذرية المضمنة في C:
#lstlisting[
void
acquire(struct spinlock *lk)
{
  push_off(); // تعطيل المقاطعات أولاً
  if(holding(lk))
    panic("acquire");

  while(__atomic_test_and_set(&lk->locked, __ATOMIC_ACQUIRE) != 0)
    ; // الانتظار النشط حتى يفرغ القفل

  __sync_synchronize(); // حاجز الذاكرة (Memory Barrier)
  lk->cpu = mycpu();
}
]

أما الدالة #lstinline("release()") فتقوم بتحرير القفل وتصفير راية #lstinline("locked") بشكل ذري وإعادة تفعيل المقاطعات عبر #lstinline("pop_off()") .

== الكود البرمجي: استخدام الأقفال

تحمي الأقفال كافة هياكل بيانات xv6 المشتركة:
- #lstinline("kmem.lock") : حماية قائمة الذاكرة الفيزيائية الحرة.
- #lstinline("bcache.lock") : حماية ذاكرة التخزين المؤقت للكتل (Buffer Cache).
- #lstinline("ptable.lock") أو #lstinline("p->lock") : حماية أجزاء هيكل العملية وقائمة العمليات.
- #lstinline("cons.lock") : حماية مخزن شاشة المنصة.

== الجمود وترتيب الأقفال

*الجمود* (Deadlock) يحدث عندما تحاول نواتان حيازة قفلين بالترتيب نفسه المعكوس.
على سبيل المثال:
- تحوز النواة A القفل $L_1$ وتطلب حيازة القفل $L_2$.
- تحوز النواة B القفل $L_2$ وتطلب حيازة القفل $L_1$.
ستنتظر كل نوات معالجة إلى الأبد حتى تحرر الأخرى قفلها، مما يتسبب في تعليق الكود والنظام كليًا.

لتجنب حالات الجمود، ينص القوانين في تصميم الأنظمة على *تحديد ترتيب موحد وثابت للأقفال* (lock ordering) يجب أن تلتزم به كافة الوظائف داخل النواة.

== الأقفال والمقاطعات

إذا كانت نوات المعالجة تحوز قفل تناوب (مثل #lstinline("cons.lock") )، ووُقِعت مقاطعة على نفس النواة وحاول معالج المقاطعة حيازة #lstinline("cons.lock") ، ستدخل النواة في حلقة انتظار نشطة غير منتهية مع نفسها مما يسبب الجمود.

لتجنب هذه الحالة، تُعطل النواة المقاطعات على النواة الحالية فورًا عبر #lstinline("push_off()") قبل تنفيذ كود الانتظار في #lstinline("acquire()") ، وتستعيد حالة المقاطعات عبر #lstinline("pop_off()") بعد #lstinline("release()") .

== ترتيب التعليمات والذاكرة

تقوم المعالجات المتقدمة والمترجمات (Compilers) بإعادة ترتيب التعليمات القراءة والكتابة لتوسيع كفاءة التنفيذ (Instruction Reordering).
ولضمان عدم إعادة ترتيب قراءة أو كتابة البيانات الخاضعة للحماية إلى خارج القسم الحرج المحمي بـ #lstinline("acquire()") و #lstinline("release()") ، تستخدم النواة حواجز الذاكرة ( #lstinline("__sync_synchronize()") أو #lstinline("fence") ).

== البرمجة بالتعليمات الذرية

تتيح بعض المعماريات هياكل بيانات غير قابلة للإغلاق (Lock-Free Data Structures) يعتمد تحويرها على التعليمات الذرية مثل Compare-And-Swap (CAS). ورغم أدائها العالي، إلا أن تعقيدها وصعوبة إثبات صحتها يفسر تفضيل الأقفال الصريحة في تصميم النواة.

== أقفال النوم

أقفال التناوب (Spinlocks) غير مناسبة للعمليات التي تحتاج إلى الاحتفاظ بالقفل لفترات زمنية طويلة (مثل العمليات التي تقرأ البيانات من القرص الصلب)؛ لأن الانتظار النشط يستهلك طاقة وحدة المعالجة لمنع العمليات الأخرى من العمل.

يوفر xv6 *أقفال النوم* (sleep-locks) الممثلة بـ #lstinline("struct sleeplock") :
تسمح #lstinline("acquiresleep()") للعملية بالدخول في حالة النوم ( #lstinline("sleep()") ) والتخلي عن وحدة المعالجة المركزية لعملية أخرى، ثم الاستيقاظ عبر #lstinline("releasesleep()") عند تحرير القفل.

== العالم الحقيقي

تستخدم الأنظمة التشغيلية المتقدمة مثل Linux تقنيات تزامن عالية مثل أقفال القراءة والكتابة (Read-Write Locks)، وآلية RCU (Read-Copy-Update) التي تتيح للقراء قراءة هياكل البيانات دون الحاجة لإيقاف المكاتبين أو حيازة أقفال تقليدية.

== تمارين

1. ما الفرق الأساسي بين قفل التناوب #lstinline("spinlock") وقفل النوم #lstinline("sleeplock") ؟ ومتى يُستخدم كل منهما؟
2. اكتب برنامج اختبار يُولّد حالة تسابق بين عمليتين وأثبت القضاء عليها باستخدام الأقفال.
"""

with open(os.path.join(typst_dir, "lock_ar.typ"), "w", encoding="utf-8") as f:
    f.write(lock_typ)

print("pgfault_ar, interrupt_ar, and lock_ar converted.")
