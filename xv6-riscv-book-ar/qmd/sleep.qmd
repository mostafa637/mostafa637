# التنسيق: النوم والاستيقاظ


يحتاج التنسيق والاتصال بين العمليات إلى آلية تتيح للعملية إيقاف تنفيذها طواعية عند انتظار حادثة ما (مثل قراءة بيانات من القرص أو الأنبوب)، تمكن العملية الأخرى أو برنامج تشغيل الجهاز من إيقاظ العملية المنتظرة فور اكتمال الحادثة.

يقدم هذا الفصل ميكانيكية النوم والاستيقاظ (Sleep and Wakeup)، وآلية عمل الأنابيب، ودوال إدارة دورة حياة العمليات (`wait`، `exit`، و `kill`).

## نظرة عامة

تُعتبر مشكلة **الاستيقاظ المفقود** (Lost Wakeup) المعضلة الأساسية في تصميم آليات الانتظار.
تحدث المشكلة عندما تتفحص العملية شرطًا ما (مثل فراغ مخزن الإدخال) وتقرر النوم، ولكن قبل أن تُغير حالتها إلى `SLEEPING`، تنفذ نوات معالجة أخرى حدث الإشارة وتدعو `wakeup()`. ولأن العملية لم تنم بعد، تفوتها الإشارة وتدخل في حالة نوم دائم.

لحل مشكلة الاستيقاظ المفقود، تفرض الدالة `sleep(chan, lock)` حيازة قفل يغطي فحص الشرط ويتم تحريره بشكل ذري بالاشتراك مع تغيير حالة العملية داخل الدالة.

## الكود البرمجي: النوم والاستيقاظ

تنفذ الدالتان `sleep()` و `wakeup()` في `kernel/proc.c`:
```c
void
sleep(void *chan, struct spinlock *lk)
{
  struct proc *p = myproc();

  // يجب حيازة p->lock قبل تحرير القفل lk
  acquire(&p->lock);
  if(lk != &p->lock){
    release(lk);
  }

  p->chan = chan;
  p->state = SLEEPING;

  sched();

  // تنظيف وقفل الاسترجاع
  p->chan = 0;
  release(&p->lock);
  if(lk != &p->lock){
    acquire(lk);
  }
}
```

أما الدالة `wakeup(chan)` فتمر على جدول العمليات `proc[]`، وتحوز قفل كل عملية، وتغير حالة أي عملية نائمة على القناة `p->chan == chan` إلى `RUNNABLE`.

## الكود البرمجي: الأنابيب

تُدار الأنابيب عبر الهيكل `struct pipe` في `kernel/pipe.c`.
يحتوي الأنبوب على مفرغ حلقي (circular buffer) بطول `PIPESIZE` (512 بايت) وقفل `spinlock`:

* `pipewrite()`: تنقل البايتات إلى المفرغ. إذا امتلأ المفرغ، تدعو `sleep(&p->nread, &p->lock)` للانتظار حتى يقرأ القارئ.
* `piperead()`: تقرأ البايتات من المفرغ. إذا كان المفرغ فارغًا، تدعو `sleep(&p->nwrite, &p->lock)` للانتظار حتى يكتب الكاتب.


## الكود البرمجي: Wait و Exit و Kill

تكتمل دورة حياة العملية عبر الاستدعاءات التالية:

* `exit(status)`: تغلق الملفات المفتوحة، وتحول حالة العملية إلى `ZOMBIE`، وتجعل أطفال العملية يتامى ليُضموا إلى العملية الأولى `init`، وتستدعي `wakeup(p->parent)`.
* `wait(status)`: تبحث العملية الأب في جدول العمليات عن أطفال في حالة `ZOMBIE`. إذا وجدت طفلاً، تحرر ذاكرته وهياكله (`proc_freepagetable`) وتسترجع معرّفه وحالة خروجه. وإذا كان الأطفال ما زالوا يعملون، تنام العملية الأب على عنوانها `sleep(p, &p->lock)`.
* `kill(pid)`: تضع راية `p->killed = 1`. إذا كانت العملية نائمة، تُوقظها لتنتهي فور دخولها معالج المصايد التالي.


## إغلاق وتزامن العمليات

تضمن الأقفال المصاحبة للهيكل `p->lock` عدم حدوث حالة تسابق أثناء تعديل حالات العمليات أو قراءتها بين الأب والابن والمجدول.

## العالم الحقيقي

تستخدم أنظمة التشغيل الحديثة متغيّرات الشرط (Condition Variables) والمقابس (Futures/Promises) والـ Futexes (Fast User-space Mutexes) لتنفيذ النوم والاستيقاظ بسرعة عالية في فضاء المستخدم دون الحاجة لدخول فضاء النواة إلا عند وجود تنافس حقيقي على القفل.

## تمارين

1. ما السبب الذي يفرض ضم الأطفال اليتامى عند خروج الأب إلى العملية الأولى `init`؟
2. اكتب برنامجًا يثبت آلية عمل الاستيقاظ المفقود عند إزالة الأقفال من `sleep()`.
